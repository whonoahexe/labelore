---
milestone: v1.0
audited: 2026-09-09T18:45:00Z
status: passed
supersedes: "the 2026-09-09T16:20:00Z gaps_found audit of this same milestone"
scores:
  requirements: 45/45
  phases: 4/4
  integration: 7/7
  flows: 7/7
gaps:
  requirements: []
  integration: []
  flows: []
previously_blocking_now_closed:
  - id: "TGT-08"
    prior_status: "unsatisfied"
    now: "satisfied"
    closed_by: "quick 260909-ogb, commits 6a82a4c (CR-01) and d615c9d (CR-02)"
    evidence: >-
      CR-01: POST /api/refresh now branches on the resolved snapshot's loadStatus BEFORE the
      `derived` swap (src/server/index.ts:235-241) — a non-ok status returns
      `{refreshed: false, error}` with HTTP 500 and returns before touching `derived`, so the
      retained bundle keeps serving every subsequent GET. Verified live during execution by
      renaming .planning away mid-session: POST /api/refresh → 500 refreshed:false, and the
      following GET /api/presentation still served the pre-refresh readAt and full tree.
      CR-02: `.snapshot-status` is no longer `display: none` under the 62rem media query
      (globals.css:1487-1496) — it takes a compact treatment that drops only the decorative
      icon and label, keeping the timestamp and Refresh button rendered and operable.
      Measured by the orchestrator via headless chromium at 1200/992/700/320px: visible at
      every width, Refresh 36x36, scrollWidth == clientWidth throughout.
  - id: "FIND-04"
    prior_status: "partial"
    now: "satisfied"
    closed_by: "quick 260909-ogb, commit 92ea3a8 (WR-01, WR-02)"
    evidence: >-
      WR-01: `extractSnippets`'s whole-string `body.toLowerCase()` replaced by `foldCase()`, a
      code-point-wise fold that substitutes only when the UTF-16 length is unchanged, so
      highlight offsets stay absolute against the original body. WR-02: the seed loop was
      restructured into three passes (seed in absolute coordinates, sort + linear merge,
      materialize once), so two windows seeded in the (windowChars/2, windowChars) interval
      merge into one snippet instead of duplicating body text or cutting a match into an
      unhighlighted fragment. 6 new regression cases in test/presentation/search.test.ts
      (19 → 24), including an explicit disjoint-windows non-regression case.
  - id: "ATT-01"
    seam: "Phase 4 refresh seam -> Phase 1 read layer"
    prior_severity: "critical"
    now: "closed"
    closed_by: "quick 260909-ogb, commit 6a82a4c"
    evidence: >-
      The test that had codified the defect (asserting `refreshed: true` on the reachable
      resolve-to-failed-snapshot path) was rewritten into three assertions: the corrected
      failure contract, retention (a GET after a failed refresh still serves the pre-refresh
      readAt/project/tree), and recovery (a later successful refresh still works). The
      previously-unreachable retention proof is no longer the only one.
  - id: "ATT-02"
    seam: "Phase 4 code review -> Phase 4 verification"
    prior_severity: "critical"
    now: "closed"
    closed_by: "quick 260909-ogb (all five 04-REVIEW findings), commit 8b9d623 closing docs"
    evidence: >-
      All five of 04-REVIEW.md's findings — CR-01, CR-02, WR-01, WR-02, WR-03 — are now
      closed with implementation, new regression tests, and (for CR-01 and CR-02) live
      verification. Phase 04's review is no longer the one review filed and left.
  - id: "FLOW-G"
    flow: "Click Refresh -> snapshot age updates and all views reflect the new read"
    prior_severity: "critical"
    now: "complete"
    evidence: >-
      Both breaks are repaired. The flow can start at every width (CR-02), and a failed
      underlying read now reports failure rather than presenting the failed attempt's
      timestamp as the age of retained data — which also satisfies plan 04-01's judgment-tier
      prohibition.
  - id: "FLOW-B"
    flow: "Search exact token -> result row -> click through to artifact at heading anchor"
    prior_severity: "warning"
    now: "complete"
    evidence: "Navigation and anchoring were always correct; snippet rendering is now correct too (WR-01, WR-02)."
tech_debt_reconciliation:
  reconciled: "2026-09-10T20:10:00.000Z"
  reconciled_by: "quick-260910-0x4"
  closed_count: 14
  open_count: 0
  finalized: "2026-09-10 — phase 03 and 04 UI reviews re-run after all CSS changes, both 24/24; focus-ring finding fixed in 233ded9"
tech_debt:
  - phase: ledger-sync
    items:
      - status: closed
        closed_by: "quick-260910-0x4, commit f7266fb"
        description: "WINDOWS.md reported open_count: 3, all three stale. Entry 3 (no-regex-spaces) re-verified: regexes now use a counted {2} quantifier, npm run lint exits 0 over the whole repo. Entry 4 (unreadable-disclosure/empty-document coherence) re-verified against 04-UAT.md test 1's identical question, result pass. Entry 5 (04-05 tone-prohibition disclosure-prose scope) re-verified against 04-UAT.md test 6's identical question, result pass. Closed via `gsd-tools windows fixed 3/4/5`; a pre-existing table/JSON drift on a prior entry was also corrected. open_count is 0."
      - status: closed
        closed_by: "quick-260910-0x4, commits f1a5355 + 7c7291e"
        description: "deferred-items.md and 03-01-SUMMARY.md's stale no-regex-spaces lint note. Struck outright from deferred-items.md (a live open-issue queue); annotated in place in 03-01-SUMMARY.md (a historical execution record, never overwritten) with a dated resolution."
  - phase: 04-portability-degradation-hardening
    items:
      - status: closed
        closed_by: "quick-260910-0x4, commits f6c8af7 + 35a9d68; ring contrast fixed in 233ded9"
        description: "CR-02 residual: keyboard Tab-reachability and both-themes focus-indicator visibility, measured live via Playwright at 320/700/992px x light/dark (six cells, real keyboard events, not a programmatic .focus() call — an early script revision using .focus() produced a false-negative reading and was caught before trusting the result). Refresh reachable at Tab stop 7 in all six cells; Enter and Space independently confirmed to trigger POST /api/refresh in all six; a real :focus-visible style change confirmed in all six. NEW finding surfaced by the measurement, distinct from this item's reachability/operability scope: focus-ring contrast against the page background is below WCAG 1.4.11's 3:1 non-text guideline in both themes — logged as WINDOWS.md entry 7, then FIXED in 233ded9: opaque outline, dark --ring oklch(0.56 0.157 37.304), and index.html's pre-hydration token snapshot demoted from :root.dark to .dark after it out-specified globals.css. First-logged figures (2.29/1.46) came from a flat-composite model; rendered pixels measured 2.36/1.93 before the fix and 5.21:1 light / 4.07:1 dark after."
      - status: closed
        closed_by: "re-run 2026-09-10 via /gsd-ui-review after all CSS changes landed, including 233ded9"
        description: "04-UI-REVIEW.md — re-audited at 24/24 (was 22/24 on evidence gathered below the 62rem breakpoint), needs_human_review: false."
  - phase: 03-search-browsing-traceability
    items:
      - status: closed
        closed_by: "re-run 2026-09-10 via /gsd-ui-review after all CSS changes landed, including 233ded9"
        description: "03-UI-REVIEW.md — re-audited at 24/24 on the current tree, needs_human_review: false."
  - phase: 02-situational-awareness-artifact-reading
    items:
      - status: closed
        closed_by: "quick-260910-0x4, commit 91235bd"
        description: "IN-02 from 02-REVIEW.md: dead critical-CSS scaffold in index.html. Re-measured with a word-boundary, className-token-scoped scan (the original substring-fuzzy grep was untrustworthy) rather than the prior ~150-line estimate: 14 confirmed-dead selectors/blocks deleted (.screen, .position-card, .error-card, .card-heading, .progress-block/-row/-track/-fill, .metadata, .status, .muted, .skeleton, @keyframes pulse, the 36rem media query), 297 -> 162 lines (135-line reduction, measured via wc -l). .eyebrow and .lede confirmed genuinely live and kept. Pinned by test."
      - status: closed
        closed_by: "quick-260910-0x4, commit beb291c"
        description: "Accepted risk R-02-01 (severity medium, below the high block threshold): the 02-06 gate closed on an explicit human decision, and D3 carried status: unknown. Reconciled: the mitigating chain was miscited as 02-09 -> 02-13 -> 02-17 — 02-09 was status: superseded (superseded_by: 02-13) and its UAT round was NOT approved (10 measured gaps, per 02-UAT.md); it produced 02-10..02-13, it did not pass evidence. The real chain is 02-13 -> 02-17 (approved). All eight of D3's named surfaces (prose, muted text, links in every state, badges, table chrome, highlighted code, Mermaid output, reference previews) individually mapped to 02-13/02-17 evidence at 02-06-SUMMARY.md's D3 coverage entry (second, dated verification item; original unknown verdict preserved unmodified). R-02-01's disposition (accepted, non-blocking) is unchanged; only the citation was wrong. threats_open stays 0."
  - phase: 01-read-layer-domain-model
    items:
      - status: closed
        closed_by: "quick-260910-0x4, commit 18766ab"
        description: "A malformed (non-array) depends_on frontmatter value degraded to [] silently with no warning recorded. Threaded the live WarningCollector through assembleDomainModel into resolveCrossReferences: a wrong-shaped depends_on now records exactly one warning naming the plan and the field, while D-12's never-throw contract and D-10's well-formed-dangling-reference silence are both preserved and pinned by dedicated tests (including a direct load of the real sparse-started fixture's dangling SLICE-99 reference). Decided and pinned: absent key and explicit YAML null both stay warning-free."
      - status: closed
        closed_by: "quick-260910-0x4, commits 7c00ebc + 575c3b0"
        description: "src/planning-fs/in-memory-fs.ts:5 imported node:fs for the test-only fromDirectory() loader, contradicting plan 01-01's literal must-have. Moved to test/helpers/from-directory.ts (typechecked, not collected as a suite). Added a durable enforcement gate (test/portability.test.ts) scanning all of src/ for the Node fs module under every real spelling, comment-aware so prose mentions are never flagged, with a positive control planting all ten spelling variants in a synthetic tmpdir tree and confirming the gate catches every one."
      - status: closed
        closed_by: "quick-260910-0x4, commit 13cea79"
        description: "IN-01 from 01-REVIEW.md: duplicated title-derivation logic in handlers/generic.ts, confirmed byte-identical to title.ts's shared export before collapsing (same regex, same trim, same path fallback, same guard) — golden files unchanged, byte-identical output."
      - status: closed
        closed_by: "quick-260910-0x4, commit 21b24ca"
        description: "IN-02 from 01-REVIEW.md: package.json carried no engines field (confirmed absent). engines.node set to >=22.18.0, derived from the measured highest package-declared floor (20.19.0, across vite/vitest/eslint/@vitejs-plugin-react/typescript/hono/@hono-node-server/react/tailwindcss) versus Node's own native-TypeScript-type-stripping-unflagged floor (22.18.0, the higher and therefore effective one) — not assumed."
  - phase: cross-milestone
    items:
      - status: closed
        closed_by: "quick-260910-0x4, commit 356c18d"
        description: "Carried forward from the v0.1 close: Mermaid diagram styling rendered correctly but looked poor ('works as expected but it looks very ugly', 02-17 gate). Root cause per the 02-13 diagnosis: the node-fill seed (primaryColor) read --secondary (carries chroma, off the app's neutral palette) instead of --card (the actual neutral surface). Fixed, and expanded to the full named palette (node/cluster fill and border, secondary/tertiary colours, edge colour and edge-label background, cluster/node text colour, note colours, font size), all through toMermaidColor() as the single conversion seam. Pinned by a source-level contract test with a positive control."
      - status: closed
        closed_by: "quick-260910-0x4, commit c08aee1"
        description: "Carried forward from the v0.1 close: `.artifact-metadata > summary span` rendered at 9.92px, under a 10px floor. Root cause was a shared design convention (six sibling selectors, three at 9.92px and three at 9.6px, smaller still), not one leaf. Fixed with one shared token, --font-size-micro-label: 0.7rem (11.2px, real headroom against rem rounding — 0.625rem was rejected for having none). Colour untouched at every site, so contrast holds by construction. Pinned across the whole cascade including media queries."
      - status: closed
        closed_by: "quick-260910-0x4, commit 51804f6"
        description: "npm run build emitted Vite's chunk-size warning. Measured: two chunks over 500 kB (entry 571.58 kB, mermaid-shared-internals chunk-FOHPRMQF 662.12 kB). Checked the more consequential question first: the server-only markdown/search pipeline does not reach the client bundle (traced the import graph; the one non-type-only cross-boundary import terminates in a dependency-free module). Entry chunk fixed for real via route-level React.lazy() (571.58 -> 388.75 kB, moving it entirely under the default limit) with the one required Suspense boundary added. The mermaid chunk is already behind mermaid's own dynamic-import boundary and not further splittable without fighting mermaid's own module graph; build.chunkSizeWarningLimit raised to 680 (just above the measured 662.12 kB) with a dated, measured comment. No log-suppression flag or output redirection anywhere. npm run build now emits no advisory line."
nyquist:
  compliant_phases: []
  partial_phases: []
  not_validated_phases: []
  missing_phases: []
  overall: not_applicable
  reason: >-
    workflow.nyquist_validation is false in .planning/config.json, no *-VALIDATION.md files
    exist anywhere under .planning/phases/, and `loop render-hooks verify:post` returns only
    the security and ui step hooks — no validate-phase step hook is active. Section skipped
    per spec.
---

# Milestone v1.0 Audit — Labelore

**Audited:** 2026-09-09T18:45:00Z
**Status:** `passed`
**Scope:** Phases 1–4 (30 executed plans), 45 v1 requirements, 4 quick tasks
**Supersedes:** the `gaps_found` audit of this milestone dated 2026-09-09T16:20:00Z

## Verdict

The milestone is done. The previous audit called it "one unclosed code review away from
shippable" — quick task `260909-ogb` closed that review. All five of `04-REVIEW.md`'s findings
(CR-01, CR-02, WR-01, WR-02, WR-03) are implemented, regression-tested, and for the two critical
ones verified live. Both requirements the previous audit downgraded are back to satisfied, both
critical integration attestation failures are closed, and both broken flows complete.

**Reconciled 2026-09-10 by `quick-260910-0x4`.** 12 of the 14 tech-debt items listed below closed
with commit evidence: the three stale WINDOWS.md entries, the stale lint note, four items of
non-web source/config debt (title-derivation dedup, `engines` field, silent `depends_on`
degradation, and the `in-memory-fs.ts`/`node:fs` seam), the dead critical-CSS scaffold, the
sub-10px micro-label family, the mermaid palette, the build's chunk-size advisory, the CR-02
keyboard-reachability residual, and R-02-01's citation. **The final 2 closed the same day:** `03-UI-REVIEW.md` and `04-UI-REVIEW.md` were re-run after every
CSS change had landed and both scored 24/24 with `needs_human_review: false`. One new finding
surfaced during reconciliation — the focus ring failed WCAG 1.4.11 in both themes — and was fixed
in `233ded9` rather than waived. **Zero tech debt remains; status is `passed`.** See Tech Debt
Summary for the full per-item record.

## Tree Health

Re-run independently by this audit, not read from a summary. Re-verified again 2026-09-10 by
`quick-260910-0x4` on the fully reconciled tree:

| Check | Result |
|-------|--------|
| Tests | 590/590 pass (40 files) |
| Typecheck | exit 0, silent |
| Lint | exit 0, silent |
| Build | succeeds; no advisory line (the >500 kB chunk-size notice from the prior audit is closed — see Tech Debt Summary) |
| Anti-patterns (TODO/FIXME/HACK/stub) | clean across all four phases' VERIFICATION scans |
| `WINDOWS.md` `open_count` | 0 |

Test count moved 547 → 555 (previous audit) → 590 (this reconciliation): the six new
WR-01/WR-02 snippet cases in `test/presentation/search.test.ts`, the rewritten refresh contract in
`test/server/refresh.test.ts`, the new `.snapshot-status` regression pin in
`test/web/refresh-contract.test.ts`, and 35 new tests added across `quick-260910-0x4`'s seven
tasks (crossref/assemble warning-threading pins, the `node:fs` import-boundary gate with its
positive control, the anti-FOUC and micro-label-family visual-contract pins, the mermaid
source-level contract suite, the route-splitting/chunk-threshold pins, and the Refresh-control
structural-guarantee suite).

## Phase Verification Aggregate

| Phase | Status | Score | Human Verification | Security |
|-------|--------|-------|--------------------|----------|
| 01 Read Layer & Domain Model | passed | 5/5 SC, 39/39 must-haves | resolved — 01-UAT 23/23 pass, 0 issues | verified, 0 open, 4 accepted risks |
| 02 Situational Awareness & Artifact Reading | passed | 6/6 SC (re-verified twice) | 02-UAT complete, 11 round-1 gaps reconciled and re-driven | verified, 0 open, 1 accepted (R-02-01) |
| 03 Search, Browsing & Traceability | passed | 4/4 truths, 7/7 REQ-IDs | 03-UAT complete, 4 backstop items pass | verified, 0 open |
| 04 Portability & Degradation Hardening | passed | 10/11 must-haves | 04-UAT complete, 9/9 pass — closes the 11th | verified, 0 open |

No phase is missing a `VERIFICATION.md`. Every phase's human-verification queue is empty:
Phase 01's deferred `~/studio-portal` smoke test, Phase 03's four `verification: backstop`
items, and Phase 04's nine judgment-tier prohibitions are all recorded `pass` in their
respective `*-UAT.md` files. Phase 04's 11th must-have — the prose-coherence backstop that kept
its report at 10/11 — is resolved by `04-UAT.md` test 1.

Phase 02 is worth naming as the positive precedent: it was re-verified a **second** time
(2026-09-09) specifically because 29 commits touched `src/web`, `src/presentation`,
`src/rendering` and `src/server` after the 02-17 approval, including `64cc42b` which restyled
the very surfaces the gate approved. Phase 04's review has now received the same treatment.

## Requirements Coverage — 3-Source Cross-Reference

| Source | Result |
|--------|--------|
| REQUIREMENTS.md traceability table | 45 REQ-IDs, all rows `Complete`, all 45 checkboxes `[x]` |
| Phase SUMMARY.md `requirements-completed` frontmatter | 45/45 appear in at least one summary |
| Phase VERIFICATION.md requirement tables | 45/45 appear in exactly the phase that owns them |
| **Orphans** (in traceability, absent from every VERIFICATION) | **0** |
| Per-phase assignment match | 9 + 23 + 7 + 6 = 45 — exact match to ROADMAP |

Applying the status-determination matrix: every REQ-ID lands `passed` / `listed` / `[x]` →
**satisfied**.

| Status | Count |
|--------|-------|
| satisfied | 45 |
| partial | 0 |
| unsatisfied | 0 |
| orphaned | 0 |

The stale-checkbox bookkeeping that Phases 01 and 02 both flagged as an Info-level anti-pattern
has since been fixed — `REQUIREMENTS.md` now carries `[x]` and `Complete` for all 45, and the
three independent sources agree.

### The two requirements the previous audit downgraded

**TGT-08** — *"Every view shows when its data was read from disk, and a Refresh action re-reads
the project."* Both clauses now hold.

`src/server/index.ts:235-241` branches on `snapshot.loadStatus.status !== 'ok'` and returns
`{refreshed: false, error}` with HTTP 500 **before** the `derived = buildDerivedViews(snapshot)`
swap on line 249. A failed read can no longer report success, and can no longer replace the good
bundle server-wide. Verified live during execution, not just under test doubles: with `.planning`
renamed away mid-session, `POST /api/refresh` returned 500 `refreshed:false`, and the immediately
following `GET /api/presentation` still served the pre-refresh `readAt` and `GET /api/tree` still
served the full retained tree; restoring `.planning` and refreshing again recovered with a new
`readAt`.

`globals.css:1487-1496` replaces the old `display: none` with a compact treatment under 62rem —
the decorative icon and "Snapshot read" label drop out, the timestamp and Refresh button stay
rendered and operable. `.shell-controls` was relaid out from an absolutely positioned overlay to
a normal full-width flowed row, so its width is bounded by the grid column's own 100% by
construction rather than by measurement. Measured at 1200/992/700/320px: `.snapshot-status`
visible at every width, Refresh 36×36, `scrollWidth == clientWidth` throughout.

**FIND-04** — *"Each result shows a snippet with the match highlighted."* Both snippet defects
are fixed. `foldCase()` replaces the whole-string `toLowerCase()` with a code-point-wise fold
that substitutes only when UTF-16 length is unchanged, so a length-changing case mapping can no
longer shift every subsequent highlight range. The seed loop became three passes — seed in
absolute coordinates, sort and linear-merge, materialize once — so overlapping windows merge
rather than duplicating body text or cutting a match into an unhighlighted fragment.

## Cross-Phase Integration

7/7 seams wired. Every export has a consumer; every API route has a caller.

| Seam | Status | Evidence |
|------|--------|----------|
| Phase 1 → 2 — snapshot to presentation | ✓ wired | `toProjectPresentation(snapshot)` has exactly one call site, `buildDerivedViews()` at `src/server/index.ts:62`. The presentation layer is a pure transformation over the immutable snapshot; it never re-reads the filesystem. |
| Phase 1/2 → 3 — search index source | ✓ wired | `buildSearchDocuments()` (`src/server/search-index.ts:218`) reads only `Artifact.body`, `title` and `frontmatter`. No renderer is invoked during index construction — FIND-02's "source markdown, not rendered output" holds at the source level. |
| Phase 2 → 3 — route agreement | ✓ wired | Tree nodes and search rows both carry `.url` values produced by the Phase 2 builders (`buildPhaseUrl`, `buildArtifactUrl`); every emitted pattern matches a `presentationRoutePatterns` entry the router serves. |
| Phase 2 ↔ 3 — reference linking | ✓ wired | One shared registry: `buildReferenceRegistry(presentation)` built once per snapshot in `buildDerivedViews()`, consumed by `linkify.ts` through `resolvePresentationReference()` for both prose and inline-code references. Unresolved IDs return null and render as plain text — NAV-04 by construction. |
| Phase 3 → 2 — traceability data source | ✓ wired | `buildTraceabilityViewModel` reads the same `Requirement[]` and `Milestone.phases` the roadmap view does; no parallel parse. |
| Phase 4 → all — atomic refresh | ✓ wired | `POST /api/refresh` flows through `PlanningRepository.refresh()` (bound via `.bind(source)`, not a bare method reference) and rebuilds **all four** derived views — presentation, artifact index, reference registry, **and the search index** — in one assignment. Concurrent refreshes coalesce onto one in-flight walk. |
| Phase 4 → all — shared damaged-artifact vocabulary | ✓ wired | `artifactWarningTone()` is the single source, called by `tree.ts:183`, `search.ts:88` and `artifact-page.tsx:364`. No consumer re-derives the warning/unreadable split locally. |

**Contract drift:** none found. No route-token or codec mismatch, no field rename, no `null`-vs-missing
disagreement, no warning-shape assumption that one phase emits and another misreads.

**Stale-data risk:** none. Every view is built from `derived.presentation`, which is replaced
wholesale; `/api/artifacts/*` and `/api/documents` each capture `activeDerived` as their literal
first statement, before any URL parsing and therefore before the handler's first `await`, closing
the D-05 race where a GET spanning a refresh could mix pre- and post-refresh data in one response.

## End-to-End Flows

7/7 complete. Each traced from entry point to rendered output.

| Flow | Status | Chain |
|------|--------|-------|
| CLI path argument → dashboard render | ✓ complete | `resolveTargetPath` → `LocalFsPlanningFilesystem` → `PlanningRepository.load()` → `buildDerivedViews` → `toProjectPresentation` → `/api/dashboard` → `DashboardPage` |
| Search token → grouped result → artifact at heading anchor | ✓ complete | `SearchField` → `/api/search` → `searchIndex` → `buildSearchGroups` → `row.url` → router → `ArtifactPage` → `/api/documents` → `artifactIndex.lookupRoute` → renderer. Snippet highlighting now offset-correct and non-overlapping. |
| Requirement ID in prose → phase route scoped to that phase | ✓ complete | markdown pipeline → `rehypeResolvedReferences` → `resolvePresentationReference` → clickable control → `buildPhaseUrl` → `RoadmapPage` opens and scrolls the deep-linked phase |
| Traceability row → covering phase → phase route | ✓ complete | `/api/traceability` → `coveringPhases[]` → same `buildPhaseUrl` the roadmap uses |
| Tree node → artifact route → rendered markdown (incl. unknown type) | ✓ complete | `/api/tree` → `TreeNavigator` → `buildArtifactUrl` → `ArtifactPage`; an unrecognized type reaches the generic handler (registered last, matching unconditionally) and renders as plain markdown |
| Refresh click → new snapshot → every view shows the new read time | ✓ complete | `RefreshControl` → `POST /api/refresh` → `refresh()` → atomic `derived` swap → `invalidateQueries()` → every view refetches. On failure: `refreshed: false`, `derived` untouched, retained `readAt` preserved. |
| Invalid target path → ProjectGate → InvalidProjectScreen | ✓ complete | `ProjectGate` (`app-router.tsx:56`) wraps all 9 routes; a non-ok `loadStatus` renders `InvalidProjectScreen`, which has no `<Outlet />`, so no child route mounts |

## Nyquist Coverage

Not applicable. `workflow.nyquist_validation` is `false`, no `*-VALIDATION.md` exists, and
`verify:post` renders only the `security` and `ui` step hooks — no `validate-phase` step hook is
active. Per the audit spec, this section is skipped rather than scored.

## Tech Debt Summary

**Reconciled 2026-09-10.** 14 items across 6 groupings, **all 14 closed**, plus one new finding
surfaced during reconciliation (focus-ring contrast), also fixed. This section records what closed,
by which commit, with what evidence. Full per-item detail with commit references lives in this
document's `tech_debt:` frontmatter block.

### Ledger sync — closed

`WINDOWS.md` reported `open_count: 3`; all three were stale and are now closed.

| id | Recorded as | Resolution |
|----|-------------|--------------|
| 3 | `no-regex-spaces` lint error at `test/web/visual-contract.test.ts:438` | Closed — regexes use a counted `{2}` quantifier; `npm run lint` exits 0 over the whole repo |
| 4 | Human must confirm the unreadable disclosure and empty-document notice read as one coherent explanation | Closed — `04-UAT.md` test 1, `pass` |
| 5 | Human must decide whether the 04-05 tone prohibition includes disclosure prose | Closed — `04-UAT.md` test 6, `pass` |

Closed via `gsd-tools windows fixed 3/4/5` (commit `f7266fb`); `open_count` is 0. The stale lint
note was struck from `deferred-items.md` (a live queue) and annotated in place in
`03-01-SUMMARY.md` (a historical record, preserved) — commits `f1a5355`/`7c7291e`.

### Stale UI reviews — both closed

Both were stale by their own stated re-run conditions, and `quick-260910-0x4`'s CSS changes re-fired
both again, so they were re-run only after every CSS change had landed, including the focus-ring
fix (`233ded9`).

- **03-UI-REVIEW.md** — re-audited **24/24**, `needs_human_review: false`. Supersedes the
  2026-09-02 review.
- **04-UI-REVIEW.md** — re-audited **24/24**, `needs_human_review: false`, reasoned per breakpoint
  (320/700/992/1200px). Supersedes the 2026-09-08 review, which gathered its evidence at 770px —
  below the then-62rem breakpoint where the snapshot age and Refresh control were hidden.

### Polish and deferred code quality — all closed

- **CR-02 residual, closed** (`f6c8af7`, `35a9d68`): keyboard reachability and operability
  measured live via Playwright at 320/700/992px × light/dark — reachable and operable (Enter and
  Space both independently confirmed) in all six cells, with a real `:focus-visible` style change
  confirmed in all six. New finding surfaced by the measurement: focus-ring contrast was below WCAG 1.4.11's 3:1
  guideline in both themes (rendered pixels 2.36:1 light, 1.93:1 dark; the first-logged 2.29/1.46
  came from a flat-composite model). Logged as `WINDOWS.md` entry 7, then **fixed** in `233ded9`:
  opaque outline, dark `--ring` `oklch(0.56 0.157 37.304)`, and `index.html`'s pre-hydration
  snapshot demoted from `:root.dark` to `.dark` after it out-specified globals.css. Measured after:
  5.21:1 light, 4.07:1 dark; pinned by `test/web/focus-ring-contrast.test.ts`.
- **Mermaid diagram styling, closed** (`356c18d`): root cause was the node-fill seed reading
  `--secondary` (carries chroma) instead of `--card` (the app's actual neutral surface). Fixed,
  and expanded to the full named palette through `toMermaidColor()` as the single conversion seam.
- **`.artifact-metadata > summary span` at 9.92px, closed** (`c08aee1`): root cause was a shared
  micro-label convention across six sibling selectors (three at 9.92px, three at 9.6px — smaller
  still), not one leaf. Fixed with one shared token, `0.7rem` (11.2px), real headroom against
  rounding.
- **Dead critical-CSS scaffold in `index.html`, closed** (`91235bd`): re-measured with a
  word-boundary, className-scoped scan (the prior ~150-line estimate used an untrustworthy
  substring-fuzzy grep) — 297 → 162 lines, a 135-line reduction, 14 confirmed-dead
  selectors/blocks removed, live selectors (`.eyebrow`, `.lede`) confirmed and kept.
- **Silent `depends_on` degradation, closed** (`18766ab`): a wrong-shaped `depends_on` now records
  exactly one warning naming the plan and the field, while the D-12 never-throw and D-10
  well-formed-dangling-reference-silence contracts are both preserved.
- **`in-memory-fs.ts:5` imports `node:fs`, closed** (`7c00ebc`, `575c3b0`): the test-only loader
  moved to `test/helpers/from-directory.ts`; `local-fs.ts` is again the sole `src/` importer of
  the Node filesystem module, enforced by a durable, comment-aware gate with a positive control.
- **01-REVIEW IN-01/IN-02, closed** (`13cea79`, `21b24ca`): duplicated title-derivation logic
  collapsed (confirmed byte-identical first); `package.json` now carries a measured
  `engines.node: >=22.18.0`.
- **Vite chunk-size warning, closed** (`51804f6`): the entry chunk was real, fixable bloat —
  route-level `React.lazy()` dropped it from 571.58 kB to 388.75 kB, entirely under the default
  limit. The remaining over-limit chunk is mermaid's own shared internals, already behind
  mermaid's dynamic-import boundary and not further splittable without fighting mermaid's own
  module graph; `chunkSizeWarningLimit` raised to 680 with a measured, dated justification. `npm
  run build` now emits no advisory line.

### Accepted risk — reconciled, disposition unchanged

**R-02-01** (medium, below the `high` block threshold): the 02-06 gate closed on an explicit
human decision, and D3 carried `status: unknown`. **Corrected 2026-09-10** (`beb291c`): the
mitigating chain was miscited as `02-09 → 02-13 → 02-17` — `02-09` carries `status: superseded`
and its UAT round was never approved (10 measured gaps, per `02-UAT.md`); it produced the
gap-closure plans `02-10`..`02-13`, it did not pass evidence. The real chain is `02-13 → 02-17`
(the latter approved). All eight of D3's named surfaces are now individually mapped to `02-13`/
`02-17` evidence at `02-06-SUMMARY.md`. R-02-01 stays **accepted**, still non-blocking;
`threats_open` stays 0.

---

*Audited: 2026-09-09T18:45:00Z*
*Auditor: Claude (gsd-audit-milestone) with gsd-integration-checker*
