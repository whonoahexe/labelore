---
phase: 04-portability-degradation-hardening
plan: 05
type: execute
wave: 5
gap_closure: true
depends_on: ["04-01", "04-03", "04-04"]
files_modified:
  - src/server/index.ts
  - src/web/pages/artifact-page.tsx
  - test/server/artifact-response.test.ts
  - test/server/derived-snapshot-isolation.test.ts
  - test/web/degradation-ui-contract.test.ts
autonomous: true
requirements: [TGT-06, TGT-08]

estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 3
  confidence: low

must_haves:
  truths:
    # --- Gap 1 (D-12, plan 04-03) — the failed truth reproduced verbatim, then strengthened. ---
    - "One vocabulary spans all three surfaces: 'Warning' when the body survived, 'Unreadable' when nothing was salvageable, derived by a single shared function rather than three independent rules (D-12)."
    - "The artifact page is demonstrably one of those three surfaces: it imports `artifactWarningTone` from src/presentation/artifact-warning-tone.ts and uses that one return value for both the header badge and the warning-disclosure summary, so no branch on that page can label a damaged artifact by a rule of its own (D-12, TGT-06)."
    - "The single-artifact server response carries the input the shared function needs: GET /api/documents and GET /api/artifacts/* return `artifact.bodyLength` alongside `artifact.warnings`, so the artifact page computes the same tone the tree and search rows already show for that same file (D-12, TGT-06)."
    # --- Gap 2 (D-05, plan 04-01) — the failed truth reproduced verbatim, then strengthened. ---
    - "A successful refresh replaces the presentation, artifact index, reference registry and search index together in one assignment; no response ever mixes fields from two different snapshots (D-05)."
    - "A GET /api/artifacts/* or GET /api/documents response whose lifecycle spans a concurrently completing POST /api/refresh is built end to end from the bundle captured when that request started: its artifact record, its phaseIdentity and its rendered document's resolved cross-references all come from that one bundle (D-05, TGT-08)."
    # --- Edge-probe fallback, newly-exposed surface only (see <edge_coverage> for what is already covered). ---
    - "A GET whose rendering spans a completing refresh resolves its cross-references against the pre-refresh reference registry: an inline-code artifact-path reference that resolved when the request started still renders as a resolved reference control, never as bare text because the post-refresh registry no longer knows that path (edge probe: TGT-06 concurrency, GET-racing-refresh)."
    - "The Warning/Unreadable split sits exactly at bodyLength 0: a warned artifact whose bodyLength is 0 is labelled 'Unreadable' and a warned artifact whose bodyLength is 1 is labelled 'Warning', on the artifact page and in the tree and search rows alike (edge probe: TGT-08 adjacency, zero-versus-one-byte body)."
    - "An artifact carrying no warnings shows no badge on the artifact page whatever its body length — a zero-length body that parsed cleanly is absence, not damage, and must not acquire the Unreadable label (edge probe: TGT-08 empty)."
    - statement: "Every response served by GET /api/artifacts/* and GET /api/documents is built from exactly one derived bundle for its whole lifecycle: the handler captures the bundle before its first await, and no code path in the response builder re-reads the module-level binding afterwards."
      verification: backstop
    # --- UI-SPEC `## UI Considerations` lift, scoped to the artifact-page badge/disclosure surface. ---
    - "The artifact-page badge stays a fixed-width `.status-chip` in both tones — the longer 'Unreadable' label introduces no new overflow surface in the `.artifact-heading` row and needs no scroll container of its own (UI Considerations overflow/damaged-artifact-indicator)."
    - "An artifact whose body did not survive still renders as a normal, navigable artifact page — marked with the Unreadable badge and its existing warning disclosure, never blanked, replaced by an error screen, demoted, or dropped (UI Considerations partial/damaged-artifact-indicator)."
  prohibitions:
    - statement: "MUST NOT present a damaged artifact under a tone that overstates what survived — an artifact whose body was entirely lost must never carry the same label as one that merely lost a frontmatter field, on any surface a reader can open."
      status: flagged
      verification: unverified
    - statement: "MUST NOT close either gap by narrowing the claim — the atomicity and shared-vocabulary guarantees must be made true in the code, never satisfied by rewording a must-have down to the narrower case that already passes."
      status: flagged
      verification: unverified
  artifacts:
    - path: "src/server/index.ts"
      provides: "artifactResponse() forwarding bodyLength and receiving the request's captured derived bundle as a parameter"
      contains: "activeDerived"
    - path: "src/web/pages/artifact-page.tsx"
      provides: "The header badge and disclosure summary both driven by the shared artifactWarningTone() return value"
      contains: "artifactWarningTone"
    - path: "test/server/artifact-response.test.ts"
      provides: "Server-route proof that the single-artifact payload carries bodyLength and that the Unreadable branch is reachable from that payload"
    - path: "test/server/derived-snapshot-isolation.test.ts"
      provides: "Proof that a GET spanning a completing refresh renders against the bundle captured at request start"
    - path: "test/web/degradation-ui-contract.test.ts"
      provides: "D-12 assertion extended so the artifact page's tone is computed, not a static literal"
  key_links:
    - from: "src/web/pages/artifact-page.tsx"
      to: "src/presentation/artifact-warning-tone.ts"
      via: "single shared tone derivation, the same one tree.ts and search.ts call"
      pattern: "artifactWarningTone"
    - from: "src/server/index.ts"
      to: "src/server/project-presentation.ts"
      via: "artifact.bodyLength forwarded on the single-artifact response, the field ArtifactDto already carries"
      pattern: "bodyLength"
    - from: "src/server/index.ts (/api/artifacts/*, /api/documents)"
      to: "src/server/index.ts (artifactResponse)"
      via: "the request's captured bundle threaded as an argument instead of re-read mid-request"
      pattern: "activeDerived"
---

<!-- planner-discipline-allow: data-tone -->
<!-- `data-tone` is unavoidable vocabulary for this surface: it is the attribute name the existing
     .status-chip rules key on. The negative gate in Task 1 targets only the *quoted-literal* form of
     that attribute in artifact-page.tsx, which appears nowhere in any <action> body below. -->

<objective>
Close the two must-haves phase 4 verification found FAILED, and nothing else.

Gap 1 (D-12, blocking): the artifact page — the one page a reader opens to find out how badly a file
is damaged — hard-codes the Warning tone and label, and the server never sends it the `bodyLength`
the shared `artifactWarningTone()` needs. A completely unreadable artifact is visually identical
there to one that merely lost a frontmatter field.

Gap 2 (D-05, non-blocking but real): `/api/artifacts/*` and `/api/documents` each read the live
module-level derived bundle twice — once synchronously for the lookup, once again after an await for
the reference registry — so a refresh completing on another connection in between yields one response
built from two different snapshots.

Purpose: make the two truths the phase already claimed actually true, with tests that fail on the
current code.
Output: `bodyLength` on the single-artifact response, the shared tone wired into both artifact-page
surfaces, one bundle captured per request and threaded through, and three test files pinning all of
it — with the existing 533-test suite still green.

Explicitly out of scope (real findings, left visible in 04-REVIEW.md for follow-up, deliberately not
folded in here): WR-02 (concurrent refreshes duplicating `buildDerivedViews()` work) and IN-01
(`src/web/components/ui/toast.tsx`'s unconditional destructive tone). Neither violates a phase
must-have; touching them here would widen a gap-closure run into a refactor.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-portability-degradation-hardening/04-VERIFICATION.md
@.planning/phases/04-portability-degradation-hardening/04-CONTEXT.md
@.planning/phases/04-portability-degradation-hardening/04-PATTERNS.md
</context>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Every symbol, file and signature this gap-closure plan creates or changes. Nothing here exists on
`master` before this plan runs; source-grounding passes should treat all of it as new.

**Changed signature (module-local closure in `src/server/index.ts`)**

- `artifactResponse(lookup)` becomes `artifactResponse(lookup, activeDerived: DerivedViews)` — a
  second required parameter carrying the bundle captured at request start.

**New response field (wire format of `GET /api/documents` and `GET /api/artifacts/*`)**

- `artifact.bodyLength: number` — forwarded from `lookup.artifact.bodyLength`. Additive; no existing
  field changes name, type, or meaning.

**New local bindings**

- `const activeDerived` in the `/api/artifacts/*` handler and in the `/api/documents` handler
  (`src/server/index.ts`).
- `const warningTone` in `ArtifactPage` (`src/web/pages/artifact-page.tsx`), typed
  `ArtifactWarningTone`.

**New imports**

- `artifactWarningTone` (and the `ArtifactWarningTone` type) imported into
  `src/web/pages/artifact-page.tsx` from `../../presentation/artifact-warning-tone.ts`.

**Changed local interface**

- `ArtifactDocumentResponse['artifact']` in `src/web/pages/artifact-page.tsx` gains
  `bodyLength: number`.

**New files**

- `test/server/artifact-response.test.ts`
- `test/server/derived-snapshot-isolation.test.ts`

**Changed test file**

- `test/web/degradation-ui-contract.test.ts` — the D-12 case is rewritten (see Task 1).

No new production file, no new export from any existing module, no dependency added.
</artifacts_this_phase_produces>

<edge_coverage>
## Edge & consideration provenance (spec-less fallback, §C lift)

No plain `-SPEC.md` exists for this phase, so the `## Edge Coverage` / `## Prohibitions` lift takes
its else-branch over the orchestrator's deterministic probe report. Ten items were surfaced; every
one is accounted for below — none silently dropped.

**Authored into `must_haves.truths` as newly-exposed surface (3 explicit + 1 backstop):**

| Probe item | Disposition |
|---|---|
| TGT-06 `concurrency` — "if interrupted or run in parallel, what is guaranteed?" | The refresh-versus-refresh half is already carried by executed plan 04-01. The half these gaps newly expose — **a GET racing a completing refresh** — is authored explicit. |
| TGT-08 `adjacency` — "when two things are exactly equal or just touch, do they merge, collide, or separate?" | The same-millisecond-readAt half is already carried by 04-01. The half these gaps newly expose — **body length exactly 0 versus exactly 1** — is authored explicit. |
| TGT-08 `empty` — "what is the result for empty, single-element, or null input?" | The empty-source-refresh half is already carried by 04-01. The half these gaps newly expose — **an artifact with an empty warnings array and a zero-length body** — is authored explicit. |
| (general D-05 atomicity across every route and every await point) | Authored **backstop** — no single test can enumerate every handler/await pair, so this abstains to `human_needed` rather than passing silently on partial evidence. |

**Already covered by an executed plan — recorded, not re-authored (duplicate coverage is not
closure):** TGT-06 concurrency (refresh-versus-refresh), TGT-08 adjacency (same-millisecond readAt),
TGT-08 empty (refresh against a source with nothing to refresh into), and TGT-08 `ordering` (stable
tree/presentation payloads across refreshes) each already appear verbatim in `04-01-PLAN.md`'s
`must_haves.truths`, tagged `edge probe:`, and are proven by `test/server/refresh.test.ts`.

**Out of scope for this gap-closure run — left to the executed plans that own them (visible
non-drop, never a silent omission):** the TGT-03 `boundary` and `precision` items, the TGT-04 and
TGT-05 `unclassified` items, and the TGT-07 `empty` and `encoding` items belong to requirements
verification already marked `✓ SATISFIED` and covered by executed plans 04-02 and 04-04; a
gap-closure plan re-claiming them would fabricate coverage it did not add.

**Flagged planner assumptions (unresolved, surfaced rather than dropped):** the two `unclassified`
probe rows (TGT-04, TGT-05) were never classified into an edge family by the probe engine and are
therefore not resolvable here; they remain unresolved and out of this run's scope, as recorded above.

## UI Considerations lift (`04-UI-SPEC.md`)

Scoped to considerations bearing on the artifact-page badge/disclosure surface this run touches.

- `overflow / damaged-artifact-indicator` (✅ covered) and `partial / damaged-artifact-indicator`
  (✅ covered) now extend to the artifact page's own badge for the first time — the `Unreadable`
  label is longer than `Warning`, and the fully-unreadable page is the "partial data" case. **Both
  authored into `must_haves.truths` above.**
- `long-text / warning-badge-disclosure` and `overflow / warning-badge-disclosure` (✅ covered) are
  already satisfied by executed plan 04-03's `overflow-wrap: anywhere` rules, pinned by
  `test/web/degradation-ui-contract.test.ts`'s globals.css case — already covered, not re-authored.
- `error / warning-badge-disclosure` and `error / damaged-artifact-indicator` are dismissed in the
  UI-SPEC; `loading / invalid-project-screen` (🧪 backstop) and `overflow / section-empty-state`
  (🧪 backstop) bear on surfaces plans 04-02 and 04-04 own and this run does not touch.
</edge_coverage>

<assumption_delta_decision>
The deterministic detector fired `detected: true`, one `optional`-family signal: the roadmap's
"Removing an **optional** artifact or directory …".

- **Primary noun:** *a recognized location group that may be absent* — already the generalized
  identity. `src/presentation/tree.ts` emits every entry in discovery's `LOCATION_ORDER` whether or
  not it holds files, and `EmptyState` renders the one generic message at every such site.
- **Decision:** `no-change`. The promotion this signal asks about happened in phases 3 and 4 already;
  this gap-closure run introduces no new optional/required transition — it adds one always-present
  numeric field to an existing response and threads an existing bundle through an existing function.
- Advisory only; nothing here blocks the phase.
</assumption_delta_decision>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: End-to-end "Unreadable" — one damaged artifact, server to badge</name>

  <reversibility rating="reversible">Adding one numeric field to a response consumed only by this
  app's own client, and replacing two hard-coded JSX literals with a call to a function that already
  ships. Undo is a two-file revert with no migration and no external contract.</reversibility>

  <precondition>`src/presentation/artifact-warning-tone.ts` exports `artifactWarningTone` (shipped by
  executed plan 04-03) — `grep -c 'export function artifactWarningTone' src/presentation/artifact-warning-tone.ts`
  returns 1.</precondition>

  <files>src/server/index.ts, src/web/pages/artifact-page.tsx, test/server/artifact-response.test.ts, test/web/degradation-ui-contract.test.ts</files>

  <read_first>
    - `src/server/index.ts` — lines 77-101 (`artifactResponse`, the returned `artifact` object literal
      that omits the field) and lines 155-191 (the two handlers that call it).
    - `src/web/pages/artifact-page.tsx` — lines 27-43 (the local `ArtifactDocumentResponse`
      interface), and the render region from `const hasWarnings` through the end of the warning
      disclosure (the header badge and the disclosure summary are the two sites to change).
    - `src/presentation/artifact-warning-tone.ts` — the whole file. It is 15 lines; its signature
      `artifactWarningTone(artifact: { warnings: unknown[]; bodyLength: number }): ArtifactWarningTone`
      is the contract this task must honor exactly, not re-derive.
    - `src/web/components/tree-navigator.tsx` — the `WarningIndicator` component at the top of the
      file. This is the *correct existing call site*: its conditional shape for the tone attribute and
      the visible label is the shape the artifact page must mirror, so all three surfaces read the same.
    - `src/presentation/tree.ts` line 183 — the other correct call site, showing the function is
      called with the artifact object itself.
    - `src/server/project-presentation.ts` lines 120-141 — `ArtifactDto` already carries
      `bodyLength` with a doc comment naming this exact purpose. Nothing in that file changes.
    - `test/server/refresh.test.ts` lines 1-80 — the `makeArtifact` / `makeProject` / `makeSnapshot`
      helpers and the `createApp(source, true, '.')` + `app.request(...)` idiom the new test file
      reuses verbatim rather than inventing a second server-test style.
  </read_first>

  <behavior>
    New file `test/server/artifact-response.test.ts`, driving the real `createApp` via `app.request`:
    - A warned artifact with `bodyLength: 0`, fetched through `GET /api/documents`, returns an
      `artifact` object whose `bodyLength` is `0` and whose `warnings` array has length 1; feeding
      that returned object straight into the imported `artifactWarningTone` yields `'unreadable'`.
      This is the reachability proof: the payload the page consumes now produces the Unreadable
      branch. (Today the field is absent and the expression is unwritable — this test fails on the
      current code.)
    - A warned artifact with `bodyLength: 1` returns `bodyLength: 1`, and the same expression yields
      `'warning'` — the split is exactly at zero, not at "some small size" (TGT-08 adjacency).
    - An artifact with an empty `warnings` array and `bodyLength: 0` returns `bodyLength: 0`, and the
      same expression yields `null` — clean-but-empty is not damage (TGT-08 empty).
    - `GET /api/artifacts/*` returns the same `bodyLength` for the same artifact as `GET /api/documents`
      does, since both are served by the one response builder.
    Route resolution in the test: request `GET /api/presentation`, find the artifact by `path`, and
    use its `key` as the `route` query parameter — do not hand-construct the artifact URL token.
  </behavior>

  <action>
Forward the missing field, then compute the tone on the page from it.

**Server (`src/server/index.ts`).** In the object literal `artifactResponse` returns, add
`bodyLength: lookup.artifact.bodyLength` to the nested `artifact` object, alongside the existing
`warnings` entry. `lookup.artifact` is a domain `Artifact`, which already carries `bodyLength` — this
is a pass-through, not a computation. Change nothing else in that function in this task.

**Page (`src/web/pages/artifact-page.tsx`).** Add `bodyLength: number` to the `artifact` member of
the local `ArtifactDocumentResponse` interface. Import `artifactWarningTone` (and the
`ArtifactWarningTone` type if you annotate) from `../../presentation/artifact-warning-tone.ts`.
Replace the `hasWarnings` boolean with a single computed `warningTone`, calling the shared function
once with `warnings: [...artifact.warnings, ...document.warnings]` and
`bodyLength: artifact.bodyLength`. Combining both warning arrays preserves the existing D-10 rule
that a document-level warning also earns a badge; the function's parameter type is `unknown[]`, so
the mixed array typechecks. Then use that one value at **both** sites — the header badge beside the
`eyebrow` kind label, and the outer `summary` of the warning disclosure — for the tone attribute and
for the visible label, mirroring `WarningIndicator`'s conditional shape in `tree-navigator.tsx`
exactly: the destructive tone with the label `Unreadable` when the tone is `'unreadable'`, otherwise
the warning tone with the label `Warning`; render neither element when the tone is `null`. Write
both conditionals **inline against the `warningTone` binding itself**, exactly as `search-page.tsx`
lines 66-70 already write them against `row.warningTone` — a pair of ternaries whose test is the
tone compared to `'unreadable'`, one supplying the tone attribute and one supplying the label. Do
not route either site through an extracted sub-component with a renamed prop, and do not introduce a
second tone binding: one `const warningTone`, referenced at both sites, is what makes the two
surfaces provably agree. Neither
site may keep a statically-quoted tone attribute — the attribute value must come from the computed
tone at both. The badge stays a `.status-chip` at both sites; do not add a wrapper element, change
its class, or alter the disclosure's structure.

**Contract test (`test/web/degradation-ui-contract.test.ts`).** Rewrite the D-12 case's
artifact-page assertions (currently the two page-level assertions in the "one Warning/Unreadable
vocabulary spans …" test) so they prove the tone is *computed* rather than that a literal is
present: the page source must import `artifact-warning-tone.ts`, must call `artifactWarningTone(`,
must contain both `'Warning'` and `'Unreadable'` as quoted labels (the same assertions this test
already makes against `tree-navigator.tsx`), and must contain no statically-quoted tone attribute at
all — assert instead that every `data-tone` occurrence in that file is expression-valued.

Then add the **mapping assertion**, which is what makes the case fail on a swapped pairing rather
than merely on missing wiring. Strip comments from the page source first
(`page.replace(/\/\*[\s\S]*?\*\//g, '').replace(/^\s*\/\/.*$/gm, '')`) so prose in a code comment
can never satisfy or break a count. Over that stripped source, collect every ternary whose test is
the tone binding compared to the unreadable literal and whose two arms are quoted strings —
`/warningTone\s*===\s*'unreadable'\s*\?\s*'([^']*)'\s*:\s*'([^']*)'/g` — and assert on the
`consequent|alternate` pairs it yields: exactly four matches in all, of which exactly two are
`Unreadable|Warning` and exactly two are `destructive|warning`. Then close the escape hatches: the
stripped source contains exactly two `'Unreadable'` and exactly two `'Warning'` quoted literals (so
neither label can be rendered outside that guard), and exactly one `const warningTone` declaration
(so both sites read the same binding). This regex is the shape `search-page.tsx` already satisfies
verbatim — running it over that file today yields exactly `destructive|warning` and
`Unreadable|Warning` — so a faithful mirror passes by construction.

Leave the tree and search assertions in that test untouched, and leave every other test in the file
untouched.

**New test file** `test/server/artifact-response.test.ts` — implement exactly the cases in
`<behavior>` above, reusing the helper shapes from `test/server/refresh.test.ts`.
  </action>

  <acceptance_criteria>
    - Behavior: `GET /api/documents?route={key}` for an artifact with one warning and `bodyLength: 0`
      returns an `artifact` object containing `bodyLength: 0`; today that key is absent from the
      payload entirely.
    - Behavior: passing that returned `artifact` object to `artifactWarningTone` returns
      `'unreadable'`; passing the same shape with `bodyLength: 1` returns `'warning'`; passing one
      with an empty `warnings` array returns `null`.
    - Behavior: `GET /api/artifacts/*` and `GET /api/documents` return the same `bodyLength` value
      for the same artifact.
    - Source: `src/web/pages/artifact-page.tsx` contains an import from
      `../../presentation/artifact-warning-tone.ts` and at least one call to `artifactWarningTone(`.
    - Source: `src/web/pages/artifact-page.tsx` contains both `'Warning'` and `'Unreadable'` as
      quoted string literals.
    - Source: no `data-tone` attribute in `src/web/pages/artifact-page.tsx` is written with a quoted
      literal value — a regex for the double-quote form of that attribute matches zero times, and the
      count of expression-valued occurrences is exactly 2 (header badge, disclosure summary).
    - Source (mapping, not co-presence): over `src/web/pages/artifact-page.tsx` with comments
      stripped, the guard-anchored regex
      `/warningTone\s*===\s*'unreadable'\s*\?\s*'([^']*)'\s*:\s*'([^']*)'/g` yields exactly four
      matches, of which exactly two have the `consequent|alternate` pair `Unreadable|Warning` and
      exactly two have the pair `destructive|warning`. A swapped label mapping
      (`? 'Warning' : 'Unreadable'`) drops the first count to 0; a swapped tone mapping drops the
      second to 0. Both directions of the mapping are therefore pinned, and the label is bound to
      its guarding condition rather than merely co-present in the file.
    - Source (both sites, one binding): in that same comment-stripped source, `'Unreadable'` appears
      exactly twice and `'Warning'` appears exactly twice — every occurrence of either label is one
      of the four guarded arms above, so neither label can be rendered outside the guard — and
      `const warningTone` is declared exactly once, so the header badge and the disclosure summary
      provably read the same tone value. If either site keeps a hard-coded literal, or the two sites
      disagree on the tone pairing, the counts fall to 1 and the criterion fails.
    - Source (satisfiability, already checked against live source): running that same regex over
      `src/web/pages/search-page.tsx` today yields exactly `destructive|warning` and
      `Unreadable|Warning` — the existing correct inline call site, shaped identically to
      `WarningIndicator` in `tree-navigator.tsx`. A faithful mirror on the artifact page satisfies
      the two criteria above by construction; the current pre-task file yields zero matches.
    - Command: `npm test -- --run test/server/artifact-response.test.ts test/web/degradation-ui-contract.test.ts`
      exits 0 with both files reporting passing tests.
    - Command: `npm run typecheck && npm run lint` exits 0.
  </acceptance_criteria>

  <verify>
    <automated>npm test -- --run test/server/artifact-response.test.ts test/web/degradation-ui-contract.test.ts</automated>
    <fails_when>non-zero exit, or the summary reports fewer than 2 test files, or the output contains "No test files found"</fails_when>
    <automated>npm run typecheck && npm run lint</automated>
    <fails_when>non-zero exit, or any line containing "error TS" appears in the output</fails_when>
  </verify>

  <done>
The artifact page derives its damaged-artifact label from the same function the tree and search rows
use, on data the server now actually sends. A file whose body did not survive reads `Unreadable`
there; one that merely lost a frontmatter field still reads `Warning`; a clean file carries no badge.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: One bundle per request — capture the derived views before the first await</name>

  <reversibility rating="reversible">Adding a parameter to a module-local closure with two call
  sites in one file. Nothing outside `src/server/index.ts` observes the signature.</reversibility>

  <files>src/server/index.ts, test/server/derived-snapshot-isolation.test.ts</files>

  <read_first>
    - `src/server/index.ts` — lines 38-45 (`interface DerivedViews`), lines 72-101 (the mutable
      bundle binding, the in-flight guard, and `artifactResponse`, whose registry read happens after
      an await), lines 155-191 (the two handlers whose lookup is synchronous), and lines 192-236
      (the refresh handler whose one-line reassignment is the swap the race straddles).
    - `test/server/refresh.test.ts` — the whole file. The `makeSpySource` helper and the
      `control.resolve` pattern in the "coalesces two concurrent calls" test are the idioms this new
      file reuses; its final test also shows the deep-equal payload-comparison style.
    - `src/rendering/linkify.ts` — the `inlineCodeReference` function and the `control()` helper it
      calls. This is the observable signal the new test keys on: a standalone inline-code node whose
      exact content is a registered artifact path becomes a button carrying a reference-key data
      attribute. Remove that artifact from the registry and the same body renders as bare text.
    - `test/rendering/references.test.ts` — the assertions that count reference-key attributes in
      rendered html; reuse that assertion vocabulary rather than inventing a new one.
    - `.planning/phases/04-portability-degradation-hardening/04-REVIEW.md` §WR-01 — the exact fix
      shape, already written out by the reviewer.
  </read_first>

  <behavior>
    New file `test/server/derived-snapshot-isolation.test.ts`:
    - **The race case (this test fails on the current code — it has been run against it).** Build a
      pre-refresh snapshot holding two artifacts: `.planning/PROJECT.md`, whose body is a line
      containing `.planning/NOTES.md` as a standalone inline-code span, and `.planning/NOTES.md`
      itself. Build a post-refresh snapshot holding only `.planning/PROJECT.md`. Start
      `app.request('/api/documents?route=…')` for `.planning/PROJECT.md` **without awaiting it**,
      then `await` a full `POST /api/refresh` that swaps in the post-refresh snapshot, then await the
      GET. Assert the GET's `document.html` still contains a reference-key attribute — it was
      resolvable in the bundle current when the request started, and that is the bundle its whole
      response must come from. On the current code this assertion fails: the html is bare
      `<p>See <code>.planning/NOTES.md</code> for detail.</p>`.
    - **The control case, proving the signal is not vacuous.** The same document served from a
      snapshot that never contained `.planning/NOTES.md` renders with no reference-key attribute at
      all. Without this, the race assertion could pass for the wrong reason.
    - **The bundle-threading contract.** Read `src/server/index.ts` as text, slice the region from
      the `artifactResponse` declaration to the first route registration that follows it, and assert
      that region reads its registry from the captured-bundle parameter and contains no property
      access on the module-level binding. Pair this with the two behavior cases above; it is a
      region-scoped source assertion and is not the primary proof on its own.
    - The race case must be the **first** test in this file, and this file must contain no other
      document-rendering test before it.
  </behavior>

  <action>
Give each request its own view of the world, once.

**`src/server/index.ts`.** Add a second required parameter to `artifactResponse` —
`activeDerived: DerivedViews` — and have the render call take its `referenceRegistry` from that
parameter rather than from the module-level bundle binding. After this change the body of
`artifactResponse` must not touch the module-level binding at all.

In the `/api/artifacts/*` handler, capture `const activeDerived = derived;` as the first statement of
the handler, before the URL parsing and before the lookup, and use `activeDerived.artifactIndex.lookup(...)`
and `artifactResponse(lookup, activeDerived)`. Apply the identical pattern to the `/api/documents`
handler around `lookupRoute` — capture first, then look up and respond from the captured bundle.
Both handlers become fully single-bundle: the value read at the top of the request is the only one
that request ever sees.

Do not change `buildDerivedViews`, the in-flight coalescing guard, the refresh handler, or any other
route. In particular, leave the duplicated `buildDerivedViews()` work on concurrent refreshes
(04-REVIEW.md WR-02) exactly as it is — it is real, it is out of scope for this run, and it stays
visible in the review for follow-up.

**New test file** `test/server/derived-snapshot-isolation.test.ts` — implement exactly the four
cases in `<behavior>` above. Use `createApp(source, true, '.')`, and resolve the document route by
reading `GET /api/presentation` and taking the artifact's own `key` field.
  </action>

  <acceptance_criteria>
    - Behavior: a `GET /api/documents` request started before a `POST /api/refresh` that removes
      `.planning/NOTES.md` still returns html containing a reference-key attribute for the inline-code
      path reference. This assertion has been executed against the current code and fails there, so it
      is a real regression net rather than a restatement.
    - Behavior: the control case — same document, a snapshot that never held `.planning/NOTES.md` —
      returns html containing no reference-key attribute, proving the assertion above cannot pass
      vacuously.
    - Source: in the region of `src/server/index.ts` from the `artifactResponse` declaration to the
      next route registration, the registry is read from the `activeDerived` parameter and no
      property is read off the module-level bundle binding.
    - Source: both the `/api/artifacts/*` and the `/api/documents` handlers contain
      `const activeDerived = derived;` before their lookup call, and pass `activeDerived` into
      `artifactResponse`.
    - Source: `buildDerivedViews`, the in-flight guard, and the `POST /api/refresh` handler are
      byte-identical to their pre-task state.
    - Command: `npm test -- --run test/server/derived-snapshot-isolation.test.ts test/server/refresh.test.ts`
      exits 0 — the new isolation file passes and every pre-existing refresh test still passes.
    - Command: `npm run typecheck && npm run lint` exits 0.
  </acceptance_criteria>

  <verify>
    <automated>npm test -- --run test/server/derived-snapshot-isolation.test.ts test/server/refresh.test.ts</automated>
    <fails_when>non-zero exit, or the summary reports fewer than 2 test files, or the output contains "No test files found"</fails_when>
    <automated>npm run typecheck && npm run lint</automated>
    <fails_when>non-zero exit, or any line containing "error TS" appears in the output</fails_when>
  </verify>

  <done>
A response to `/api/artifacts/*` or `/api/documents` is built from exactly one derived bundle for its
whole lifecycle. A refresh landing mid-request changes what the *next* request sees, never what this
one is already rendering.
  </done>
</task>

<task type="auto">
  <name>Task 3: Whole-phase regression gate — the suite, the types, the build, the smoke probe</name>

  <files>src/server/index.ts, src/web/pages/artifact-page.tsx — no edits expected; fix forward in these two files only if a gate below fails</files>

  <read_first>
    - `package.json` — the `test`, `typecheck`, `lint`, `build` and `smoke` scripts, so the commands
      below are run as declared rather than improvised.
    - `.planning/phases/04-portability-degradation-hardening/04-VERIFICATION.md` — the "Behavioral
      Spot-Checks" table records `npx vitest run` at 36 files / 533 tests / all passed at verification
      time. That count is the floor this task defends; Tasks 1 and 2 add files and tests, so the
      number may only go up.
  </read_first>

  <action>
Run the four gates below and hold the line the phase already had.

The full suite must stay green at or above its verification-time floor: 533 passing tests across 36
files, plus whatever Tasks 1 and 2 added. A *lower* count means a file stopped being collected — that
is a silent regression, not a pass, and it is the specific failure this task exists to catch.

If any gate fails, fix forward in `src/server/index.ts` or `src/web/pages/artifact-page.tsx` only.
Do not weaken, skip, or delete an assertion to make a gate pass; do not touch the plans, summaries,
or tests belonging to 04-01 through 04-04 beyond the one D-12 case Task 1 rewrites.
  </action>

  <acceptance_criteria>
    - Command: `npx vitest run` exits 0 and its summary reports at least 533 passing tests across at
      least 36 files, with a failed count of 0.
    - Command: `npm run typecheck && npm run lint` exits 0 with no `error TS` line.
    - Command: `npm run build && npm run smoke` exits 0 and prints a line beginning
      `GSD Lore smoke passed for`.
    - Source: `git status --porcelain` shows changes confined to the five paths in this plan's
      `files_modified` — no executed plan's PLAN.md or SUMMARY.md is modified.
  </acceptance_criteria>

  <verify>
    <automated>npx vitest run</automated>
    <fails_when>non-zero exit, or a summary line reporting fewer than 533 passed, or a failed count above 0</fails_when>
    <automated>npm run typecheck && npm run lint</automated>
    <fails_when>non-zero exit, or any line containing "error TS" appears in the output</fails_when>
    <automated>npm run build && npm run smoke</automated>
    <fails_when>non-zero exit, or stdout with no line beginning "GSD Lore smoke passed for"</fails_when>
    <automated>git status --porcelain -- .planning/phases/04-portability-degradation-hardening</automated>
    <fails_when>output naming any of 04-01-PLAN.md, 04-02-PLAN.md, 04-03-PLAN.md, 04-04-PLAN.md or any *-SUMMARY.md as modified</fails_when>
  </verify>

  <done>
Both gaps are closed, the pre-existing 533-test suite is still green, types and lint are clean, the
production bundle builds, the smoke probe passes, and no executed plan's artifacts were disturbed.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| target project filesystem → parser | Markdown, YAML and JSON from an arbitrary GSD project the tool did not author. `ParseWarning` records — including `path` and `message` — originate here and reach the artifact page. |
| browser → loopback Hono API | The only network boundary. Bound to `127.0.0.1`; `POST /api/refresh` already carries the same-origin gate added in 04-01. This plan adds no route and no new request shape. |

## STRIDE Threat Register

ASVS level 1; blocking threshold `high`. No threat below is rated high or critical, so no blocking
checkpoint is inserted.

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-05-01 | Information Disclosure | `artifactResponse()`'s new `artifact.bodyLength` field | low | accept | `bodyLength` is a non-sensitive integer already carried by `ArtifactDto` to `/api/tree` and `/api/search` for every artifact. Forwarding it on the single-artifact response exposes no datum the same client cannot already read, on a loopback-bound read-only server. |
| T-04-05-02 | Tampering | Badge tone and label rendered from project-supplied warning data in `artifact-page.tsx` | medium | mitigate | The tone is a closed union (`'warning'`/`'unreadable'`/`null`) computed from array length and a number — never interpolated from artifact-supplied text. The label is one of two fixed literals. The existing degradation-ui-contract case pinning the absence of raw-HTML injection in the disclosure region stays untouched by Task 1. |
| T-04-05-03 | Denial of Service | Per-request capture of the derived bundle in the two artifact handlers | low | accept | Capturing the bundle holds one extra reference to a superseded bundle for the lifetime of an in-flight request, bounded by concurrent request count on a single-user loopback server. Strictly smaller than the pre-existing retention of the bundle itself. |
| T-04-05-SC | Tampering | npm/pip/cargo installs | low | accept | This plan installs no package and adds no dependency — every symbol it uses already ships in the repo. The Package Legitimacy Gate has no install task to gate; no `[ASSUMED]`/`[SUS]` package exists to verify. |
</threat_model>

<verification>
Phase-level checks after all three tasks:

1. `npx vitest run` — at least 533 passing tests, zero failures, at least 36 files.
2. `npm run typecheck && npm run lint` — clean.
3. `npm run build && npm run smoke` — the smoke probe prints its pass line.
4. The two failed verification truths are now provable by a test that fails on the pre-task code:
   the Unreadable branch is reachable from the payload the artifact page consumes (Task 1), and a GET
   spanning a completing refresh renders against the bundle captured at request start (Task 2).
5. `04-01` through `04-04` PLAN.md and SUMMARY.md files are unmodified.
</verification>

<success_criteria>
- `GET /api/documents` and `GET /api/artifacts/*` return `artifact.bodyLength`.
- `src/web/pages/artifact-page.tsx` imports and calls `artifactWarningTone`, and no `data-tone`
  attribute on that page carries a statically-quoted value.
- A warned artifact with a zero-length body is labelled `Unreadable` on the artifact page; one with a
  surviving body is labelled `Warning`; one with no warnings carries no badge.
- `artifactResponse` receives the request's bundle as a parameter and reads no property off the
  module-level binding; both artifact handlers capture that bundle as their first statement.
- The full suite, typecheck, lint, build and smoke all pass.
- WR-02 and IN-01 remain untouched and still visible in `04-REVIEW.md`.
</success_criteria>

<output>
Create `.planning/phases/04-portability-degradation-hardening/04-05-SUMMARY.md` when done.
</output>
