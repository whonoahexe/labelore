---
phase: 02-situational-awareness-artifact-reading
plan: 04
type: execute
wave: 4
depends_on: [02-03]
files_modified:
  - src/server/artifact-index.ts
  - src/rendering/markdown.ts
  - src/rendering/frontmatter-views.ts
  - src/server/index.ts
  - src/web/pages/artifact-page.tsx
  - src/web/app-router.tsx
  - test/rendering/markdown.test.ts
autonomous: true
requirements: [READ-01, READ-02, READ-03, READ-04, READ-06]

estimate:
  tokens: 59000
  raw_tokens: 59000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "[EDGE READ-01/adjacency] Adjacent table, task-list, blockquote, and fenced-code blocks remain separate nodes in source order even without an intervening prose paragraph."
    - "[EDGE READ-01/empty] An empty artifact body renders title, structured metadata, and an honest empty-document state without fabricating body content."
    - "[EDGE READ-01/ordering] The document preserves source block and row order while applying GFM and syntax highlighting."
    - "[EDGE READ-02/empty] Known frontmatter keys receive guarded structured panels, while an empty, unknown, or wrong-shaped value remains visible through the unconditional generic recursive view."
    - "[EDGE READ-03/empty] An empty recognized PLAN wrapper renders a labeled semantic section, and mismatched or unclosed wrappers render escaped literal source plus a local warning."
    - "[EDGE READ-03/encoding] Unicode nested markdown and allowlisted `type`, `gate`, and `tdd` attribute values survive PLAN segmentation as text; fenced pseudo-tags remain code."
    - "[EDGE READ-04/encoding] Encoded and mixed-case scripts, event attributes, unsafe schemes, iframes, and active SVG/MathML are absent from finalized HTML; only the sanitized string reaches the mount."
    - "[EDGE READ-06/adjacency] Duplicate headings receive deterministic distinct stable IDs, and copy controls bind to the correct adjacent heading without scroll-driven URL changes."
  artifacts:
    - path: "src/server/artifact-index.ts"
      provides: "Snapshot-only canonical artifact lookup with typed not-found results"
      exports: ["ArtifactIndex", "ArtifactLookupResult", "buildArtifactIndex"]
    - path: "src/rendering/markdown.ts"
      provides: "Sanitized GFM/Shiki/heading rendering pipeline"
      exports: ["RenderedDocument", "ArtifactRenderer", "createArtifactRenderer"]
    - path: "src/rendering/frontmatter-views.ts"
      provides: "Known-field builder registry with unconditional generic remainder"
      exports: ["FrontmatterPanel", "FrontmatterPanelBuilder", "FRONTMATTER_PANEL_BUILDERS", "buildFrontmatterPanels"]
    - path: "src/web/pages/artifact-page.tsx"
      provides: "Document-first artifact reader and sole sanitized HTML mount"
      exports: ["DocumentView", "ArtifactPage"]
    - path: "test/rendering/markdown.test.ts"
      provides: "PLAN/GFM/security/frontmatter/heading regression suite"
  key_links:
    - from: "src/rendering/markdown.ts"
      to: "src/rendering/plan-segments.ts"
      via: "only `kind === plan` bodies use the already-shared segmenter before Markdown conversion"
      pattern: "segmentPlanBody"
    - from: "src/server/index.ts"
      to: "src/server/artifact-index.ts"
      via: "artifact route resolves canonical tokens only against the current snapshot index"
      pattern: "buildArtifactIndex|lookup"
    - from: "src/web/pages/artifact-page.tsx"
      to: "src/rendering/markdown.ts"
      via: "DocumentView mounts only finalized sanitized HTML and runs selected strict Mermaid placeholders"
      pattern: "dangerouslySetInnerHTML|RenderedDocument"
---

<objective>
Make every artifact readable through one PLAN-aware, hostile-content-safe document pipeline before adding inferred cross-links.

Purpose: Implement D-09 through D-11 and D-16 at the artifact-to-DOM trust boundary, reuse the earlier PLAN segmenter, and ensure unknown artifact/frontmatter shapes remain visible.
Output: Snapshot artifact index, sanitized renderer, frontmatter registry, document-first ArtifactPage, and hostile/real-corpus regression suite.
</objective>

<execution_context>
@/home/cinedise/.codex/gsd-core/workflows/execute-plan.md
@/home/cinedise/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-situational-awareness-artifact-reading/02-CONTEXT.md
@.planning/phases/02-situational-awareness-artifact-reading/02-RESEARCH.md
@.planning/phases/02-situational-awareness-artifact-reading/02-PATTERNS.md
@src/domain/model.ts
@src/planning-repo/handlers/plan.ts
@src/planning-repo/handlers/index.ts
@.planning/phases/02-situational-awareness-artifact-reading/02-02-SUMMARY.md
@.planning/phases/02-situational-awareness-artifact-reading/02-03-SUMMARY.md

<interfaces>
Reuse `segmentPlanBody` and its semantic/checkpoint records from plan 02-02. `Artifact` supplies open kind/frontmatter, verbatim body, warnings, and structured data. Artifact URLs and tokens come only from `src/presentation/routes.ts`; route text never becomes a filesystem path.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Safe PLAN-aware Markdown rendering through the artifact API</name>
  <read_first>
    - `src/rendering/plan-segments.ts` — shared semantic PLAN scanner from plan 02-02
    - `src/planning-repo/handlers/plan.ts` — verbatim PLAN body handoff
    - `.planning/phases/01-read-layer-domain-model/01-04-PLAN.md` — real multi-task PLAN corpus
    - `.planning/phases/02-situational-awareness-artifact-reading/02-RESEARCH.md` — sanitizer order and security domain
  </read_first>
  <files>src/server/artifact-index.ts, src/rendering/markdown.ts, test/rendering/markdown.test.ts, src/server/index.ts</files>
  <behavior>
    - GFM tables, task lists, blockquotes, adjacent blocks, source ordering, and Shiki dual-theme code survive.
    - D-11 recognized PLAN wrappers become semantic sections through the existing `segmentPlanBody`; empty/malformed/fenced/Unicode cases degrade locally.
    - Raw HTML payloads lose executable scripts/events/unsafe schemes/active SVG and MathML before trusted heading/highlight enrichment.
    - Mermaid becomes a strict browser mount target; source above 256 KiB or invalid source remains escaped with an artifact-local warning.
    - Invalid artifact tokens return typed not-found and never reach filesystem APIs.
  </behavior>
  <action>Write real-corpus and hostile-payload tests first. Initialize one renderer/highlighter lifecycle in the load-bearing order `remarkParse -> remarkGfm -> remarkRehype(allowDangerousHtml) -> rehypeRaw -> rehypeSanitize -> rehypeSlug -> trusted highlight/heading enrichment -> rehypeStringify`. For PLAN bodies, consume `segmentPlanBody` from plan 02-02 rather than introducing another parser. Extend the sanitizer only for required safe GFM/Shiki/heading data; post-sanitize plugins may generate trusted nodes but may not copy source event/style/URL attributes. Emit stable duplicate-heading IDs and dormant copy-link metadata. Represent Mermaid as escaped browser mount source with the 256 KiB execution cap. Build ArtifactIndex once from the loaded snapshot and expose typed artifact responses/fallback warnings through `src/server/index.ts`; invalid request tokens only query the index.</action>
  <verify>
    <automated>npm test -- --run test/rendering/markdown.test.ts &amp;&amp; npm run typecheck &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>READ-01, READ-03, and READ-04 edge cases have named tests; the real Phase 1 plan renders semantic wrappers and nested markdown; hostile fixtures expose no executable surface; and traversal-shaped tokens cannot escape the index.</acceptance_criteria>
  <done>The artifact API returns finalized sanitized document HTML with stable headings and scoped degradation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Structured frontmatter and document-first artifact canvas</name>
  <read_first>
    - `src/planning-repo/handlers/index.ts` — specialized-first, generic-last registry precedent
    - `.planning/phases/02-situational-awareness-artifact-reading/02-PATTERNS.md` — frontmatter/document assignments
    - `src/web/styles/globals.css` — containment/theme contract from plan 02-03
    - `src/presentation/routes.ts` — canonical breadcrumb and heading URL builders
  </read_first>
  <files>src/rendering/frontmatter-views.ts, src/web/pages/artifact-page.tsx, src/web/app-router.tsx, test/rendering/markdown.test.ts</files>
  <behavior>
    - D-10/READ-02: guarded `must_haves`, `coverage`, `key_links`, and `progress` panels precede an unconditional generic recursive remainder.
    - D-09: breadcrumbs, title, type, and compact structured summary lead directly into the full-width document.
    - D-16/READ-06: heading controls appear on hover/focus, copy and update only on activation, and reload to the same duplicate-safe heading.
    - Unknown artifact kinds and wrong-shaped known frontmatter use the same safe generic document path.
  </behavior>
  <action>Write frontmatter/open-kind/duplicate-heading tests first. Implement a builder registry over `Record<string, unknown>` with named builders for `must_haves`, `coverage`, `key_links`, and `progress`; record handled keys and route every other or invalid value through the recursive text-only fallback. Export `DocumentView` as the only sanitized HTML mount, run Mermaid manually with `securityLevel: strict` and `startOnLoad: false`, and activate copy-heading metadata only on click/keyboard focus. Build D-09 ArtifactPage with canonical breadcrumbs, compact structured summary, full-width body, local overflow wrappers, explicit empty state, and generic unknown-kind behavior. Register document routes through `appRouter` without duplicating codec strings.</action>
  <verify>
    <automated>npm test -- --run test/rendering/markdown.test.ts &amp;&amp; npm run typecheck &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>READ-02 and READ-06 preserve every open field, unknown artifacts remain readable, heading URLs are stable and activation-only, and only sanitized renderer output enters the HTML mount.</acceptance_criteria>
  <done>A user can safely read any artifact document-first with structured metadata and stable section links.</done>
</task>

</tasks>

## Artifacts this phase produces

- `src/server/artifact-index.ts` — `ArtifactIndex`, `ArtifactLookupResult`, `buildArtifactIndex`.
- `src/rendering/markdown.ts` — `RenderedDocument`, `ArtifactRenderer`, `createArtifactRenderer`.
- `src/rendering/frontmatter-views.ts` — `FrontmatterPanel`, `FrontmatterPanelBuilder`, `FRONTMATTER_PANEL_BUILDERS`, `buildFrontmatterPanels`.
- `src/web/pages/artifact-page.tsx` — `DocumentView`, `ArtifactPage`.
- `test/rendering/markdown.test.ts` — PLAN/GFM/security/frontmatter/heading regression suite.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Artifact Markdown/HTML -> sanitized HTML | Local project content is untrusted active-content input. |
| Sanitized HTML -> browser DOM | Only finalized renderer output may enter the HTML mount; Mermaid is isolated. |
| Route token -> artifact index | Request parameters may select only snapshot-indexed artifacts. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-12 | Tampering / Elevation | `src/rendering/markdown.ts` | high | mitigate | Enforce raw-before-sanitize order, hostile snapshots, safe schema, and trusted post-sanitize enrichment only. |
| T-02-13 | Elevation | `DocumentView` Mermaid mount | high | mitigate | Use strict mode, manual selected-node execution, source cap, and escaped local fallback. |
| T-02-14 | Information Disclosure / Tampering | artifact endpoint | high | mitigate | Resolve canonical tokens against ArtifactIndex; request text never reaches filesystem methods. |
| T-02-15 | Denial of Service | PLAN/Markdown processing | medium | mitigate | Reuse bounded linear segmentation, singleton renderer/highlighter, Mermaid cap, and artifact-local failure isolation. |
</threat_model>

<verification>
Run the focused renderer suite, complete test suite, typecheck, lint, and build. Exercise a real multi-task plan, hostile fixtures, unknown kind, duplicate headings, and invalid artifact tokens through the API and document page contracts.
</verification>

<success_criteria>
- READ-01 through READ-04 and READ-06 pass explicit automated edge/security tests.
- D-09 through D-11 and D-16 are implemented through one renderer and one document mount.
- Unknown artifact kinds and frontmatter keys remain visible through unconditional fallbacks.
- No artifact-authored active content executes and route input cannot become a filesystem path.
</success_criteria>

<output>
Create `.planning/phases/02-situational-awareness-artifact-reading/02-04-SUMMARY.md` when done.
</output>
