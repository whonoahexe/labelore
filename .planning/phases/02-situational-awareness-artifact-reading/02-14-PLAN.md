---
phase: 02-situational-awareness-artifact-reading
plan: 14
type: execute
wave: 11
depends_on: [02-13]
files_modified:
  - src/web/styles/globals.css
  - src/web/components/reference-preview.tsx
  - src/web/pages/artifact-page.tsx
  - test/web/visual-contract.test.ts
  - test/rendering/references.test.ts
  - fixtures/dense/.planning/phases/01-identity-slice/01-01-PLAN.md
  - test/__golden__/dense.json
autonomous: true
gap_closure: true
requirements: [UI-01, READ-01, READ-06, NAV-02, NAV-03, DASH-02, DASH-03]

estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Nested PLAN sections add no repeated enclosing rule, so the measured page-to-code path has at most the code boundary plus one functional outer container (UI-01, G2-01, D-11)."
    - "The truth-to-coverage table and Markdown tables use the same horizontal-only cell rules and every tbody receives zebra striping (READ-01, G2-04)."
    - "The Needs-attention provenance note occupies the content column instead of the 1rem icon column and remains readable at 390px (DASH-02, DASH-03, G2-06, D-03)."
    - "The reference preview is anchored and stacked by Base UI's Positioner, clears the sticky shell header, and keeps preview-first Open behavior (NAV-02, NAV-03, G2-02, D-14, D-15)."
    - "Mermaid output uses the app font and semantic light/dark tokens, and its SVG is proportionally bounded instead of repeating the measured 672x805 four-node footprint; invalid browser parsing remains readable as source (READ-06, G2-05)."
  prohibitions:
    - statement: "MUST NOT flatten G2-01 by removing the functional code/table scroll boundary; authored wide content must remain locally reachable."
      category: honesty
      requirement_id: UI-01
    - statement: "MUST NOT place stacking responsibility on a statically positioned popup when the Base UI Positioner owns geometry."
      category: transparency
      requirement_id: NAV-02
  artifacts:
    - path: "src/web/styles/globals.css"
      provides: "Root-caused nesting, table, source-note, popover-stack, and Mermaid theme corrections"
    - path: "src/web/components/reference-preview.tsx"
      provides: "Named Positioner hook for collision-aware placement and stacking"
    - path: "fixtures/dense/.planning/phases/01-identity-slice/01-01-PLAN.md"
      provides: "Committed rendered and browser-rejected Mermaid branches for the final gate"
  key_links:
    - from: "Popover.Positioner"
      to: ".reference-preview-positioner"
      via: "the positioned ancestor carries the z-index above .shell-header"
      pattern: "positioner-owns-stack"
    - from: "Mermaid initialization and SVG output"
      to: "root theme tokens"
      via: "base theme configuration plus token-backed CSS overrides"
      pattern: "theme-follows-root"
---

<objective>
Close the five root-caused presentation gaps from the latest 02-13 ledger: G2-01, G2-02, G2-04,
G2-05, and G2-06.

Purpose: These failures already have measured selectors and root causes. This plan corrects those
specific seams and adds an invalid Mermaid branch the final gate can actually exercise.
Output: corrected CSS/component behavior, deterministic contract tests, and reachable Mermaid cases.
</objective>

<execution_context>
@/home/cinedise/.codex/gsd-core/workflows/execute-plan.md
@/home/cinedise/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-situational-awareness-artifact-reading/02-13-SUMMARY.md
@.planning/phases/02-situational-awareness-artifact-reading/02-UI-SPEC.md
@.planning/phases/02-situational-awareness-artifact-reading/02-12-SUMMARY.md
</context>

<assumption_delta_decision>
Primary noun: presentation reference identity.
Decision: no-change.
Rationale: the detector matched existing "fallback" prose; these fixes add no second identity model or source of truth.
</assumption_delta_decision>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove recursive section rules, unify table chrome, and place provenance in the content column</name>
  <files>src/web/styles/globals.css, test/web/visual-contract.test.ts</files>
  <read_first>
    - src/web/styles/globals.css
    - test/web/visual-contract.test.ts
    - .planning/phases/02-situational-awareness-artifact-reading/02-13-SUMMARY.md
  </read_first>
  <behavior>
    - A top-level plan section retains one separator; a plan section nested inside another plan section has no enclosing or top separator of its own.
    - Coverage-matrix cells have a bottom rule only, matching artifact Markdown tables.
    - Even tbody rows receive the same zebra token in coverage and artifact tables.
    - A direct `.source-note` child of an attention row occupies grid column 2 and may shrink within that column.
  </behavior>
  <action>
First add failing region-scoped assertions for all four behaviors. Then update both `.plan-section`
rule regions so only top-level sections draw the hairline and nested `.plan-section` descendants
remove that separator and avoid additional horizontal inset; preserve the code block's functional
scroll edge per UI-01 and D-11. Change `.coverage-table-boundary :is(th, td)` from a four-sided grid
to the same bottom-only rule used by `.artifact-document :is(th, td)`, and add coverage-table zebra
using the existing muted mix. Place `.attention-list > li > .source-note` in grid column 2 with
`min-width: 0`; this is the exact missing placement that left the third grid child in the 1rem icon
column. Do not change attention destinations in this task.
  </action>
  <acceptance_criteria>
    - The visual-contract suite asserts top-level and nested PLAN-section behavior separately and passes.
    - The coverage-table cell block contains a bottom border and no four-sided border declaration.
    - Both table families have an even-row zebra selector.
    - The attention-row source note is assigned to column 2 and retains `min-width: 0`.
    - The code block keeps its enclosing border and local horizontal scrolling.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run test/web/visual-contract.test.ts test/web/shell-contract.test.ts</automated>
    <fails_when>non-zero exit, any failed test, or the test runner reports zero passing tests</fails_when>
  </verify>
  <reversibility rating="reversible">All changes are selector-scoped CSS plus source-contract assertions; no public data or route shape changes.</reversibility>
  <done>G2-01, G2-04, and G2-06 have selector-level regression coverage and their measured root causes are removed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Put reference-preview geometry and stacking on the Base UI Positioner</name>
  <files>src/web/components/reference-preview.tsx, src/web/styles/globals.css, test/rendering/references.test.ts</files>
  <read_first>
    - src/web/components/reference-preview.tsx
    - src/web/styles/globals.css
    - test/rendering/references.test.ts
    - package.json
    - package-lock.json
  </read_first>
  <behavior>
    - Positioner receives the live trigger anchor and a dedicated class.
    - The positioned ancestor carries a stack level greater than the sticky header's 20.
    - Popup remains width-bounded and focus returns to the trigger after close.
    - Positioner collision handling keeps the popup within the viewport at desktop and 390px.
  </behavior>
  <action>
Add `className="reference-preview-positioner"` to `Popover.Positioner`, retain `anchor`, `sideOffset`,
and `align`, and set `positionMethod="fixed"` so the portalled geometry is viewport-relative while
Base UI still computes the anchor coordinates and collision shifts. Move the stack-level declaration
from `.reference-preview` to `.reference-preview-positioner`; the measured popup is statically
positioned, so its own z-index cannot establish the intended order. Keep Popup responsible only for
visual chrome and transitions. Extend the existing reference source contract to assert the named
Positioner, fixed method, stack ownership, viewport-bounded popup, and exact focus return per D-14
and D-15.
  </action>
  <acceptance_criteria>
    - The Positioner has the named class, retains the live anchor, and uses fixed positioning.
    - `.reference-preview-positioner` carries a z-index greater than 20; `.reference-preview` does not carry the stack-level declaration.
    - The popup remains bounded by `calc(100vw - 2rem)`.
    - Existing click/Enter/Space activation, Open URL, and final-focus assertions pass unchanged.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run test/rendering/references.test.ts</automated>
    <fails_when>non-zero exit, any failed activation/source-contract case, or the test runner reports zero passing tests</fails_when>
  </verify>
  <reversibility rating="reversible">This moves two positioning declarations to the component that owns geometry; reverting touches one component and one selector.</reversibility>
  <done>G2-02 is corrected at the positioned ancestor without replacing Base UI's anchoring or focus management.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Theme Mermaid from app tokens and commit an invalid-diagram fallback case</name>
  <files>src/web/pages/artifact-page.tsx, src/web/styles/globals.css, test/web/visual-contract.test.ts, fixtures/dense/.planning/phases/01-identity-slice/01-01-PLAN.md, test/__golden__/dense.json</files>
  <read_first>
    - src/web/pages/artifact-page.tsx
    - src/web/styles/globals.css
    - fixtures/dense/.planning/phases/01-identity-slice/01-01-PLAN.md
    - test/snapshot.golden.test.ts
    - .planning/phases/02-situational-awareness-artifact-reading/02-13-SUMMARY.md
  </read_first>
  <behavior>
    - Mermaid initialization keeps strict security and selects the base theme with the app font.
    - Diagram container/SVG, nodes, labels, edges, and markers resolve through app semantic tokens in light and dark mode.
    - The valid four-node fixture preserves its SVG viewBox aspect ratio while fitting within the content width and a `min(70vh, 36rem)` height ceiling at desktop and 390px.
    - A renderer-recognized but browser-invalid Mermaid source falls into `.mermaid-fallback`, retains its source, and records `data-mermaid-rejected="browser-parse"`.
    - The existing valid flowchart remains renderer-pending and renders normally.
  </behavior>
  <action>
Extend the Mermaid initialization with `theme: 'base'`, the app font family, and semantic colors read
from the root computed tokens while retaining `securityLevel: 'strict'` and manual `run()`. Add
token-backed CSS overrides for the SVG background, node shapes, label text, edge paths, and markers;
use the root variables so an already-rendered SVG follows the light/dark class without a second
renderer instance. Correct the measured 672x805 four-node geometry with a proportional SVG sizing
contract: centered auto width/height, `max-width: 100%`, and `max-height: min(70vh, 36rem)`, retaining
the Mermaid viewBox so neither axis stretches independently. Keep the existing parse catch and source
restoration intact. Add a second Mermaid
fence to the dense PLAN that passes the server's diagram-start recognition but fails browser parsing,
so the final gate can inspect the fallback branch. Extend the visual-contract test to assert strict
mode, base theme/font configuration, token-backed SVG selectors, the proportional width/height ceiling,
and both valid/invalid fixture branches. Regenerate the dense golden only after confirming the diff is confined to the edited
fixture artifact.
  </action>
  <acceptance_criteria>
    - Strict Mermaid security and `startOnLoad: false` remain present.
    - Initialization specifies the base theme and app font; CSS covers background, nodes, labels, edges, and markers with app tokens.
    - The valid four-node SVG is centered, preserves its viewBox proportion, never exceeds content width, and is capped at `min(70vh, 36rem)` rather than rendering at the measured 805px height.
    - The dense fixture contains one valid flowchart and one recognized source that exercises browser-parse rejection.
    - The catch branch preserves source text, applies `.mermaid-fallback`, and sets the rejection dataset.
    - Dense golden and degradation tests show no changes outside the edited fixture entry.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run test/web/visual-contract.test.ts test/rendering/markdown.test.ts test/snapshot.golden.test.ts test/degradation.test.ts</automated>
    <fails_when>non-zero exit, any failed test, an obsolete snapshot, or a golden diff outside the dense fixture artifact</fails_when>
  </verify>
  <reversibility rating="reversible">Theme values and the added fixture branch are additive; strict parsing and the server renderer contract remain unchanged.</reversibility>
  <done>G2-05 follows the app theme, has bounded proportional geometry, and exposes a reachable invalid Mermaid fallback for the final gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| artifact Mermaid source -> client renderer | Authored diagram text reaches Mermaid and then SVG output. |
| inert document trigger -> portalled popover | A sanitized control supplies the anchor and reference key for the preview. |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-54 | Elevation of Privilege | Mermaid rendering | high | mitigate | Preserve strict security, the source-size/start-pattern gates, per-node parse catching, and inert-source fallback; Task 3 tests the controls while changing theme only. |
| T-02-55 | Spoofing | reference-preview placement | medium | mitigate | Keep the live trigger as Base UI's anchor and final-focus target; do not synthesize coordinates or a second trigger identity. |
| T-02-56 | Denial of Service | theme/fallback rerender path | medium | mitigate | Keep one Mermaid instance and one parse/run attempt per pending node; CSS variables handle theme toggles without rerender loops. |
| T-02-SC | Tampering | package installation | high | mitigate | This plan installs nothing; package manifests are outside `files_modified`. Any proposed install blocks execution for legitimacy review. |
</threat_model>

<verification>
Run the three targeted task suites, then `npm test`, `npm run typecheck`, `npm run lint`,
`npm run build`, and `npm run smoke -- fixtures/dense`. Static assertions prove declared contracts;
the final plan performs the theme/viewport/browser judgment.
</verification>

<success_criteria>
- G2-01, G2-02, G2-04, G2-05, and G2-06 each have a root-cause fix and a focused regression assertion.
- The invalid Mermaid fallback is reachable from committed fixture content.
- Sanitization, strict Mermaid security, reference focus restoration, local overflow, and the full suite remain green.
</success_criteria>

## Artifacts this phase produces

- `02-14-SUMMARY.md` — execution evidence for G2-01, G2-02, G2-04, G2-05, and G2-06.
- A dense-fixture invalid Mermaid branch and the corresponding confined golden update.
- No new dependency, route shape, or filesystem access path.

<output>
Create `.planning/phases/02-situational-awareness-artifact-reading/02-14-SUMMARY.md` when done.
</output>
