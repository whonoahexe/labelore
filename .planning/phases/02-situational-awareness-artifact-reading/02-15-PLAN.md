---
phase: 02-situational-awareness-artifact-reading
plan: 15
type: execute
wave: 12
depends_on: [02-14]
files_modified:
  - src/presentation/references.ts
  - src/rendering/linkify.ts
  - test/rendering/references.test.ts
autonomous: true
gap_closure: true
requirements: [NAV-02, NAV-03, READ-06]
estimate:
  tokens: 26000
  raw_tokens: 26000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "As a separately named partial G-09 capability, an exact .planning artifact path in rendered prose opens the matching artifact preview and offers its in-app Open destination."
    - "Artifact-path linking works for phase-local, milestone-root, and archived artifact records already present in the snapshot; it never synthesizes a destination for an unknown path."
    - "G2-03 remains blocked for its reproduced `backend/src/auth/mod.rs`-style source paths until the domain model supplies a canonical repository/source-file inventory and stable in-app source route; per D-17 those unresolved paths remain plain text."
  artifacts:
    - path: src/presentation/references.ts
      provides: "Exact artifact-path preview records backed by the presentation snapshot"
    - path: src/rendering/linkify.ts
      provides: "Safe prose and inline-code artifact-path triggers"
    - path: test/rendering/references.test.ts
      provides: "Resolution, non-resolution, and rendered-link regression coverage"
  key_links:
    - from: src/rendering/linkify.ts
      to: src/presentation/references.ts
      via: "token resolution against the exact artifact registry"
      pattern: "artifact"
    - from: src/presentation/references.ts
      to: src/server/project-presentation.ts
      via: "presentation.artifacts registry records"
      pattern: "artifacts"
---

<objective>
Deliver the independently actionable `.planning` artifact-path portion of G-09 while recording the concrete prerequisite that blocks G2-03's reproduced source-code paths.

Purpose: Add registry-backed planning-artifact references without claiming that source-code paths such as `backend/src/auth/mod.rs` have a destination the current domain model does not provide.
Output: Snapshot-backed artifact previews and safe prose/inline-code linkification with regression coverage.
</objective>

<execution_context>
@/home/cinedise/.codex/gsd-core/workflows/execute-plan.md
@/home/cinedise/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-situational-awareness-artifact-reading/02-CONTEXT.md
@.planning/phases/02-situational-awareness-artifact-reading/02-13-SUMMARY.md
@src/presentation/references.ts
@src/rendering/linkify.ts
@src/server/project-presentation.ts
@test/rendering/references.test.ts
</context>

<prohibitions>
- Do not turn arbitrary filesystem text into a URL; resolution is limited to exact artifact records already present in the presentation snapshot.
- Do not scan fenced code blocks, authored anchors, or nested interactive elements.
- Do not create routes for source-code paths or commit hashes. G2-03's reproduced source path cannot close until a canonical repository/source-file inventory and stable in-app source route exist; D-17 requires it to stay plain text meanwhile.
- Do not weaken the existing requirement, phase, or plan reference behavior while adding the artifact reference type.
</prohibitions>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Resolve one exact artifact path from prose through preview to Open destination</name>
  <files>src/presentation/references.ts, src/rendering/linkify.ts, test/rendering/references.test.ts</files>
  <read_first>
    - src/presentation/references.ts
    - src/rendering/linkify.ts
    - src/server/project-presentation.ts
    - test/rendering/references.test.ts
    - .planning/phases/02-situational-awareness-artifact-reading/02-CONTEXT.md
    - .planning/phases/02-situational-awareness-artifact-reading/02-13-SUMMARY.md
  </read_first>
  <behavior>
    - Test 1: A prose token equal to a registered phase artifact path resolves to an artifact preview whose Open URL is that artifact's existing app URL.
    - Test 2: A registered milestone-root or archived artifact path resolves from the same registry without phase-number assumptions.
    - Test 3: An unknown `.planning` path, source-code path, and commit hash remain text per D-17.
    - Test 4: Trailing sentence punctuation is excluded from the lookup while the visible reference remains faithful to the authored path.
  </behavior>
  <action>Extend the reference preview union and registry with an `artifact` record constructed from `presentation.artifacts`. Index only each record's canonical snapshot path; populate identity, title, artifact kind/status, location, detail, and the artifact's existing in-app URL from that same record. Extend prose token discovery so an exact `.planning/...` token can ask the registry for a preview, trimming only sentence punctuation already handled for the existing identifiers. Wire one real phase artifact path end to end first, then cover root and archived records through the same data-driven implementation. This implements the planning-artifact-path branch of G-09 while preserving D-14, D-15, and D-17; it deliberately does not claim G2-03, whose `backend/src/auth/mod.rs` reproduction lacks a canonical source-file record and stable application route.</action>
  <acceptance_criteria>
    - A rendered prose path for a registered artifact produces the same preview interaction and Open affordance as other reference types.
    - Preview content and destination come from the artifact snapshot record, not path-shape inference.
    - Unknown paths, source-code paths, and commit hashes remain non-interactive text.
    - Existing requirement, phase, and plan reference tests remain green.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run test/rendering/references.test.ts</automated>
    <fails_when>The test command exits non-zero, a registered artifact has no artifact preview/Open URL, or an unregistered path becomes interactive.</fails_when>
  </verify>
  <done>One exact `.planning` artifact path travels from authored prose to a snapshot-backed preview and its existing Open destination, with negative cases protected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Support exact artifact paths authored as inline code without entering code fences</name>
  <files>src/rendering/linkify.ts, test/rendering/references.test.ts</files>
  <read_first>
    - src/rendering/linkify.ts
    - src/presentation/references.ts
    - test/rendering/references.test.ts
    - .planning/phases/02-situational-awareness-artifact-reading/02-13-SUMMARY.md
  </read_first>
  <behavior>
    - Test 1: An inline-code node whose complete text is an exact registered `.planning` path becomes one artifact preview trigger.
    - Test 2: A fenced/preformatted code block containing the same path remains untouched.
    - Test 3: Inline code containing surrounding prose, an unknown path, a source path, or a hash remains untouched.
    - Test 4: Existing authored links and interactive descendants are never nested inside the generated trigger.
  </behavior>
  <action>Add a narrow inline-code transformation after registry construction: only a standalone inline `code` node with a single exact registered artifact path may be replaced by the existing preview trigger. Explicitly exclude `pre` ancestry, authored anchors/buttons, mixed-content code nodes, and unresolved values. Reuse the artifact resolver from Task 1 so prose and inline-code paths have identical preview data and destinations; do not duplicate path parsing.</action>
  <acceptance_criteria>
    - Common markdown inline-code artifact references are interactive when and only when their exact path exists in the registry.
    - Code fences, authored links, unknown references, source paths, and hashes retain their original rendering.
    - The generated markup has no nested interactive controls.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run test/rendering/references.test.ts &amp;&amp; npm run typecheck</automated>
    <fails_when>Either command exits non-zero, a preformatted block is transformed, or TypeScript rejects the expanded preview union.</fails_when>
  </verify>
  <done>Exact artifact paths work in prose and inline code through one registry-backed resolver, with unsafe or unsupported contexts left untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| authored markdown → rendered DOM | Untrusted artifact-looking text may request an interactive preview |
| artifact path → application route | A filesystem-shaped token could otherwise escape the known artifact registry |

## STRIDE Threat Register (OWASP ASVS L1; unresolved high severity blocks completion)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-15-01 | Tampering | artifact path resolver | high | mitigate | Resolve exact canonical keys only against snapshot artifact records; never construct filesystem or application destinations from user text. |
| T-02-15-02 | Elevation of privilege | linkified DOM | high | mitigate | Exclude anchors, buttons, preformatted blocks, and nested interactive ancestry before generating a trigger. |
| T-02-15-03 | Information disclosure | artifact preview | medium | mitigate | Expose only fields already present in the phase presentation snapshot and its existing artifact URL. |
| T-02-15-04 | Denial of service | token scanner | low | mitigate | Preserve the bounded single-pass tree transformation and exact registry lookup. |
</threat_model>

<verification>
- `npm test -- --run test/rendering/references.test.ts` proves artifact resolution and all non-linking boundaries.
- `npm run typecheck` proves every preview renderer handles the new artifact discriminant.
- Confirm every path named in `<read_first>` is tracked with `git ls-files -- &lt;path&gt;` before implementation.
</verification>

<success_criteria>
- The `.planning` artifact-path branch is delivered as partial G-09 capability and is not reported as closure of G2-03.
- G2-03 is explicitly BLOCKED on a canonical repository/source-file inventory plus stable in-app source route for paths such as `backend/src/auth/mod.rs`; until that prerequisite ships, D-17 governs the plain-text result.
- Registered `.planning` artifact paths produce previews and use existing Open destinations in prose and inline code.
- Unsupported source paths and commit hashes remain plain text per D-17 because the product has no destination for them.
- No ASVS L1 high-severity threat remains open.
</success_criteria>

## Artifacts this phase produces

- `src/presentation/references.ts` — artifact reference records and exact registry resolution.
- `src/rendering/linkify.ts` — prose and inline-code artifact preview triggers.
- `test/rendering/references.test.ts` — positive, negative, and markup-safety coverage.

<assumption_delta>
no-change — The plan adds a concrete artifact reference kind backed by existing snapshot identity; it does not generalize identity or fallback rules.
</assumption_delta>

<output>
Create `.planning/phases/02-situational-awareness-artifact-reading/02-15-SUMMARY.md` when done.
</output>
