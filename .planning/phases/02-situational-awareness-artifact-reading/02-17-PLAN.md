---
phase: 02-situational-awareness-artifact-reading
plan: 17
type: execute
wave: 13
depends_on: [02-14, 02-15, 02-16]
files_modified: []
autonomous: false
gap_closure: true
requirements: [DASH-01, DASH-02, DASH-03, READ-01, READ-06, NAV-02, NAV-03, NAV-06, UI-01, UI-02]
user_setup:
  - service: host-browser
    why: "The latest failures are human visual/interaction observations that require the host browser at desktop and 390px widths in both themes."
    dashboard_config:
      - task: "Open the fresh local application URL emitted by the executor and keep browser zoom at 100%."
        location: "Host browser"
estimate:
  tokens: 14000
  raw_tokens: 14000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "All current G2-01 through G2-10 outcomes receive explicit per-theme and per-viewport verdicts against a fresh build containing plans 02-14 through 02-16."
    - "All nine selectors named by the latest muted-surface ledger receive separate verdicts in light/dark at desktop/390px rather than sampled page-level approval."
    - "The horizontal scrollbar under the 200-character code-block line has explicit visible-at-rest, hover, drag, and Shift+wheel/trackpad verdicts in all four theme/viewport cells."
    - "The valid Mermaid fixture has proportionate bounded dimensions in all four theme/viewport cells, and the invalid browser-parse fixture exercises the visible raw-source fallback without breaking other diagrams."
    - "Both blocker destination branches—resolvable target and fallback artifact—are opened, and every other attention producer's primary/provenance destinations are checked separately."
  artifacts:
    - path: .planning/phases/02-situational-awareness-artifact-reading/02-17-SUMMARY.md
      provides: "Fresh-build re-gate matrix with per-theme, per-viewport, and per-branch verdicts"
  key_links:
    - from: .planning/phases/02-situational-awareness-artifact-reading/02-14-SUMMARY.md
      to: host browser gate
      via: "root-cause visual fixes deployed in the tested build"
      pattern: "G2-01"
    - from: .planning/phases/02-situational-awareness-artifact-reading/02-15-SUMMARY.md
      to: host browser gate
      via: "artifact path preview/Open interaction"
      pattern: "G-09"
    - from: .planning/phases/02-situational-awareness-artifact-reading/02-16-SUMMARY.md
      to: host browser gate
      via: "diagnosis results and named attention/token matrices"
      pattern: "G2-07"
---

<objective>
Re-run the Phase 02 human gate on a fresh build with a complete matrix that closes the six process shortfalls from 02-13.

Purpose: Produce defensible final verdicts for every current gap, theme, viewport, muted surface, fallback, scrollbar interaction, and attention destination branch.
Output: A single evidence-rich 02-17 summary that execute-phase `--gaps-only` can hand back for final verification.
</objective>

<execution_context>
@/home/cinedise/.codex/gsd-core/workflows/execute-plan.md
@/home/cinedise/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-situational-awareness-artifact-reading/02-CONTEXT.md
@.planning/phases/02-situational-awareness-artifact-reading/02-UI-SPEC.md
@.planning/phases/02-situational-awareness-artifact-reading/02-13-SUMMARY.md
@.planning/phases/02-situational-awareness-artifact-reading/02-14-SUMMARY.md
@.planning/phases/02-situational-awareness-artifact-reading/02-15-SUMMARY.md
@.planning/phases/02-situational-awareness-artifact-reading/02-16-SUMMARY.md
</context>

<prohibitions>
- Do not reuse a browser tab backed by a server started before the 02-14 through 02-16 commits.
- Do not collapse viewport/theme results into a single pass/fail statement.
- Do not mark G2-07 closed from unit tests alone; exercise primary and provenance links in the host browser.
- Do not substitute the server-recognized invalid Mermaid fixture for the browser-parse-invalid fixture.
- Do not omit a branch or surface because a neighboring example appears correct.
</prohibitions>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Execute the complete fresh-build G2 re-gate matrix</name>
  <precondition>Plans 02-14, 02-15, and 02-16 have completed with summaries, their high-severity ASVS L1 threats are resolved, and the executor can start a fresh server from their resulting commit.</precondition>
  <read_first>
    - .planning/phases/02-situational-awareness-artifact-reading/02-13-SUMMARY.md
    - .planning/phases/02-situational-awareness-artifact-reading/02-14-SUMMARY.md
    - .planning/phases/02-situational-awareness-artifact-reading/02-15-SUMMARY.md
    - .planning/phases/02-situational-awareness-artifact-reading/02-16-SUMMARY.md
    - .planning/phases/02-situational-awareness-artifact-reading/02-UI-SPEC.md
    - fixtures/dense/.planning/STATE.md
  </read_first>
  <action>First run the automated quality gate and prove all three prerequisite summaries exist. Stop if either gate fails. Start a new application process on an unused port from the current commit, record the commit hash and URL in the summary, then ask the human to perform the matrix below at 100% zoom. Record `pass`, `fail`, or `not applicable` plus one sentence of evidence for every matrix cell; any failure remains an open gap and must name the route, theme, viewport, fixture, and observed behavior. Use the 02-16 summary's G2-07 deterministic diagnosis to target the browser checks without treating it as a human-gate substitute.</action>
  <what-built>Plans 02-14 through 02-16 close the root-caused visual gaps, add exact `.planning` artifact-path previews as partial G-09 capability, classify G2-03's source-path reproduction against its missing route prerequisite, and turn the remaining intermittent/token reports into deterministic contracts. This checkpoint verifies their integrated browser behavior without converting the G2-03 block into a pass.</what-built>
  <how-to-verify>Run both automated gates, start the application from the resulting commit on a fresh unused port, then complete every numbered row in the four-column human-check matrix below and capture evidence in 02-17-SUMMARY.md.</how-to-verify>
  <resume-signal>Reply `approved` only when every required matrix cell passes; otherwise reply with the failed row numbers and the newly recorded gap IDs.</resume-signal>
  <acceptance_criteria>
    - The quality gate passes and all prerequisite summaries exist before the browser session begins.
    - The summary identifies the exact tested commit and fresh server URL.
    - Every G2 item and every process-shortfall row has an explicit verdict and evidence; there are no blanket or sampled verdicts.
    - Any failing ASVS L1 high-severity condition blocks completion rather than becoming a follow-up note.
  </acceptance_criteria>
  <verify>
    <automated>npm test &amp;&amp; npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run build &amp;&amp; npm run smoke -- fixtures/dense</automated>
    <fails_when>Any command exits non-zero, the dense fixture smoke check fails, or the build used for the browser gate is not current.</fails_when>
    <automated>for plan_id in 14 15 16; do test -f ".planning/phases/02-situational-awareness-artifact-reading/02-${plan_id}-SUMMARY.md" || exit 1; done</automated>
    <fails_when>Any prerequisite summary is absent, so the browser gate cannot prove it includes all planned fixes and diagnoses.</fails_when>
    <human-check>
      1. Create a four-column verdict matrix: light desktop (at least 1280px), light 390px, dark desktop, dark 390px. In every column inspect G2-01 nested PLAN sections, G2-02 edge-positioned reference preview, G2-03's reproduced source path plus the separate partial G-09 `.planning` path behavior, G2-04 coverage and markdown tables, G2-05 Mermaid theme/geometry, G2-06 attention source-note width, G2-08 long Next descriptions, G2-09 section labels, and G2-10 chip tones.
      2. For G2-01, verify only top-level PLAN sections receive separators and nested sections preserve hierarchy without equal-weight rules.
      3. For G2-02, open references near top, bottom, left, and right viewport edges; verify the popup stays within the viewport, above page content, and supports Escape plus focus return.
      4. Record G2-03 as BLOCKED, not passed: reproduce `backend/src/auth/mod.rs` (or the exact studio-portal source path from 02-13) and verify it remains plain text because no canonical repository/source-file inventory or stable in-app source route exists. Separately label partial G-09 and activate one phase-local `.planning` path, one milestone-root path, and one archived artifact path in prose/inline code; verify their preview and Open destinations. A commit hash must also remain plain text per D-17.
      5. For G2-04, verify bottom-only row rules, no boxed-cell grid, and subtle zebra striping on both coverage and rendered markdown tables in all four matrix columns.
      6. For G2-05, measure the valid four-node diagram in every matrix cell. Verify it follows the current theme/app font, preserves its viewBox proportion without axis distortion, fits the content width, stays at or below `min(70vh, 36rem)`, and does not repeat the recorded 672x805 desktop footprint. Record width, height, and verdict for each cell. Then open the browser-parse-invalid Mermaid fixture and verify the raw source plus readable fallback message appears while other diagrams remain interactive.
      7. For G2-06, verify every attention source note occupies the content column without collapsing beside the icon at desktop and 390px.
      8. For G2-07, exercise discrepancy, dependency, checkpoint, coverage, resolvable blocker, and fallback blocker rows. For each row open the primary action and, separately, the provenance/source-evidence link. The resolvable blocker must open its concrete destination; the fallback blocker must open the fallback artifact. Record both blocker branches by name.
      9. For G2-08, use the deliberately long primary and preview descriptions; verify the panel remains bounded in all matrix columns and keyboard/screen-reader inspection still exposes the complete text.
      10. For the muted-surface process gap, record a separate verdict in every one of the four matrix cells for all nine selectors named by 02-13: `.snapshot-status small`, `.lede`, `.next-preview p`, `.attention-list small`, `.discrepancy-callout p`, `.plan-section-label`, `.artifact-metadata > summary span`, `.document-outline > p`, and `.reference-preview-facts dt`. Do not substitute descriptive page names or one aggregate verdict for the selector-level inventory.
      11. For G2-10, compare active/current, complete/exact, pending/quiet, and inferred chips in both themes; verify tone changes reflect semantic state/confidence and remain legible.
      12. Use the dense fixture's 200-character code-block line to test its horizontal scrollbar—not navigation scrolling—in all four matrix cells. For light desktop, light 390px, dark desktop, and dark 390px separately record: visible at rest, stronger/clear on hover or focus, draggable thumb, and horizontal movement with Shift+wheel or trackpad. This produces sixteen named scrollbar verdicts.
      13. Recheck the 02-13 non-regression set: layout containment, mobile breadcrumb return, mobile Next placement, artifact-key readability, narrative prose styling, and non-monospace prose.
      14. Approve only when every required cell passes. On failure, record a new gap ID with exact route, fixture, theme, viewport, steps, expected behavior, observed behavior, and screenshot location.
    </human-check>
  </verify>
  <done>The exact current commit passes automated gates and the complete host-browser matrix, with all six prior process shortfalls represented as explicit evidence rows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed implementation → fresh local server | A stale process could produce false verification evidence |
| application → host browser | Responsive, theme, focus, and pointer behavior cross the automated-test boundary |
| rendered attention action → navigation target | Incorrect primary/provenance destinations can misdirect the user |

## STRIDE Threat Register (OWASP ASVS L1; unresolved high severity blocks completion)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-17-01 | Tampering | stale-server verification | high | mitigate | Record the tested commit, start a fresh unused-port server after prerequisite summaries exist, and reject evidence from older processes. |
| T-02-17-02 | Spoofing | attention destinations | high | mitigate | Exercise primary and provenance destinations separately for every producer and both blocker branches. |
| T-02-17-03 | Repudiation | aggregate visual verdicts | medium | mitigate | Require per-theme, per-viewport, per-surface evidence in the summary. |
| T-02-17-04 | Denial of service | invalid Mermaid input | medium | mitigate | Exercise the browser-parse-invalid fallback and verify sibling diagrams continue functioning. |
</threat_model>

<verification>
- Automated: full test, type, lint, build, and dense smoke commands must pass before human verification.
- Freshness: all prerequisite summaries must exist and the summary must record the exact tested commit and newly started URL.
- Human: the four-column responsive/theme matrix, named muted-surface inventory, scrollbar interaction matrix, invalid Mermaid fallback, and attention destination branches must each have explicit evidence.
</verification>

<success_criteria>
- G2-01 through G2-10 have evidence-based final dispositions against the latest implementation; G2-03 remains explicitly BLOCKED until its canonical source inventory/route prerequisite exists and cannot be reported closed by this gate.
- The six 02-13 process shortfalls are closed: per-theme verdicts, per-viewport verdicts, all muted surfaces, named scrollbar affordance, invalid Mermaid fallback, and both blocker branches.
- No ASVS L1 high-severity threat remains open.
- The output is sufficient for execute-phase `--gaps-only` handoff without relying on older gate descriptions.
</success_criteria>

## Artifacts this phase produces

- `.planning/phases/02-situational-awareness-artifact-reading/02-17-SUMMARY.md` — exact commit/URL, automated results, complete G2 matrix, process-shortfall evidence, and any newly identified gap records.

## Multi-Source Coverage Audit

| Source | Item | Disposition | Plan coverage |
|--------|------|-------------|---------------|
| GOAL | Latest Phase 02 gate outcome: trustworthy artifact reading and navigation under the tested visual/interaction contract | COVERED | 02-14 root fixes; 02-15 artifact reference route; 02-16 diagnosis/token fixes; 02-17 final gate |
| REQ | DASH-01, DASH-02, DASH-03 | COVERED | 02-14 and 02-16; re-gated in 02-17 |
| REQ | READ-01, READ-06 | COVERED | 02-14 and 02-15; re-gated in 02-17 |
| REQ | NAV-02, NAV-03, NAV-06 | COVERED | 02-14 and 02-15; re-gated in 02-17 |
| REQ | UI-01, UI-02 | COVERED | 02-14 and 02-16; re-gated in 02-17 |
| RESEARCH | Existing parser/presentation/renderer layering, strict Mermaid security, snapshot-backed routes, and no new dependency | COVERED | Preserved by 02-14 through 02-16; verified in 02-17 |
| CONTEXT | D-03 attention prioritization and destinations | COVERED | 02-16 deterministic producer matrix and 02-17 branch gate |
| CONTEXT | D-04 Next action panel | COVERED | 02-16 bounded description and 02-17 viewport gate |
| CONTEXT | D-09 artifact source page and D-14/D-15 preview/Open behavior | COVERED | 02-15 artifact reference previews and destinations |
| CONTEXT | D-11 PLAN section structure | COVERED | 02-14 nested section hierarchy fix |
| CONTEXT | D-17 unresolved references remain plain text | COVERED | 02-15 negative path/hash contract and 02-17 browser gate |
| LATEST LEDGER | G2-01, G2-02, G2-04, G2-05, G2-06 | COVERED | 02-14 root-cause plan and 02-17 re-gate |
| LATEST LEDGER | G2-03 source-path reproduction (`backend/src/auth/mod.rs` class) | BLOCKED — DESTINATION PREREQUISITE | The domain model has neither a canonical repository/source-file inventory nor a stable in-app source route. 02-15 preserves D-17 plain text; 02-17 reproduces and records the block instead of claiming closure. |
| LATEST LEDGER | Actionable `.planning` artifact-path portion of G-09 | COVERED AS PARTIAL G-09 | 02-15 implements registry-backed preview/Open behavior; 02-17 re-gates it separately from G2-03. |
| LATEST LEDGER | G2-07 | COVERED | 02-16 diagnosis-first matrix and 02-17 all-branch browser gate |
| LATEST LEDGER | G2-08, G2-09, G2-10 | COVERED | 02-16 named reproduction/token diagnosis and 02-17 matrix |
| PROCESS | Per-theme and per-viewport verdicts; all nine named muted selectors; 200-character code-block horizontal scrollbar affordance; invalid Mermaid fallback; both blocker branches | COVERED | Explicit selector- and interaction-level 02-17 human-check rows |
| EXCLUSION | Commit hashes from older G-09 | DEPENDENCY CONFLICT | No product destination exists in current artifacts; D-17 requires plain text, so 02-15 tests the boundary rather than fabricating navigation. Source-code paths are not hidden in this exclusion: their G2-03 block is recorded above. |
| EXCLUSION | E14 artifact index from older UAT | DEFERRED BY AUTHORITATIVE 02-13 GATE | The latest ledger marks it coordinator-deferred and outside current actionable G2 scope |
| EXCLUSION | Phase requirements and CONTEXT decisions unrelated to current G2 ledger | ALREADY VERIFIED / OUT OF GAP SCOPE | Existing 02-01 through 02-13 plans and summaries; not restated in a gaps-only plan set |

<assumption_delta>
no-change — The gate verifies concrete current identities, routes, and fallbacks; it does not introduce or generalize predicates.
</assumption_delta>

<output>
Create `.planning/phases/02-situational-awareness-artifact-reading/02-17-SUMMARY.md` when done.
</output>
