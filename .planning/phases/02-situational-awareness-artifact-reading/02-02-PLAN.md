---
phase: 02-situational-awareness-artifact-reading
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/presentation/routes.ts
  - src/rendering/plan-segments.ts
  - src/planning-repo/handlers/state.ts
  - src/server/project-presentation.ts
  - src/presentation/dashboard.ts
  - src/web/app-router.tsx
  - test/presentation/routes.test.ts
  - test/server/deep-links.test.ts
  - test/server/project-presentation.test.ts
  - test/presentation/dashboard.test.ts
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03, DASH-04, ROAD-04, NAV-06]

estimate:
  tokens: 65000
  raw_tokens: 65000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "[EDGE ROAD-04/adjacency] Active and archived phases that share a phase number receive different canonical keys and different milestone-qualified URLs, proven by duplicate-identity round trips."
    - "[EDGE NAV-06/encoding] A copied URL reversibly encodes Unicode, spaces, dots, slashes, milestone, phase, plan, artifact, and optional heading identity, and a direct nested request returns the SPA shell."
    - "[EDGE DASH-01/boundary] Zero and complete STATE progress values render as recorded, while a missing or unknown progress member renders `Not recorded` and is never coerced to zero."
    - "[EDGE DASH-01/empty] If STATE supplies no current phase or milestone, the current-position record remains present with an honest unavailable label instead of borrowing ROADMAP data."
    - "[EDGE DASH-01/encoding] Unicode milestone and phase names survive DTO serialization byte-for-byte."
    - "[EDGE DASH-01/precision] Progress uses exact STATE numerator and denominator values, computes a percentage only for a positive denominator, and does not round the underlying counts."
    - "[EDGE DASH-02/ordering] Immediate next work is the first dependency-ready incomplete plan, otherwise the next incomplete phase; one reason and two quieter previews are returned in deterministic order per D-01 and D-04."
    - "[EDGE DASH-03/idempotency] Rebuilding the same snapshot reports each incomplete blocking-human checkpoint or unsatisfied `coverage.human_judgment` wait once; checkpoint status stays pending unless an explicitly linked passing verification or UAT result marks that same wait passed."
    - "[EDGE DASH-04/boundary] `roadmapComplete: null` remains Unknown, zero-plan phases retain observed disk status, and neither becomes the other signal's default."
    - "[EDGE DASH-04/precision] Formal progress counts roadmap plan checkboxes while observed progress counts matching SUMMARY files; each exact numerator and denominator retains its source."
    - "[EDGE DASH-04/concurrency] One DashboardViewModel derives from one immutable snapshot/readAt, so a refresh cannot mix formal values from one load with observed values from another."
  prohibitions:
    - statement: "The interface must not present an inferred phase identity as if a bare phase number were globally unique; milestone context must remain visible wherever a phase, plan, or artifact destination is named."
      status: unverified
      flagged: true
    - statement: "The dashboard must not disguise a derived or observed status as STATE-authored truth; every completion/progress signal must retain visible provenance and any disagreement must remain visible."
      status: unverified
      flagged: true
  artifacts:
    - path: "src/presentation/routes.ts"
      provides: "Milestone-qualified canonical key and URL codec"
      exports: ["milestoneKeyOf", "phaseKeyOf", "artifactTokenOf", "buildArtifactUrl", "parsePresentationUrl"]
    - path: "src/rendering/plan-segments.ts"
      provides: "Reusable bounded PLAN semantic/checkpoint syntax projection"
      exports: ["PlanSegment", "PlanCheckpoint", "segmentPlanBody", "projectPlanCheckpoints"]
    - path: "src/server/project-presentation.ts"
      provides: "Cycle-free project DTO including structured STATE blockers and PLAN checkpoints"
      exports: ["ProjectPresentation", "toProjectPresentation"]
    - path: "src/presentation/dashboard.ts"
      provides: "Pure current/next/attention/formal-vs-observed selectors"
      exports: ["DashboardViewModel", "AttentionItem", "NextWorkItem", "buildDashboardViewModel"]
    - path: "test/presentation/routes.test.ts"
      provides: "Route codec edge regression suite"
    - path: "test/server/deep-links.test.ts"
      provides: "Nested production-route SPA fallback integration suite"
  key_links:
    - from: "src/server/project-presentation.ts"
      to: "src/domain/model.ts"
      via: "StateHandler's `Artifact.structured.blockersConcerns` and shared PLAN checkpoint syntax project once into blocker/checkpoint DTO records; resolved objects become keys or null"
      pattern: "blockersConcerns|projectPlanCheckpoints|target.*Key|null"
    - from: "src/presentation/dashboard.ts"
      to: "src/server/project-presentation.ts"
      via: "Dashboard selectors consume structured blocker/checkpoint fields and never parse artifact body text"
      pattern: "ProjectPresentation"
    - from: "src/web/app-router.tsx"
      to: "src/presentation/routes.ts"
      via: "route declarations and lookup parameters use the shared codec"
      pattern: "build.*Url|parsePresentationUrl"
---

<objective>
Establish the canonical identity/data contract that makes copied routes, dashboard blockers, and human-verification waits truthful before the full UI renders them.

Purpose: Implement D-13 and the D-01 through D-04 source semantics at a single server-side projection boundary. This removes any need for dashboard selectors or later document views to add a second PLAN/STATE body parser.
Output: Route codec with exhaustive tests, deep-link server integration tests, reusable PLAN checkpoint projection, cycle-free ProjectPresentation, and tested DashboardViewModel.
</objective>

<execution_context>
@/home/cinedise/.codex/gsd-core/workflows/execute-plan.md
@/home/cinedise/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-situational-awareness-artifact-reading/02-CONTEXT.md
@.planning/phases/02-situational-awareness-artifact-reading/02-RESEARCH.md
@.planning/phases/02-situational-awareness-artifact-reading/02-PATTERNS.md
@src/domain/model.ts
@src/planning-repo/handlers/state.ts
@src/planning-repo/handlers/plan.ts
@src/planning-repo/assemble.ts
@.planning/phases/02-situational-awareness-artifact-reading/02-01-SUMMARY.md

<interfaces>
`Artifact` carries open `kind`, frontmatter, verbatim `body`, warnings, and handler `structured`. StateHandler already projects `{ sections, quickTasksCompleted }`; PlanHandler intentionally leaves `structured: {}` and preserves the body. `PhaseIdentity` is `{ milestoneVersion, number, projectCode, slug }`; number alone is never identity. `Reference<T>` remains `{ raw, resolved: T | null }`.
</interfaces>
</context>

<assumptions_resolved>
- Immediate-next policy: select the first incomplete plan whose sibling dependencies have summaries; if none, select the next incomplete phase. Explain the selection and expose only two subsequent previews.
- Attention policy: prioritize status discrepancy, authored blocker, dependency block, then human-verification wait. STATE blockers come from structured matching sections; checkpoint waits come from the shared PLAN segmenter; an explicit structured passing verification/UAT link suppresses only its matching wait.
- Unknown/open status values render through a neutral generic badge and are not rejected by an exhaustive switch.
</assumptions_resolved>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Milestone-qualified route codec and direct nested-route delivery</name>
  <reversibility rating="costly">D-13 rates the copied URL shape costly because changing it later invalidates bookmarks, cross-links, and future search destinations; the codec centralizes that coordinated contract.</reversibility>
  <read_first>
    - `src/domain/model.ts` — PhaseIdentity and artifact path contracts
    - `src/planning-repo/assemble.ts` — compound identity precedent
    - `src/server/index.ts` — API/static/fallback order from plan 02-01
    - `.planning/phases/02-situational-awareness-artifact-reading/02-RESEARCH.md` — route recommendation and deep-link pitfall
  </read_first>
  <files>src/presentation/routes.ts, src/web/app-router.tsx, test/presentation/routes.test.ts, test/server/deep-links.test.ts</files>
  <behavior>
    - D-13/ROAD-04: active and archived identical phase numbers encode to distinct keys and URLs.
    - D-13/NAV-06: milestone/phase/plan/artifact/heading URLs round-trip Unicode, spaces, dots, slashes, and encoded separators without identity loss.
    - Malformed, truncated, duplicate, and empty tokens return typed parse failures and never become artifact lookup paths.
    - Direct GET requests for valid dashboard, roadmap, plan, artifact, and heading-bearing nested URLs return the SPA entry document; `/api/*` and real static assets do not fall through.
  </behavior>
  <action>Implement D-13's delegated mechanics as `/milestones/:milestoneKey/phases/:phaseKey`, with `/plans/:planId` or `/artifacts/:artifactToken`, and reserve the fragment for stable heading identity. Encode a missing active milestone as `current`; derive phase keys from milestone context, project code, and phase number, with slug only as readable decoration. Export every builder/parser from `src/presentation/routes.ts` and make `app-router.tsx` use them. Exercise the API/static/fallback ordering already established in `src/server/index.ts` with dedicated route-codec and server integration tests covering duplicate active/archive identities; Unicode, spaces, dots, and slashes; malformed/empty tokens; all nested route shapes; and direct-request SPA fallback. No component may concatenate a canonical destination.</action>
  <verify>
    <automated>npm test -- --run test/presentation/routes.test.ts test/server/deep-links.test.ts &amp;&amp; npm run typecheck &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>ROAD-04 and NAV-06 are mechanically proven across every listed route/encoding/failure case, and pasted nested routes return the shell without allowing request text to become a path.</acceptance_criteria>
  <done>The shared codec is the only milestone-qualified URL construction and parsing contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Structured STATE and PLAN projection before dashboard selection</name>
  <read_first>
    - `src/domain/model.ts` — Project/Milestone/Phase/Plan/open-value contracts
    - `src/planning-repo/handlers/state.ts` — authoritative `structured.sections` shape
    - `src/planning-repo/handlers/plan.ts` — verbatim PLAN body handoff
    - `.planning/phases/02-situational-awareness-artifact-reading/02-RESEARCH.md` — resolved blocker/human-wait source policy
  </read_first>
  <files>src/planning-repo/handlers/state.ts, src/rendering/plan-segments.ts, src/server/project-presentation.ts, test/server/project-presentation.test.ts</files>
  <behavior>
    - StateHandler projects `Artifact.structured.blockersConcerns` from the authored `Blockers` or `Blockers/Concerns` subsection through its existing section utilities.
    - PLAN checkpoints are parsed once by `projectPlanCheckpoints` into name/type/gate syntax records; malformed wrappers degrade locally.
    - ProjectPresentation combines checkpoint syntax with structured verification/UAT links into `pending` or `passed` status, preserving the exact plan/checkpoint or coverage identity required for suppression later.
    - Resolved objects become canonical keys or null; `JSON.stringify(ProjectPresentation)` succeeds with no cycle marker and retains `readAt`.
  </behavior>
  <action>Write projection tests first for structured STATE blockers, non-blocker sections, incomplete blocking-human checkpoints, explicit matching passed checkpoint evidence, malformed PLAN wrappers, unrelated passing verification/UAT records, active/archive duplicate identities, and dense-graph serialization. Extend StateHandler's existing `splitSections`/`splitSubsections` pass to project a dedicated `structured.blockersConcerns` value; downstream code must not rescan its body. Implement the bounded PLAN semantic scanner so it returns reusable `PlanSegment` and normalized `PlanCheckpoint` syntax records containing name, type, and gate; later rendering must consume the same scanner. Implement `toProjectPresentation` as a cycle-free projection that consumes `Artifact.structured.blockersConcerns`, projects every PLAN body through `projectPlanCheckpoints`, and creates `PlanCheckpointDto.status` as `pending` unless a structured verification/UAT pass explicitly links the same plan/checkpoint, in which case it is `passed` with the evidence key. Retain coverage-wait linkage and replace resolved objects with canonical keys or null. Do not implement dashboard selectors, API wiring, or any body scan outside the shared handler/segmenter seams in this task.</action>
  <verify>
    <automated>npm test -- --run test/server/project-presentation.test.ts &amp;&amp; npm run typecheck &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>ProjectPresentation exposes structured blockers/checkpoints/pass links and cycle-free canonical identities from one readAt; no downstream consumer needs to parse STATE or PLAN body text again.</acceptance_criteria>
  <done>The complete reusable input contract exists before any dashboard selector is authored.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Truthful dashboard selectors over the structured projection</name>
  <read_first>
    - `src/server/project-presentation.ts` — sole dashboard input contract from Task 2
    - `.planning/phases/02-situational-awareness-artifact-reading/02-RESEARCH.md` — resolved blocker/human-wait/next-work policies
    - `.planning/phases/02-situational-awareness-artifact-reading/02-CONTEXT.md` — D-01 through D-04
  </read_first>
  <files>src/presentation/dashboard.ts, test/presentation/dashboard.test.ts</files>
  <behavior>
    - D-01/DASH-01: current milestone, STATE phase number/name, status, and exact progress lead the DTO; absent members stay unavailable.
    - D-02/DASH-04: formal roadmap completion and observed SUMMARY completion retain separate values and provenance; disagreement creates one typed attention item.
    - D-03/DASH-02/DASH-03: projected STATE blockers, dependency blocks, checkpoint waits, coverage waits, and discrepancies form one stable-priority typed list.
    - D-04: one dependency-ready immediate item includes a reason and at most two quieter previews.
    - Only an explicitly linked passing record suppresses the matching wait; repeated builds from one ProjectPresentation are deterministic.
  </behavior>
  <action>Write table-driven selector tests first for healthy, discrepant, zero-plan, null-formal, Unicode, blocked dependency, checkpoint wait, unsatisfied `human_judgment`, matching pass suppression, unrelated pass non-suppression, and old/new snapshot isolation. Implement `buildDashboardViewModel` only over ProjectPresentation per D-01 through D-04; it may not scan raw artifact body text or create a second PLAN/STATE parser. Preserve open status values and exact provenance. Plan 02-03 wires the finished selector to the user-facing endpoint alongside its page.</action>
  <verify>
    <automated>npm test -- --run test/presentation/dashboard.test.ts test/server/project-presentation.test.ts &amp;&amp; npm run typecheck &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>All nine DASH edge candidates have named tests, every attention item traces to a structured projected source, linked pass suppression is exact, and dashboard code contains no body parser.</acceptance_criteria>
  <done>The complete sourced DashboardViewModel contract exists for plan 02-03 to expose and render.</done>
</task>

</tasks>

## Artifacts this phase produces

- `src/presentation/routes.ts` — `milestoneKeyOf`, `phaseKeyOf`, `artifactTokenOf`, `buildMilestoneUrl`, `buildPhaseUrl`, `buildPlanUrl`, `buildArtifactUrl`, `parsePresentationUrl`.
- `src/rendering/plan-segments.ts` — `PlanSegment`, `PlanCheckpoint`, `segmentPlanBody`, `projectPlanCheckpoints`.
- `src/planning-repo/handlers/state.ts` — extended StateHandler `structured.blockersConcerns` projection.
- `src/server/project-presentation.ts` — `ProjectPresentation`, `ProjectStateDto`, `PlanCheckpointDto`, `ProjectBlockerDto`, `toProjectPresentation`.
- `src/presentation/dashboard.ts` — `DashboardViewModel`, `AttentionItem`, `NextWorkItem`, `buildDashboardViewModel`.
- `test/presentation/routes.test.ts` — route codec behavior suite.
- `test/server/deep-links.test.ts` — direct nested-route/static/API fallback suite.
- `test/server/project-presentation.test.ts` — STATE/PLAN/pass-link/cycle-free projection suite.
- `test/presentation/dashboard.test.ts` — provenance, checkpoint, blocker, suppression, and snapshot regression suite.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Route parameter -> codec | Untrusted URL tokens request milestone, phase, plan, or artifact identities. |
| Cyclic domain graph -> DTO | Resolved references must flatten without recursive embedding. |
| Artifact structured/body -> dashboard facts | Open STATE/PLAN/SUMMARY content may influence attention labels only through one guarded projection. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-04 | Spoofing | canonical phase identity | high | mitigate | Include milestone, project code, and phase number in reversible keys; test duplicate archive/live identities. |
| T-02-05 | Tampering / Information Disclosure | route decoding | high | mitigate | Typed parsers fail closed and never decode request text into a filesystem path; integration-test traversal-shaped tokens. |
| T-02-06 | Denial of Service | project serialization | medium | mitigate | Return explicit cycle-free records and assert JSON serialization over the dense fixture. |
| T-02-07 | Spoofing / Repudiation | dashboard attention | medium | mitigate | Preserve source/provenance, use one PLAN/STATE projection, and require explicit linked passing records for suppression. |
</threat_model>

<verification>
Run the four focused route/deep-link/projection/dashboard suites, complete test suite, typecheck, lint, and build. Start the existing tracer server, paste active and archived duplicate-phase URLs, and verify distinct codec identities plus SPA responses; plan 02-03 owns presentation/dashboard endpoint wiring.
</verification>

<success_criteria>
- D-13 routes are reversible, milestone-qualified, malformed-token-safe, and direct-request-safe.
- D-01 through D-04 dashboard inputs are structured before selectors run.
- Formal, observed, STATE-authored, dependency-derived, and human-verification facts remain visibly distinct.
- The reusable PLAN segmenter precedes every checkpoint consumer and later semantic renderer.
</success_criteria>

<output>
Create `.planning/phases/02-situational-awareness-artifact-reading/02-02-SUMMARY.md` when done.
</output>
