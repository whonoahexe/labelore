---
phase: 02-situational-awareness-artifact-reading
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/server/index.ts
  - src/server/project-presentation.ts
  - src/presentation/dashboard.ts
  - src/presentation/roadmap.ts
  - src/web/app-router.tsx
  - src/web/pages/dashboard-page.tsx
  - src/web/pages/roadmap-page.tsx
  - src/web/components/app-shell.tsx
  - src/web/components/theme-toggle.tsx
  - src/web/components/ui/button.tsx
  - src/web/styles/globals.css
  - components.json
  - test/presentation/dashboard.test.ts
  - test/presentation/roadmap.test.ts
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03, DASH-04, ROAD-01, ROAD-02, ROAD-03, HIST-01, HIST-02, UI-01, UI-02, UI-03]

estimate:
  tokens: 76000
  raw_tokens: 76000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "[EDGE DASH-01/boundary] Zero and complete STATE progress values render as recorded, a missing/unknown progress member renders `Not recorded`, and no missing field is coerced to zero."
    - "[EDGE DASH-01/empty] If STATE supplies no current phase or milestone value, the current-position panel remains present with an honest unavailable label instead of disappearing or borrowing ROADMAP data."
    - "[EDGE DASH-01/encoding] Unicode milestone and phase names survive DTO serialization and render byte-for-byte as authored."
    - "[EDGE DASH-01/precision] Progress uses the exact STATE numerator/denominator values, computes a percentage only when the denominator is positive, and does not round the underlying counts."
    - "[EDGE DASH-02/unclassified] Immediate next work is the first dependency-ready incomplete plan, otherwise the next incomplete phase; one reason is shown with two quieter previews and blockers remain separately typed in Needs attention, per D-01 and D-04."
    - "[EDGE DASH-03/unclassified] An incomplete blocking-human checkpoint or unsatisfied `coverage.human_judgment` entry appears as a typed Human verification item, and an explicitly linked passed verification/UAT artifact suppresses only that resolved wait."
    - "[EDGE DASH-04/boundary] `roadmapComplete: null` remains Unknown, zero-plan phases retain their observed disk status, and neither becomes the other signal's default."
    - "[EDGE DASH-04/precision] Formal progress counts roadmap plan checkboxes while observed progress counts matching SUMMARY files; each numerator and denominator is displayed with its source."
    - "[EDGE DASH-04/concurrency] One DashboardViewModel is derived from one immutable snapshot/readAt, so a refresh cannot mix formal values from one load with observed values from another."
    - "[EDGE ROAD-01/adjacency] Consecutive phases, including dotted phase numbers, remain separate ordered rows with their own goal, dependencies, success criteria, and requirements."
    - "[EDGE ROAD-01/empty] A milestone with no phases renders a labeled empty phase flow; a phase with no success criteria or requirements renders explicit empty detail when expanded."
    - "[EDGE ROAD-01/ordering] Active and archived phases retain deterministic milestone then `comparePhaseNumbers` order regardless of filesystem enumeration order."
    - "[EDGE ROAD-02/unclassified] Plans are grouped by the open `frontmatter.wave` value into vertically stacked bands, sorted deterministically, and every unresolved/incomplete sibling dependency is shown verbatim as `blocked by`."
    - "[EDGE ROAD-03/unclassified] The primary dependency presentation is an ordered vertical spine with compact connectors and authored dependency labels; source ASCII art is never substituted for the flow and no graph edge is invented from prose."
    - "[EDGE HIST-01/unclassified] Archived milestones are reachable in a dedicated History section, visibly labeled archived, and visually subordinate to the active milestone without being hidden."
    - "[EDGE HIST-02/unclassified] Each archived milestone expands to its own milestone-qualified phase tree and artifacts, including when several archives contain Phase 1."
    - "[EDGE UI-01/unclassified] The shell uses the authorized studio-portal OKLCH palette, base-sera configuration, lucide icons, and squared geometry without any runtime import from studio-portal."
    - "[EDGE UI-02/unclassified] The same root `.dark` class controls tokens, stored theme choice, and code-theme selectors; storage failure still applies the theme for the current page and first paint does not flash the opposite theme."
    - "[EDGE UI-03/unclassified] At 390px and desktop widths, the page body never scrolls horizontally while tables, preformatted code, and diagram containers can scroll inside bounded local wrappers."
  prohibitions:
    - statement: "The dashboard must not disguise a derived or observed status as STATE-authored truth; every completion/progress signal must retain visible provenance and any disagreement must remain visible."
      status: resolved
      verification: judgment
    - statement: "Archived work must not be visually or semantically merged into the active milestone in a way that can make old completion, status, or Phase numbers look current."
      status: resolved
      verification: judgment
  artifacts:
    - path: "src/server/project-presentation.ts"
      provides: "Cycle-free project DTO with canonical reference keys"
      exports: ["ProjectPresentation", "toProjectPresentation"]
    - path: "src/presentation/dashboard.ts"
      provides: "Pure current/next/attention/formal-vs-observed selectors"
      exports: ["DashboardViewModel", "buildDashboardViewModel"]
    - path: "src/presentation/roadmap.ts"
      provides: "Ordered active/history phase and wave view models"
      exports: ["RoadmapViewModel", "buildRoadmapViewModel"]
    - path: "src/web/pages/dashboard-page.tsx"
      provides: "Current-position-first landing page"
      exports: ["DashboardPage"]
    - path: "src/web/pages/roadmap-page.tsx"
      provides: "Vertical roadmap and archived milestone browser"
      exports: ["RoadmapPage"]
    - path: "src/web/styles/globals.css"
      provides: "OKLCH light/dark tokens and overflow containment contract"
  key_links:
    - from: "src/server/project-presentation.ts"
      to: "src/domain/model.ts"
      via: "resolved object references are projected to canonical keys or null, never recursively embedded"
      pattern: "target.*Key|null"
    - from: "src/web/pages/dashboard-page.tsx"
      to: "src/presentation/dashboard.ts"
      via: "component renders DashboardViewModel and derives no planning facts"
      pattern: "DashboardViewModel"
    - from: "src/web/pages/roadmap-page.tsx"
      to: "src/presentation/roadmap.ts"
      via: "accessible disclosures render ordered milestone/phase/wave DTOs"
      pattern: "RoadmapViewModel"
    - from: "src/web/components/app-shell.tsx"
      to: "src/web/styles/globals.css"
      via: "min-width zero layout boundaries and root theme tokens"
      pattern: "min-w-0|dark"
---

<objective>
Deliver the at-a-glance dashboard, execution-oriented roadmap, archived milestone browser, and full visual shell over one cycle-free presentation DTO.

Purpose: Answer “where does the work stand” without opening a file while keeping formal roadmap completion and observed SUMMARY-on-disk progress separate. Implement D-01 through D-08 and the visual/containment requirements without moving source-of-truth logic into React.
Output: Dashboard and roadmap/history selectors, pages, API projections, light/dark studio-portal-derived shell, and edge-complete tests.
</objective>

<execution_context>
@/home/cinedise/.codex/gsd-core/workflows/execute-plan.md
@/home/cinedise/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-situational-awareness-artifact-reading/02-CONTEXT.md
@.planning/phases/02-situational-awareness-artifact-reading/02-RESEARCH.md
@.planning/phases/02-situational-awareness-artifact-reading/02-PATTERNS.md
@.planning/phases/01-read-layer-domain-model/01-03-SUMMARY.md
@.planning/phases/01-read-layer-domain-model/01-04-SUMMARY.md
@src/domain/model.ts
@src/planning-repo/assemble.ts
@src/planning-repo/crossref.ts
@.planning/phases/02-situational-awareness-artifact-reading/02-01-SUMMARY.md

<interfaces>
`Phase` already carries `identity`, `archived`, `goal`, `dependsOnRaw`, `requirementRefs`, `successCriteria`, `roadmapComplete: boolean | null`, `diskStatus: string`, `plans`, and `artifacts`. `Plan` carries `frontmatter`, `summary`, and `dependsOnRefs`. `Reference<T>` is always `{ raw, resolved: T | null }`; null is ordinary data. Consume the canonical key/builders established by plan 02-01 for every DTO link and breadcrumb.
</interfaces>
</context>

<assumptions_resolved>
- Immediate-next policy (research A2): select the first incomplete plan whose sibling dependencies have summaries; if none, select the next incomplete phase. Explain the selection and expose only two subsequent previews.
- Attention policy (research A3): prioritize status discrepancy, authored blocker, dependency block, then human-verification wait; an explicit structured passing verification/UAT reference suppresses only the matching wait.
- Unknown/open status values are rendered through a neutral generic badge. They are never rejected by an exhaustive switch.
</assumptions_resolved>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Current-position dashboard with sourced progress and prioritized attention</name>
  <read_first>
    - `src/domain/model.ts` — Project/Milestone/Phase/Plan/open-value contracts
    - `src/planning-repo/assemble.ts` — independent `roadmapComplete` and `diskStatus` derivation
    - `src/planning-repo/snapshot.ts` — immutable refresh/readAt boundary
    - `.planning/phases/02-situational-awareness-artifact-reading/02-PATTERNS.md` — dashboard and project-presentation assignments
  </read_first>
  <files>src/server/project-presentation.ts, src/presentation/dashboard.ts, test/presentation/dashboard.test.ts, src/web/pages/dashboard-page.tsx, src/server/index.ts</files>
  <behavior>
    - D-01/DASH-01: current milestone, STATE phase number/name, status, and exact progress lead the page; missing fields stay explicitly unavailable.
    - D-02/DASH-04: formal roadmap completion is primary, observed SUMMARY completion is compact and sourced, and disagreement yields a prominent typed attention item.
    - D-03/DASH-02/DASH-03: blockers, human waits, and discrepancies share one stable-priority Needs attention list with type labels.
    - D-04: one immediate dependency-ready item includes a reason and at most two quieter previews.
    - Unicode, zero totals, null formal state, and repeated builds from one snapshot remain deterministic.
  </behavior>
  <action>Write table-driven tests first using `InMemoryPlanningFilesystem` corpora, including healthy, discrepant, zero-plan, null-formal, Unicode, blocked dependency, incomplete blocking-human checkpoint, passed UAT suppression, and a simulated old/new snapshot pair. Implement `toProjectPresentation` as a pure cycle-free projection that replaces resolved objects with canonical keys or null and preserves `readAt`, warnings, body metadata, open frontmatter, active/archive identities, and unknown values; assert `JSON.stringify` has no `$circularRef`. Implement `buildDashboardViewModel` from that DTO with the exact assumptions above and no markdown recounting. Extend `src/server/index.ts` with snapshot-derived presentation/dashboard endpoints. Render only `DashboardViewModel` in `DashboardPage`, following D-01 through D-04 hierarchy and provenance labels. Keep routine dangling references out of Needs attention unless they actually block an incomplete plan.</action>
  <verify>
    <automated>npm test -- --run test/presentation/dashboard.test.ts &amp;&amp; npm run typecheck &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>All nine DASH edge candidates are covered by named tests; the dense fixture shows its deliberate roadmap-vs-summary disagreement as two sourced figures plus one discrepancy item; and current-position data is traceable to STATE frontmatter.</acceptance_criteria>
  <done>The landing page answers current position, next work, blockers, human waits, and completion discrepancy at a glance without deriving source facts in React.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Vertical roadmap flow, wave bands, and milestone history</name>
  <read_first>
    - `src/domain/model.ts` — milestone-qualified phase tree and open wave frontmatter
    - `src/planning-repo/assemble.ts` — live/archive grouping and deterministic phase order
    - `src/planning-repo/crossref.ts` — sibling plan dependency and dangling-reference contract
    - `src/presentation/routes.ts` — canonical destination builders from plan 02-01
  </read_first>
  <files>src/presentation/roadmap.ts, test/presentation/roadmap.test.ts, src/web/pages/roadmap-page.tsx, src/web/app-router.tsx, src/server/index.ts</files>
  <behavior>
    - D-05/ROAD-03: phases render as an ordered vertical spine with compact connectors, status markers, and authored dependency labels, never as a graph canvas or raw ASCII substitute.
    - D-06/ROAD-01: identity/status/goal/dependencies/progress are always visible; criteria, requirement mappings, and plans expand in place.
    - D-08/ROAD-02: open wave values form deterministic vertical bands and incomplete/dangling sibling dependencies render explicit `blocked by` labels.
    - D-07/HIST-01/HIST-02: archived milestones are separately labeled/subdued and each expands to its own milestone-qualified phase tree.
    - Empty milestones, empty detail lists, dotted phases, and three duplicate Phase 1 identities remain distinct and ordered.
  </behavior>
  <action>Write the dense active-plus-two-archives collision tests first, then implement `buildRoadmapViewModel` as a pure projection. Preserve milestone order and `comparePhaseNumbers` semantics; group plan rows by the open `frontmatter.wave` value, use an `Unspecified` band only when absent, sort bands and rows deterministically, and derive row blocking only from `dependsOnRefs`. Keep `dependsOnRaw` visible without manufacturing phase edges. Add roadmap/history endpoints from the same `ProjectPresentation`. Build accessible compact-by-default disclosures, stacked wave bands, the dedicated subdued History section, and canonical links. Update the router using only route builders/parsers from plan 02-01. Do not add the deferred graph canvas, full filesystem tree, or requirements traceability page.</action>
  <verify>
    <automated>npm test -- --run test/presentation/roadmap.test.ts &amp;&amp; npm run typecheck &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>Every phase exposes ROAD-01 fields, every plan is in one wave band with correct blocked-by labels, the primary flow is legible without source ASCII art, and active/v1.0/v2.0 Phase 1 rows navigate to three distinct destinations.</acceptance_criteria>
  <done>A user can scan the active execution narrative, expand phase detail in place, and browse archived milestone trees without identity collisions.</done>
</task>

<task type="auto">
  <name>Task 3: Studio-portal-derived shell, themes, and containment</name>
  <read_first>
    - `/home/cinedise/studio-portal/frontend/app/globals.css` — authorized OKLCH token and Tailwind source
    - `/home/cinedise/studio-portal/frontend/components.json` — base-sera/lucide/squared-corner conventions
    - `/home/cinedise/studio-portal/frontend/components/theme-toggle.tsx` — DOM `.dark` and storage-failure behavior
    - `.planning/phases/02-situational-awareness-artifact-reading/02-PATTERNS.md` — app-shell, theme-toggle, globals, and UI component assignments
  </read_first>
  <files>components.json, src/web/components/app-shell.tsx, src/web/components/theme-toggle.tsx, src/web/components/ui/button.tsx, src/web/styles/globals.css</files>
  <action>Adapt the authorized theme source into this Vite project: `components.json` uses `base-sera`, `rsc: false`, CSS variables, neutral base, lucide, and local aliases. Commit the required base-sera Button source locally, build a minimal document-width-friendly AppShell, and adapt ThemeToggle so the DOM `.dark` class is the source of truth, localStorage failure is non-fatal, lucide icons are decorative, and the control has an accessible name. Port both OKLCH token blocks and `.dark` variant, then enforce `max-width: 100%`, `min-width: 0` at every layout boundary, and local overflow wrappers for tables, `pre`, and Mermaid. Do not use global horizontal clipping as the containment mechanism and never import studio-portal at runtime.</action>
  <verify>
    <automated>npm run lint &amp;&amp; npm run typecheck &amp;&amp; npm run build</automated>
    <human-check>At the end-of-phase browser UAT, open the dense fixture at desktop and 390px. In light and dark themes confirm current position leads, discrepancy becomes prominent only when present, archived history is subdued but reachable, keyboard expansion works, the theme persists across reload, and the body cannot pan sideways while an oversized roadmap/detail table scrolls locally.</human-check>
  </verify>
  <acceptance_criteria>UI-01, UI-02, and UI-03 are observable on real dense content in both themes; the shell remains horizontally fixed at 390px; theme storage failure does not break toggling; and no runtime dependency points to studio-portal.</acceptance_criteria>
  <done>The dashboard and roadmap/history ship inside the required accessible light/dark visual language with robust long-content containment.</done>
</task>

</tasks>

## Artifacts this phase produces

- `src/server/project-presentation.ts` — `ProjectPresentation`, canonical DTO records, `toProjectPresentation`.
- `src/presentation/dashboard.ts` — `DashboardViewModel`, `AttentionItem`, `NextWorkItem`, `buildDashboardViewModel`.
- `src/presentation/roadmap.ts` — `RoadmapViewModel`, `MilestoneFlow`, `WaveBand`, `buildRoadmapViewModel`.
- `src/web/pages/dashboard-page.tsx` — `DashboardPage`.
- `src/web/pages/roadmap-page.tsx` — `RoadmapPage` and archived milestone disclosures.
- `src/web/components/app-shell.tsx` — `AppShell`.
- `src/web/components/theme-toggle.tsx` — `ThemeToggle`.
- `src/web/components/ui/button.tsx` — local base-sera `Button` primitive.
- `src/web/styles/globals.css` and `components.json` — theme/config/containment contract.
- `test/presentation/dashboard.test.ts`, `test/presentation/roadmap.test.ts` — edge and provenance regression suites.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Snapshot -> presentation DTO | Open, partly malformed artifact/frontmatter values become user-visible status facts. |
| DTO -> React | The browser must display provenance without reinterpreting source truth. |
| Active -> archived identity | Repeated phase numbers cross milestone contexts. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05 | Spoofing | dashboard progress/status | medium | mitigate | DTO fields retain source/provenance; selector tests keep formal, observed, derived, null, and unknown values distinct. |
| T-02-06 | Tampering | browser-derived planning facts | medium | mitigate | React pages accept view models only; authoritative selection/counting remains in pure presentation selectors covered by table-driven tests. |
| T-02-07 | Spoofing | active/archive phase collision | medium | mitigate | Every row and link uses the milestone-qualified key/route codec and dense three-Phase-1 tests. |
| T-02-08 | Denial of Service | oversized roadmap content | low | accept | Layout contains wide children locally; the source corpus is local and read-only, and renderer-specific caps/failure isolation are handled in plan 02-03. |
</threat_model>

<verification>
Run the two focused presentation suites, the complete test suite, typecheck, lint, and production build. Then execute the consolidated end-of-phase browser UAT against `fixtures/dense` and the read-only studio-portal project: current-position hierarchy, healthy/discrepant states, active/archive duplicate identities, wave blocking, keyboard disclosure, both themes, 390px containment, and explicit `readAt` are all checked.
</verification>

<success_criteria>
- DASH-01 through DASH-04 are rendered from one sourced DashboardViewModel.
- ROAD-01 through ROAD-03 and HIST-01/HIST-02 are navigable as a vertical execution flow with wave bands.
- Formal roadmap completion and observed SUMMARY presence remain separate everywhere.
- D-01 through D-08 are implemented exactly, including the one-next-plus-two-preview limit.
- UI-01 through UI-03 pass build checks and end-of-phase browser judgment on real long-form content.
</success_criteria>

<output>
Create `.planning/phases/02-situational-awareness-artifact-reading/02-02-SUMMARY.md` when done.
</output>
