---
phase: 02-situational-awareness-artifact-reading
plan: 05
type: execute
wave: 5
depends_on: [02-04]
files_modified:
  - src/presentation/coverage.ts
  - src/presentation/references.ts
  - src/rendering/linkify.ts
  - src/rendering/markdown.ts
  - src/web/pages/artifact-page.tsx
  - src/web/pages/plan-pair-page.tsx
  - src/web/components/reference-preview.tsx
  - src/web/app-router.tsx
  - test/presentation/coverage.test.ts
  - test/rendering/references.test.ts
autonomous: true
requirements: [READ-05, NAV-02, NAV-03, NAV-04]

estimate:
  tokens: 66000
  raw_tokens: 66000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "[EDGE READ-05/ordering] Exact normalized truth/coverage matches precede inferred matches; inference requires textual Jaccard evidence plus any applicable shared-ID guard, mutual-unique ties remain unmatched, and input order never changes pair identity."
    - "[EDGE NAV-02/adjacency] Requirement mentions with the same text near punctuation or other IDs resolve independently to the milestone-qualified covering phase and open preview before navigation."
    - "[EDGE NAV-03/adjacency] Identical phase and plan IDs in different milestones resolve to the containing artifact's milestone; project-root documents use the active milestone."
    - "[EDGE NAV-04/empty] Empty, malformed, or definition-less ID-shaped text remains ordinary text with no anchor, preview trigger, warning, or fabricated destination."
    - "[EDGE NAV-04/encoding] Unicode adjacent to unresolved IDs is preserved byte-for-byte, and authored links, code, pre, and Mermaid source are never linkified."
  prohibitions:
    - statement: "The plan/summary reader must not imply execution evidence that the source does not support; inferred matches must be labeled, unmatched rows must remain visible, and array position must never be presented as proof."
      status: unverified
      flagged: true
    - statement: "A prose mention must not be presented as a valid destination or contextual preview unless the loaded snapshot resolves it in the containing artifact's milestone context."
      status: unverified
      flagged: true
  artifacts:
    - path: "src/presentation/coverage.ts"
      provides: "Conservative deterministic truth-to-coverage matrix"
      exports: ["CoverageMatrix", "CoverageMatch", "buildCoverageMatrix"]
    - path: "src/presentation/references.ts"
      provides: "Milestone-contextual requirement/phase/plan reference registry"
      exports: ["ReferenceRegistry", "ReferencePreviewDto", "buildReferenceRegistry", "resolvePresentationReference"]
    - path: "src/rendering/linkify.ts"
      provides: "Post-sanitize HAST reference metadata plugin"
      exports: ["rehypeResolvedReferences"]
    - path: "src/web/pages/plan-pair-page.tsx"
      provides: "Truth/coverage-first stacked plan-summary reader"
      exports: ["PlanPairPage"]
    - path: "src/web/components/reference-preview.tsx"
      provides: "Controlled type-aware preview bound to delegated DocumentView activation"
      exports: ["ReferencePreview"]
    - path: "test/rendering/references.test.ts"
      provides: "Resolution, linkifier, metadata-to-event bridge, and keyboard/pointer regression suite"
  key_links:
    - from: "src/web/pages/plan-pair-page.tsx"
      to: "src/presentation/coverage.ts"
      via: "matrix precedes full stacked plan and summary documents"
      pattern: "CoverageMatrix"
    - from: "src/rendering/markdown.ts"
      to: "src/rendering/linkify.ts"
      via: "install `rehypeResolvedReferences` after sanitize and before stringify"
      pattern: "rehypeSanitize.*rehypeResolvedReferences.*rehypeStringify"
    - from: "src/web/pages/artifact-page.tsx"
      to: "src/web/components/reference-preview.tsx"
      via: "DocumentView delegates click and Enter/Space from sanitized `data-reference-key` controls to one controlled React preview anchored to the trigger"
      pattern: "data-reference-key|ReferencePreview"
    - from: "src/rendering/linkify.ts"
      to: "src/presentation/references.ts"
      via: "sanitized HAST text becomes preview metadata only after contextual resolution"
      pattern: "resolvePresentationReference"
---

<objective>
Complete truthful plan-summary pairing and preview-first milestone-aware cross-links through one executable sanitized-HTML-to-React interaction bridge.

Purpose: Implement D-12, D-14, D-15, and D-17 without allowing shared requirement IDs, array order, or unresolved prose to masquerade as evidence or navigation authority.
Output: Conservative coverage matrix, contextual reference registry, installed post-sanitize linkifier, delegated React preview interaction, plan-pair page, and integration regressions.
</objective>

<execution_context>
@/home/cinedise/.codex/gsd-core/workflows/execute-plan.md
@/home/cinedise/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-situational-awareness-artifact-reading/02-CONTEXT.md
@.planning/phases/02-situational-awareness-artifact-reading/02-RESEARCH.md
@.planning/phases/02-situational-awareness-artifact-reading/02-PATTERNS.md
@src/domain/model.ts
@src/planning-repo/crossref.ts
@src/planning-repo/assemble.ts
@.planning/phases/02-situational-awareness-artifact-reading/02-04-SUMMARY.md

<interfaces>
`Reference<T>` authorizes a destination only when `resolved !== null`. Use the canonical route codec for every preview Open destination. `DocumentView` is the sole sanitized HTML mount; this plan extends it through event delegation rather than attempting to instantiate React components inside `dangerouslySetInnerHTML`.
</interfaces>
</context>

<assumptions_resolved>
- Authoritative READ-05 rule: exact normalized text first. Remaining candidates require significant-token Jaccard >= 0.60; if both rows carry requirement IDs they must also share one. A pair is inferred only when it is the unique highest score for both rows. Ties and competing best candidates stay unmatched.
- Preview interaction uses one controlled Base UI Popover outside the sanitized mount. Pointer click and Enter/Space on emitted metadata open it; Escape/outside click dismisses and focus returns to the original trigger per D-14/D-15.
</assumptions_resolved>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Conservative truth-to-coverage pairing and stacked document review</name>
  <read_first>
    - `src/planning-repo/assemble.ts` — actual plan/summary artifact pairing contract
    - `.planning/phases/02-situational-awareness-artifact-reading/02-RESEARCH.md` — resolved authoritative matching rule
    - `.planning/phases/02-situational-awareness-artifact-reading/02-PATTERNS.md` — coverage and plan-pair assignments
    - `/home/cinedise/studio-portal/.planning/phases/04-bulk-archive-downloads/04-05-PLAN.md` and matching SUMMARY — qualitative mismatch-sensitive pair only
  </read_first>
  <files>src/presentation/coverage.ts, test/presentation/coverage.test.ts, src/web/pages/plan-pair-page.tsx, src/web/app-router.tsx</files>
  <behavior>
    - Exact normalized matches always precede inference.
    - A shared requirement ID without Jaccard >= 0.60 remains unmatched; rows missing IDs may infer only through the same textual threshold.
    - Inference requires a mutual unique best candidate; equal ties and competing best candidates remain unmatched.
    - Reordering either input does not change pair identities; exact, inferred, unmatched-truth, and unmatched-coverage rows all remain visible.
    - D-12 places the matrix first, then full stacked plan and summary documents with jump links and full reading width.
  </behavior>
  <action>Write exact/text-only/shared-ID-only/tie/competing/reordered/unmatched tests first. Normalize case, whitespace, and punctuation; compute significant-token Jaccard as intersection divided by union. Apply exact one-to-one matches first. For remaining candidates, require Jaccard >= 0.60 and, when both rows declare IDs, at least one shared ID; accept only mutual unique highest scores and leave every ambiguity unmatched. Label heuristic rows `inferred` and never zip arrays. Build D-12 PlanPairPage with the matrix first, full plan and summary in stacked sections, jump links, canonical breadcrumbs, and local overflow containment.</action>
  <verify>
    <automated>npm test -- --run test/presentation/coverage.test.ts &amp;&amp; npm run typecheck &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>READ-05 cannot infer from a shared requirement ID or position alone, ties remain honest, all rows are count-preserved, and both full documents remain readable after the matrix.</acceptance_criteria>
  <done>A user can compare plan intent with actual summary evidence without false or hidden matches.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Contextual reference resolution and installed post-sanitize linkifier</name>
  <read_first>
    - `src/planning-repo/crossref.ts` — scoped resolution and null-preserving precedent
    - `src/domain/model.ts` — requirement, phase, plan, and mention contracts
    - `src/rendering/markdown.ts` — sanitizer pipeline from plan 02-04
    - `src/presentation/routes.ts` — exclusive URL builder/parser contract
  </read_first>
  <files>src/presentation/references.ts, src/rendering/linkify.ts, src/rendering/markdown.ts, test/rendering/references.test.ts</files>
  <behavior>
    - Resolved requirement, phase, and plan mentions receive trusted preview metadata only after contextual resolution.
    - Archived references resolve inside their containing milestone; root documents resolve against the active milestone.
    - Null, malformed, and empty IDs stay plain text and add no warning or preview.
    - The sanitized HAST walker skips authored links, code, pre, and Mermaid nodes and preserves adjacent Unicode.
    - Renderer output contains reference metadata only because `rehypeResolvedReferences` runs after sanitize and before stringify.
  </behavior>
  <action>Write active/archive/null/code/Unicode fixtures first. Build lookup maps once per ProjectPresentation and resolve requirement IDs through covering phases plus phase/plan IDs relative to the containing artifact milestone. Implement exported `rehypeResolvedReferences` as a bounded sanitized-HAST text walker that skips `a`, `code`, `pre`, and Mermaid ancestry. For successful targets, emit keyboard-focusable trusted controls carrying only a canonical `data-reference-key`; unresolved text remains unchanged. Install the plugin in `createArtifactRenderer` after `rehypeSanitize` and before stringify, passing only the snapshot-built registry and canonical route output. Do not add raw-Markdown rewriting or a second route builder.</action>
  <verify>
    <automated>npm test -- --run test/rendering/references.test.ts test/rendering/markdown.test.ts &amp;&amp; npm run typecheck &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>NAV-02 through NAV-04 have automated active/archive/dangling/code/Unicode coverage, the plugin is installed at the safe pipeline position, and only resolved IDs receive metadata.</acceptance_criteria>
  <done>Sanitized documents carry trustworthy milestone-contextual reference controls without changing unresolved source text.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Delegated DocumentView preview interaction and final document routes</name>
  <read_first>
    - `src/web/pages/artifact-page.tsx` — sole sanitized mount and heading/Mermaid lifecycle
    - `src/rendering/linkify.ts` — emitted `data-reference-key` contract
    - `src/presentation/references.ts` — type-aware preview DTO
    - `.planning/phases/02-situational-awareness-artifact-reading/02-CONTEXT.md` — D-14, D-15, and D-17
  </read_first>
  <files>src/web/pages/artifact-page.tsx, src/web/components/reference-preview.tsx, src/web/app-router.tsx, test/rendering/references.test.ts</files>
  <behavior>
    - Clicking or pressing Enter/Space on emitted reference metadata opens one React preview before any navigation occurs.
    - The controlled preview shows identity, title, status, location, exactly one type-specific detail, and an explicit canonical Open action.
    - Escape and outside click dismiss; focus returns to the exact sanitized-document trigger.
    - The metadata-to-delegation bridge test starts from renderer output and proves both pointer and keyboard activation without direct navigation.
  </behavior>
  <action>Keep sanitized HTML inert and add event delegation to DocumentView's container. On click or Enter/Space, find the closest focusable `data-reference-key` control, prevent implicit navigation, resolve its already-authorized preview DTO, store the actual trigger element as the Base UI Popover anchor, and render exactly one controlled `ReferencePreview` outside `dangerouslySetInnerHTML`. On dismiss, restore focus to that trigger. Implement D-14/D-15 preview fields and explicit Open action through the canonical codec; complete artifact and plan-pair routes in `appRouter`. Extend `test/rendering/references.test.ts` into an integration seam that renders a resolved mention through `createArtifactRenderer`, feeds the emitted metadata to the exported delegation handler for click and Enter/Space, and asserts preview state opens while navigation remains untouched; also cover Escape dismissal/focus-return state.</action>
  <verify>
    <automated>npm test -- --run test/rendering/references.test.ts &amp;&amp; npm test &amp;&amp; npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>The sanitized renderer and React preview are connected by one tested delegation bridge; pointer/keyboard open preview before navigation, Open uses the canonical destination, and dismiss restores focus.</acceptance_criteria>
  <done>All resolved prose references are previewable and navigable while unresolved references remain ordinary text.</done>
</task>

</tasks>

## Artifacts this phase produces

- `src/presentation/coverage.ts` — `CoverageMatrix`, `CoverageMatch`, `buildCoverageMatrix`.
- `src/presentation/references.ts` — `ReferenceRegistry`, `ReferencePreviewDto`, `buildReferenceRegistry`, `resolvePresentationReference`.
- `src/rendering/linkify.ts` — `rehypeResolvedReferences`.
- `src/web/pages/plan-pair-page.tsx` — `PlanPairPage`.
- `src/web/components/reference-preview.tsx` — `ReferencePreview`.
- `src/web/pages/artifact-page.tsx` — extended `DocumentView` delegation plus existing `ArtifactPage`.
- `test/presentation/coverage.test.ts` — exact/inferred/tie/ambiguity/reorder matrix suite.
- `test/rendering/references.test.ts` — contextual resolution, linkifier installation, and metadata-to-React interaction bridge suite.

<edge_coverage_accounting>
The deterministic spec-less probe surfaced 34 candidates. Manual classification resolved every formerly zero-classified row into one of the closed eight categories with an explicit, testable truth: plan 02-02 covers ROAD-04, NAV-06, DASH-01 x4, DASH-02, DASH-03, and DASH-04 x3 (11); plan 02-03 covers ROAD-01 x3, ROAD-02, ROAD-03, HIST-01, HIST-02, UI-01, UI-02, and UI-03 (10); plan 02-04 covers READ-01 x3, READ-02, READ-03 x2, READ-04, and READ-06 (8); plan 02-05 covers READ-05, NAV-02, NAV-03, and NAV-04 x2 (5). Equality: 34 surfaced = 34 authored + 0 flagged edge assumptions. Every authored edge names one of the closed eight categories.
</edge_coverage_accounting>

<prohibition_recall>
Two-stage recall/precision ran across all 23 requirements. Routine correctness candidates remain on the edge/task verification axis. Stored-XSS, unsafe schemes, traversal, denial, and generic accessibility/security candidates are canon referrals modeled in ASVS threat registers and eligible for `$gsd-secure-phase`, not duplicate bespoke prohibitions. Five bespoke transparency/product-integrity candidates survived: two in plan 02-02, one in plan 02-03, and two here. All five use the canonical descriptor-less fallback shape `status: unverified` plus `flagged: true`, with no `verification` or `check_*` fields. Equality: 5 surfaced = 5 authored + 0 dropped.
</prohibition_recall>

<source_coverage_audit>
## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Plan | Status |
|--------|----|----------------------|------|--------|
| GOAL | — | At-a-glance work status plus safely formatted cross-linked artifacts | 01-06 | COVERED |
| REQ | DASH-01..04 | Current position, next/blockers/human waits, dual progress | 01-03 | COVERED |
| REQ | ROAD-01..04 | Full phase detail, wave bands, legible flow, qualified identity | 02-03 | COVERED |
| REQ | HIST-01..02 | Distinct browsable archived milestone trees | 03 | COVERED |
| REQ | UI-01..03 | Visual language, themes, horizontal containment | 03, 06 | COVERED |
| REQ | READ-01..06 | GFM, frontmatter, PLAN structure, sanitization, pairing, anchors | 04-06 | COVERED |
| REQ | NAV-02..04, NAV-06 | Preview-first contextual links, dangling text, bookmarkable routes | 02, 05-06 | COVERED |
| RESEARCH | — | Package legitimacy checkpoint and exact pinned stack | 01 | COVERED |
| RESEARCH | — | One snapshot seam, cycle-free DTO, structured blocker/checkpoint projection | 01-02 | COVERED |
| RESEARCH | — | Canonical route codec shared by all consumers | 02-05 | COVERED |
| RESEARCH | — | Bounded PLAN grammar and sanitize-before-enrichment pipeline | 02, 04-05 | COVERED |
| RESEARCH | — | Conservative mutual-unique matching with textual evidence | 05 | COVERED |
| RESEARCH | — | Strict Mermaid/local degradation and blocking host-browser UAT | 04, 06 | COVERED |
| CONTEXT | D-01..D-04 | Landing hierarchy and attention/next limits | 01-03 | COVERED |
| CONTEXT | D-05..D-08 | Vertical roadmap, expansion, history, waves | 03 | COVERED |
| CONTEXT | D-09..D-12 | Document canvas, open frontmatter, PLAN semantics, pair matrix | 04-05 | COVERED |
| CONTEXT | D-13 | Costly milestone-qualified route contract | 02 | COVERED |
| CONTEXT | D-14..D-17 | Preview-first links, type-aware preview, copy anchors, dangling text | 04-06 | COVERED |

Deferred graph canvas, search, full filesystem tree, traceability view, live watching, write-back, and multi-project switching are source exclusions and not gaps.
</source_coverage_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Truth/coverage text -> evidence matrix | Similar prose must not become unsupported execution evidence. |
| Sanitized HAST -> trusted reference metadata | Only resolved snapshot identities gain interaction authority. |
| Sanitized HTML -> React event delegation | Metadata triggers one controlled React preview outside the HTML mount. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-16 | Repudiation | `src/presentation/coverage.ts` | high | mitigate | Require exact or mutual-unique textual inference, reject ties/shared-ID-only candidates, label inference, and retain unmatched rows. |
| T-02-17 | Spoofing | `src/rendering/linkify.ts` | medium | mitigate | Resolve in milestone context before emitting metadata; null targets remain text; routes come only from the codec. |
| T-02-18 | Elevation of Privilege | DocumentView delegation | high | mitigate | Accept only post-sanitize application-generated keys, prevent implicit navigation, and render controlled React content outside innerHTML. |
| T-02-19 | Denial of Service | reference HAST walk | medium | mitigate | Use bounded linear walking with skipped code/pre/link/Mermaid ancestry and one registry built per presentation. |
</threat_model>

<verification>
Run coverage and reference focused suites, the renderer suite, complete tests, typecheck, lint, and build. The reference integration starts from rendered sanitized output and proves click/keyboard preview-before-navigation plus dismissal/focus restoration state.
</verification>

<success_criteria>
- READ-05 cannot claim evidence from shared IDs, array order, ties, or unmatched rows.
- NAV-02 through NAV-04 resolve only in correct milestone context and preserve null text.
- D-12, D-14, D-15, and D-17 are implemented through canonical routes and one executable interaction bridge.
- The 34-edge and five-prohibition no-silent-drop equalities close exactly.
</success_criteria>

<output>
Create `.planning/phases/02-situational-awareness-artifact-reading/02-05-SUMMARY.md` when done.
</output>
