---
phase: 02-situational-awareness-artifact-reading
plan: 07
type: execute
wave: 7
depends_on: [02-06]
files_modified:
  - src/presentation/dashboard.ts
  - test/presentation/dashboard.test.ts
autonomous: true
gap_closure: true
requirements: [DASH-02, DASH-03]

estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The dashboard's primary 'Next up' call-to-action for a phase-kind item resolves through parsePresentationUrl to a kind: 'phase' route whose identity round-trips back to the same phase, so following it reaches the roadmap phase view instead of the router's catch-all NotFound (DASH-02, D-04, D-13)."
    - "The blocker next-work item's url is the current phase's canonical buildPhaseUrl route when a current phase is resolvable, and the literal /roadmap when none is (DASH-02, D-03, D-13)."
    - "A phase-kind next-work item and the roadmap phase row for the same phase carry byte-identical url values — one phase never yields two different route shapes across two producers (edge: DASH-02 adjacency)."
    - "With no resolvable current phase and exactly one authored blocker, next.immediate.url is /roadmap and parses to the roadmap route (edge: DASH-02 empty)."
    - statement: "For a fixed presentation snapshot, next.immediate and next.previews are emitted in a stable, repeatable order across successive buildDashboardViewModel calls."
      verification: backstop
    - "A pending blocking-human checkpoint and the plan it gates remain two distinct next-work entries — the checkpoint is next.immediate and the gated plan is the first preview; equal-priority items never merge into one entry (edge: DASH-03 adjacency)."
    - statement: "With zero pending checkpoints and zero coverage waits, no human-verification next-work item and no checkpoint or coverage attention row is emitted."
      verification: backstop
    - statement: "Attention rows are emitted in a stable, deterministic order for a fixed presentation snapshot."
      verification: backstop
    - "The 'Next up' surface still renders exactly one primary item plus at most two quieter previews per D-04 — this fix changes destinations only, not item count or panel shape (UI-SPEC E1 populated/partial)."
    - "current.progress.computedPercent.display renders at most one decimal place and never a full floating-point expansion, so the public /api/dashboard contract cannot ship an unreadable percentage to a future consumer."
  prohibitions:
    - statement: "MUST NOT present a next-work item whose url the application's own router cannot resolve as an actionable call-to-action — a destination the tool itself cannot reach must never be styled as the recommended next step."
      category: transparency
      requirement_id: DASH-02
    - statement: "MUST NOT fabricate or infer a next-work item when the snapshot reports no dependency-ready plan and no incomplete phase; absence must render as the authored quiet state, never as invented work."
      category: transparency
      requirement_id: DASH-02
    - statement: "MUST NOT silently omit a pending human-verification wait from the dashboard, which would make the project read as further along than its own artifacts support."
      category: transparency
      requirement_id: DASH-03
  artifacts:
    - path: "src/presentation/dashboard.ts"
      provides: "phaseWork() and blockerWork() emitting canonical buildPhaseUrl routes, and a rounded computedPercent.display"
    - path: "test/presentation/dashboard.test.ts"
      provides: "Identity-grounded phase fixtures and route round-trip regression coverage for phase-kind and blocker-kind next-work urls"
  key_links:
    - from: "src/presentation/dashboard.ts (phaseWork/blockerWork)"
      to: "src/presentation/routes.ts (buildPhaseUrl)"
      via: "direct import and call on PhaseDto.identity"
      pattern: "buildPhaseUrl"
    - from: "test/presentation/dashboard.test.ts"
      to: "src/presentation/routes.ts (parsePresentationUrl, phaseKeyOf)"
      via: "round-trip assertion over the emitted next-work url"
      pattern: "parsePresentationUrl"
---

<objective>
Close verification gap 1 (02-REVIEW.md CR-01): make the dashboard's most prominent interactive
element — the "Next up" call-to-action and the blocker-resolution link — point at the canonical phase
route instead of the bare `PhaseDto` identity segment, and close the test-fixture weakness that let a
non-navigable url ship past 236 passing tests.

Purpose: Success Criterion 1 requires the landing view to answer "where am I" without opening a file.
A recommended action that 404s inside the app sends the user back to hunting through the collapsed
roadmap for exactly the information the dashboard exists to surface (DASH-02, DASH-03, D-01, D-04,
D-13).
Output: `src/presentation/dashboard.ts` emitting router-resolvable phase routes, plus regression
coverage in `test/presentation/dashboard.test.ts` built from the real identity helpers rather than an
opaque fixture string.
</objective>

<execution_context>
@/home/cinedise/.claude/gsd-core/workflows/execute-plan.md
@/home/cinedise/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-situational-awareness-artifact-reading/02-CONTEXT.md
@.planning/phases/02-situational-awareness-artifact-reading/02-VERIFICATION.md
@.planning/phases/02-situational-awareness-artifact-reading/02-REVIEW.md
@.planning/phases/02-situational-awareness-artifact-reading/02-02-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Route phase-kind and blocker-kind next work through the canonical phase-URL builder</name>
  <read_first>
    - `src/presentation/dashboard.ts` — current `phaseWork()` (~line 183), `blockerWork()` (~lines 218-232), and the `NextWorkItem` interface (~line 24) that declares the `url` field
    - `src/presentation/routes.ts` lines 89-162 — `milestoneKeyOf`, `phaseKeyOf`, `buildMilestoneUrl`, `buildPhaseUrl`; note `buildPhaseUrl(identity)` composes `/milestones/<milestoneKey>/phases/<phaseKey>`
    - `src/presentation/references.ts` lines 60-90 — `requirementPreview()` and `phasePreview()`, the in-repo analog that already builds phase destinations correctly; copy this call shape exactly
    - `src/presentation/roadmap.ts` lines 159-192 — `phaseRow()`, the second correct producer; its `url` must end up byte-identical to the dashboard's for the same phase
    - `src/web/app-router.tsx` lines 41-59 — the registered route table; confirm no pattern matches a bare `p~…` identity segment
    - `src/web/pages/dashboard-page.tsx` lines 68-79 and 157-168 — `NextWork` passing `item.url` straight into `<Link to>`, rendered as the `primary` CTA
    - `test/presentation/dashboard.test.ts` lines 1-80 — the existing fixture builders you will extend in Task 2
    - `.planning/phases/02-situational-awareness-artifact-reading/02-REVIEW.md` lines 98-165 — CR-01, including the already-specified fix
  </read_first>
  <files>src/presentation/dashboard.ts, test/presentation/dashboard.test.ts</files>
  <behavior>
    - Given a `PhaseDto` whose `identity` is `{ milestoneVersion: 'v2.0', number: '02', projectCode: null, slug: 'later' }`, the emitted phase-kind `NextWorkItem.url` equals `buildPhaseUrl(identity)`.
    - `parsePresentationUrl(view.next.immediate.url)` returns `{ ok: true }` with `route.kind === 'phase'`, and `phaseKeyOf(route.phaseIdentity)` equals the phase's own `key`.
    - Given an authored blocker and a resolvable current phase, the blocker-kind `NextWorkItem.url` equals `buildPhaseUrl(currentPhase.identity)`.
    - Given an authored blocker and no resolvable current phase, the blocker-kind `NextWorkItem.url` is the string `/roadmap`, and `parsePresentationUrl` on it returns `route.kind === 'roadmap'`.
  </behavior>
  <action>
Add `import { buildPhaseUrl } from './routes.ts';` to `src/presentation/dashboard.ts` (the file
currently imports only the `project-presentation.ts` types — this is the first runtime import).

In `phaseWork(phase: PhaseDto)`, replace the value assigned to the `url` field — today the bare
`PhaseDto` identity segment, which no pattern in `src/web/app-router.tsx` registers — with
`buildPhaseUrl(phase.identity)`. Leave `key` and `phaseKey` on that object untouched: they remain the
opaque DTO identity key and are consumed as identity, not as routes (`checkpointWork()` compares
`candidate.phaseKey === currentPhase.key`, and `nextWork()` compares `phase.key !== currentPhase.key`
— breaking those would regress dependency selection).

In `blockerWork(presentation, currentPhase)`, replace the `url` fallback expression — today the
current phase's bare identity key with a `'/roadmap'` fallback — with a conditional that yields
`buildPhaseUrl(currentPhase.identity)` when `currentPhase` is non-null and the literal `'/roadmap'`
otherwise. Leave `phaseKey` on that object as the identity key with its empty-string fallback.

This is the fix specified verbatim in 02-REVIEW.md CR-01 and required by 02-VERIFICATION.md gap 1.
Do not introduce a new URL-building helper: `buildPhaseUrl` is the single canonical producer already
used by `references.ts` and `roadmap.ts`, and minting a second one is the exact asymmetry that caused
this defect.

Write the failing round-trip test first, in `test/presentation/dashboard.test.ts`. Import
`buildPhaseUrl`, `parsePresentationUrl`, and `phaseKeyOf` from `../../src/presentation/routes.ts`.
Add one test that drives `buildDashboardViewModel` to a phase-kind `next.immediate` (the existing
`'blocked dependency: …'` test at ~line 270 already constructs that state — model the new test on it)
and asserts all four behaviors above. Confirm it fails against the unfixed selector before applying
the change.
  </action>
  <reversibility rating="reversible">Swapping the `url` expression back is a two-line revert; no data, route shape, or public type changes.</reversibility>
  <verify>
    <automated>npx vitest run test/presentation/dashboard.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run test/presentation/dashboard.test.ts` exits 0.
    - `grep -c "buildPhaseUrl" src/presentation/dashboard.ts` returns at least 3 (the import plus one call in each of `phaseWork` and `blockerWork`).
    - `grep -nE "url: (phase|currentPhase\?)\.key" src/presentation/dashboard.ts` produces no output and exits non-zero.
    - `grep -n "parsePresentationUrl" test/presentation/dashboard.test.ts` returns at least one match.
    - The new test asserts `route.kind === 'phase'` on the parse result of `view.next.immediate.url`, not merely that the string is non-empty.
  </acceptance_criteria>
  <done>A phase-kind and a blocker-kind next-work item both emit a url that `parsePresentationUrl` resolves to a route registered in `app-router.tsx`, proven by a test that fails on the pre-fix selector.</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild the dashboard test fixtures on the real identity helpers so the route contract cannot silently regress</name>
  <read_first>
    - `test/presentation/dashboard.test.ts` in full — every `'phase:01'` / `'phase:02'` literal (lines 15, 29, 57, 73, 173, 175, 273, 285, 313) and the `plan()`, `phase()`, `checkpoint()`, `coverageWait()`, `presentation()` builders
    - `src/presentation/routes.ts` lines 89-112 — `phaseKeyOf` and the `p~…` key shape it produces
    - `src/server/project-presentation.ts` lines 426-431 and 505-520 — how the real `PhaseDto.key` and `milestoneKey` are minted (`phaseKeyOf(phase.identity)` and `milestoneKeyOf(milestone.version)`), which is the contract the fixture must mirror
    - `.planning/phases/02-situational-awareness-artifact-reading/02-VERIFICATION.md` lines 33-35 — the `missing` items for gap 1, which require the fixture gap be closed, not just the symptom
  </read_first>
  <files>test/presentation/dashboard.test.ts</files>
  <action>
The root cause 02-VERIFICATION.md identifies is that this file's `phase()` helper mints
`key` as a literal fixture string decoupled from `phaseKeyOf`, so no test could ever have caught the
identity-key-as-route confusion. Close that.

Declare two module-level identity constants at the top of the file, after the imports:
`LIVE_IDENTITY` = `{ milestoneVersion: 'v2.0', number: '01', projectCode: null, slug: 'live' }` and
`LATER_IDENTITY` = the same with `number: '02'` and `slug: 'later'`. Derive
`const LIVE_PHASE_KEY = phaseKeyOf(LIVE_IDENTITY);` and
`const LATER_PHASE_KEY = phaseKeyOf(LATER_IDENTITY);` from the real helper, and
`const LIVE_MILESTONE_KEY = milestoneKeyOf('v2.0');`.

Replace every occurrence of the two opaque colon-form phase fixture strings currently used in this
file — the ones enumerated by line number in `<read_first>` — with those derived constants. Every
site: `plan()`'s `phaseKey`, `phase()`'s `key` and `identity`,
`checkpoint()`'s `phaseKey`, `coverageWait()`'s `phaseKey` if present, the discrepancy-provenance
`ref` at ~line 173 and its assertion at ~line 175, the `phase({ key: … })` override blocks at ~lines
273 and 313, and the `kind: 'phase'` assertion at ~line 285. Replace `phase()`'s `milestoneKey`
literal with `LIVE_MILESTONE_KEY`. The cross-references must stay internally consistent: `phaseKey`
on plans/checkpoints/coverage waits must equal the owning phase's `key`, because `checkpointWork()`
and `nextWork()` both filter on that equality.

Then add the remaining gap-1 regression cases:
1. A blocker-kind case with a resolvable current phase — assert `next.immediate.kind` is `'blocker'`
   and its `url` equals `buildPhaseUrl(LIVE_IDENTITY)`.
2. A blocker-kind case with no resolvable current phase (a presentation whose `state` yields no
   matching phase) — assert the url is `/roadmap` and `parsePresentationUrl` returns
   `route.kind === 'roadmap'`. This is the DASH-02 `empty` edge.
3. A cross-producer equality case — build the same `PhaseDto` through `buildRoadmapViewModel`'s
   `phaseRow` shape or simply assert `view.next.immediate.url === buildPhaseUrl(LATER_IDENTITY)`, so
   the dashboard and the roadmap can never diverge on one phase's route. This is the DASH-02
   `adjacency` edge.
4. A DASH-03 `adjacency` case — with one pending blocking-human checkpoint present, assert
   `next.immediate.kind` is `'human-verification'` and `next.previews[0].kind` is `'plan'` with a
   different `key`, proving the checkpoint and the plan it gates stay two distinct entries. The
   existing test at ~line 298 already covers most of this; extend it with the distinct-`key`
   assertion rather than duplicating it.

Do not add jsdom, testing-library, or any new dependency: `vitest.config.ts` includes only
`test/**/*.test.ts` and this file is a pure selector test.
  </action>
  <verify>
    <automated>npx vitest run test/presentation/dashboard.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run test/presentation/dashboard.test.ts` exits 0 with a test count strictly greater than the pre-change count for this file.
    - `grep -n "'phase:0" test/presentation/dashboard.test.ts` produces no output and exits non-zero.
    - `grep -c "phaseKeyOf" test/presentation/dashboard.test.ts` returns at least 2 (the import plus the derived key constants).
    - The file contains an assertion comparing a next-work `url` to `buildPhaseUrl(...)` and an assertion comparing a next-work `url` to the string `/roadmap`.
    - `npm test` exits 0 and reports a total strictly greater than 236 tests.
  </acceptance_criteria>
  <done>Every phase key in this test file is derived from `phaseKeyOf`, and phase-kind, blocker-kind (both with and without a current phase), and human-verification next-work items all carry asserted, route-resolvable urls.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Round the derived completion percentage before it reaches the public dashboard contract</name>
  <read_first>
    - `src/presentation/dashboard.ts` lines 346-390 — `buildDashboardViewModel`, the `computedPercent` derivation at ~351-354 and its `SourcedValue` projection at ~380-384
    - `src/presentation/dashboard.ts` lines 30-56 — the `SourcedValue` and progress interfaces, so the `value` field's contract is preserved
    - `.planning/phases/02-situational-awareness-artifact-reading/02-REVIEW.md` lines 227-254 — WR-03 and its specified fix
  </read_first>
  <files>src/presentation/dashboard.ts, test/presentation/dashboard.test.ts</files>
  <behavior>
    - Given `completedPlans: 2` and `totalPlans: 7`, `current.progress.computedPercent.display` is `'28.6%'`.
    - Given `completedPlans: 1` and `totalPlans: 2`, the display is `'50%'` or `'50.0%'` — one consistent formatting rule, asserted exactly.
    - Given a null `completedPlans` or `totalPlans`, or a zero `totalPlans`, the display remains the existing `'Not recorded'` string and `value` remains `null`.
    - `computedPercent.value` remains the unrounded number — only the human-facing `display` string is formatted.
  </behavior>
  <action>
This is WR-03 from 02-REVIEW.md, filed Info. It is folded into this plan rather than declined because
it lives in the same function and the same file as the gap-1 fix, is a one-expression change, and is
part of the public `/api/dashboard` response contract that Phase 3 will consume.

In the `computedPercent` projection inside `buildDashboardViewModel`, format the `display` string
through a single rounding step instead of interpolating the raw float. Use
`computedPercent.toFixed(1)` and strip a trailing `.0` so whole percentages read cleanly, or
equivalently `Math.round(computedPercent * 10) / 10` — pick one and apply it consistently; the
`<behavior>` block above pins the exact expected strings, so make the assertions match the rule you
choose. Leave the `value` field, the `null` branch (`'Not recorded'`), and the `provenance` object
byte-identical.

Add the four `<behavior>` cases as assertions in `test/presentation/dashboard.test.ts` before making
the change and confirm the 2/7 case fails first.
  </action>
  <reversibility rating="reversible">A one-expression formatting change on a display-only string; no consumer exists today.</reversibility>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0.
    - `grep -nE "toFixed|Math\.round" src/presentation/dashboard.ts` returns at least one match inside the `computedPercent` projection.
    - `test/presentation/dashboard.test.ts` contains an exact-string assertion on `computedPercent.display` for a non-evenly-dividing plan ratio.
    - `node -e` or a test assertion confirms `computedPercent.value` for 2/7 is still the unrounded number, not the rounded one.
  </acceptance_criteria>
  <done>`/api/dashboard` cannot emit a long floating-point percentage string, and the `Not recorded` and `value` contracts are unchanged.</done>
</task>

</tasks>

## Artifacts this phase produces

New and modified symbols introduced by this plan — the plan-review-convergence source-grounding pass
must exclude these from drift verification because they do not exist before this plan runs:

- `src/presentation/dashboard.ts` — new runtime import of `buildPhaseUrl` from `./routes.ts`; modified
  `phaseWork()` and `blockerWork()` `url` expressions; modified `computedPercent.display` formatting
  inside `buildDashboardViewModel()`. No new exported symbol.
- `test/presentation/dashboard.test.ts` — new module-level constants `LIVE_IDENTITY`,
  `LATER_IDENTITY`, `LIVE_PHASE_KEY`, `LATER_PHASE_KEY`, `LIVE_MILESTONE_KEY`; new imports of
  `buildPhaseUrl`, `parsePresentationUrl`, `phaseKeyOf`, `milestoneKeyOf` from
  `../../src/presentation/routes.ts`; new test cases for phase-kind url round-trip, blocker-kind url
  with and without a current phase, cross-producer url equality, human-verification/plan distinctness,
  and `computedPercent.display` rounding.
- `.planning/phases/02-situational-awareness-artifact-reading/02-07-SUMMARY.md` — execution record.

No new files are created outside `.planning/`. No package is added, removed, or upgraded.

## Planner assumptions and declined items (explicit, not silent drops)

**Spec-less probe fallback accounting (this plan owns 6 of the 12 edge-probe rows):**
- DASH-02 `adjacency` → resolved (explicit) → `must_haves.truths` string 3.
- DASH-02 `empty` → resolved (explicit) → `must_haves.truths` string 4.
- DASH-02 `ordering` → resolved (backstop) → structured marker, truth 5. No new determinism evidence is
  produced by this fix, so the verifier must abstain to `human_needed` rather than pass it silently.
- DASH-03 `adjacency` → resolved (explicit) → `must_haves.truths` string 6.
- DASH-03 `empty` → resolved (backstop) → structured marker, truth 7.
- DASH-03 `ordering` → resolved (backstop) → structured marker, truth 8.

6 surfaced = 6 authored + 0 flagged. No-silent-drop equality holds for this plan's share.

**Prohibition probe accounting:** Stage 1 over-produced 10 candidates across DASH-02/DASH-03; Stage 2
kept 3 (authored above, descriptor-less). Canon-referral breadcrumbs, dropped rather than minted:
read-only write-protection and path-traversal are canon — covered by `/gsd-secure-phase` and the
existing `artifact-index.ts` fail-closed lookup; absolute-host-path information disclosure is canon —
covered by `/gsd-secure-phase`. 3 kept = 3 authored + 0 flagged.

**Capability gates:**
- API coverage (`ai-integration`): the phase already carries a reasoned declaration at
  `.planning/phases/02-situational-awareness-artifact-reading/COVERAGE.md` — "No external API
  integration: Phase 2 adds a loopback-only Hono API over the local snapshot." No matrix is required
  and none is invented.
- Assumption-delta: `gsd query assumption-delta scan 02 --json` returned `detected: false`. The
  checkpoint does not fire and is not raised.
- Schema push: this project has no ORM and no database. No `prisma/`, `drizzle/`, `supabase/`,
  `src/collections/`, `src/entities/`, or `src/db/` exists, and `package.json` declares no ORM
  dependency. The gate correctly detects nothing; no push task is invented.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.planning/` authored content -> presentation selectors | Phase names, goals, and blocker text are untrusted author-controlled strings that flow into the dashboard payload. |
| Presentation selectors -> browser URL / react-router | A `url` string emitted here becomes a navigation target the router acts on. |
| Browser address bar -> `parsePresentationUrl` | A hand-typed or bookmarked route is untrusted input that must fail closed. |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-23 | Tampering | phase identity segments composed into a route | medium | mitigate | Route through `buildPhaseUrl`, whose `encodePart` percent-encodes every identity part (including `~`) before joining; never string-concatenate a raw DTO key into a path. |
| T-02-24 | Spoofing | a next-work destination that does not match the item it labels | medium | mitigate | Task 1's regression test asserts the emitted url round-trips through `parsePresentationUrl` to the same phase identity the item names, so a mislabelled destination fails the suite. |
| T-02-25 | Information Disclosure | `next[].url` / `next[].description` on the `/api/dashboard` payload | low | accept | Loopback-only read-only server over a project the operator already owns; urls carry milestone and phase identity only and never absolute host filesystem paths. |
| T-02-26 | Denial of Service | `buildPhaseUrl` throwing on a malformed identity (`assertNonEmpty` on phase number) | low | accept | A phase with an empty number cannot be produced by `project-presentation.ts`, and a throw surfaces as the existing page-scoped error boundary rather than a silent blank panel. |
| T-02-SC | Tampering | npm/pip/cargo installs | high | mitigate | This plan installs nothing — `package.json` and the lockfile are not in `files_modified`. Every dependency already in the tree was cleared by the Package Legitimacy Audit in `02-RESEARCH.md` during Phase 02 planning. If an executor finds an install is needed, stop and raise a blocking human checkpoint before running it. |
</threat_model>

<verification>
1. `npm test` exits 0 with a total strictly greater than 236.
2. `npm run typecheck` and `npm run lint` both exit 0.
3. `npm run build` succeeds.
4. Live reproduction of the original failure, now inverted: start `node src/server/index.ts /home/cinedise/labelore`,
   `curl -s localhost:<port>/api/dashboard | node -e` (or `jq`) to read `next.immediate.url`, and confirm the value
   begins with `/milestones/` rather than `p~`. Repeat against `/home/cinedise/studio-portal`, the second corpus the
   verifier used.
5. `npm run smoke -- fixtures/dense` passes.
</verification>

<success_criteria>
- Verification gap 1 (CR-01) is closed: no next-work item emits a bare identity segment as a url.
- The regression test that closes it is grounded in `phaseKeyOf`/`buildPhaseUrl`, not an opaque fixture string.
- WR-03 is closed and the `/api/dashboard` percentage contract is readable.
- No shipped behavior outside `phaseWork`, `blockerWork`, and the `computedPercent` display string changes.
</success_criteria>

<output>
Create `.planning/phases/02-situational-awareness-artifact-reading/02-07-SUMMARY.md` when done.
</output>
