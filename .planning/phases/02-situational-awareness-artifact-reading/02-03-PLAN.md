---
phase: 02-situational-awareness-artifact-reading
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - src/presentation/roadmap.ts
  - test/presentation/roadmap.test.ts
  - src/web/pages/roadmap-page.tsx
  - src/web/pages/dashboard-page.tsx
  - src/web/app-router.tsx
  - src/server/index.ts
  - src/web/components/app-shell.tsx
  - src/web/components/theme-toggle.tsx
  - src/web/components/ui/button.tsx
  - src/web/styles/globals.css
autonomous: true
requirements: [DASH-01, DASH-02, DASH-03, DASH-04, ROAD-01, ROAD-02, ROAD-03, HIST-01, HIST-02, UI-01, UI-02, UI-03]

estimate:
  tokens: 67000
  raw_tokens: 67000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A user sees D-01 current position first, one D-02 primary formal-progress display, compact sourced observed progress, conditional discrepancy, one D-04 next item plus two previews, and the D-03 typed Needs attention list."
    - "[EDGE ROAD-01/adjacency] Consecutive phases, including dotted numbers, remain separate ordered rows with their own goal, dependencies, success criteria, and requirements."
    - "[EDGE ROAD-01/empty] A milestone with no phases renders a labeled empty flow; a phase with no criteria or requirements renders explicit empty detail when expanded."
    - "[EDGE ROAD-01/ordering] Active and archived phases retain deterministic milestone then `comparePhaseNumbers` order regardless of filesystem enumeration."
    - "[EDGE ROAD-02/ordering] Plans group by open `frontmatter.wave` into deterministic vertical bands, and every unresolved or incomplete sibling dependency is shown verbatim as `blocked by`."
    - "[EDGE ROAD-03/ordering] The primary dependency presentation is an ordered vertical spine with compact connectors and authored dependency labels; no graph edge is inferred from prose."
    - "[EDGE HIST-01/ordering] Archived milestones appear in a dedicated History section after the active milestone, visibly labeled and subordinate without being hidden."
    - "[EDGE HIST-02/adjacency] Archives containing identical phase numbers expand to separate milestone-qualified phase trees and artifact destinations."
    - "[EDGE UI-01/encoding] Authorized OKLCH token values, base-sera configuration, lucide icon semantics, and squared geometry are preserved exactly in local source with no runtime studio-portal import."
    - "[EDGE UI-02/idempotency] Reapplying stored theme choice or toggling repeatedly leaves one root `.dark` truth shared by tokens and code themes; storage failure still applies the current-page choice."
    - "[EDGE UI-03/boundary] At 390px and desktop widths, the page body never scrolls horizontally while tables, code, and diagrams can scroll inside bounded local wrappers."
  prohibitions:
    - statement: "Archived work must not be visually or semantically merged into the active milestone in a way that can make old completion, status, or phase numbers look current."
      status: unverified
      flagged: true
  artifacts:
    - path: "src/presentation/roadmap.ts"
      provides: "Ordered active/history phase and wave view models"
      exports: ["RoadmapViewModel", "MilestoneFlow", "WaveBand", "buildRoadmapViewModel"]
    - path: "src/web/pages/dashboard-page.tsx"
      provides: "Current-position-first landing page"
      exports: ["DashboardPage"]
    - path: "src/web/pages/roadmap-page.tsx"
      provides: "Vertical roadmap and archived milestone browser"
      exports: ["RoadmapPage"]
    - path: "src/web/components/app-shell.tsx"
      provides: "Document-width-friendly application shell"
      exports: ["AppShell"]
    - path: "src/web/components/theme-toggle.tsx"
      provides: "Root-class theme control"
      exports: ["ThemeToggle"]
    - path: "src/web/styles/globals.css"
      provides: "Authorized OKLCH light/dark tokens and overflow containment contract"
  key_links:
    - from: "src/web/pages/dashboard-page.tsx"
      to: "src/presentation/dashboard.ts"
      via: "component renders DashboardViewModel and derives no planning facts"
      pattern: "DashboardViewModel"
    - from: "src/web/pages/roadmap-page.tsx"
      to: "src/presentation/roadmap.ts"
      via: "accessible disclosures render ordered milestone/phase/wave records"
      pattern: "RoadmapViewModel"
    - from: "src/web/components/app-shell.tsx"
      to: "src/web/styles/globals.css"
      via: "min-width-zero layout boundaries and shared root theme tokens"
      pattern: "min-w-0|dark"
---

<objective>
Deliver the complete at-a-glance dashboard, vertical roadmap/history browser, and authorized light/dark visual shell over the contracts proven in plan 02-02.

Purpose: Implement D-01 through D-08 as a readable execution narrative while retaining the exact provenance and milestone identity carried by the presentation DTOs.
Output: Dashboard page, roadmap/history view model and page, shell, theme control, base-sera Button, and long-content containment styling.
</objective>

<execution_context>
@/home/cinedise/.codex/gsd-core/workflows/execute-plan.md
@/home/cinedise/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-situational-awareness-artifact-reading/02-CONTEXT.md
@.planning/phases/02-situational-awareness-artifact-reading/02-RESEARCH.md
@.planning/phases/02-situational-awareness-artifact-reading/02-PATTERNS.md
@src/domain/model.ts
@.planning/phases/02-situational-awareness-artifact-reading/02-02-SUMMARY.md

<interfaces>
Consume `ProjectPresentation`, `DashboardViewModel`, `buildDashboardViewModel`, and the canonical route builders from plan 02-02. React pages assign hierarchy and interaction only; they do not parse markdown, count files, resolve dependencies, or infer source provenance.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Vertical roadmap flow, wave bands, and milestone history</name>
  <read_first>
    - `src/domain/model.ts` — milestone-qualified phase tree and open wave frontmatter
    - `src/planning-repo/assemble.ts` — deterministic milestone/phase ordering
    - `src/presentation/routes.ts` — canonical destination builders from plan 02-02
    - `.planning/phases/02-situational-awareness-artifact-reading/02-CONTEXT.md` — D-05 through D-08
  </read_first>
  <files>src/presentation/roadmap.ts, test/presentation/roadmap.test.ts, src/web/pages/roadmap-page.tsx, src/web/app-router.tsx, src/server/index.ts</files>
  <behavior>
    - D-05/ROAD-03: phases render as an ordered vertical spine with compact connectors, status markers, and authored dependency labels, never raw ASCII or an invented graph.
    - D-06/ROAD-01: identity, status, goal, dependencies, and progress are always visible; criteria, requirement mappings, and plans expand in place.
    - D-08/ROAD-02: open wave values form deterministic vertical bands with explicit incomplete/dangling sibling `blocked by` labels.
    - D-07/HIST-01/HIST-02: archived milestones are separately labeled/subdued and each expands to its own milestone-qualified tree.
    - Empty milestones/detail lists, dotted phases, and duplicate phase-number identities remain distinct and ordered.
  </behavior>
  <action>Write dense active-plus-two-archives tests first, then implement `buildRoadmapViewModel` as a pure projection from ProjectPresentation. Preserve milestone order and `comparePhaseNumbers`; group rows by open `frontmatter.wave`, use `Unspecified` only when absent, sort deterministically, and derive row blocking only from `dependsOnRefs`. Keep `dependsOnRaw` visible without manufacturing phase edges. Add roadmap/history endpoints and the D-05 through D-08 accessible compact-by-default disclosure UI. Extend `appRouter` only through canonical builders/parsers; do not add the deferred graph canvas, search, tree browser, or traceability view.</action>
  <verify>
    <automated>npm test -- --run test/presentation/roadmap.test.ts &amp;&amp; npm run typecheck &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>Every phase exposes ROAD-01 fields, every plan belongs to one wave band with correct blocked-by labels, the primary flow is legible without source ASCII, and duplicate phase numbers navigate to distinct milestone destinations.</acceptance_criteria>
  <done>A user can scan active execution, expand phase detail in place, and browse archived trees without identity collisions.</done>
</task>

<task type="auto">
  <name>Task 2: Current-position dashboard and application hierarchy</name>
  <read_first>
    - `src/presentation/dashboard.ts` — source-complete DashboardViewModel contract
    - `src/presentation/routes.ts` — navigation builders
    - `.planning/phases/02-situational-awareness-artifact-reading/02-CONTEXT.md` — D-01 through D-04 hierarchy
  </read_first>
  <files>src/web/pages/dashboard-page.tsx, src/web/components/app-shell.tsx, src/web/app-router.tsx, src/server/index.ts</files>
  <action>Wire `/api/presentation` and `/api/dashboard` in `src/server/index.ts` from the same snapshot/readAt using ProjectPresentation and the selector completed in plan 02-02. Build `DashboardPage` strictly from DashboardViewModel. Per D-01, lead with current milestone, phase, status, and immediate work. Per D-02, render formal progress as the single primary display and observed SUMMARY progress as compact sourced secondary data; show discrepancy prominently only when present. Per D-03, render one prioritized Needs attention list with type labels. Per D-04, render exactly one explained next item and no more than two quieter previews. Build AppShell around the dashboard and roadmap routes with visible `readAt`, canonical navigation, accessible landmarks, and `min-width: 0` content boundaries; preserve named loading/error states.</action>
  <verify>
    <automated>npm test -- --run test/presentation/dashboard.test.ts &amp;&amp; npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>The landing page implements D-01 through D-04 exactly, shows provenance from the view model, renders no more than three next-work items, and performs no source-data derivation in React.</acceptance_criteria>
  <done>The first screen answers where work stands, what comes next, and what needs attention at a glance.</done>
</task>

<task type="auto">
  <name>Task 3: Studio-portal-derived themes, components, and containment</name>
  <read_first>
    - `/home/cinedise/studio-portal/frontend/app/globals.css` — authorized OKLCH token and Tailwind source
    - `/home/cinedise/studio-portal/frontend/components.json` — base-sera/lucide/squared-corner conventions
    - `/home/cinedise/studio-portal/frontend/components/theme-toggle.tsx` — DOM `.dark` and storage-failure behavior
    - `.planning/phases/02-situational-awareness-artifact-reading/02-PATTERNS.md` — shell/theme assignments
  </read_first>
  <files>src/web/components/theme-toggle.tsx, src/web/components/ui/button.tsx, src/web/styles/globals.css, src/web/components/app-shell.tsx</files>
  <action>Adapt the authorized visual source locally using the `components.json` contract established in plan 02-01; commit the required Button source. Implement ThemeToggle so the DOM `.dark` class is the single state, storage failure remains non-fatal, lucide icons are decorative, and the control has an accessible name. Port both OKLCH token blocks and the shared `.dark` variant. Enforce `max-width: 100%` and `min-width: 0` at every shell boundary plus local overflow wrappers for tables, `pre`, and Mermaid. Preserve UI-01 through UI-03 without a runtime import from studio-portal and without global horizontal clipping as the containment mechanism.</action>
  <verify>
    <automated>npm run lint &amp;&amp; npm run typecheck &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>The authorized tokens/config/icon/geometry contract exists in local source, the same `.dark` class controls theme and future code styles, storage errors do not break toggling, and containment rules are ready for real long documents.</acceptance_criteria>
  <done>The dashboard and roadmap/history use the required accessible visual language in both theme states.</done>
</task>

</tasks>

## Artifacts this phase produces

- `src/presentation/roadmap.ts` — `RoadmapViewModel`, `MilestoneFlow`, `WaveBand`, `buildRoadmapViewModel`.
- `src/web/pages/dashboard-page.tsx` — `DashboardPage`.
- `src/web/pages/roadmap-page.tsx` — `RoadmapPage`.
- `src/web/components/app-shell.tsx` — `AppShell`.
- `src/web/components/theme-toggle.tsx` — `ThemeToggle`.
- `src/web/components/ui/button.tsx` — local base-sera `Button`.
- `src/web/styles/globals.css` — authorized OKLCH tokens and document containment rules.
- `test/presentation/roadmap.test.ts` — roadmap/history/wave/identity regression suite.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DTO -> React | Open status and provenance values become user-visible planning claims. |
| Active -> archived identity | Repeated phase numbers cross milestone contexts. |
| Read-only visual source -> local code | Authorized tokens/components may be adapted, never imported at runtime. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-08 | Spoofing | dashboard progress/status | medium | mitigate | React renders source-labeled view-model fields and never recomputes planning facts. |
| T-02-09 | Spoofing | active/archive phase rows | medium | mitigate | Every row and link uses milestone-qualified keys and duplicate-number tests. |
| T-02-10 | Tampering | copied visual source | low | mitigate | Copy only authorized tokens/config/component source and assert no runtime studio-portal import. |
| T-02-11 | Denial of Service | oversized roadmap content | low | accept | Layout contains wide children locally; later renderer plans add artifact-specific isolation and limits. |
</threat_model>

<verification>
Run dashboard and roadmap focused suites, the complete test suite, typecheck, lint, and build. Automated source checks confirm canonical navigation, no runtime studio-portal imports, and local containment selectors; plan 02-06 owns the required browser judgment.
</verification>

<success_criteria>
- DASH-01 through DASH-04 render from one sourced DashboardViewModel.
- ROAD-01 through ROAD-03 and HIST-01/HIST-02 form a vertical execution flow with expandable wave bands.
- D-01 through D-08 are implemented without merging formal/observed or active/archive state.
- UI-01 through UI-03 are implemented in local source and build successfully.
</success_criteria>

<output>
Create `.planning/phases/02-situational-awareness-artifact-reading/02-03-SUMMARY.md` when done.
</output>
