---
phase: 02-situational-awareness-artifact-reading
plan: 03
type: execute
wave: 3
depends_on: [02-01, 02-02]
files_modified:
  - src/server/index.ts
  - src/server/artifact-index.ts
  - src/presentation/references.ts
  - src/presentation/coverage.ts
  - src/rendering/plan-segments.ts
  - src/rendering/markdown.ts
  - src/rendering/linkify.ts
  - src/rendering/frontmatter-views.ts
  - src/web/app-router.tsx
  - src/web/pages/artifact-page.tsx
  - src/web/pages/plan-pair-page.tsx
  - src/web/components/reference-preview.tsx
  - test/rendering/markdown.test.ts
  - test/rendering/references.test.ts
  - test/presentation/coverage.test.ts
autonomous: true
requirements: [READ-01, READ-02, READ-03, READ-04, READ-05, READ-06, NAV-02, NAV-03, NAV-04]

estimate:
  tokens: 79000
  raw_tokens: 79000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "[EDGE READ-01/adjacency] Adjacent table, task-list, blockquote, and fenced-code blocks remain separate nodes in source order, including when no blank prose paragraph sits between them."
    - "[EDGE READ-01/empty] An artifact with an empty body renders its title and structured metadata plus an honest empty-document state, without throwing or fabricating body content."
    - "[EDGE READ-01/ordering] The rendered document preserves source block order and list/table row order exactly while applying GFM and syntax highlighting."
    - "[EDGE READ-02/unclassified] `must_haves`, `coverage`, `key_links`, and `progress` receive guarded artifact-aware panels, while every unknown or wrong-shaped frontmatter key remains visible through the unconditional generic recursive view."
    - "[EDGE READ-03/empty] An empty recognized PLAN wrapper renders a labeled empty semantic section, and a mismatched/unclosed wrapper renders escaped literal source plus a local warning rather than blanking the document."
    - "[EDGE READ-03/encoding] Unicode nested markdown and allowlisted `type`, `gate`, and `tdd` attribute values survive PLAN segmentation as text; fenced pseudo-tags are never treated as wrappers."
    - "[EDGE READ-04/unclassified] Scripts, event handlers, dangerous URL schemes, iframes, and active SVG/MathML payloads are absent from finalized HTML; only that sanitized string reaches the document mount."
    - "[EDGE READ-05/unclassified] Plan truths and summary coverage use exact normalized matches first and conservative one-to-one inferred matches second; exact, inferred, unmatched-truth, and unmatched-coverage states are all visible and array order never creates a match."
    - "[EDGE READ-06/unclassified] Duplicate headings receive deterministic stable IDs, and the copy-link control appears on pointer hover or keyboard focus, updates/copies only when activated, and never changes the URL on scroll."
    - "[EDGE NAV-02/unclassified] A resolved requirement mention opens a type-aware preview whose explicit Open action targets the milestone-qualified covering phase URL."
    - "[EDGE NAV-03/unclassified] Phase and plan prose references resolve relative to the containing artifact's milestone; project-root documents use the active milestone."
    - "[EDGE NAV-04/empty] Empty, malformed, or definition-less ID-shaped text remains ordinary text with no anchor, preview trigger, warning, or fabricated destination."
    - "[EDGE NAV-04/encoding] Unicode immediately adjacent to an unresolved ID is preserved byte-for-byte, and authored links, code spans, code fences, pre blocks, and Mermaid source are never linkified."
  prohibitions:
    - statement: "The plan/summary reader must not imply execution evidence that the source does not support; inferred matches must be labeled, unmatched rows must remain visible, and array position must never be presented as proof."
      status: resolved
      verification: judgment
    - statement: "A prose mention must not be presented as a valid destination or contextual preview unless the loaded snapshot resolves it in the containing artifact's milestone context."
      status: resolved
      verification: judgment
  artifacts:
    - path: "src/rendering/plan-segments.ts"
      provides: "Bounded PLAN pseudo-XML semantic segmenter"
      exports: ["PlanSegment", "segmentPlanBody"]
    - path: "src/server/artifact-index.ts"
      provides: "Snapshot-only canonical artifact lookup with typed not-found results"
      exports: ["ArtifactIndex", "buildArtifactIndex"]
    - path: "src/rendering/markdown.ts"
      provides: "Sanitized GFM/Shiki/heading rendering pipeline"
      exports: ["RenderedDocument", "createArtifactRenderer"]
    - path: "src/rendering/frontmatter-views.ts"
      provides: "Known-field registry with unconditional generic remainder"
      exports: ["FrontmatterPanel", "buildFrontmatterPanels"]
    - path: "src/presentation/coverage.ts"
      provides: "Conservative truth-to-coverage matrix"
      exports: ["CoverageMatrix", "buildCoverageMatrix"]
    - path: "src/presentation/references.ts"
      provides: "Milestone-contextual requirement/phase/plan reference registry"
      exports: ["ReferenceRegistry", "buildReferenceRegistry", "resolvePresentationReference"]
    - path: "src/web/pages/artifact-page.tsx"
      provides: "Document-first generic and known-artifact reader plus the sole sanitized HTML mount"
      exports: ["DocumentView", "ArtifactPage"]
    - path: "src/web/pages/plan-pair-page.tsx"
      provides: "Truth/coverage-first stacked plan-summary reader"
      exports: ["PlanPairPage"]
    - path: "src/web/components/reference-preview.tsx"
      provides: "Keyboard/pointer-equivalent type-aware preview with explicit Open action"
      exports: ["ReferencePreview"]
  key_links:
    - from: "src/server/index.ts"
      to: "src/rendering/markdown.ts"
      via: "artifact endpoint renders snapshot-derived bodies and returns finalized sanitized HTML plus warnings"
      pattern: "createArtifactRenderer|render"
    - from: "src/rendering/markdown.ts"
      to: "src/rendering/plan-segments.ts"
      via: "only `kind === plan` bodies are segmented before the unified pipeline"
      pattern: "segmentPlanBody"
    - from: "src/rendering/linkify.ts"
      to: "src/presentation/references.ts"
      via: "sanitized HAST text nodes become preview controls only after contextual resolution"
      pattern: "resolvePresentationReference"
    - from: "src/web/pages/plan-pair-page.tsx"
      to: "src/presentation/coverage.ts"
      via: "matrix precedes full stacked plan and summary documents"
      pattern: "CoverageMatrix"
---

<objective>
Make every artifact safe, readable, semantically structured, cross-linked, and pairable with its execution summary.

Purpose: Complete the phase goal for real long-form PLAN/SUMMARY/research/unknown markdown while defending the artifact-to-DOM trust boundary and preserving unresolved references as ordinary text. Implement D-09 through D-12 and D-14 through D-17.
Output: Server-side renderer, PLAN segmenter, frontmatter panels, truth/coverage matcher, milestone-aware linkifier/previews, document routes, and security/edge regression suites.
</objective>

<execution_context>
@/home/cinedise/.codex/gsd-core/workflows/execute-plan.md
@/home/cinedise/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-situational-awareness-artifact-reading/02-CONTEXT.md
@.planning/phases/02-situational-awareness-artifact-reading/02-RESEARCH.md
@.planning/phases/02-situational-awareness-artifact-reading/02-PATTERNS.md
@.planning/phases/01-read-layer-domain-model/01-03-SUMMARY.md
@.planning/phases/01-read-layer-domain-model/01-04-SUMMARY.md
@src/domain/model.ts
@src/planning-repo/handlers/plan.ts
@src/planning-repo/handlers/index.ts
@src/planning-repo/crossref.ts
@.planning/phases/02-situational-awareness-artifact-reading/02-01-SUMMARY.md
@.planning/phases/02-situational-awareness-artifact-reading/02-02-SUMMARY.md

<interfaces>
`Artifact` carries open `kind`, open frontmatter, verbatim `body`, warnings, and handler `structured` data. `PlanHandler` intentionally returns `{ body: fm.body, structured: {} }`; the renderer owns pseudo-XML semantics. `Reference<T>` authorizes a destination only when `resolved !== null`. Use plan 02-01's route builders for every Open, breadcrumb, heading, and artifact URL, and plan 02-02's cycle-free presentation/artifact keys for API lookup.
</interfaces>
</context>

<assumptions_resolved>
- Coverage matching (research A4): normalize case/whitespace/punctuation, match exact text first, then require one-to-one assignment with either a shared requirement ID or at least 60% significant-token overlap. Label the latter `inferred`; never claim complete coverage while either side has unmatched rows.
- Rendering limits (research A5): keep the Markdown/PLAN scan linear and allow the full document to render; cap only browser Mermaid execution at 256 KiB of diagram source. Oversized/invalid Mermaid remains visible as escaped source with an artifact-local warning, not a blank page.
- Preview placement uses Base UI Popover; pointer click and Enter/Space open the same control, Escape/outside click dismiss, and focus returns to the trigger per D-14/D-15.
</assumptions_resolved>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Safe PLAN-aware Markdown rendering through the artifact API</name>
  <read_first>
    - `src/planning-repo/handlers/plan.ts` — verbatim PLAN body handoff
    - `src/planning-repo/registry.ts` — ordered local-degradation precedent
    - `.planning/phases/01-read-layer-domain-model/01-04-PLAN.md` — real multi-task PLAN corpus
    - `.planning/phases/02-situational-awareness-artifact-reading/02-RESEARCH.md` — pipeline order, bounded grammar, security domain
  </read_first>
  <files>src/rendering/plan-segments.ts, src/rendering/markdown.ts, test/rendering/markdown.test.ts, src/server/artifact-index.ts, src/server/index.ts</files>
  <behavior>
    - GFM tables, task lists, blockquotes, adjacent blocks, and source ordering survive.
    - Code blocks receive Shiki dual-theme markup from one initialized highlighter; Mermaid becomes a strict browser mount target and oversized/invalid source degrades locally.
    - D-11: recognized objective/task/decision wrappers outside fences become semantic sections; nested markdown remains live; allowlisted attributes become text badges.
    - Empty, mismatched, unclosed, Unicode, fenced, and unknown pseudo-tag cases never throw or disappear.
    - Raw HTML payloads lose executable scripts/events/unsafe schemes/active SVG and MathML before post-sanitize trusted enrichment.
  </behavior>
  <action>Write corpus and hostile-payload tests first. Implement a bounded linear stack scanner over recognized PLAN wrappers, ignoring fenced regions, preserving inner markdown, and returning scoped warnings for mismatch/unclosed cases. Initialize one unified processor/highlighter lifecycle in the exact order `remarkParse -> remarkGfm -> remarkRehype(allowDangerousHtml) -> rehypeRaw -> rehypeSanitize -> rehypeSlug -> trusted highlight/heading enrichment -> rehypeStringify`. Extend the sanitizer schema only for required safe GFM/Shiki/heading data; no source event/style/URL attribute may be copied after sanitization. Emit stable duplicate-heading IDs and dormant copy-link metadata. Render Mermaid only as escaped browser mount source; the browser stage will use strict mode. Build `ArtifactIndex` once from the just-loaded snapshot, keyed by canonical token/path and returning a typed not-found result. Extend the artifact endpoint to use that index so invalid keys cannot become filesystem paths; renderer failures return safe escaped body plus a local warning.</action>
  <verify>
    <automated>npm test -- --run test/rendering/markdown.test.ts &amp;&amp; npm run typecheck &amp;&amp; npm run lint</automated>
  </verify>
  <acceptance_criteria>All READ-01, READ-03, and READ-04 edge truths have named tests; a real multi-task Phase 1 plan renders labeled sections with nested markdown; hostile HTML fixtures contain no executable surface; and an invalid artifact token cannot traverse or crash the app.</acceptance_criteria>
  <done>The local artifact API returns finalized sanitized document HTML with stable headings and scoped degradation for ordinary and PLAN bodies.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Structured frontmatter, document canvas, and truthful plan-summary pairing</name>
  <read_first>
    - `src/planning-repo/handlers/index.ts` — specialized-first, generic-last registry pattern
    - `src/planning-repo/assemble.ts` — plan/summary path pairing contract
    - `.planning/phases/02-situational-awareness-artifact-reading/02-PATTERNS.md` — frontmatter, coverage, document, and plan-pair assignments
    - `/home/cinedise/studio-portal/.planning/phases/04-bulk-archive-downloads/04-05-PLAN.md` and matching SUMMARY — qualitative real mismatch-sensitive pair; do not add committed cross-project assertions
  </read_first>
  <files>src/rendering/frontmatter-views.ts, src/presentation/coverage.ts, src/web/pages/artifact-page.tsx, src/web/pages/plan-pair-page.tsx, test/presentation/coverage.test.ts</files>
  <behavior>
    - Known frontmatter shapes render structured panels; unknown and wrong-shaped known keys remain visible in a generic recursive panel.
    - Exact truth/coverage matches precede conservative one-to-one inferred matches; reordering either array does not change pair identity.
    - All unmatched truths and coverage entries remain visible and inferred rows carry the `inferred` label.
    - Document pages put breadcrumbs/title/type/compact summary before the full-width body; pair pages put the matrix first, then full stacked plan and summary with jump links.
    - Heading controls appear only on hover/focus, copy/update only on activation, and do not react to scroll.
  </behavior>
  <action>Write exact/inferred/unmatched/reordered coverage tests first. Build a frontmatter view registry over `Record<string, unknown>` for `must_haves`, `coverage`, `key_links`, and `progress`, recording handled keys and sending every other or invalid value through the safe recursive fallback. Implement the documented exact-then-60%-or-requirement-ID one-to-one matcher, including unmatched rows and no index zipping. Export `DocumentView` from `artifact-page.tsx` as the only sanitized HTML mount plus strict Mermaid lifecycle and activated heading-copy behavior, and export the document-first `ArtifactPage` alongside it. Build D-12 PlanPairPage with full-width reading, local overflow classes, matrix-first stacked documents, and jump links. Unknown artifact kinds use the same generic markdown path and all open frontmatter remains visible.</action>
  <verify>
    <automated>npm test -- --run test/presentation/coverage.test.ts test/rendering/markdown.test.ts &amp;&amp; npm run typecheck &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>READ-02, READ-05, READ-06, D-09, D-10, D-12, and D-16 are covered: unfamiliar keys never disappear, mismatched plan/summary arrays never create false evidence, both full documents remain readable, and stable heading links are keyboard accessible.</acceptance_criteria>
  <done>A user can read any artifact document-first and can review a plan beside its actual summary evidence without losing width or unmatched information.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Milestone-aware prose references, previews, and final document routes</name>
  <read_first>
    - `src/planning-repo/crossref.ts` — scoped resolution and null-preserving precedent
    - `src/domain/model.ts` — requirement, phase, plan, and mention contracts
    - `src/presentation/routes.ts` — exclusive URL builder/parser contract
    - `.planning/phases/02-situational-awareness-artifact-reading/02-CONTEXT.md` — D-14 through D-17
  </read_first>
  <files>src/presentation/references.ts, src/rendering/linkify.ts, src/web/components/reference-preview.tsx, src/web/app-router.tsx, test/rendering/references.test.ts</files>
  <behavior>
    - Resolved requirement, phase, and plan mentions become preview controls only after contextual resolution.
    - Archived artifact references resolve inside their own milestone; project-root documents resolve against active milestone.
    - Null, malformed, and empty IDs stay plain text and add no warning or preview.
    - The HAST walker skips authored links, code, pre, and Mermaid nodes and preserves adjacent Unicode.
    - Pointer and keyboard open the same compact type-aware preview; Open uses the canonical route and dismissal restores focus.
  </behavior>
  <action>Write active/archive/null/code/Unicode interaction fixtures first. Build lookup maps once per presentation and resolve requirement IDs through covering phases plus phase/plan IDs relative to the containing artifact milestone. Walk only sanitized HAST text nodes, skip `a`, `code`, `pre`, and Mermaid ancestry, and emit trusted preview-control metadata only for a successful resolution; generate the destination exclusively through `src/presentation/routes.ts`. Implement Base UI Popover previews with identity, title, status, location, exactly one type-specific detail, and an explicit Open action per D-14/D-15. Complete app-router document/plan-pair routes and ensure breadcrumbs/heading links/previews all use the shared codec. Preserve D-17 ordinary-text behavior for every null target.</action>
  <verify>
    <automated>npm test -- --run test/rendering/references.test.ts test/rendering/markdown.test.ts &amp;&amp; npm test &amp;&amp; npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run build</automated>
    <human-check>At end-of-phase UAT, open a real multi-task plan and its summary. Verify literal PLAN wrappers are semantic, tables/code/task lists render, every frontmatter key is visible, matrix unmatched rows are honest, requirement/phase/plan clicks preview before navigation, Escape/focus behavior works, undefined IDs remain text, copied heading URLs reload at the exact section, both themes remain legible, and only wide child containers scroll at 390px.</human-check>
  </verify>
  <acceptance_criteria>NAV-02, NAV-03, NAV-04, D-14, D-15, and D-17 pass automated active/archive/dangling tests and the consolidated browser check; all 23 Phase 2 requirement IDs pass the complete suite/build and are reachable through canonical routes.</acceptance_criteria>
  <done>All artifacts are safely formatted, milestone-contextually cross-linked, previewable, deep-linkable, and readable on real long-form content.</done>
</task>

</tasks>

## Artifacts this phase produces

- `src/rendering/plan-segments.ts` — `PlanSegment`, `segmentPlanBody`.
- `src/server/artifact-index.ts` — `ArtifactIndex`, `buildArtifactIndex`, typed lookup result.
- `src/rendering/markdown.ts` — `RenderedDocument`, `createArtifactRenderer`, sanitizer/highlighter lifecycle.
- `src/rendering/linkify.ts` — trusted sanitized-HAST reference enrichment.
- `src/rendering/frontmatter-views.ts` — `FrontmatterPanel`, known-field registry, generic remainder.
- `src/presentation/coverage.ts` — `CoverageMatrix`, match-state records, `buildCoverageMatrix`.
- `src/presentation/references.ts` — `ReferenceRegistry`, `ReferencePreviewDto`, contextual resolver.
- `src/web/pages/artifact-page.tsx` — `DocumentView` sanitized mount, strict Mermaid, heading-copy interaction, and `ArtifactPage`.
- `src/web/components/reference-preview.tsx` — `ReferencePreview`.
- `src/web/pages/artifact-page.tsx` — `ArtifactPage`.
- `src/web/pages/plan-pair-page.tsx` — `PlanPairPage`.
- `test/rendering/markdown.test.ts`, `test/rendering/references.test.ts`, `test/presentation/coverage.test.ts` — renderer/security/link/matching regressions.

<edge_coverage_accounting>
The deterministic spec-less probe surfaced 34 candidates. All 34 are resolved to explicit, mechanically verifiable truths across this plan set; none uses a backstop and none remains unclassified after the acceptance criteria above: plan 02-01 covers ROAD-04 and NAV-06 (2); plan 02-02 covers DASH-01 x4, DASH-02, DASH-03, DASH-04 x3, ROAD-01 x3, ROAD-02, ROAD-03, HIST-01, HIST-02, UI-01, UI-02, UI-03 (19); plan 02-03 covers READ-01 x3, READ-02, READ-03 x2, READ-04, READ-05, READ-06, NAV-02, NAV-03, NAV-04 x2 (13). Equality: 34 surfaced = 34 authored + 0 flagged assumptions.
</edge_coverage_accounting>

<prohibition_recall>
Two-stage recall/precision was run across all 23 requirements. Routine correctness candidates were dropped into the edge/task verification axis. Stored-XSS, unsafe URL schemes, path traversal, regex/size denial, and generic accessibility/security candidates are canonical; breadcrumb: they are modeled in the ASVS threat registers here and remain eligible for `$gsd-secure-phase`, not minted as duplicate prohibitions. Four bespoke transparency/product-integrity prohibitions survived and are authored descriptor-less under `must_haves.prohibitions` across plans 02-01 through 02-03; each therefore remains judgment-tier and fail-closed/flagged-unverified until reviewed. No kept prohibition was dismissed or silently dropped.
</prohibition_recall>

<source_coverage_audit>
## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Plan | Status |
|--------|----|----------------------|------|--------|
| GOAL | — | At-a-glance work status plus safely formatted cross-linked artifacts | 01-03 | COVERED |
| REQ | ROAD-04, NAV-06 | Milestone-qualified identity and bookmarkable hierarchy | 01 | COVERED |
| REQ | DASH-01..04 | Current position, next/blockers/human waits, dual progress | 02 | COVERED |
| REQ | ROAD-01..03 | Full phase detail, wave bands, legible flow | 02 | COVERED |
| REQ | HIST-01..02 | Distinct browsable archived milestones/phase trees | 02 | COVERED |
| REQ | UI-01..03 | Visual language, themes, horizontal containment | 02 | COVERED |
| REQ | READ-01..06 | GFM, frontmatter, PLAN structure, sanitization, pairing, anchors | 03 | COVERED |
| REQ | NAV-02..04 | Requirement/phase/plan links and dangling plain text | 03 | COVERED |
| RESEARCH | — | Package legitimacy checkpoint and exact pinned stack | 01 | COVERED |
| RESEARCH | — | One snapshot seam, cycle-free DTO, snapshot-only artifact lookup | 01-02 | COVERED |
| RESEARCH | — | Canonical route codec shared by every consumer | 01-03 | COVERED |
| RESEARCH | — | Pure dashboard/roadmap/coverage/reference selectors | 02-03 | COVERED |
| RESEARCH | — | Bounded PLAN grammar and sanitize-before-enrichment pipeline | 03 | COVERED |
| RESEARCH | — | Strict Mermaid/local degradation and real-corpus browser UAT | 03 | COVERED |
| CONTEXT | D-01, D-02, D-03, D-04 | Landing hierarchy and attention/next limits | 02 | COVERED |
| CONTEXT | D-05, D-06, D-07, D-08 | Vertical roadmap, expansion, history, waves | 02 | COVERED |
| CONTEXT | D-09, D-10, D-11, D-12 | Document canvas, open frontmatter, PLAN semantics, pair matrix | 03 | COVERED |
| CONTEXT | D-13 | Costly milestone-qualified route contract | 01 | COVERED |
| CONTEXT | D-14, D-15, D-16, D-17 | Preview-first links, type-aware preview, copy anchors, dangling text | 03 | COVERED |

Deferred graph-canvas, search, full filesystem tree, traceability view, live watching, write-back, and multi-project switching are excluded by source scope and do not count as gaps.
</source_coverage_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Artifact Markdown/HTML -> sanitized HTML | Local project content is untrusted active-content input. |
| Sanitized HTML -> browser DOM | Only finalized renderer output may enter the HTML mount; Mermaid is a separate strict browser stage. |
| Prose ID -> canonical route | ID-shaped text must resolve in milestone context before it gains navigation authority. |
| Route token -> artifact index | Request parameters may select only already-indexed snapshot artifacts. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-09 | Tampering / Elevation | `src/rendering/markdown.ts` | high | mitigate | Enforce raw -> sanitize order, hostile-payload snapshots, safe schema, and one finalized HTML mount with no post-sanitize source attribute copying. |
| T-02-10 | Elevation | `src/web/pages/artifact-page.tsx` Mermaid mount | high | mitigate | Use `securityLevel: strict`, `startOnLoad: false`, selected placeholders only, 256 KiB source cap, and local escaped-source fallback. |
| T-02-11 | Spoofing | `src/rendering/linkify.ts` | medium | mitigate | Resolve through milestone-contextual registry before emitting a preview; null targets remain text; URLs come only from the canonical codec. |
| T-02-12 | Information Disclosure / Tampering | artifact endpoint | high | mitigate | Map canonical tokens to the snapshot-built artifact index; traversal-shaped or invalid tokens return typed not-found and never reach filesystem calls. |
| T-02-13 | Denial of Service | PLAN/link scanners | medium | mitigate | Use bounded linear scans/HAST walks, skip nested code/pre/Mermaid regions, and isolate failures to one artifact. |
| T-02-14 | Repudiation | truth-to-coverage claims | medium | mitigate | Label exact/inferred/unmatched states, require one-to-one matching, keep all unmatched rows, and test reorder invariance. |
</threat_model>

<verification>
Run the three focused suites, the complete test suite, typecheck, lint, and production build. Use `fixtures/dense` for automated active/archive/dangling/unknown-kind/hostile-render cases. Use the project’s own real Phase 1 multi-task plan and summary for committed corpus tests. For qualitative end-of-phase UAT, also open the read-only studio-portal corpus and verify long tables/code/PLAN wrappers/pair coverage/previews/heading deep links in both themes and at 390px; do not commit assertions that depend on studio-portal.
</verification>

<success_criteria>
- READ-01 through READ-06 and NAV-02 through NAV-04 pass explicit automated edge/security tests.
- D-09 through D-12 and D-14 through D-17 are implemented without scope reduction.
- Unknown artifact kinds and unknown frontmatter keys remain visible through unconditional fallbacks.
- No artifact-authored active content executes, and route input cannot become a filesystem path.
- The 34-edge no-silent-drop equality and the four-source audit both close with zero missing items.
- Consolidated real-corpus browser UAT verifies previews, anchors, pairing honesty, themes, keyboard access, and local overflow.
</success_criteria>

<output>
Create `.planning/phases/02-situational-awareness-artifact-reading/02-03-SUMMARY.md` when done.
</output>
