---
phase: 02-situational-awareness-artifact-reading
plan: 09
type: execute
wave: 8
depends_on: [02-07, 02-08]
files_modified: []
autonomous: false
gap_closure: true
requirements: [UI-02]
status: superseded
superseded_by: 02-13
superseded_reason: >
  This gate ran on 2026-08-29 and was NOT approved; the outcome is recorded verbatim in
  02-UAT.md (4 defects, 5 quality objections). Those findings are what produced gap-closure
  plans 02-10, 02-11, 02-12 and 02-13. Plan 02-13 is this same blocking-human browser gate,
  broadened to cover the new fixes, and depends_on [02-10, 02-11, 02-12]. Re-running 02-09
  before those fixes land would fail again for the reasons already on record, and re-running
  it after them would duplicate 02-13's surfaces. UI-02 is therefore carried by 02-13.
  Superseded on 2026-08-30 by explicit user decision during /gsd-execute-phase 02.

user_setup:
  - service: host-browser
    why: "No browser executable exists in the agent shell environment. Contrast judgment on rendered output, and the behavioral half of the deep-link fix, cannot be derived from source, grep, or build output."
    dashboard_config:
      - task: "Open the running GSD Lore dev server at a desktop viewport and at a 390px-wide viewport, in both light and dark themes"
        location: "Host machine browser"

estimate:
  tokens: 9000
  raw_tokens: 9000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "On real long-form artifact content, in both light and dark themes, at desktop and 390px, every named surface reads as legible against its actual rendered background: body prose, muted text, links in default/hover/focus-visible/visited states, badges and status chips, table headers/cells/hairline borders, Shiki-highlighted code tokens and the code block background, Mermaid diagram labels/lines/background, and the reference-preview trigger, popup text, and Open action (UI-02, SC6)."
    - "The dashboard's primary Next up call-to-action, when the recommended action is a phase, navigates to the roadmap with exactly that phase's disclosure opened and scrolled into view — the end-to-end flow gaps 1 and 2 broke, confirmed in a real rendering engine (DASH-02, ROAD-01, NAV-06)."
    - "A phase reference clicked in prose, followed through its Open action, produces the same scoped destination as the dashboard CTA (NAV-03, D-14)."
    - "The approval record names every inspected surface individually; an unnamed or unreached surface is recorded as a remaining gap rather than absorbed into a blanket pass."
  prohibitions:
    - statement: "MUST NOT record contrast approval for any named surface that was not actually inspected in a browser in both themes — a skipped surface is a remaining gap, not a pass."
      category: transparency
      requirement_id: UI-02
  artifacts:
    - path: ".planning/phases/02-situational-awareness-artifact-reading/02-09-SUMMARY.md"
      provides: "Per-surface light/dark contrast approval on real long-form content, and browser confirmation of the gap-1 and gap-2 fixes"
  key_links:
    - from: "host browser inspection"
      to: "plans 02-07 and 02-08 automated outputs"
      via: "the gate runs only after the full suite, typecheck, lint, build, and server smoke pass on the integrated tree"
      pattern: "approved"
---

<objective>
Close verification gap 3: the light/dark contrast pass on real long-form artifact content that
`02-VERIFICATION.md` recorded as `behavior_unverified`, and that `02-06-SUMMARY.md` itself already
recorded as `status: unknown`, `human_judgment: true`.

Purpose: Success Criterion 6 requires legible contrast on real long-form artifact content in both
themes. The contrast-affecting styling commit `b523070` added 589 lines to `globals.css` and was never
re-inspected in a browser afterward, by the executing agent's own account. This gate also supplies the
only behavioral proof available for plan 02-08's page wiring — this project has no jsdom and no
testing-library, so the deep-link fix is otherwise source-asserted only (UI-02, DASH-02, ROAD-01,
NAV-03, NAV-06, D-02, D-07, D-14).
Output: one recorded per-surface approval, or the exact failing surface, theme, viewport, and route.
</objective>

<execution_context>
@/home/cinedise/.claude/gsd-core/workflows/execute-plan.md
@/home/cinedise/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-situational-awareness-artifact-reading/02-UI-SPEC.md
@.planning/phases/02-situational-awareness-artifact-reading/02-VERIFICATION.md
@.planning/phases/02-situational-awareness-artifact-reading/02-06-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Gate: approve per-surface light and dark contrast on real long-form content, and the repaired navigation flow</name>
  <precondition>Plans 02-07 and 02-08 are complete on the integrated tree, and `npm test &amp;&amp; npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run build &amp;&amp; npm run smoke -- fixtures/dense` exits 0. A host browser is available outside the agent shell; the agent shell has no browser executable.</precondition>
  <read_first>
    - `.planning/phases/02-situational-awareness-artifact-reading/02-VERIFICATION.md` lines 56-60 and 178-185 — the `behavior_unverified_items` entry, its exact test, its expectation, and the `why_human` rationale
    - `.planning/phases/02-situational-awareness-artifact-reading/02-UI-SPEC.md` lines 99-123 — the Color section, which names the exact accent-reserved and destructive-reserved surfaces to inspect and the hairline-border/hard-corner treatment they sit on
    - `.planning/phases/02-situational-awareness-artifact-reading/02-06-SUMMARY.md` — deliverable D3, recorded `status: unknown` with `human_judgment: true`; this gate supersedes that unknown with recorded evidence
    - `.planning/phases/02-situational-awareness-artifact-reading/02-07-SUMMARY.md` and `02-08-SUMMARY.md` — the two gap fixes whose end-to-end behavior this gate confirms
    - `src/web/styles/globals.css` — the stylesheet changed by commit `b523070`; skim the `:root` and `.dark` token blocks so a failing surface can be reported against a named token
  </read_first>
  <files>None. This checkpoint records human judgment after implementation and automation are complete; it modifies no source file.</files>
  <what-built>The Phase 2 reader as integrated after gap closure: the dashboard with a navigable Next up CTA and blocker action (02-07), the route-aware roadmap that opens and scrolls the deep-linked phase (02-08), and the existing document canvas, frontmatter panels, plan-pair matrix, and reference previews shipped by 02-01 through 02-06.</what-built>
  <action>
Run the automated gate first; it must exit 0 before the browser pass begins.

Start GSD Lore against two corpora in turn: `fixtures/dense` (the synthetic worst case) and
`/home/cinedise/studio-portal` (the read-only real corpus with genuine long-form multi-task
`PLAN.md` files). The studio-portal directory is observed only — never written to, never committed
against, never added as a dependency.

In a host browser, at a desktop viewport and again at a 390px-wide viewport, in light theme and then
separately in dark theme, open a real long-form `PLAN.md` and walk the surfaces named in the
human-check below one at a time. Judge each against its actual rendered background, not against the
token values in the stylesheet — the whole reason this item is human-gated is that a token pair can
look fine in source and fail in composition.

Then confirm the two repaired navigation flows in the same session, because this project has no jsdom
and no testing-library, so plan 02-08's page wiring carries only source assertions and this is its
only behavioral proof.

Record the outcome surface by surface. Approving a surface you did not reach — or collapsing the list
into a single blanket "looks fine" — defeats the purpose of this gate: `02-06-SUMMARY.md` already
recorded this exact deliverable as unknown once, and a second unexamined pass would bury it rather
than close it.
  </action>
  <how-to-verify>Run the automated command; if it exits 0, open both corpora in a host browser, complete every item in the human-check at both viewports in both themes, then reply with per-surface approval or the exact failing surface, theme, viewport, route, and observed foreground/background.</how-to-verify>
  <verify>
    <automated>npm test &amp;&amp; npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run build &amp;&amp; npm run smoke -- fixtures/dense</automated>
    <human-check>
Contrast pass — inspect each of these separately in light theme and again in dark theme, at desktop and at 390px, on a real long-form PLAN.md from `/home/cinedise/studio-portal` and on `fixtures/dense`:
(1) body prose in `.artifact-document`;
(2) muted/secondary text — `.source-note`, `.empty-note`, `.eyebrow`, and `small` inside wave-plan rows;
(3) links in every interaction state — default, hover, keyboard `:focus-visible`, and visited — including document links, requirement chips, and the roadmap phase-title link;
(4) badges and status chips — `.status-chip` at each `data-tone`, `.milestone-version`, and task-type badges inside rendered plan segments;
(5) table chrome — header row background, cell text, and hairline borders, including inside the horizontally scrolling `.coverage-table-boundary` and `.table-scroll` containers;
(6) Shiki-highlighted code — token colors against the `--secondary` code-block background, and a long unbroken line scrolling inside its own `pre`;
(7) Mermaid output — node labels, edge lines, and diagram background, plus the `.mermaid-fallback` monospace source when a diagram fails to parse;
(8) reference-preview popover — trigger text in the document flow, popup body text, and the accent-colored Open action;
(9) the discrepancy/degradation signals in `--destructive` — the discrepancy callout, the shell snapshot-error notice, `.blocked-by` labels, and `.plan-segment-warning` borders.

Navigation pass — confirm in the same browser session, once per theme:
(10) on the dashboard, when the Next up item is a phase, clicking it lands on the roadmap with that phase's disclosure open and scrolled into view — not on the Not Found page and not on a fully collapsed roadmap;
(11) the same holds for the blocker action when an authored blocker is present, and it falls back to `/roadmap` when no current phase resolves;
(12) every other phase disclosure on that roadmap is still collapsed, and the History section is still collapsed unless the target phase lives inside an archived milestone;
(13) a phase reference clicked in prose opens its preview, and its Open action reaches the same scoped destination;
(14) reloading the deep link directly in the address bar reproduces the scoped view (bookmarkability, NAV-06).
    </human-check>
  </verify>
  <acceptance_criteria>
    - The automated command exits 0 on the integrated post-gap-closure tree before the browser pass begins.
    - The recorded reply names each of items (1) through (9) individually with a light-theme and a dark-theme verdict; a surface named without a verdict, or omitted, is recorded as still-open rather than approved.
    - The recorded reply gives a verdict for each of items (10) through (14).
    - Any failure is reported with its exact surface, theme, viewport, route, and the observed foreground/background pair or observed navigation destination — enough to reproduce without re-deriving it.
    - `02-09-SUMMARY.md` records the evidence verbatim; it does not restate the deliverable as `status: unknown`, which is the state this gate exists to replace.
    - Silence, a skipped surface, or a blanket approval does not complete this plan.
  </acceptance_criteria>
  <done>A host browser has confirmed, surface by surface and theme by theme, that real long-form artifact content is legible, and that the repaired dashboard CTA and phase deep link reach a correctly scoped view — or the exact failing case is recorded for a further gap-closure round.</done>
  <resume-signal>Reply `approved` with the per-surface verdicts for items (1) through (14), or report the failing surface, theme, viewport, route, and observed behavior.</resume-signal>
</task>

</tasks>

## Artifacts this phase produces

This plan produces no source symbols and modifies no source file. It produces exactly one artifact:

- `.planning/phases/02-situational-awareness-artifact-reading/02-09-SUMMARY.md` — the recorded
  per-surface light/dark contrast approval on real long-form content, plus the browser confirmation of
  the 02-07 and 02-08 navigation fixes, replacing the `status: unknown` / `human_judgment: true`
  record that `02-06-SUMMARY.md` carries for deliverable D3.

## Planner assumptions and declined items (explicit, not silent drops)

**Flagged planner assumption — the one unclassified edge-probe row this plan owns:**

1. **UI-02 — `unclassified`.** The deterministic edge probe could not classify "light and dark themes
   both render correctly, with contrast verified on real long-form artifact content" into an
   `adjacency` / `empty` / `ordering` category, so it stays `unresolved` and is **not** auto-resolved
   with a backstop marker. **Assumption this plan makes:** contrast legibility is a human judgment with
   no defensible mechanical acceptance criterion available in this repo — there is no browser in the
   agent shell, no headless renderer, and no automated contrast-ratio tool in the dependency tree, and
   adding one is out of gap scope. The gate therefore substitutes a *named-surface* human checklist for
   a computed criterion. If a future phase adds a headless renderer, this row becomes mechanically
   resolvable and should be re-probed. Not silently dropped: recorded here for the verifier.

Edge-probe accounting for this plan's share: 1 surfaced (UI-02 `unclassified`) = 0 authored as truths
+ 1 flagged assumption. Across all three gap plans: 12 surfaced = 9 authored into `must_haves.truths`
(6 in 02-07, 3 in 02-08) + 3 flagged assumptions (NAV-03 and NAV-06 in 02-08, UI-02 here).
No-silent-drop equality holds for the full probe report.

**Prohibition probe accounting:** Stage 1 over-produced 2 candidates for UI-02; Stage 2 collapsed them
into 1 kept transparency prohibition (authored above, descriptor-less). 1 kept = 1 authored + 0
flagged. Across all three gap plans: 7 kept = 7 authored + 0 flagged.

**UI-SPEC `## UI Considerations` lift, scoped to this gap:** the UI-SPEC Color table (lines 99-123) is
the source of the named accent-reserved and destructive-reserved surfaces in the human-check; it is
lifted into the checklist rather than restated as a truth, because a color-role table is an
implementation contract, not an observable state consideration. No E-numbered consideration row is
`unresolved` for a surface this gate inspects.

**Explicitly declined, with rationale (not silent drops):**
- **UI-03 horizontal-containment behavior at 390px**, which `02-VERIFICATION.md` marks `⚠ SATISFIED
  (static), NEEDS HUMAN (behavior)` in its Requirements Coverage table. It is **not** listed in
  `behavior_unverified_items` and is **not** one of the three open gaps, so re-owning it here would
  expand a `--gaps-only` run beyond its scope. It remains covered by the existing `02-06` approval
  record. The human is at 390px anyway during items (1) through (9), so a spontaneous observation is
  welcome — but it is not an acceptance criterion of this plan.
- **A headless-browser or automated contrast-ratio harness.** Adding Playwright, jsdom, or an axe-style
  checker to satisfy UI-02 mechanically would introduce new dependencies and a new test runtime into a
  gap-closure run whose whole premise is that the code is otherwise shipped. Recorded as the natural
  follow-up if contrast regressions recur.

**Capability gates:**
- API coverage (`ai-integration`): satisfied by the existing reasoned declaration in
  `.planning/phases/02-situational-awareness-artifact-reading/COVERAGE.md`. No matrix invented.
- Assumption-delta: `gsd query assumption-delta scan 02 --json` returned `detected: false`. Not raised.
- Schema push: no ORM and no database in this project. The gate correctly detects nothing; no push
  task is invented.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Automated evidence -> human approval | A passing suite cannot establish rendered contrast, and in this repo it cannot establish the deep-link page wiring either (no jsdom, no testing-library). |
| Read-only external corpus -> local viewer | `/home/cinedise/studio-portal` is observed only; it must never become a write target, a committed fixture, or a runtime dependency. |
| Human verdict -> phase completion record | The approval string is the sole evidence closing UI-02; a vague or partial verdict silently weakens the record. |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-32 | Repudiation | the contrast approval record | medium | mitigate | Require a per-surface verdict for items (1) through (14); an omitted surface is recorded as still-open, and a blanket approval does not complete the plan. This is precisely the failure mode that produced `02-06-SUMMARY.md`'s `status: unknown`. |
| T-02-33 | Tampering | the read-only studio-portal corpus | medium | mitigate | The viewer runs read-only by project constraint; the action forbids writing to, committing against, or depending on that directory. |
| T-02-34 | Elevation of Privilege | auto-approval of a blocking-human gate | high | mitigate | This checkpoint is `gate="blocking-human"` and is never auto-approvable; `workflow.auto_advance` must not satisfy it. An executor that cannot obtain a human verdict halts rather than proceeding. |
| T-02-35 | Spoofing | a locally served page impersonating project state | low | accept | The server is loopback-only over a directory the operator selected on the command line; there is no remote origin to spoof. |
| T-02-SC | Tampering | npm/pip/cargo installs | high | mitigate | This plan installs nothing and modifies no file outside `.planning/`. The action explicitly declines adding Playwright, jsdom, or a contrast-checking dependency. If an executor concludes an install is needed, stop — this gate is already blocking-human. |
</threat_model>

<verification>
The automated command must pass on the integrated tree immediately before the browser checklist begins.
The per-surface verdicts, or the exact failing surface/theme/viewport/route, are recorded verbatim in
`02-09-SUMMARY.md`. No item is marked passed without a stated verdict.
</verification>

<success_criteria>
- The full automated gate passes on the post-gap-closure integrated tree.
- Every named contrast surface carries an explicit light-theme and dark-theme verdict on real
  long-form content.
- The repaired dashboard CTA and phase deep link are confirmed behaviorally in a real rendering engine.
- No surface is silently skipped, and `UI-02` no longer rests on a `status: unknown` record.
</success_criteria>

<output>
Create `.planning/phases/02-situational-awareness-artifact-reading/02-09-SUMMARY.md` when done and
record the per-surface approval evidence verbatim.
</output>
