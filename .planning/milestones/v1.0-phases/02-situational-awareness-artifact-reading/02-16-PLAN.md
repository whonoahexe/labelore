---
phase: 02-situational-awareness-artifact-reading
plan: 16
type: execute
wave: 12
depends_on: [02-14]
files_modified:
  - src/presentation/dashboard.ts
  - src/web/pages/dashboard-page.tsx
  - src/web/pages/plan-pair-page.tsx
  - src/web/styles/globals.css
  - test/presentation/dashboard.test.ts
  - test/web/attention-row-contract.test.ts
  - test/web/visual-contract.test.ts
autonomous: true
gap_closure: true
requirements: [DASH-01, DASH-02, DASH-03, UI-01, UI-02]
estimate:
  tokens: 34000
  raw_tokens: 34000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "G2-07 has a deterministic attention-row destination matrix before any routing change, and every producer/consumer mismatch found by that matrix is corrected at its root."
    - "Per D-04 and G2-08, unusually long Next descriptions keep the panel bounded while their full text remains present for assistive technology."
    - "Per G2-09, plan section labels use one named muted-text token contract across nested and top-level sections."
    - "Per G2-10, status chips derive color from a documented semantic tone matrix rather than page-specific color overrides."
  artifacts:
    - path: test/web/attention-row-contract.test.ts
      provides: "Deterministic main-destination and provenance-destination reproduction matrix"
    - path: src/web/styles/globals.css
      provides: "Next clamping and shared label/chip token contracts"
    - path: test/web/visual-contract.test.ts
      provides: "Named reproductions for G2-08, G2-09, and G2-10"
  key_links:
    - from: src/presentation/dashboard.ts
      to: src/web/pages/dashboard-page.tsx
      via: "AttentionItem.url rendered as the row's primary destination"
      pattern: "AttentionItem"
    - from: src/web/pages/dashboard-page.tsx
      to: src/web/styles/globals.css
      via: "Next and attention semantic classes"
      pattern: "next-primary"
    - from: src/web/pages/plan-pair-page.tsx
      to: src/web/styles/globals.css
      via: "semantic status-chip tone assignment"
      pattern: "status-chip"
---

<objective>
Diagnose the unreproduced interaction and token reports from G2-07 through G2-10, then apply only fixes justified by deterministic reproductions.

Purpose: Replace intermittent or visual-only reports with executable contracts and root-caused corrections.
Output: An attention destination matrix, bounded Next descriptions, and consistent label/chip token behavior.
</objective>

<execution_context>
@/home/cinedise/.codex/gsd-core/workflows/execute-plan.md
@/home/cinedise/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-situational-awareness-artifact-reading/02-CONTEXT.md
@.planning/phases/02-situational-awareness-artifact-reading/02-UI-SPEC.md
@.planning/phases/02-situational-awareness-artifact-reading/02-13-SUMMARY.md
@src/presentation/dashboard.ts
@src/web/pages/dashboard-page.tsx
@src/web/pages/plan-pair-page.tsx
@src/web/styles/globals.css
</context>

<prohibitions>
- Do not change attention routing from the human report alone; establish the exact producer, row type, primary URL, and provenance URL first.
- Do not hide or delete long Next description content; visual clamping must leave the complete text in the accessibility tree.
- Do not assign raw colors per page or per chip instance; use the existing semantic token and tone vocabulary.
- Do not make every chip identical: active/complete and quiet/inferred states must remain semantically distinguishable.
</prohibitions>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reproduce G2-07 across every attention producer before correcting destinations</name>
  <files>src/presentation/dashboard.ts, src/web/pages/dashboard-page.tsx, test/presentation/dashboard.test.ts, test/web/attention-row-contract.test.ts</files>
  <read_first>
    - src/presentation/dashboard.ts
    - src/web/pages/dashboard-page.tsx
    - test/presentation/dashboard.test.ts
    - .planning/phases/02-situational-awareness-artifact-reading/02-13-SUMMARY.md
    - .planning/phases/02-situational-awareness-artifact-reading/02-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: Discrepancy, resolvable blocker, fallback blocker, dependency, checkpoint, and coverage attention fixtures each expose the expected primary destination.
    - Test 2: Every row with provenance exposes a separately labelled source-evidence destination; it is never mistaken for the row's primary action.
    - Test 3: Serialization and parse round-trips preserve both destinations for every attention item kind.
    - Test 4: A fixture whose producer URL disagrees with the documented matrix fails with the item kind and both observed destinations in the assertion message.
  </behavior>
  <action>Create a deterministic attention-row contract that names every producer branch and asserts the primary `AttentionItem.url` independently from the provenance SourceLink URL. Start by running the matrix against current code and capture whether G2-07 reproduces as a producer mismatch, serialization mismatch, or ambiguous rendered affordance. Correct only the implicated root: producer selection in `dashboard.ts`, consumer binding in `dashboard-page.tsx`, or accessible link labelling that distinguishes primary action from source evidence. If the complete matrix is green, retain routing unchanged, make the two link roles unambiguous in accessible naming, and record `not reproduced by deterministic matrix` with the exact covered branches in the summary for the final browser re-gate.</action>
  <acceptance_criteria>
    - The reproduction matrix names all six attention branches and both possible link roles.
    - Any destination fix is preceded by a failing assertion that identifies its root layer.
    - Primary action and provenance links have distinct accessible names and independently asserted destinations.
    - A green initial matrix cannot trigger speculative producer or routing changes.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run test/presentation/dashboard.test.ts test/web/attention-row-contract.test.ts</automated>
    <fails_when>The command exits non-zero, any attention producer lacks an asserted primary destination, or the primary and provenance roles cannot be distinguished.</fails_when>
  </verify>
  <done>G2-07 is either reproduced and root-corrected or deterministically bounded as not reproduced, with every branch ready for the final human gate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reproduce and bound long Next descriptions for G2-08</name>
  <files>src/web/pages/dashboard-page.tsx, src/web/styles/globals.css, test/web/visual-contract.test.ts</files>
  <read_first>
    - src/web/pages/dashboard-page.tsx
    - src/web/styles/globals.css
    - test/web/visual-contract.test.ts
    - .planning/phases/02-situational-awareness-artifact-reading/02-UI-SPEC.md
    - .planning/phases/02-situational-awareness-artifact-reading/02-13-SUMMARY.md
  </read_first>
  <behavior>
    - Test 1: The visual contract first detects that primary and preview Next descriptions have no bounded line policy.
    - Test 2: Primary descriptions clamp at three visual lines and preview descriptions clamp at two visual lines at narrow widths.
    - Test 3: The complete description remains in the DOM and accessible name/text; only its visual presentation is clipped.
  </behavior>
  <action>Add a named failing visual-contract assertion for the absent Next description policy, then implement a shared multi-line clamp: three lines for `.next-primary p` and two for `.next-preview p`, with vertical box orientation and overflow containment. Keep the full authored description in the DOM so assistive technology receives the complete text. Do not clamp headings or remove semantic paragraph content. This is the reversible layout choice used under the agent's discretion to satisfy D-04 and G2-08.</action>
  <reversibility rating="reversible">The line counts are isolated CSS presentation values and can be adjusted without changing data or component contracts.</reversibility>
  <acceptance_criteria>
    - The pre-fix contract fails specifically because the description has no bounded line policy.
    - Long primary and preview descriptions cannot expand their panels beyond the named line limits.
    - Full description text remains available in semantic DOM content.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run test/web/visual-contract.test.ts &amp;&amp; npm run typecheck</automated>
    <fails_when>Either command exits non-zero, the line limits are absent, or the implementation truncates the underlying description string.</fails_when>
  </verify>
  <done>G2-08 has a deterministic pre-fix reproduction and a bounded, accessible Next description policy.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Diagnose and normalize section-label and status-chip tokens for G2-09 and G2-10</name>
  <files>src/web/pages/plan-pair-page.tsx, src/web/styles/globals.css, test/web/visual-contract.test.ts</files>
  <read_first>
    - src/web/pages/plan-pair-page.tsx
    - src/web/pages/dashboard-page.tsx
    - src/web/styles/globals.css
    - test/web/visual-contract.test.ts
    - .planning/phases/02-situational-awareness-artifact-reading/02-UI-SPEC.md
    - .planning/phases/02-situational-awareness-artifact-reading/02-13-SUMMARY.md
  </read_first>
  <behavior>
    - Test 1: All `.plan-section-label` declarations resolve to the named muted text token in light and dark themes, including nested sections.
    - Test 2: Status chips use semantic tones only: active/current and complete/exact use the primary status treatment; pending, quiet, and inferred use the muted treatment.
    - Test 3: No page-specific raw foreground/background override changes a chip's meaning outside the tone contract.
  </behavior>
  <action>Encode the G2-09/G2-10 diagnosis as a visual token matrix before editing styles. Remove the conflicting later section-label color so both top-level and nested labels resolve to the shared muted token. Audit every `status-chip` call site against semantic meaning; keep active/current and complete/exact on the primary treatment, assign inferred matches to the quiet treatment because they are lower-confidence rather than active, and retain pending/quiet on muted treatment. Consolidate duplicate declarations when safe, but preserve layout differences that are unrelated to color. Use CSS variables already defined for both themes; add no raw per-page color values.</action>
  <acceptance_criteria>
    - The diagnosis test identifies the conflicting section-label cascade and every status-chip tone call site.
    - Section labels render from the same muted token in both themes and nesting levels.
    - Chip color variation maps to semantic state/confidence and has no page-local override.
    - Inferred plan matches are visually quiet while exact matches remain complete.
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --run test/web/visual-contract.test.ts &amp;&amp; npm run lint</automated>
    <fails_when>Either command exits non-zero, a section label resolves to the primary text token, or a status chip uses an unrecognized/raw color treatment.</fails_when>
  </verify>
  <done>G2-09 and G2-10 are closed by named, theme-aware token contracts backed by executable diagnosis coverage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| presentation snapshot → attention action | Generated row data determines the user's primary navigation target |
| attention row → browser navigation | Two visually adjacent links may have different intended destinations |
| authored description → constrained panel | Unbounded content can disrupt navigation or obscure nearby actions |

## STRIDE Threat Register (OWASP ASVS L1; unresolved high severity blocks completion)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-16-01 | Tampering | attention destination producers | high | mitigate | Assert every producer branch and round-trip against a named destination matrix before accepting routing changes. |
| T-02-16-02 | Spoofing | adjacent primary/provenance links | medium | mitigate | Give each link role a distinct accessible label and verify destinations separately. |
| T-02-16-03 | Denial of service | long Next copy | low | mitigate | Bound visual line count while retaining complete semantic content. |
| T-02-16-04 | Information disclosure | attention fixtures | low | accept | Fixtures contain only synthetic planning paths and statuses; no credentials or external data are introduced. |
</threat_model>

<verification>
- Run the focused attention contract before and after any G2-07 code correction and preserve its initial diagnosis in the summary.
- Run the visual contract after each G2-08/G2-09/G2-10 fix so each human report has a named executable reproduction.
- Run `npm run typecheck` and `npm run lint` after focused tests.
- Confirm every existing path named in `<read_first>` is tracked with `git ls-files -- &lt;path&gt;` before implementation.
</verification>

<success_criteria>
- G2-07 is no longer an unbounded intermittent report: all producer branches and both link roles are deterministic, and any observed mismatch is corrected.
- G2-08 has an accessible visual clamp with a named pre-fix reproduction.
- G2-09 and G2-10 have theme-aware label/chip token matrices and no conflicting cascade or arbitrary override.
- No ASVS L1 high-severity threat remains open.
</success_criteria>

## Artifacts this phase produces

- `test/web/attention-row-contract.test.ts` — exhaustive attention destination and link-role matrix.
- `test/web/visual-contract.test.ts` — named G2-08/G2-09/G2-10 reproduction and token assertions.
- `src/web/styles/globals.css` — bounded Next descriptions and shared visual-token contracts.
- `src/presentation/dashboard.ts`, `src/web/pages/dashboard-page.tsx` — root-layer G2-07 correction only if the deterministic matrix reproduces a mismatch.
- `src/web/pages/plan-pair-page.tsx` — quiet semantic tone for lower-confidence inferred matches.

<assumption_delta>
no-change — Diagnosis distinguishes existing producer and link roles; it introduces no new identity or fallback predicate.
</assumption_delta>

<output>
Create `.planning/phases/02-situational-awareness-artifact-reading/02-16-SUMMARY.md` when done.
</output>
