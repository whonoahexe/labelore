---
phase: 02-situational-awareness-artifact-reading
plan: 13
type: execute
wave: 10
depends_on: [02-10, 02-11, 02-12]
files_modified: []
autonomous: false
gap_closure: true
requirements: [UI-01, UI-02, UI-03, READ-01, READ-03, READ-06, NAV-02, NAV-03, NAV-06, DASH-02, DASH-03, ROAD-01]

user_setup:
  - service: host-browser
    why: "No browser executable exists in the agent shell, and the project has no Playwright, Puppeteer or jsdom dependency — a deliberate v1 stack decision recorded in 02-UAT.md. Contrast judgment, visual nesting depth, scrollbar perceptibility, table appearance, scroll resting position and click behavior cannot be derived from source, grep or build output."
    dashboard_config:
      - task: "Open the running Labelore server against both corpora at a desktop viewport and at a 390px-wide viewport, in light theme and again in dark theme"
        location: "Host machine browser"

estimate:
  tokens: 11000
  raw_tokens: 11000
  tasks: 1
  confidence: low

must_haves:
  truths:
    - "Every one of the fourteen surfaces and flows enumerated in 02-UAT.md receives an individual recorded verdict in this round, including the two — muted/faded text and mermaid diagrams — that the first gate recorded as NOT REACHED (UI-02, SC6)."
    - "On a real long-form PLAN.md, no literal wrapper tag text is visible anywhere in the rendered body, and a read-first wrapper's contents read as a list rather than as one run-on paragraph (READ-03; G-01, G-11)."
    - "A fenced code block containing a 200-character unbroken line shows a scrollbar the reader can see and use to reach the end of the line, at a desktop viewport and at 390px, in both themes — and no line ends mid-token with no affordance (UI-03; G-10)."
    - "The artifact page reads as at most two boxed layers deep on any path from the app root to a leaf, replacing the four levels the first gate measured (UI-01; G-08)."
    - "A document whose data carries N resolved references shows N inline trigger elements at rest, each visually distinguishable from both plain prose and an authored link, and each opening the preview on click and on keyboard activation (NAV-02, NAV-03; G-03)."
    - "Tables, status chips and highlighted code are each accepted or rejected on their own terms against the revised contract, with a named reason on rejection (READ-01, UI-01; G-05, G-06, G-07)."
    - "The Needs-attention blocker row navigates to the scoped phase, and falls back to the roadmap when no current phase resolves (DASH-02, DASH-03; G-02)."
    - "A deep link into a long phase body comes to rest with the target phase visible below the sticky header, not scrolled past and not underneath it, and a reload of that URL reproduces the same view (NAV-06, ROAD-01; G-04)."
    - "The approval record names every inspected surface individually; an unnamed, unreached or unrenderable surface is recorded as a remaining gap rather than absorbed into a blanket pass."
  prohibitions:
    - statement: "MUST NOT record contrast approval for any named surface that was not actually inspected in a browser in both themes — a skipped surface is a remaining gap, not a pass."
      category: transparency
      requirement_id: UI-02
    - statement: "MUST NOT record a verdict for a fix belonging to a plan that is not merged into the tree under inspection — a gate run against a partial tree produces a verdict about something that was never shipped."
      category: transparency
      requirement_id: UI-02
  artifacts:
    - path: ".planning/phases/02-situational-awareness-artifact-reading/02-13-SUMMARY.md"
      provides: "The per-surface, per-theme verdict for all fourteen UAT items on the post-gap-closure tree, including the two items the first gate could not reach"
  key_links:
    - from: "host browser inspection"
      to: "plans 02-10, 02-11 and 02-12"
      via: "the gate runs only after the full suite, typecheck, lint, build and server smoke pass on the tree with all three merged"
      pattern: "approved"
---

<objective>
Re-run the human UAT gate that plan 02-09 opened and that `02-UAT.md` recorded as **not approved**,
against the tree with all three gap-closure fix plans merged.

Purpose: 02-09's gate found four defects and five quality objections, and recorded two surfaces as
NOT REACHED rather than as passes. Plans 02-10, 02-11 and 02-12 close the nine in-scope findings,
but every one of them has a residue that only a browser can decide — whether a scrollbar is
perceivable, whether a trigger reads as interactive prose, how many boxes a reader actually
perceives, whether a row click navigates, where a settled scroll comes to rest. This gate is the
only place those verdicts exist. It also carries forward 02-09's discipline that an uninspected
surface is a remaining gap, which is what made the first round's honest NOT REACHED record possible
and is why items 2 and 7 are still live rather than buried.
Output: one recorded verdict per surface per theme, or the exact failing surface, theme, viewport,
route and observation.
</objective>

<execution_context>
@/home/cinedise/.claude/gsd-core/workflows/execute-plan.md
@/home/cinedise/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-situational-awareness-artifact-reading/02-UAT.md
@.planning/phases/02-situational-awareness-artifact-reading/02-UI-SPEC.md
@.planning/phases/02-situational-awareness-artifact-reading/02-09-PLAN.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Gate: re-run every UAT surface on the post-gap-closure tree, including the two never reached</name>
  <precondition>Plans 02-10, 02-11 and 02-12 are all merged into the tree under inspection, and `npm test &amp;&amp; npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run build &amp;&amp; npm run smoke -- fixtures/dense` exits 0 on that tree. A host browser is available outside the agent shell; the agent shell has no browser executable and the project has no browser test runtime.</precondition>
  <read_first>
    - `.planning/phases/02-situational-awareness-artifact-reading/02-UAT.md` in full — the fourteen enumerated items, the nine recorded gaps, and the "Confirmed visually" list. This gate re-runs that exact enumeration; do not substitute a shorter list.
    - `.planning/phases/02-situational-awareness-artifact-reading/02-09-PLAN.md` — the gate this one re-runs, including its per-surface human-check and its prohibition against absorbing a skipped surface into a blanket pass.
    - `.planning/phases/02-situational-awareness-artifact-reading/02-UI-SPEC.md` § "Structural Nesting Rule", § "Code Block Overflow", § "Reference Trigger Resting Appearance", § "Tables", § "Badges", and the "Code syntax theme" row of the Design System table — the six revised clauses whose acceptance is a judgment this gate makes, each with the exact values to judge against.
    - `.planning/phases/02-situational-awareness-artifact-reading/02-10-SUMMARY.md`, `02-11-SUMMARY.md` and `02-12-SUMMARY.md` — what each fix plan claims, so a verdict can be attached to a claim rather than to a guess.
    - `src/web/styles/globals.css` — skim the `:root` and `.dark` token blocks so a failing surface can be reported against a named token rather than as "looks wrong."
  </read_first>
  <files>None. This checkpoint records human judgment after implementation and automation are complete; it modifies no source file.</files>
  <what-built>The Phase 2 reader after gap closure: unconditional PLAN wrapper segmentation with a generic labelled fallback and the locked warm code theme (02-10); attention rows with routable destinations and a settle-loop deep-link scroll (02-11); the flattened nesting, enforced overflow chain with a drawn scrollbar, revised tables, filled badges and the reference-trigger resting appearance, plus fixture content carrying an unrecognised wrapper, a 200-character unbroken code line and a mermaid diagram (02-12).</what-built>
  <action>
Run the automated gate first; it must exit 0 on the tree with all three fix plans merged before the
browser pass begins. If any of the three is not merged, stop — a verdict recorded against a partial
tree describes something that was never shipped.

Start Labelore against two corpora in turn: `fixtures/dense`, which now carries the unrecognised
wrapper, the unbreakable long line and the diagram that plan 02-12 added, and
`/home/cinedise/studio-portal`, the read-only real corpus with genuine long-form multi-task
`PLAN.md` files. The studio-portal directory is observed only — never written to, never committed
against, never added as a dependency.

In a host browser, at a desktop viewport and again at a 390px-wide viewport, in light theme and then
separately in dark theme, walk the fourteen items in the human-check below one at a time. Judge each
against its actual rendered composition, not against the token values in the stylesheet — the whole
reason this is human-gated is that a declaration can be correct in source and fail on screen, which
is exactly how G-10 shipped: `overflow-x: auto` was declared and the reader still could not tell
text was missing.

Two items need deliberate effort because the first round could not reach them, and the human-check
below gives each a concrete starting point rather than leaving them to be hunted for:

- **Item 2, muted and faded text.** The first tester could not locate these surfaces. They are not
  hidden, they are diffuse. The human-check names nine of them by selector and by the page and
  state that reveals each. Reach at least six, in both themes, and record which ones you reached.
- **Item 7, mermaid diagrams.** Neither corpus contained one, which is why no verdict existed. The
  dense fixture now does. Inspect both the rendered diagram and, separately, the fallback treatment
  that a diagram which fails to parse degrades into.

Record the outcome item by item, theme by theme. Approving an item you did not reach — or collapsing
the list into a single blanket judgment — defeats the purpose of this gate twice over: the first
round already produced an honest NOT REACHED record for exactly these two items, and burying them on
the second pass would destroy the one thing that round got right.
  </action>
  <how-to-verify>Run the automated command; if it exits 0 and all three fix plans are merged, open both corpora in a host browser and complete every item in the human-check at both viewports in both themes, then reply with per-item verdicts or the exact failing item, theme, viewport, route and observation.</how-to-verify>
  <verify>
    <automated>npm test &amp;&amp; npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run build &amp;&amp; npm run smoke -- fixtures/dense</automated>
    <fails_when>non-zero exit, or the vitest summary line reports any failed test, or eslint prints any line matching "error"</fails_when>
    <automated>git log --oneline -20 | grep -c '02-1[012]'</automated>
    <fails_when>the output is "0" — no commit from any of the three fix plans is present, so the tree under inspection is not the tree this gate is about</fails_when>
    <human-check>
**Contrast and rendering pass** — inspect each item separately in light theme and again in dark theme, at a desktop viewport and at 390px, on a real long-form `PLAN.md` from `/home/cinedise/studio-portal` and on `fixtures/dense`:

(1) **Body prose** in the artifact document. The first round failed this because raw pseudo-tags rendered as literal text. Confirm no literal `&lt;tag&gt;` or `&lt;/tag&gt;` text is visible anywhere outside a fenced code block, that a read-first wrapper's contents read as a bulleted list rather than one run-on paragraph, and that a section whose tag the tool does not recognise is indistinguishable in treatment from one it does.

(2) **Muted and faded text** — the item the first round could not locate. Each of these is a real surface; reach at least six and name which:
  · `.snapshot-status small` — app shell header, the snapshot read time, present on every page;
  · `.lede` — the intro paragraph under the page title on the dashboard and the roadmap;
  · `.next-preview p` — the description line on the two secondary Next-up rows, dashboard;
  · `.attention-list small` — the provenance line inside a Needs-attention row, dashboard;
  · `.discrepancy-callout p` — dashboard, visible only when formal and observed completion disagree; use a corpus where they do;
  · `.plan-section-label` — the label above every PLAN section on an artifact page;
  · `.artifact-metadata > summary span` — the collapsed frontmatter panel's summary line, artifact page;
  · `.document-outline > p` — the outline column heading, artifact page, on a document with three or more headings;
  · `.reference-preview-facts dt` — the field labels inside an open reference popover.

(3) **Links in every interaction state** — default, hover, keyboard focus-visible, and visited — including document links, requirement chips, and the roadmap phase-title link. The first round failed this on "hover does nothing."

(4) **Badges and status chips** — every chip should now read as a filled shape rather than outline-only text, with the active and complete tones clearly stronger than neutral. Judge against the revised Badges clause; "could be improved" is a rejection and needs a named reason.

(5) **Table chrome** — header row tint, cell text, and the hairline rules. Confirm rules are horizontal only with no vertical cell borders, that the header divider and the row dividers are the same weight and color, that an identifier-led first column no longer force-wraps while the second column runs wide, and that zebra striping appears on every table rather than some. This item was rejected unqualified last round; a pass needs to be affirmative, not merely less bad.

(6) **Highlighted code** — token colors against the code block background under the locked warm theme. Separately from the theme: on the dense fixture's 200-character unbroken line, confirm a scrollbar is visible at rest or on hover, that it can be used to reach the end of the line, and that no line ends mid-token with no way to see the remainder. This second half is the severe half.

(7) **Mermaid diagrams** — the item the first round could not reach. The dense fixture now contains a `flowchart TD` diagram. Inspect node labels, edge lines and diagram background in both themes, and separately inspect the monospace fallback that an unparseable diagram degrades into.

(8) **Reference triggers and the preview popover** — the first round found zero triggers in the DOM on a document whose data carried five references. Open a document with resolved references and confirm each trigger is visible at rest as muted, dotted-underlined inline text; that it does not look like an authored link, which is accent-colored, weighted and solid-underlined; that hover and keyboard focus each change it visibly; that clicking and pressing Enter each open the popover; and that the popover's Open action reaches the named destination.

(9) **Degradation and discrepancy signals** in the destructive color — the discrepancy callout, the shell snapshot-error notice, blocked-by labels, and the malformed-wrapper warning border.

(15) **Structural nesting** — on the artifact page, count the boxed layers along one path from the page background to a fenced code block. The first round measured four. The contract's maximum is two, and the intended result on this path is one: the code block's own edge. Report the number you count, not a verdict.

**Navigation pass** — confirm in the same browser session, once per theme:

(10) On the dashboard, when the Next up item is a phase, clicking it lands on the roadmap with that phase's disclosure open and **scrolled into position** — this is the half that failed last round on long bodies. Use a phase with a long body. Confirm the phase heading is visible below the sticky header, not underneath it and not scrolled past.

(11) The Needs-attention blocker row navigates to the scoped phase — this failed outright last round — and falls back to the roadmap when no current phase resolves. Confirm the row reads as activatable before you click it.

(12) Every other phase disclosure on that roadmap is still collapsed, and the History section is still collapsed unless the target phase lives inside an archived milestone.

(13) A phase reference clicked in prose opens its preview, and its Open action reaches the same scoped destination as the dashboard route.

(14) Reloading the deep link directly in the address bar reproduces the scoped view, and a heading anchor on a long artifact document also comes to rest on its heading rather than above or below it.
    </human-check>
  </verify>
  <acceptance_criteria>
    - The automated command exits 0 on a tree containing commits from all three of plans 02-10, 02-11 and 02-12, and the merge state is stated in the reply rather than assumed.
    - The recorded reply names each of items (1) through (9) and (15) individually with a light-theme and a dark-theme verdict. A surface named without a verdict, or omitted, is recorded as still-open rather than approved.
    - For item (2), the reply names which of the nine listed muted surfaces were reached; at least six are named. Any of the nine not reached is recorded as a remaining gap, not as a pass by omission.
    - For item (7), the reply gives a separate verdict for the rendered diagram and for the unparseable-diagram fallback. "No diagram encountered" is not an acceptable answer this round — the fixture contains one, so its absence is a defect report about the fixture or the renderer.
    - For item (6), the reply states explicitly whether the end of the 200-character line was reachable and by what affordance.
    - For item (15), the reply states a counted number of boxed layers, not an adjective.
    - The recorded reply gives a verdict for each of items (10) through (14), with item (10) tested against a phase with a long body and item (11) tested both with and without a resolving current phase.
    - Any failure is reported with its exact item, theme, viewport, route, and the observed foreground/background pair, observed destination, or observed resting position — enough to reproduce without re-deriving it.
    - `02-13-SUMMARY.md` records the evidence verbatim, including every item recorded as still-open.
    - Silence, a skipped item, or a blanket approval does not complete this plan.
  </acceptance_criteria>
  <done>A host browser has confirmed, item by item and theme by theme, that the nine in-scope UAT gaps are closed on real long-form content and that the two surfaces the first round never reached now have verdicts — or the exact failing cases are recorded for a further gap-closure round.</done>
  <resume-signal>Reply `approved` with the per-item verdicts for items (1) through (15), naming which muted surfaces were reached and stating the counted boxed-layer depth; or report the failing item, theme, viewport, route and observation.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| the read-only reference corpus -> the running app | `/home/cinedise/studio-portal` is another project's real planning tree, opened by this tool during the gate. |
| the gate's recorded verdict -> the phase's completion state | A verdict recorded here is the evidence on which the phase closes; a fabricated or absorbed verdict is unrecoverable downstream. |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-51 | Repudiation | a surface approved without having been inspected | high | mitigate | The two prohibitions above are the control, and the acceptance criteria operationalise them: item (2) requires the reached surfaces to be named, item (7) rejects "no diagram encountered" as an answer, item (15) requires a counted number rather than an adjective, and a surface named without a verdict is recorded as still-open. The first round proved this control load-bearing — it is why items 2 and 7 survived as open gaps rather than disappearing into a pass. |
| T-02-52 | Tampering | writing into the read-only reference corpus during the gate | high | mitigate | The tool has no write path by construction — the project constraint is read-only filesystem access to the target `.planning/`, enforced at the filesystem interface from Phase 01. The gate additionally instructs that the corpus is observed only and never committed against. Verify with `git -C /home/cinedise/studio-portal status --porcelain` before and after the session; any change is a defect report, not a gate outcome. |
| T-02-53 | Spoofing | a verdict recorded against a tree that does not contain the fixes it judges | high | mitigate | The precondition names all three fix plans, one runnable verify command greps the recent log for their commits, and the first acceptance criterion requires the merge state to be stated in the reply rather than assumed. |
| T-02-SC | Tampering | npm/pip/cargo installs | high | mitigate | This plan installs nothing and modifies no file. `files_modified` is empty. Every dependency already in the tree was cleared by the Package Legitimacy Audit in `02-RESEARCH.md`. If an executor concludes an install is needed to run the gate, stop and raise a blocking human checkpoint before running it. |
</threat_model>

<verification>
The automated half is the same integrated-tree gate the phase has used since plan 02-06, run on the
tree with all three fix plans merged:

`npm test && npm run typecheck && npm run lint && npm run build && npm run smoke -- fixtures/dense`

Everything else in this plan is human judgment, deliberately. The residue this gate owns is exactly
the set of claims the three fix plans could not close from source: perceptibility of a scrollbar,
perceived nesting depth, resting appearance of a trigger, appearance of a table, resting position of
a settled scroll, and behavior of a click. Each of those is carried as a `backstop` truth in the
plan that authored the fix, and each abstains to this gate rather than being recorded as passed.
</verification>

<success_criteria>
- All fourteen original UAT items, plus the nesting-depth count, carry an individual per-theme verdict.
- Items 2 and 7, recorded as NOT REACHED in the first round, are reached and have verdicts — or are recorded as still-open with the reason.
- Items 10 and 11, which failed or partially failed, are re-confirmed against a long body and against both blocker fallback branches.
- No surface is approved by omission, and no verdict is recorded against a tree missing any of the three fix plans.
- The automated gate exits 0 before the browser pass begins.
</success_criteria>

## Artifacts this phase produces

This plan produces no source symbols and modifies no source file. It produces exactly one artifact:

- `.planning/phases/02-situational-awareness-artifact-reading/02-13-SUMMARY.md` — the recorded
  per-item, per-theme verdict for all fourteen UAT items plus the boxed-layer count, replacing the
  `gaps-found` record that `02-UAT.md` carries and closing (or reopening) plan 02-09's gate.

## Planner assumptions and declined items (explicit, not silent drops)

**Edge-probe accounting — this plan's share, and the phase-level rollup.** All seventeen
`unclassified` rows in `.edge-coverage-fallback.json` are surfaced here as flagged planner
assumptions. Per the fallback protocol an unclassified row is never auto-resolved with a backstop
marker and never silently dropped, so none of them is authored into `must_haves.truths`:
DASH-02, DASH-03, ROAD-02, ROAD-03, ROAD-04, READ-02, READ-04, READ-05, READ-06, NAV-02, NAV-03,
NAV-06, HIST-01, HIST-02, UI-01, UI-02, UI-03.

**The assumption each carries:** the deterministic probe could not classify these requirements into
an `adjacency` / `empty` / `ordering` / `boundary` / `encoding` / `precision` / `concurrency`
category, so no mechanical edge criterion is derivable for them from the probe alone. Where such a
requirement is nonetheless exercised by this run, it is covered by a named acceptance criterion in
the plan that touches it (READ-04 by plan 02-10's sanitize-ordering threat mitigation; NAV-02 and
NAV-03 by 02-10's trigger-count assertion and 02-12's resting-appearance assertions; DASH-02 and
DASH-03 by 02-11's round-trip assertions; UI-01, UI-02, UI-03 and READ-06 by this gate's items).
Where it is not — ROAD-02, ROAD-03, READ-02, READ-05, HIST-01, HIST-02 — the assumption is that the
behavior shipped by plans 02-02 through 02-06 is unchanged by this run, which is checkable in the
diff and is not re-derived here. Seventeen surfaced = 0 authored + 17 flagged.

**Phase-level arithmetic, so the no-silent-drop equality is checkable:**
`34 applicable rows = 11 authored into must_haves.truths + 23 flagged planner assumptions + 0
dropped.` Authored: 7 in plan 02-10 (READ-03 `empty`, READ-03 `encoding`, READ-01 `adjacency`,
READ-01 `empty`, READ-01 `ordering`, NAV-04 `empty`, NAV-04 `encoding`) and 4 in plan 02-11
(DASH-04 `boundary`, ROAD-01 `adjacency`, ROAD-01 `empty`, ROAD-01 `ordering`). Flagged: 6 in plan
02-11 (DASH-01 `boundary`/`empty`/`encoding`/`precision`, DASH-04 `precision`, DASH-04
`concurrency`) and 17 here. Plan 02-12 authors and flags none, and drops none. `7 + 4 = 11`;
`6 + 17 = 23`; `11 + 23 = 34`.

**UI-SPEC `## UI Considerations` lift — phase-level rollup.** Twenty rows are in scope for this
gap-closure run, scoped to the surfaces these four plans touch rather than to all 103 rows in the
file, because E1–E14's rows are truths already read out of code shipped by plans 02-01 through
02-06 and re-authoring them here would restate settled work as new acceptance criteria. In scope:
E15's eight rows (5 authored explicit and 1 dismissed-with-reason in plan 02-10; 1 authored explicit
and 1 authored as a `backstop` marker in plan 02-12), E13's two rows (both authored explicit in
02-12), E11's new resting-appearance row (authored explicit in 02-12), E9's `long-text` row (closed
by explicit inclusion in 02-12 rather than deferred, and stated as such), and E14's eight rows (all
flagged in 02-12). `20 = 10 explicit + 1 backstop + 1 dismissed + 8 flagged + 0 dropped.`

**Deferrals recorded, not implemented.** Two items are deferred to a later phase by the
coordinator's ruling and are named here so their absence from every plan in this run is deliberate
and auditable:
1. **G-09 — paths, source paths and commit hashes are not linked.** Any `.planning/` path,
   source-code path or commit hash appearing in artifact prose should become an in-app link. It is
   new scope, adjacent to NAV-02 and NAV-03 but outside phase 2 as written. **Not implemented in
   this run.** It remains an open finding against the phase's core value, and it should be picked up
   with the artifact index rather than separately, because both answer the same question.
2. **E14 — the browsable artifact index has no client surface.** Carried forward as an unresolved
   open gap in the UI-SPEC and deferred with G-09; plan 02-12 carries the fuller record and all
   eight of its state considerations as flagged assumptions. **Not implemented in this run.**

**Prohibition probe — this plan's share and the phase-level rollup.** Stage 1 over-produced three
candidates for UI-02; Stage 2 kept two, both authored above descriptor-less: the contrast-approval
prohibition carried forward verbatim from `02-09-PLAN.md`, which the first gate proved load-bearing
when items 2 and 7 were correctly recorded as NOT REACHED rather than absorbed, and a new one
forbidding a verdict against a tree missing a fix plan. One was dropped as routine engineering.
Phase-level: `6 kept = 6 authored + 0 flagged` — 1 in plan 02-10, 1 in plan 02-11, 2 in plan 02-12,
2 here. Two further candidates were dropped by canon referral (injection via the widened rendering
path, and unencoded identity segments in a route) and appear as `T-02-37` and `T-02-41` in the
respective threat models rather than as minted project prohibitions.

**Explicitly declined, with rationale:**
- **A headless browser, Playwright, Puppeteer or jsdom.** Adding one would introduce a dependency
  and a new test runtime into a gap-closure run, against the deliberate v1 stack decision recorded
  in `02-UAT.md`. Recorded as the natural follow-up if visual regressions recur across a third
  round — at that point the cost of a browser runtime is less than the cost of a third human gate.
- **The three Revision Considerations in the UI-SPEC** — path-as-title duplication, the near-empty
  outline column at low heading counts, and the theme default ignoring `prefers-color-scheme`. The
  tester did not raise any of them and the UI-SPEC records all three as non-blocking observations
  needing a taste decision first. No plan in this run implements any of them, and this gate does not
  judge them. A spontaneous observation at the gate is welcome and should be recorded as an
  observation, not as a verdict.

**Capability gates:**
- API coverage (`ai-integration`): satisfied by the existing reasoned declaration in
  `.planning/phases/02-situational-awareness-artifact-reading/COVERAGE.md`. No matrix invented.
- Assumption-delta: the detector returned `detected: false` for phase 02. Not raised.
- Package legitimacy: no install task exists in this plan; see `T-02-SC`.

<output>
Create `.planning/phases/02-situational-awareness-artifact-reading/02-13-SUMMARY.md` when done.
</output>
</content>
