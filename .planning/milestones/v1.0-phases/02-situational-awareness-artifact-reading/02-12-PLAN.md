---
phase: 02-situational-awareness-artifact-reading
plan: 12
type: execute
wave: 9
depends_on: [02-08]
files_modified:
  - src/web/styles/globals.css
  - test/web/visual-contract.test.ts
  - fixtures/dense/.planning/phases/01-identity-slice/01-01-PLAN.md
  - test/__golden__/dense.json
autonomous: true
gap_closure: true
requirements: [UI-01, UI-03, READ-01, READ-06, NAV-02, NAV-03]

estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The artifact document container declares no enclosing border, no distinct background fill and no shadow, so it is no longer a boxed layer and the page background is the document background (UI-01; G-08)."
    - "The plan-section shell declares no enclosing border and no background fill, keeping only a single hairline top rule and its label typography, so it contributes no boxed layer to any nesting path (UI-01; G-08; UI-SPEC E15 overflow)."
    - "Inline code inside the artifact document carries a background tint and no border, so it reads as an inline text treatment rather than as a chip (UI-01; G-08)."
    - "A fenced code block is the only element in the document-canvas-to-code path that declares an enclosing border, and it does so because it is the functional scroll boundary (UI-01, UI-03; G-08, G-10)."
    - "Every ancestor on the path from the app root to a fenced code block declares an explicit zero minimum width, including the document overflow boundary class that is applied in four components and had no stylesheet rule at all (UI-03; G-10)."
    - "A fenced code block never switches to a wrapping white-space mode; a long unbroken line scrolls horizontally inside the block instead (UI-03, READ-01; G-10)."
    - "A horizontal scrollbar is explicitly styled for code blocks with a thumb drawn from a theme token, so the affordance is visible rather than relying on a platform overlay scrollbar that renders nothing at rest (UI-03; G-10)."
    - "Tables inside the artifact document give the first column a minimum width floor and prevent it from wrapping, so an identifier-led first column stops force-wrapping while an unconstrained second column runs past it (READ-01; G-05)."
    - "Table rules are horizontal only, at one weight and one color token for both the header divider and the inter-row dividers, with no vertical cell borders (READ-01; G-05)."
    - "Zebra striping is applied to every table inside the artifact document rather than selectively (READ-01; G-05)."
    - "Status chips render as filled shapes in every tone, with the active and complete tones carrying a stronger accent border mix than the neutral fill (UI-01; G-07)."
    - "A resolved reference trigger renders inline with muted foreground color and a dotted underline offset from the baseline, with no border and no background, and is visually distinct at rest from an authored markdown link, which is accent-colored, weighted and solid-underlined (NAV-02, NAV-03; G-03; UI-SPEC E11 resting appearance)."
    - "A reference trigger's hover and focus state moves to the accent color with a solid underline, and its keyboard focus-visible state carries its own visible ring rather than relying on the decoration change alone (NAV-02, NAV-03; G-03)."
    - "Document outline entries wrap an unbroken long token rather than overflowing the sticky outline column (READ-06; UI-SPEC E9 long-text)."
    - "Wide content — tables, code blocks and mermaid containers — scrolls inside its own container and the page body never scrolls horizontally, with the zero-minimum-width ancestor chain holding through the generic plan-section shell as well as the bespoke one (UI-03; UI-SPEC E13 overflow, E15 overflow)."
    - "A diagram that cannot render falls back to scrollable monospace source rather than vanishing, and a long unbroken code line scrolls rather than wrapping (UI-03; UI-SPEC E13 long-text)."
    - statement: "No path from the app root to any leaf, on any page, crosses more than two elements that read as containers — measured by counting border, background-step and shadow changes along a single ancestor chain in a browser."
      verification: backstop
    - statement: "An auto-derived plan-section label from an unusually long tag name wraps rather than overflowing its column or forcing horizontal page scroll."
      verification: backstop
    - statement: "The code block scrollbar thumb is visible against the code block background in both light and dark themes, and a reader can reach the end of a 200-character unbroken line at a desktop viewport and at 390px."
      verification: backstop
  prohibitions:
    - statement: "MUST NOT render an artifact in a way that silently omits authored content — a reader must never be left unable to tell that text is missing, because the whole promise of this tool is that a planning artifact can be read without opening the file by hand."
      category: honesty
      requirement_id: UI-03
    - statement: "MUST NOT let a reference trigger converge on the same resting appearance as an authored link — if the two look identical, the preview-first behavior the trigger exists to signal becomes visually undiscoverable and the reader is misled about what a click will do."
      category: transparency
      requirement_id: NAV-02
  artifacts:
    - path: "src/web/styles/globals.css"
      provides: "The revised visual system — flattened nesting, an enforced overflow chain with a visible scrollbar, the table and badge treatments, and the reference-trigger resting appearance"
    - path: "test/web/visual-contract.test.ts"
      provides: "Stylesheet-source assertions pinning every mechanically checkable clause of the revised contract"
    - path: "fixtures/dense/.planning/phases/01-identity-slice/01-01-PLAN.md"
      provides: "The real content the human gate needs — an unrecognised wrapper, a 200-character unbroken code line, and a mermaid diagram"
  key_links:
    - from: "the document overflow boundary class applied in artifact-page and plan-pair-page"
      to: "a stylesheet rule that actually exists"
      via: "an explicit zero minimum width declaration"
      pattern: "overflow-containment-chain"
    - from: "the reference trigger resting appearance"
      to: "the preview-first interaction it signals"
      via: "a resting treatment deliberately unlike an authored link"
      pattern: "affordance-distinct-from-link"
---

<objective>
Close the five UAT findings that are stylesheet concerns: G-10 (fenced code clipped with no
scrollbar and no wrapping), G-08 (four levels of boxing on the artifact page), G-05 (tables), G-07
(badges), and the resting-appearance half of G-03 (references exist in the data but nothing in the
page reads as a trigger). Also supply the real fixture content the re-run gate needs to reach the
two surfaces it never reached.

Purpose: G-10 is the severe one and was found while capturing evidence for a merely aesthetic
objection. Fenced code truncates mid-token with no affordance to reach the rest, so the document is
not ugly, it is incomplete, and the reader cannot tell. That is a direct hit on the phase's core
value. The other four are the quality objections the UI-SPEC revision converted from taste into
normative, checkable clauses. G-08 in particular is a sitewide structural rule, not a per-component
tweak, and the developer chose the flattest option.
Output: one stylesheet that satisfies the revised contract clause by clause, a committed suite that
pins every mechanically checkable clause, and fixture content that makes the remaining visual
clauses reachable at the gate.
</objective>

<execution_context>
@/home/cinedise/.claude/gsd-core/workflows/execute-plan.md
@/home/cinedise/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-situational-awareness-artifact-reading/02-UI-SPEC.md
@.planning/phases/02-situational-awareness-artifact-reading/02-UAT.md
@.claude/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flatten the boxed layers and enforce the overflow containment chain</name>
  <files>src/web/styles/globals.css, test/web/visual-contract.test.ts</files>
  <read_first>
    - `.planning/phases/02-situational-awareness-artifact-reading/02-UI-SPEC.md` § "Structural Nesting Rule (Revision — G-08)" lines 100-161 — the normative two-layer maximum, the definition of a boxed layer, the four retroactive changes and the explicit list of panels the rule does not change. Read the contract text; it is normative and this plan paraphrases it.
    - `.planning/phases/02-situational-awareness-artifact-reading/02-UI-SPEC.md` § "Code Block Overflow (Revision — G-10)" lines 292-345 — the four rules, especially rule 3's ancestor-chain diagnosis and the warning that a short-line fixture will not surface this class of bug.
    - `src/web/styles/globals.css` lines 1275-1300 — the shared overflow utility block and the thin-scrollbar block. The first of these is asserted by an existing test with a selector-order-sensitive regex; do not reorder those three selectors.
    - `src/web/styles/globals.css` lines 1584-1600, 2085-2100 and 2195-2245 — the two `.plan-section` rule blocks, the `.document-canvas` rule, the two `.artifact-document pre` rules and the inline-code rule. These are the five sites the nesting rule changes.
    - `src/web/pages/artifact-page.tsx` line 199 and `src/web/pages/plan-pair-page.tsx` lines 147, 196 and 202 — the four components that already apply the `document-overflow-boundary` class. The class is applied four times and asserted by `test/presentation/coverage.test.ts` line 145, and has zero stylesheet rules. That absent rule is the concrete break in the ancestor chain.
    - `test/web/shell-contract.test.ts` lines 40-50 — the existing containment assertions this task must keep green.
  </read_first>
  <action>
Apply the four retroactive nesting changes and the overflow chain, then pin them.

Nesting. In the `.document-canvas` rule, delete the enclosing outline declaration and the offset
hard-edge shadow declaration and the `--card` fill, keeping `min-width: 0`, `max-width: 70rem` and
the padding; the page background becomes the document background. In both `.plan-section` rule
blocks, delete the enclosing outline, the accent left rule and the mixed fill, and replace them with
a single `border-top: 1px solid var(--border)`, keeping the label typography and the vertical
rhythm — a single rule on one edge is not an enclosing container and does not count as a layer. In
the `.artifact-document :not(pre) > code` rule, delete the outline declaration and set the
background to `color-mix(in oklch, var(--secondary) 70%, transparent)`, keeping the padding, color
and font. Leave `pre` with exactly one enclosing outline: it survives as the single Layer-1 box
because it is the functional scroll boundary. Leave `.immediate-work`, `.attention-panel`,
`.metadata-panel`, `.wave-band`, `.coverage-matrix` and the reference-preview popover exactly as
they are; the contract names them as unchanged Layer-1 panels that sit inside nothing.

Overflow chain. Add a `.document-overflow-boundary` rule declaring `min-width: 0` and
`max-width: 100%`. This class is applied in four components and has no rule at all today, which is
the concrete missing link the contract's rule 3 describes — a flex or grid child defaults to an
automatic minimum width, so one unstyled ancestor forces the whole subtree to the code block's
intrinsic width and the page-level clamp then clips it with no scrollbar anywhere. Add
`min-width: 0` to both `.plan-section` blocks so the chain holds through the generic section shell
as well as the bespoke one.

Scrollbar visibility. Add `.artifact-document pre::-webkit-scrollbar { height: 8px; }`,
`.artifact-document pre::-webkit-scrollbar-thumb { background: var(--border); border-radius: 0; }`
and a `:hover` variant on the thumb using `var(--muted-foreground)`. The existing
`scrollbar-width: thin` covers one engine only; a platform overlay scrollbar draws nothing at rest,
which is precisely why the tester saw truncation with no affordance. Confirm the code block keeps
the non-wrapping white-space mode it has by default and that nothing overrides it to a wrapping
mode.

Cross-plan fragments named by plan 02-11, authored here because this stylesheet is this plan's:
add an `.attention-action` rule declaring `text-decoration: none` and a `grid-column` placement
keeping it in the second column of the attention list grid, and add `scroll-margin-top: 6rem` to
the `.roadmap-phase` rule so a settled deep-link target rests below the sticky header rather than
underneath it. Both are named identically in 02-11; neither is invented here.

Create `test/web/visual-contract.test.ts` and pin every clause above with region-scoped
stylesheet-source assertions, following the `source()` helper pattern already used in
`test/web/shell-contract.test.ts`.
  </action>
  <verify>
    <automated>npx vitest run test/web/visual-contract.test.ts test/web/shell-contract.test.ts test/presentation/coverage.test.ts</automated>
    <fails_when>non-zero exit, or the vitest summary line reports any failed test</fails_when>
    <automated>sed -n '/^\.document-canvas {/,/^}/p' src/web/styles/globals.css | grep -cE '^ *(border|box-shadow|background) *:'</automated>
    <fails_when>any output other than the single line "0"</fails_when>
    <automated>sed -n '/^\.plan-section {/,/^}/p' src/web/styles/globals.css | grep -cE '^ *(border|border-left|border-right|border-bottom|background) *:'</automated>
    <fails_when>any output other than the single line "0"</fails_when>
    <automated>grep -c 'document-overflow-boundary' src/web/styles/globals.css</automated>
    <fails_when>the output is "0" — the class is applied in four components and must have a rule</fails_when>
  </verify>
  <acceptance_criteria>
    - `sed -n '/^\.document-canvas {/,/^}/p' src/web/styles/globals.css | grep -cE '^ *(border|box-shadow|background) *:'` outputs `0`.
    - `sed -n '/^\.plan-section {/,/^}/p' src/web/styles/globals.css | grep -cE '^ *(border|border-left|border-right|border-bottom|background) *:'` outputs `0`, and the same region contains at least one occurrence of `border-top: 1px solid var(--border)`.
    - `sed -n '/^\.artifact-document :not(pre) > code {/,/^}/p' src/web/styles/globals.css | grep -cE '^ *border *:'` outputs `0`, and the same region contains `color-mix(in oklch, var(--secondary) 70%, transparent)`.
    - `sed -n '/^\.artifact-document pre {/,/^}/p' src/web/styles/globals.css | grep -c 'border: 1px solid var(--border)'` returns at least 1 — the code block keeps the one surviving enclosing box.
    - `grep -c 'document-overflow-boundary' src/web/styles/globals.css` returns at least 1, and that rule declares `min-width: 0`.
    - `sed -n '/^\.plan-section {/,/^}/p' src/web/styles/globals.css | grep -c 'min-width: 0'` returns at least 2 — both blocks carry it.
    - `grep -c 'artifact-document pre::-webkit-scrollbar' src/web/styles/globals.css` returns at least 2 — the track height rule and the thumb rule.
    - `grep -cE 'white-space: *pre-wrap' src/web/styles/globals.css` returns `0` for any selector matching a code block.
    - `grep -c 'attention-action' src/web/styles/globals.css` returns at least 1, and `sed -n '/^\.roadmap-phase {/,/^}/p' src/web/styles/globals.css | grep -c 'scroll-margin-top'` returns `1`.
    - `test/web/shell-contract.test.ts` passes unmodified, proving the selector-order-sensitive containment regex still matches.
  </acceptance_criteria>
  <reversibility rating="costly">Removing the enclosing boxes from the document canvas and the plan section changes the resting appearance of every artifact page at once, and the developer chose the flattest of the offered options. Reverting is mechanical but re-opens a taste decision that was made once and should not be re-litigated per component.</reversibility>
  <done>The path from the app root to a fenced code block crosses exactly one element that reads as a container, that element is the code block's own scroll boundary, every ancestor above it can shrink below the content width, and the horizontal scrollbar is drawn rather than left to the platform.</done>
</task>

<task type="auto">
  <name>Task 2: Supply the real content the gate needs to reach the surfaces it could not</name>
  <files>fixtures/dense/.planning/phases/01-identity-slice/01-01-PLAN.md, test/__golden__/dense.json, test/web/visual-contract.test.ts</files>
  <read_first>
    - `fixtures/dense/.planning/phases/01-identity-slice/01-01-PLAN.md` in full — the existing wrapper vocabulary in this fixture is entirely inside the recognised set (`objective`, `context`, `decision`, `tasks`, `task`, `name`, `files`, `action`, `done`, `verify`, `verification`, `success_criteria`, `output`, `what-built`, `how-to-verify`, `resume-signal`), which is exactly why the first UAT never exercised the unrecognised path against a fixture.
    - `fixtures/README.md` — the fixture contract, including the note that the dense tree's golden permanently contains warnings by design and that assertions against it are made by named path, never by total count.
    - `test/snapshot.golden.test.ts` — the goldens are asserted through `toMatchFileSnapshot` against `buildSnapshotJson(..., { stable: true, withBodies: false })`. Bodies are excluded, so a body-only edit may leave the golden untouched; extracted structured fields may not.
    - `test/degradation.test.ts` — warnings on the dense tree are asserted by stage and by named path. New valid content must add no warning to any named path.
    - `.planning/phases/02-situational-awareness-artifact-reading/02-UAT.md` § "Tests" rows 2 and 7 — the two surfaces recorded as NOT REACHED, and the reason each was unreachable.
    - `src/rendering/markdown.ts` lines 19-22 and 173-203 — the mermaid recognition pattern, the 256 KiB source cap and the strict-security dormant-target emission, so the fixture's diagram is written in a form the renderer will actually accept.
  </read_first>
  <action>
Append content to the dense fixture's plan body so the human gate can reach the surfaces the first
gate could not, and so the overflow fix is exercised against content that genuinely triggers it.

Add three things inside the existing plan body:

1. A wrapper whose tag name is outside the recognised set — use `read_first` enclosing a
   three-item markdown bullet list, and `execution_context` enclosing one line of prose. These are
   the exact tag names the UAT observed leaking, and they give the gate a real instance of plan
   02-10's generic fallback to look at rather than a synthetic one.
2. A fenced code block containing at least one line with a 200-character unbroken token — no
   spaces, no hyphens, nothing the browser can break on. The contract is explicit that a short-line
   fixture will not surface this class of bug, and that is how it shipped undetected the first time.
   Use a `bash` or `text` fence so the highlighter has a loaded grammar for it.
3. A mermaid fence containing a small `flowchart TD` diagram of four nodes. Neither corpus contained
   one, which is the entire reason UAT item 7 was recorded as NOT REACHED. Keep it well under the
   256 KiB cap and start it with a token the renderer's diagram pattern recognises, so it becomes a
   live dormant target rather than the fallback — the gate needs to inspect a rendered diagram, and
   should separately be able to see the fallback by reading the existing invalid-mermaid path.

Write nothing into the fixture that names an absolute host filesystem path.

Then run the golden suite. If `test/snapshot.golden.test.ts` reports a mismatch, read the diff and
confirm it is confined to this one fixture file's extracted content; if it is, regenerate with
`npx vitest run -u test/snapshot.golden.test.ts` and commit the regenerated golden. If the diff
touches any other fixture or any other file's entry, stop — that means something other than this
edit moved.

Add an assertion to `test/web/visual-contract.test.ts` that reads the fixture file and confirms it
contains a line of at least 200 characters with no whitespace, so a later well-meaning reformat
cannot quietly remove the only content that exercises the overflow path.
  </action>
  <verify>
    <automated>npx vitest run test/snapshot.golden.test.ts test/degradation.test.ts test/web/visual-contract.test.ts</automated>
    <fails_when>non-zero exit, or the vitest summary line reports any failed test, or the output contains "Snapshot" together with "obsolete"</fails_when>
    <automated>npm run smoke -- fixtures/dense</automated>
    <fails_when>non-zero exit, or the output contains "error" or "failed"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'read_first' fixtures/dense/.planning/phases/01-identity-slice/01-01-PLAN.md` returns at least 2 — an opening and a closing wrapper.
    - `grep -c 'execution_context' fixtures/dense/.planning/phases/01-identity-slice/01-01-PLAN.md` returns at least 2.
    - `grep -c 'flowchart TD' fixtures/dense/.planning/phases/01-identity-slice/01-01-PLAN.md` returns at least 1, and the line immediately preceding it opens a fence labelled `mermaid`.
    - `awk 'length >= 200 && $0 !~ / /' fixtures/dense/.planning/phases/01-identity-slice/01-01-PLAN.md | wc -l` returns at least 1.
    - `grep -c '/home/cinedise' fixtures/dense/.planning/phases/01-identity-slice/01-01-PLAN.md` returns `0`.
    - `npx vitest run test/snapshot.golden.test.ts` exits 0, either because the golden was unchanged or because it was regenerated and committed.
    - `npx vitest run test/degradation.test.ts` exits 0 with no new warning on any named path.
    - `npm run smoke -- fixtures/dense` exits 0.
  </acceptance_criteria>
  <done>The dense fixture now contains an unrecognised wrapper, a genuinely unbreakable long line, and a real diagram — so the two surfaces the first gate could not reach are reachable, and the overflow fix is exercised by content that would actually have caught the original defect.</done>
</task>

<task type="auto">
  <name>Task 3: Tables, badges, reference triggers and the outline wrap gap</name>
  <files>src/web/styles/globals.css, test/web/visual-contract.test.ts</files>
  <read_first>
    - `.planning/phases/02-situational-awareness-artifact-reading/02-UI-SPEC.md` § "Tables (Revision — G-05)" lines 408-460, § "Badges (Revision — G-07)" lines 463-489, and § "Reference Trigger Resting Appearance (Revision — G-03)" lines 348-405. All three are normative and carry exact values; read them rather than paraphrasing from this plan.
    - `src/web/styles/globals.css` lines 1567-1590 — the artifact table rules, including the current cell rule that draws a full enclosing outline on every cell, which is what reads as "cramped."
    - `src/web/styles/globals.css` lines 2195-2200 — the existing zebra rule, which the contract keeps and requires to be applied consistently.
    - `src/web/styles/globals.css` lines 498-536 — the `.status-chip` rules and the three tones.
    - `src/web/styles/globals.css` lines 1652-1670 — the current `.document-reference` rule. It is accent-colored, weight 650 and carries a bottom-border affordance, which makes it look exactly like an authored link. The contract requires the opposite: muted, dotted, inheriting weight, distinct from a link at rest.
    - `src/web/styles/globals.css` lines 2078-2090 — `.document-outline a`, the one selector on this surface that the wrap pass missed.
    - `.planning/phases/02-situational-awareness-artifact-reading/02-UAT.md` § "G-03" and the "Confirmed visually" list — the DOM query returned zero triggers on a document whose data carried five references.
  </read_first>
  <action>
Tables. Add `.artifact-document table :is(th, td):first-child { min-width: 8ch; white-space: nowrap; }`
— a floor, not a fixed layout, because GSD table content is too irregular in shape for
`table-layout: fixed` to behave predictably. In the artifact cell rule, replace the full enclosing
cell outline with a horizontal rule only: `border-bottom: 1px solid var(--border)` and nothing on
the left, right or top, so tables read as horizontally ruled. Keep the header row's existing
background tint and label typography as the only differentiator between the header and the body —
not a heavier or differently colored rule. Keep the existing zebra declaration and confirm its
selector covers every table inside the artifact document rather than a subset.

Badges. Give `.status-chip` the neutral fill
`background: color-mix(in oklch, var(--muted) 55%, transparent)` so every chip reads as a filled
shape rather than outline-only text. Raise the icon `stroke-width` to `2.25` at the existing
`0.7rem` icon size — presence at small size without changing the size. Raise the active and
complete tones' border accent mix from its current 58 percent to
`color-mix(in oklch, var(--primary) 65%, var(--border))` so the two meaningful tones read as "on"
against the now-filled neutral chips. Quiet-tone chips keep their muted foreground text and receive
the same neutral fill. No new tone, no shape change — corners stay square.

Reference triggers. Rewrite `.document-reference` to the contract's resting appearance: color
`var(--muted-foreground)`; `text-decoration: underline` with `text-decoration-style: dotted` and
`text-underline-offset: 0.15em`; no border and no background; `cursor: pointer`; font size, weight
and family all inherited from the surrounding body text, which means removing the current weight
override so it reads as prose that happens to be interactive. On `:hover` and `:focus`, move color
to `var(--primary)` and `text-decoration-style` to `solid`. Keep the existing `:focus-visible`
outline ring — a `button` needs its own visible keyboard focus indicator and must not rely on the
decoration change alone. Do not add an inline icon; the existing arrow icon stays on the popup's
Open action only. Leave the `.artifact-document a` link treatment untouched — accent-colored, weight
600, solid underline — because the whole point is that the two must not converge.

Outline wrap. Add `overflow-wrap: anywhere` to `.document-outline a`. This is the E9 `long-text` gap
the UI-SPEC carries forward as unresolved, and it is being closed here rather than deferred: ten
other selectors on this surface already carry the same declaration, the omission is a one-line
oversight rather than a design question, and the sticky outline column is a surface the re-run gate
inspects. Recorded as an explicit inclusion, not a silent absorption.

Extend `test/web/visual-contract.test.ts` with region-scoped assertions for each clause above.
  </action>
  <verify>
    <automated>npx vitest run test/web/visual-contract.test.ts &amp;&amp; npm test</automated>
    <fails_when>non-zero exit, or the vitest summary line reports any failed test</fails_when>
    <automated>sed -n '/^\.document-reference {/,/^}/p' src/web/styles/globals.css | grep -cE 'dashed|var\(--primary\)|font-weight'</automated>
    <fails_when>any output other than the single line "0" — a resting trigger that is accent-colored, weighted, or bordered has converged on the authored-link treatment</fails_when>
    <automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run build</automated>
    <fails_when>non-zero exit, or eslint prints any line matching "error"</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -c 'min-width: 8ch' src/web/styles/globals.css` returns `1`, and the same rule declares `white-space: nowrap` for the first-child cell.
    - `sed -n '/^\.artifact-document :is(th, td) {/,/^}/p' src/web/styles/globals.css | grep -cE '^ *border *:'` outputs `0`, and the same region contains `border-bottom: 1px solid var(--border)`.
    - `grep -c 'tr:nth-child(even)' src/web/styles/globals.css` returns at least 1, and its selector is scoped to `.artifact-document table` so it applies to every table in the document.
    - `grep -c 'color-mix(in oklch, var(--muted) 55%, transparent)' src/web/styles/globals.css` returns at least 1.
    - `grep -c 'stroke-width: 2.25' src/web/styles/globals.css` returns at least 1.
    - `grep -c 'var(--primary) 65%, var(--border)' src/web/styles/globals.css` returns at least 1, and `grep -c 'var(--primary) 58%' src/web/styles/globals.css` returns `0`.
    - `sed -n '/^\.document-reference {/,/^}/p' src/web/styles/globals.css` contains all four of `var(--muted-foreground)`, `text-decoration-style: dotted`, `text-underline-offset: 0.15em` and `cursor: pointer`, and contains none of a border declaration, a background declaration or a font-weight declaration.
    - `grep -c 'document-reference:hover' src/web/styles/globals.css` returns at least 1 and `grep -c 'document-reference:focus-visible' src/web/styles/globals.css` returns at least 1.
    - `sed -n '/^\.document-outline a {/,/^}/p' src/web/styles/globals.css | grep -c 'overflow-wrap'` returns `1`.
    - `npm test`, `npm run typecheck`, `npm run lint` and `npm run build` all exit 0.
  </acceptance_criteria>
  <done>Tables are horizontally ruled with a first-column floor and consistent zebra, chips are filled with the two meaningful tones reading as on, a reference trigger reads at rest as interactive prose that is unmistakably not an authored link, and the one selector the wrap pass missed now wraps.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fixture artifact content -> the rendering pipeline -> the browser | Fixture content is committed input that the app parses, renders and, for mermaid, executes a diagram renderer against in the browser. |
| stylesheet tokens -> rendered artifact surface | Theme tokens decide whether a scroll affordance and a trigger affordance are perceivable; an invisible affordance is a correctness failure, not a cosmetic one. |
| authored artifact prose -> reference trigger elements | Trigger elements are produced only for resolved references; the resting-appearance rule must never be applied to unresolved text. |

## STRIDE Threat Register (ASVS L1, block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-46 | Elevation of Privilege | the mermaid diagram added to the dense fixture, executed by the client-side diagram renderer | high | mitigate | The fixture's diagram is authored by this plan and contains no script, link or click directive. The renderer's existing controls are unchanged and remain the enforcement point: `securityLevel: 'strict'`, a 256 KiB source cap, a diagram-start pattern check before the source is treated as a diagram, and a per-node parse guarded so a rejection falls back to inert monospace source. Task 2 must not relax any of those. |
| T-02-47 | Tampering | the unrecognised wrapper added to the dense fixture flowing through the widened segmentation path | high | mitigate | The widened path is plan 02-10's, and its own threat model covers escaping and sanitize ordering (`T-02-36`, `T-02-37`). What this plan owes is that the fixture exercises the path with ordinary content and adds no markup that would be meaningful only if sanitization failed — the fixture contains no script tag, no event-handler attribute and no data or javascript scheme. |
| T-02-48 | Information Disclosure | an absolute host filesystem path committed into a fixture | medium | mitigate | Task 2 forbids it and Task 2's acceptance criteria assert `grep -c '/home/cinedise'` returns `0` on the edited fixture. This mirrors the existing shell-contract assertion that no shipped source file names the reference corpus path. |
| T-02-49 | Repudiation | an affordance that exists in the stylesheet but is invisible at rest, so a reader cannot tell content is unreachable | high | mitigate | This is the mechanism of G-10 itself: `overflow-x: auto` was declared and the reader still could not tell text was missing. The mitigation is that the scrollbar thumb is drawn from an explicit theme token rather than left to a platform overlay, that the ancestor chain is asserted in the committed suite rather than assumed, and that the residual perceptual claim is authored as a `backstop` truth that abstains to the human gate rather than being recorded as passed. |
| T-02-50 | Denial of Service | a pathological table or code line forcing the page body to scroll horizontally | medium | mitigate | Every wide-content container keeps `max-width: 100%` with its own `overflow-x: auto`, the zero-minimum-width chain is asserted at four named selectors, and `test/web/shell-contract.test.ts` already asserts that no root-level rule sets a hidden or clipped horizontal overflow — which would mask the failure instead of fixing it. |
| T-02-SC | Tampering | npm/pip/cargo installs | high | mitigate | This plan installs nothing — `package.json` and the lockfile are not in `files_modified`, and every value it writes is plain CSS using tokens already in the theme. Every dependency already in the tree was cleared by the Package Legitimacy Audit in `02-RESEARCH.md`. If an executor concludes an install is needed, stop and raise a blocking human checkpoint before running it. |
</threat_model>

<verification>
Run in order, from the repository root:

1. `npx vitest run test/web/visual-contract.test.ts` — the new suite alone.
2. `npx vitest run test/web/shell-contract.test.ts test/presentation/coverage.test.ts` — the two
   existing suites with assertions over this stylesheet and over the overflow-boundary class name.
3. `npx vitest run test/snapshot.golden.test.ts test/degradation.test.ts` — the fixture edit's blast
   radius.
4. `npm test`
5. `npm run typecheck`
6. `npm run lint`
7. `npm run build`
8. `npm run smoke -- fixtures/dense`

What these commands cannot decide, and must not be reported as if they had: whether the scrollbar
thumb is actually perceivable against the code background in each theme, whether the boxed-layer
count along a real DOM path is at most two, and whether the trigger reads as interactive prose. All
three are authored above as `backstop` truths and are handed to plan 02-13's gate. A stylesheet-source
assertion proves the declaration exists; it does not prove the reader can see it.
</verification>

<success_criteria>
- No fenced code content is reachable only by opening the file by hand: the block scrolls, and the scrollbar is drawn.
- The document canvas and the plan-section shell are no longer boxed layers; the code block's own edge is the single surviving box on that path.
- Tables are horizontally ruled with a first-column floor and universally applied zebra.
- Every status chip reads as a filled shape, with the active and complete tones clearly stronger.
- A reference trigger is visible at rest and is unmistakably not an authored link.
- The dense fixture contains an unrecognised wrapper, a 200-character unbroken line and a real diagram.
- `npm test`, `npm run typecheck`, `npm run lint`, `npm run build` and `npm run smoke -- fixtures/dense` all exit 0.
</success_criteria>

## Artifacts this phase produces

New symbols and files introduced by this plan. The source-grounding pass should treat these as
newly created, not as drift against existing code:

- `src/web/styles/globals.css` — new rules for the previously unstyled class
  `.document-overflow-boundary`; new rule `.attention-action` (the class is emitted by plan 02-11);
  new pseudo-element rules `.artifact-document pre::-webkit-scrollbar` and
  `.artifact-document pre::-webkit-scrollbar-thumb` and its `:hover` variant; new rule
  `.artifact-document table :is(th, td):first-child`; new state rules `.document-reference:hover`
  and `.document-reference:focus`.
- `test/web/visual-contract.test.ts` — new test file.
- `fixtures/dense/.planning/phases/01-identity-slice/01-01-PLAN.md` — new fixture content: a
  `read_first` wrapper, an `execution_context` wrapper, a 200-character unbroken code line, and a
  `flowchart TD` mermaid diagram.

No new component, function or exported symbol is introduced. No dependency is added.

## Planner assumptions and declined items (explicit, not silent drops)

**Named cross-plan split (stated in both plans, invented by neither).** `.attention-action`
(`text-decoration: none` plus a second-column `grid-column` placement) and
`scroll-margin-top: 6rem` on `.roadmap-phase` are authored here because this plan owns the
stylesheet in wave 9, while the markup that uses the first and the scroll behavior that needs the
second are authored by plan 02-11. Both plans name the same class and the same declarations.
Neither fragment is load-bearing for behavior: without them the attention row still navigates and
the deep-link target still settles, they merely appear worse.

**Edge-probe rows this plan owns (§A lift from `.edge-coverage-fallback.json`).** None. Every
classified row bearing on a gap in this run's scope is owned by plan 02-10 (READ-01, READ-03,
NAV-04) or plan 02-11 (DASH-04, ROAD-01), and the requirements this plan touches most directly —
UI-01, UI-03, READ-06, NAV-02, NAV-03 — are all `unclassified` in the probe report and are therefore
surfaced as flagged assumptions in plan 02-13 alongside the rest of the unclassified set, per the
rule that an unclassified row is never auto-resolved with a backstop marker. Zero authored here,
zero dropped here.

**UI-SPEC `## UI Considerations` lift, this plan's share.** Four rows are authored above as truths:
E13 `overflow` and E13 `long-text` (both `resolved (explicit)`, revised by this pass), E15
`overflow` (`resolved (explicit)`), and E11's new `resting appearance` row
(`resolved (explicit, this revision)`). One row is authored as a `backstop` marker: E15 `long-text`,
which the UI-SPEC itself records as `verification: backstop` because no real GSD tag is currently
long enough to exercise the label case. One row, E9 `long-text`, is recorded in the UI-SPEC as
**unresolved and carried forward** — this plan **closes it** rather than deferring it, as an
explicit inclusion stated in Task 3's action, because it is a one-line declaration in a file this
plan already owns, ten sibling selectors already carry it, and the outline column is a surface the
re-run gate inspects. Six rows accounted for: 5 authored as truths (4 explicit, 1 backstop) + 1
closed by inclusion and stated.

**E14's eight rows and G-09 are deferred, not dropped.** The UI-SPEC records all eight of E14's
state considerations (`empty`, `loading`, `error`, `populated`, `partial`, `overflow`,
`zero-one-many`, `long-text`) as unresolved because the artifact index has no client surface at
all — `src/server/artifact-index.ts` resolves a single artifact by token, the route table offers
only the two artifact routes, and nothing in the UI answers *what artifacts exist*. The coordinator
deferred E14 together with G-09 (linking `.planning/` paths, source paths and commit hashes in
prose) to a later phase, so none of the eight is answerable from anything this run builds and none
is implemented here. All eight are surfaced as flagged planner assumptions: **the assumption is that
a browsable artifact index is a later phase's surface and that its eight state considerations will
be answered when that surface is designed, not inferred from the current single-token resolver.**
Eight surfaced = 0 authored + 8 flagged.

**Prohibition probe, this plan's share.** Stage 1 over-produced five candidates across
UI-01/UI-03/NAV-02; Stage 2 kept two (the silent-omission prohibition and the
trigger-versus-link-convergence prohibition, both authored above, both descriptor-less). Three were
dropped as routine engineering — aesthetic consistency, token reuse and corner radius are all
ordinary styling discipline with no values content. Two kept = 2 authored + 0 flagged.

**The three Revision Considerations are not absorbed.** The UI-SPEC records path-as-title
duplication, the near-empty outline column at low heading counts, and the theme default ignoring
`prefers-color-scheme` as non-blocking observations the tester did not raise. **None of the three is
implemented by this plan.** They are not silently folded into the styling work, and this plan takes
no position on any of them; each still needs a taste decision before a plan picks it up. Recorded
here so a reader of the diff can confirm their absence was deliberate.

**Explicitly declined, with rationale:**
- **A computed nesting-depth check.** The contract's own checkable test walks a real DOM path and
  counts computed-style changes, which needs a browser this environment does not have. The
  stylesheet-source assertions in Task 1 are a proxy that proves the four named selectors no longer
  declare box properties; they cannot prove the count along an arbitrary path. The residual claim is
  authored as a `backstop` truth and abstains to the gate rather than being recorded as passed.
- **A contrast-ratio or headless-rendering tool.** Adding one would introduce a dependency and a new
  test runtime into a gap-closure run, against the deliberate v1 stack decision recorded in
  `02-UAT.md`. Recorded as the natural follow-up if visual regressions recur.

**Capability gates:**
- API coverage (`ai-integration`): satisfied by the existing reasoned declaration in
  `.planning/phases/02-situational-awareness-artifact-reading/COVERAGE.md`. No matrix invented.
- Assumption-delta: the detector returned `detected: false` for phase 02. Not raised.
- Package legitimacy: no install task exists in this plan; see `T-02-SC`.

<output>
Create `.planning/phases/02-situational-awareness-artifact-reading/02-12-SUMMARY.md` when done.
</output>
</content>
