---
phase: 01-read-layer-domain-model
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - fixtures/README.md
  - fixtures/sparse-started/.planning/PROJECT.md
  - fixtures/sparse-started/.planning/REQUIREMENTS.md
  - fixtures/sparse-started/.planning/ROADMAP.md
  - fixtures/sparse-started/.planning/STATE.md
  - fixtures/sparse-started/.planning/config.json
  - fixtures/sparse-started/.planning/phases/01-first-slice/01-CONTEXT.md
  - fixtures/sparse-started/.planning/phases/01-first-slice/01-01-PLAN.md
  - fixtures/dense/.planning/PROJECT.md
  - fixtures/dense/.planning/ROADMAP.md
  - fixtures/dense/.planning/REQUIREMENTS.md
  - fixtures/dense/.planning/STATE.md
  - fixtures/dense/.planning/MILESTONES.md
  - fixtures/dense/.planning/RETROSPECTIVE.md
  - fixtures/dense/.planning/BACKLOG.md
  - fixtures/dense/.planning/LEARNINGS.md
  - fixtures/dense/.planning/WINDOWS.md
  - fixtures/dense/.planning/config.json
  - fixtures/dense/.planning/HANDOFF.json
  - fixtures/dense/.planning/estimation-calibration.json
  - fixtures/dense/.planning/v3.0-CAPACITY-PLAN.md
  - fixtures/dense/.planning/phases/**
  - fixtures/dense/.planning/quick/**
  - fixtures/dense/.planning/milestones/**
  - fixtures/dense/.planning/research/**
  - fixtures/dense/.planning/ui-reviews/.gitignore
autonomous: true
requirements: [DATA-06, DATA-03]

estimate:
  tokens: 72000
  raw_tokens: 72000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Three fixture trees exist under `fixtures/` — `sparse-empty`, `sparse-started`, and `dense` — and every one of them loads to `loadStatus.status: ok` through the harness."
    - "`sparse-started` contains one phase and a `ROADMAP.md` and has no `milestones/` directory, proving the parser survives structure without history."
    - "The dense fixture contains three distinct milestones, and an archived phase directory whose phase number collides with a live phase directory's number, so milestone-qualified phase identity is exercised at N greater than 2."
    - "The dense fixture contains artifact types studio-portal has never produced — at minimum `SPEC.md`, `AI-SPEC.md`, and at least one invented future document type — and every one of them appears in the snapshot rather than being dropped."
    - "The dense fixture's `config.json` carries keys that appear in no GSD reference document, and those keys survive the load into the snapshot unchanged."
    - "The dense fixture contains three deliberately corrupted files sitting among healthy ones — a tab-broken YAML block in a `PLAN.md`, a trailing comma in `HANDOFF.json`, and a file with frontmatter and no body — and each degrades to its raw content plus a recorded warning while every surrounding file parses cleanly."
    - "Every structural claim any fixture file makes about GSD's shape is traceable to a named line in `~/.claude/gsd-core/templates/` or to a named `gsd-core/bin/lib/*.cjs` source, recorded in `fixtures/README.md`; no fixture shape is justified by what happens to sit in `~/studio-portal`."
    - "No committed test asserts anything about `~/studio-portal`."
  artifacts:
    - path: "fixtures/README.md"
      provides: "Traceability ledger mapping every fixture file to the gsd-core template or source line that justifies its shape (D-01)"
      contains: "gsd-core/templates"
      min_lines: 60
    - path: "fixtures/sparse-started/.planning/ROADMAP.md"
      provides: "Structure-without-history case: one phase, no milestones"
    - path: "fixtures/dense/.planning/config.json"
      provides: "Open-map round-trip case — carries keys absent from every GSD reference doc"
    - path: "fixtures/dense/.planning/HANDOFF.json"
      provides: "Deliberately corrupted JSON (trailing comma) sitting among healthy files (D-02)"
    - path: "fixtures/dense/.planning/MILESTONES.md"
      provides: "Three-milestone history so multi-milestone logic is not validated only at N=2"
  key_links:
    - from: "fixtures/dense/.planning/phases"
      to: "fixtures/dense/.planning/milestones"
      via: "an archived phase directory reuses a phase number that a live phase directory also uses — the identity-collision case"
      pattern: "milestones/v[0-9]+\\.[0-9]+-phases"
    - from: "fixtures/README.md"
      to: "~/.claude/gsd-core/templates/"
      via: "every fixture row cites the template file (and where useful the line) its shape came from"
      pattern: "templates/"
---

<objective>
Build the adversarial fixture corpus this phase's correctness claims — and all of Phase 4's
degradation claims — rest on. Two trees are authored here: `sparse-started` (structure but no
history) and `dense` (three milestones, artifact types studio-portal has never produced, unknown
config keys, an archived/live phase-number collision, and three deliberately corrupted files sitting
among healthy ones). A traceability ledger records, for every fixture file, the `gsd-core` template
or source line that justifies its shape.

Purpose: the highest-severity risk named in this project's own research is a parser overfit to
`~/studio-portal`'s shape. The only real defense is a machine-checked contract derived entirely from
GSD's published templates. These trees are that contract.

Output: `fixtures/sparse-started/`, `fixtures/dense/`, `fixtures/README.md`.

Implements: DATA-06, DATA-03; user decisions D-01, D-02, D-03, D-04.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-read-layer-domain-model/01-CONTEXT.md
@.planning/research/GSD-DOMAIN.md
@.planning/research/PITFALLS.md
@.planning/phases/01-read-layer-domain-model/01-01-SUMMARY.md
</context>

<flagged_assumptions>
- **DATA-06 — the edge probe returned `unclassified`**, so no edge predicate could be derived for
  it. Assumption this plan proceeds on: "two synthetic test fixtures... and the dashboard renders
  both" is satisfied in this phase by *loading* the fixtures through the harness, since no rendering
  surface exists until Phase 2; the rendering half of DATA-06 is Phase 4's success criterion 1.
- **DATA-06 says "two" fixtures; this plan delivers three** (`sparse-empty` from plan `01-01`, plus
  `sparse-started` and `dense` here). This is user decision D-03, taken because the roadmap describes
  the sparse fixture two incompatible ways — Phase 1 SC2 says "fresh project, no `ROADMAP.md`, no
  `phases/`" while Phase 4 SC1 says "one phase, no milestones". Those are different trees exercising
  different code paths. Per D-03 this extension is flagged to the roadmap rather than silently
  satisfying DATA-06's letter with the wrong trees. **Action for the developer:** decide whether to
  amend DATA-06's wording from "two" to "three" in `.planning/REQUIREMENTS.md`, or to leave it and
  accept the third tree as an over-delivery. Nothing in this phase blocks on that decision.
</flagged_assumptions>

<tasks>

<task type="auto">
  <name>Task 1: `sparse-started` fixture — structure without history</name>
  <precondition>`~/.claude/gsd-core/templates/` is readable and contains `roadmap.md`, `state.md`, `project.md`, `requirements.md`, `context.md`, and `config.json` — this directory is the contract every fixture line must trace to (D-01); if it is absent, fixture construction cannot proceed correctly.</precondition>
  <read_first>
    - `~/.claude/gsd-core/templates/roadmap.md` — the phase-block grammar: the `### Phase N: {Name}` heading, and the `**Goal**`, `**Depends on**`, `**Requirements**`, `**Success Criteria**`, `**Plans**` lines
    - `~/.claude/gsd-core/templates/state.md` — the frontmatter contract and the fixed body section order
    - `~/.claude/gsd-core/templates/project.md`, `~/.claude/gsd-core/templates/requirements.md`, `~/.claude/gsd-core/templates/context.md`
    - `~/.claude/gsd-core/templates/config.json` — the shipped example (note: `GSD-DOMAIN.md` records this as a stale, differently-nested example; use it for shape only)
    - `.planning/research/GSD-DOMAIN.md` — § "Naming Conventions and Parsing Rules" (the phase directory and plan/artifact filename regexes) and § "ROADMAP.md — the richest hand-authored structure"
    - `fixtures/sparse-empty/.planning/STATE.md` — the earliest-shape STATE authored in plan `01-01`; `sparse-started` is one step further along and must stay consistent with it
  </read_first>
  <files>fixtures/sparse-started/.planning/PROJECT.md, fixtures/sparse-started/.planning/REQUIREMENTS.md, fixtures/sparse-started/.planning/ROADMAP.md, fixtures/sparse-started/.planning/STATE.md, fixtures/sparse-started/.planning/config.json, fixtures/sparse-started/.planning/phases/01-first-slice/01-CONTEXT.md, fixtures/sparse-started/.planning/phases/01-first-slice/01-01-PLAN.md</files>
  <action>
Author the second sparse variant per D-03: a project that has structure but no history — the shape
any real project has in its first weeks. It has exactly one phase, a `ROADMAP.md`, and **no**
`milestones/` directory, **no** `quick/` directory, **no** `research/` directory, and no shipped
milestone. This is deliberately a different tree from `sparse-empty`, which has no `ROADMAP.md` and
no `phases/` at all.

`ROADMAP.md` follows `templates/roadmap.md`'s single-milestone (non-milestone-grouped) form: an
`## Overview` paragraph, a `## Phases` checkbox list with exactly one unchecked entry, a
`## Phase Details` section containing one `### Phase 1: First Slice` block carrying `**Goal**`,
`**Depends on**` (the literal text for a first phase), `**Requirements**` naming two requirement IDs
that exist in the fixture's own `REQUIREMENTS.md`, a numbered `**Success Criteria**` list, and
`**Plans**: 1 plan`, followed by a `## Progress` table. Include the free-form ASCII dependency-shape
block from the template verbatim in the overview — it is carried through as opaque text, never
structurally parsed, and the fixture must contain one so that claim is testable.

`STATE.md` carries frontmatter one stage past `sparse-empty`: the three template-guaranteed keys plus
the conventional-but-not-guaranteed `milestone`, `current_phase`, `current_phase_name`, and
`last_activity`. It carries **no** `phase_numbering` key, so the parser's default branch for that
field is exercised. Body sections follow the template's fixed order.

`REQUIREMENTS.md` follows `templates/requirements.md`: a `## v1 Requirements` section with one
category holding at least three unchecked requirement IDs matching the
`[A-Z][A-Z0-9]*-\d{2,}` grammar, an `## Out of Scope` table, and a `## Traceability` table mapping
each ID to Phase 1. Include exactly one requirement ID in the `**Requirements**` line of `ROADMAP.md`
that does **not** appear in `REQUIREMENTS.md` — a dangling reference, which per D-10 is normal-path
data and must resolve to a null target without producing a warning. Note this deliberate dangler in
the traceability ledger so a later reader does not "fix" it.

`config.json` carries a minimal, valid, well-formed object: `mode`, `granularity`, and a `workflow`
object with two boolean keys. Keep it small; the open-map stress case belongs in the dense fixture.

The phase directory is `phases/01-first-slice/` holding `01-CONTEXT.md` (following
`templates/context.md`'s six-tag grammar, with no YAML frontmatter — that absence is itself the point,
since it is what makes filename dispatch necessary) and `01-01-PLAN.md` (frontmatter carrying
`phase`, `plan`, `type`, `wave`, `depends_on`, `files_modified`, `autonomous`, `requirements`, and a
`must_haves` block with `truths`, `artifacts`, `key_links`). There is deliberately **no**
`01-01-SUMMARY.md`: this phase is planned but unexecuted, which is what makes the fixture prove that
`roadmapComplete` and observed disk state can legitimately disagree.

Body prose can be one to three lines per section. Structure — frontmatter keys, heading text, the
pseudo-XML tag names, the checkbox syntax — must match the templates exactly; prose volume does not
affect what the parser exercises.
  </action>
  <acceptance_criteria>
    - `npm run snapshot -- fixtures/sparse-started --stable` exits 0 and its stdout JSON contains `"status": "ok"`
    - `test ! -d fixtures/sparse-started/.planning/milestones` and `test ! -d fixtures/sparse-started/.planning/quick` and `test ! -d fixtures/sparse-started/.planning/research` all succeed
    - `test -f fixtures/sparse-started/.planning/ROADMAP.md` succeeds and `test ! -f fixtures/sparse-started/.planning/phases/01-first-slice/01-01-SUMMARY.md` succeeds
    - `grep -c '^### Phase 1:' fixtures/sparse-started/.planning/ROADMAP.md` is 1
    - `grep -Ec '^\*\*(Goal|Depends on|Requirements|Success Criteria|Plans)\*\*' fixtures/sparse-started/.planning/ROADMAP.md` is at least 5
    - `python3 -c "import json,sys; json.load(open('fixtures/sparse-started/.planning/config.json'))"` exits 0
    - `head -1 fixtures/sparse-started/.planning/phases/01-first-slice/01-CONTEXT.md` does not begin with `---`
    - The `--stable` snapshot of `sparse-started` reports zero warnings (this tree is entirely healthy; only the dense tree carries deliberate corruption)
  </acceptance_criteria>
  <verify>
    <automated>npm run snapshot -- fixtures/sparse-started --stable | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const j=JSON.parse(s);if(j.loadStatus.status!=='ok')process.exit(1);if((j.warnings||[]).length!==0)process.exit(1)})"</automated>
  </verify>
  <done>A one-phase, no-history project tree exists, loads cleanly with zero warnings, contains a
  deliberate dangling requirement reference, and has a planned-but-unexecuted phase so formal and
  observed completion can disagree.</done>
</task>

<task type="auto">
  <name>Task 2: `dense` fixture — three milestones, unfamiliar artifact types, open-map config</name>
  <precondition>`~/.claude/gsd-core/templates/` is readable and contains `SPEC.md` (or `spec.md`), `AI-SPEC.md`, `SECURITY.md`, `UI-SPEC.md`, `VALIDATION.md`, `UAT.md`, `summary.md`, `milestone.md`, `retrospective.md`, and `research.md`; each dense-fixture artifact type must be authored from its template rather than invented (D-01).</precondition>
  <reversibility rating="costly">Per CONTEXT.md D-01 this fixture becomes Phase 4's adversarial substrate — Phase 4 success criterion 1 names it directly. Reshaping it later invalidates every committed golden file and every Phase 4 degradation claim built on top of it.</reversibility>
  <read_first>
    - `~/.claude/gsd-core/templates/README.md` — the canonical artifact registry (note `GSD-DOMAIN.md` records it as stale on the root file set; use it for producer/purpose mapping, not completeness)
    - `~/.claude/gsd-core/bin/lib/artifacts.cjs` — `CANONICAL_EXACT` and `CANONICAL_PATTERNS`; this is the only ground truth that cannot drift from runtime behavior, and the root file set must be authored against it
    - `~/.claude/gsd-core/bin/lib/phase-id.cjs` — `PHASE_NUMBER_TOKEN_SOURCE`, the phase-number token grammar the directory names must satisfy
    - `~/.claude/gsd-core/bin/lib/init.cjs` — `cmdInitQuick`, the literal generation code for `quick/<YYMMDD>-<base36>-<slug>` directory names
    - `~/.claude/gsd-core/templates/` — `spec.md`, `AI-SPEC.md`, `SECURITY.md`, `UI-SPEC.md`, `VALIDATION.md`, `UAT.md`, `summary.md`, `milestone.md`, `milestone-archive.md`, `retrospective.md`, `research.md`, `roadmap.md`, `state.md`
    - `.planning/research/GSD-DOMAIN.md` — § "The `.planning/` File Inventory", § "Naming Conventions and Parsing Rules", § "Structured Data Schemas" (STATE frontmatter, the `config.json` key inventory, `HANDOFF.json`, `estimation-calibration.json`, `WINDOWS.md`), § "Per-phase-artifact YAML frontmatter"
  </read_first>
  <files>fixtures/dense/.planning/PROJECT.md, fixtures/dense/.planning/ROADMAP.md, fixtures/dense/.planning/REQUIREMENTS.md, fixtures/dense/.planning/STATE.md, fixtures/dense/.planning/MILESTONES.md, fixtures/dense/.planning/RETROSPECTIVE.md, fixtures/dense/.planning/BACKLOG.md, fixtures/dense/.planning/LEARNINGS.md, fixtures/dense/.planning/WINDOWS.md, fixtures/dense/.planning/config.json, fixtures/dense/.planning/estimation-calibration.json, fixtures/dense/.planning/v3.0-CAPACITY-PLAN.md, fixtures/dense/.planning/phases/**, fixtures/dense/.planning/quick/**, fixtures/dense/.planning/milestones/**, fixtures/dense/.planning/research/**, fixtures/dense/.planning/ui-reviews/.gitignore</files>
  <action>
Author the healthy majority of the dense fixture. Every structural decision below must be justified
by a template or a `gsd-core/bin/lib/*.cjs` line, never by what sits in `~/studio-portal` — that is
D-01, and it is the mitigation for the highest-severity risk in this project.

**Three milestones (D-01 — do not validate multi-milestone logic only at N=2).** The active milestone
is `v3.0`. Two are archived: `v1.0` and `v2.0`. `MILESTONES.md` lists all three with their shipped
dates and archive links. `STATE.md` frontmatter carries `milestone: v3.0`, `milestone_name`, the three
template-guaranteed keys, and — this one matters —
`phase_numbering: restarts-per-milestone`, so the compound-identity branch is exercised.

**The phase-number collision.** Live `phases/` holds `01-identity-slice/` and `02-transport-layer/`.
Archived `milestones/v2.0-phases/` holds `01-legacy-ingest/` and `02-batch-export/`; archived
`milestones/v1.0-phases/` holds `01-bootstrap/`. Three different directories are therefore "Phase 1"
and two are "Phase 2" — the exact case that makes phase number alone an invalid key. Each archived
milestone also carries `milestones/vX.Y-ROADMAP.md`, `milestones/vX.Y-REQUIREMENTS.md`, and
`milestones/vX.Y-MILESTONE-AUDIT.md`, matching the `^v\d+\.\d+(?:\.\d+)?-.*\.md$` canonical pattern.

**Artifact types studio-portal has never produced (D-01).** Inside `phases/01-identity-slice/` place
`01-SPEC.md` and `01-AI-SPEC.md` (the latter deliberately carrying no YAML frontmatter — its structure
is numbered `##` sections, which is precisely why frontmatter presence cannot be the dispatch signal).
Add the invented future document type `01-COST-MODEL.md` — a name that appears in no GSD template and
no reference document, whose only correct handling is the generic markdown fallback. At the root, add
`v3.0-CAPACITY-PLAN.md`, a version-stamped document matching `CANONICAL_PATTERNS` but matching no
exact-name entry, so pattern-matched canonical detection is exercised separately from exact matching.

**Full phase artifact spread.** `phases/01-identity-slice/` additionally carries `01-01-PLAN.md`,
`01-01-SUMMARY.md`, `01-02-PLAN.md`, `01-02-SUMMARY.md`, `01-CONTEXT.md`, `01-RESEARCH.md`,
`01-SECURITY.md`, `01-UI-SPEC.md`, `01-VALIDATION.md`, `01-UAT.md`, `01-VERIFICATION.md`,
`01-LEARNINGS.md`, and `01-PATTERNS.md`. `phases/02-transport-layer/` is deliberately thinner —
`02-01-PLAN.md` and `02-CONTEXT.md` only, no summary — so a mid-flight phase is represented. Each
artifact's frontmatter must carry exactly the keys its template mandates (see `GSD-DOMAIN.md`
§ "Per-phase-artifact YAML frontmatter"), plus at least one key that appears in no template, so
pass-through of unrecognized frontmatter fields is exercised on a typed artifact rather than only on
`config.json`. The two `SUMMARY.md` files carry the full `coverage` array shape, including the
`D1`/`D2`-style `coverage[].id` values — deliberately colliding visually with `D-NN` decision IDs,
which is the namespace the mention scanner must keep separate in plan `01-04`.

**`quick/`.** Two quick-task directories whose names satisfy the six-digit date, three-character
base-36 time-token, and slug grammar generated by `init.cjs`, one carrying `{quickId}-PLAN.md` plus `{quickId}-SUMMARY.md` and one
carrying only `{quickId}-PLAN.md`. `STATE.md`'s body carries the corresponding
`### Quick Tasks Completed` table, which is the authoritative status index for these — the directory
listing is not.

**`research/` and the visible exclusion.** Author `research/SUMMARY.md`, `research/STACK.md`, and
`research/PITFALLS.md` following `templates/research-project/` shapes, plus
`research/.cache/deadbeef.json` holding a small JSON object. The cache file is the fixture that proves
the discovery walk's exclusion is recorded and visible rather than silent. Also create
`ui-reviews/.gitignore` — a directory whose only committed content is a gitignore, exactly the shape
that makes "present on a live filesystem but not in a git checkout" a real condition.

**Root structured files.** `config.json` must exercise DATA-03's open-map requirement hard: include a
representative set of real keys across the `workflow`, `git`, `features`, `hooks`, `learnings`,
`intel`, and `planning` namespaces; include `parallelization` in its **object** form (the key is
documented as polymorphic — boolean or object — and the object form is the one a naive typed parser
breaks on); and include at least three keys that appear in no GSD reference document at all, one of
them nested two levels deep inside a namespace that itself is unknown. Those unknown keys are the
DATA-03 round-trip subjects. `estimation-calibration.json` follows its documented
`{schema_version, samples: [{estimateTokens, actualTokens}]}` shape. `WINDOWS.md` carries all three of
its encodings — YAML frontmatter with the count fields, the markdown table with the documented
columns, and the fenced JSON array repeating the same rows — with at least one row in each.
`LEARNINGS.md`, `BACKLOG.md`, `RETROSPECTIVE.md`, and `PROJECT.md` follow their templates.

**`ROADMAP.md`** uses the milestone-grouped form: a `## Milestones` section, `<details>`-wrapped
archived-milestone blocks whose `<summary>` text is the milestone header, and a `## Phase Details`
section with one block per live phase carrying the full `**Goal**` / `**Depends on**` /
`**Requirements**` / `**Success Criteria**` / `**Plans**` field set and a plan checklist with mixed
checked and unchecked entries. Include at least one phase whose `**Requirements**` line uses the
bracketed form and one that does not, since the template comment marks brackets optional. Leave one
phase checkbox unchecked whose plans all have committed summaries — the legitimate disagreement
between formal and observed completion that plan `01-03` must keep as two separate fields.

Body prose stays minimal — one to three lines per section — except for `01-01-PLAN.md`, which must
carry a realistic specimen of the plan pseudo-XML structure: an objective block, at least two typed
task blocks each with a type attribute, and a decision block, with nested markdown (a list and a
fenced code block) inside the tags. Phase 2's renderer will reuse this exact file, so the tag nesting
has to be genuine rather than a one-line stub.

Do not author any corrupted file in this task — all three deliberate corruptions land in Task 3, so
that this task's output is independently verifiable as a clean load.
  </action>
  <acceptance_criteria>
    - `npm run snapshot -- fixtures/dense --stable` exits 0 with `loadStatus.status` of `ok`
    - The `--stable` snapshot of the dense tree at the end of THIS task reports zero warnings (corruption arrives in Task 3)
    - `ls fixtures/dense/.planning/milestones | grep -c 'phases$'` is 2, and `ls fixtures/dense/.planning/phases | wc -l` is 2
    - `test -d fixtures/dense/.planning/phases/01-identity-slice` and `test -d fixtures/dense/.planning/milestones/v2.0-phases/01-legacy-ingest` and `test -d fixtures/dense/.planning/milestones/v1.0-phases/01-bootstrap` all succeed — three directories carrying phase number 01
    - `grep -c 'phase_numbering: restarts-per-milestone' fixtures/dense/.planning/STATE.md` is 1
    - `test -f fixtures/dense/.planning/phases/01-identity-slice/01-SPEC.md` and `test -f fixtures/dense/.planning/phases/01-identity-slice/01-AI-SPEC.md` and `test -f fixtures/dense/.planning/phases/01-identity-slice/01-COST-MODEL.md` and `test -f fixtures/dense/.planning/v3.0-CAPACITY-PLAN.md` all succeed
    - `head -1 fixtures/dense/.planning/phases/01-identity-slice/01-AI-SPEC.md` does not begin with `---`
    - `python3 -c "import json;d=json.load(open('fixtures/dense/.planning/config.json'));assert isinstance(d['parallelization'],dict)"` exits 0
    - `test -f fixtures/dense/.planning/research/.cache/deadbeef.json` succeeds
    - `ls fixtures/dense/.planning/quick | grep -Ec '^[0-9]{6}-[0-9a-z]{3}-'` is 2
    - `grep -c '```json' fixtures/dense/.planning/WINDOWS.md` is at least 1 and `head -1 fixtures/dense/.planning/WINDOWS.md` is `---`
    - `grep -Ec '^\s+- id: D[0-9]' fixtures/dense/.planning/phases/01-identity-slice/01-01-SUMMARY.md` is at least 2
    - The snapshot JSON contains an artifact entry for `01-COST-MODEL.md` with `"kind": "unknown"`
  </acceptance_criteria>
  <verify>
    <automated>npm run snapshot -- fixtures/dense --stable | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const j=JSON.parse(s);if(j.loadStatus.status!=='ok')process.exit(1);if((j.warnings||[]).length!==0)process.exit(1);const t=JSON.stringify(j);for(const f of ['01-COST-MODEL.md','01-AI-SPEC.md','01-SPEC.md','v3.0-CAPACITY-PLAN.md'])if(!t.includes(f))process.exit(1)})"</automated>
  </verify>
  <done>A three-milestone project tree with colliding phase numbers, unfamiliar and invented artifact
  types, an open-map config carrying keys no reference document knows, and a full spread of GSD
  artifact types loads cleanly with zero warnings — every shape traceable to a template or a
  `gsd-core` source file.</done>
</task>

<task type="auto">
  <name>Task 3: Deliberate corruption inside the dense tree, and the traceability ledger</name>
  <reversibility rating="costly">D-02 makes the dense fixture's golden snapshot permanently non-empty in its warnings channel; every later assertion about warnings is written against that fact, so relocating the corrupted files afterwards invalidates them.</reversibility>
  <read_first>
    - `fixtures/dense/.planning/` — the healthy tree from Task 2; the corrupted files must sit among these, not beside them
    - `src/planning-repo/frontmatter.ts` and `src/planning-repo/types.ts` — the guarded parse functions and the `ParseWarning` shape whose `stage` and `salvage` fields the corruptions must exercise
    - `.planning/research/PITFALLS.md` — Pitfall 4, specifically the frontmatter/body independence claim the `stage` field encodes
    - `.planning/phases/01-read-layer-domain-model/01-CONTEXT.md` — the `<decisions>` block, for the exact wording of D-01 through D-04
  </read_first>
  <files>fixtures/dense/.planning/phases/02-transport-layer/02-01-PLAN.md, fixtures/dense/.planning/HANDOFF.json, fixtures/dense/.planning/phases/01-identity-slice/01-VERIFICATION.md, fixtures/README.md</files>
  <action>
Introduce exactly the three corruptions D-02 names, each sitting among healthy files rather than
isolated, because isolation is only meaningfully proven when there is a healthy majority around the
broken file.

**Corruption 1 — tab-broken YAML.** In `fixtures/dense/.planning/phases/02-transport-layer/02-01-PLAN.md`,
indent one nested key inside the frontmatter block with a literal tab character instead of spaces.
YAML forbids tabs as indentation, so the parse throws and the guarded call degrades to the raw body
with a stage `frontmatter` warning. The rest of the file — the full body with its task blocks — must
be intact and substantial, because the claim being proven is that the body survives when the
frontmatter does not. Add a note in `fixtures/README.md` stating the tab is deliberate, since an
editor or formatter will otherwise silently convert it.

**Corruption 2 — trailing comma in JSON.** Author `fixtures/dense/.planning/HANDOFF.json` following
the documented paused-phase field set (`version`, `timestamp`, `phase`, `phase_name`, `phase_dir`,
`plan`, `task`, `total_tasks`, `status`, `completed_tasks`, `remaining_tasks`, `blockers`,
`async_jobs`, `human_actions_pending`, `decisions`, `uncommitted_files`, `next_action`,
`context_notes`) and then leave a trailing comma after the final property. It must be a genuinely
plausible file that fails only on that one character, so the degradation is realistic rather than
degenerate. The expected outcome is a stage `structured-extraction` warning with the raw text
retained.

**Corruption 3 — frontmatter with no body.** Rewrite
`fixtures/dense/.planning/phases/01-identity-slice/01-VERIFICATION.md` so it contains a complete,
valid frontmatter block (the documented `phase`, `verified`, `status`, `score`,
`behavior_unverified` keys) followed by nothing at all — no heading, no prose, not even a blank
line's worth of content. This is the inverse case of Corruption 1: the structured half survives and
the prose half is empty. It must **not** produce a warning at all, because an empty body is a
legitimate document state, not a parse failure — and proving the tool distinguishes "empty" from
"broken" is exactly why this file is in the set.

**The traceability ledger — `fixtures/README.md`.** This is the artifact that makes D-01 a build rule
rather than a review note. Write it as a markdown document with one section per fixture tree and, in
each, a table with the columns `file`, `shape traced to`, and `what it proves`. The `shape traced to`
cell must name a specific source — `templates/roadmap.md § Phase Details`, `bin/lib/artifacts.cjs
CANONICAL_EXACT`, `bin/lib/phase-id.cjs PHASE_NUMBER_TOKEN_SOURCE`, `bin/lib/init.cjs cmdInitQuick`,
and so on — never a real project. Cover all three trees, including `sparse-empty` from plan `01-01`.

Open `fixtures/README.md` with a short preamble stating four things a later reader needs and cannot
infer: that these trees are hand-authored synthetic and derived from `gsd-core` templates rather than
from any real project; that the reference project named in CONTEXT.md decision D-04 is a manual smoke
target only and that no committed test may assert anything about it — name that project's path
explicitly in the README, quoting D-04; that the dense tree's golden snapshot permanently contains warnings,
so a zero-warning assertion against it is always wrong (D-02); and that `sparse-started`'s
`ROADMAP.md` carries one deliberately dangling requirement reference which is normal-path data rather
than a defect (D-10). Add a closing section listing each deliberate defect with its file, its exact
nature, and the warning stage it is expected to produce, so a future reader cannot mistake any of
them for an accident and repair it.
  </action>
  <acceptance_criteria>
    - `npm run snapshot -- fixtures/dense --stable` still exits 0 with `loadStatus.status` of `ok` — a corrupted file degrades, it does not fail the load
    - The dense `--stable` snapshot now reports at least one warning with `"stage": "frontmatter"` naming the `02-01-PLAN.md` path, and at least one with `"stage": "structured-extraction"` naming `HANDOFF.json` (assert presence by stage and path, never an exact total — plan `01-03` adds typed handlers that may legitimately add stages for the same two files)
    - No warning at any stage names `01-VERIFICATION.md` — an empty body is a legitimate state, not a parse failure
    - No warning names any file other than `02-01-PLAN.md` and `HANDOFF.json` — the failures are isolated to the two deliberately corrupted files
    - The dense snapshot still contains parsed frontmatter for `01-01-PLAN.md` and for every other healthy `PLAN.md`, proving the failure is isolated to the one broken file
    - `npm run snapshot -- fixtures/dense --stable --with-bodies` shows a non-empty `body` for `02-01-PLAN.md`, proving the raw body survived its frontmatter failure
    - `grep -Pc '^\t' fixtures/dense/.planning/phases/02-transport-layer/02-01-PLAN.md` is at least 1
    - `python3 -c "import json;json.load(open('fixtures/dense/.planning/HANDOFF.json'))"` exits nonzero — the corruption is real, not merely described
    - `fixtures/README.md` exists, is at least 60 lines, contains a row for every file in all three fixture trees, and every `shape traced to` cell names a `templates/` or `bin/lib/` source
    - `! grep -rq "studio-portal" test/ src/` — no committed source or test references the smoke target (D-04)
  </acceptance_criteria>
  <verify>
    <automated>npm run snapshot -- fixtures/dense --stable | node -e "let s='';process.stdin.on('data',d=>s+=d).on('end',()=>{const j=JSON.parse(s);if(j.loadStatus.status!=='ok')process.exit(1);const w=j.warnings||[];if(!w.some(x=>x.stage==='frontmatter'&amp;&amp;x.path.includes('02-01-PLAN.md')))process.exit(1);if(!w.some(x=>x.stage==='structured-extraction'&amp;&amp;x.path.includes('HANDOFF.json')))process.exit(1);if(w.some(x=>!x.path.includes('02-01-PLAN.md')&amp;&amp;!x.path.includes('HANDOFF.json')))process.exit(1)})"</automated>
  </verify>
  <done>Three deliberate defects sit inside a healthy dense tree; the load still succeeds; each
  broken file degrades to its raw content with a correctly-staged warning while every surrounding
  file parses cleanly; an empty body is correctly not treated as a failure; and a ledger records the
  template or source line behind every fixture file so no parser assumption can trace back to a real
  project.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
All files created by this plan are new; none exist in the repository beforehand.

**Fixture trees:** `fixtures/sparse-started/.planning/` (7 files, one phase, no history) and
`fixtures/dense/.planning/` (three milestones, two live phase directories, three archived phase
directories, two quick-task directories, a `research/` tree with an excluded `.cache/`, a
`ui-reviews/` directory, and the full root structured-file set).

**New document names introduced by this plan** (none is an existing GSD artifact type; the invented
one is deliberate per D-01): `01-COST-MODEL.md`, `v3.0-CAPACITY-PLAN.md`.

**New non-fixture file:** `fixtures/README.md` — the D-01 traceability ledger and the deliberate-defect
register.

No source symbols, functions, classes, types, or CLI flags are created or modified by this plan.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Fixture files → the parser under test | These files are deliberately malformed; they are the untrusted-shape input the degradation contract is proven against. |
| `gsd-core` templates → fixture content | Read-only reference material outside this repository; nothing is written back to it. |
| Fixture tree → the git working tree | Committed synthetic files become permanent test substrate; anything unsafe committed here is unsafe forever. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-08 | Tampering | A committed fixture symlink pointing outside the repository could make a later test read arbitrary host files | high | mitigate | No fixture file in this plan is a symlink; the containment-escape case is constructed at runtime in a temp directory by plan `01-01` Task 2, never committed. Verified by `find fixtures -type l` returning nothing. |
| T-01-02 | Denial of Service | The three deliberate corruptions are exactly the crash-the-loader inputs Pitfall 4 names | medium | mitigate | Guarded parse plus per-file isolation, both built in plan `01-01`; this plan's acceptance criteria assert the load still returns `ok` with the failure scoped to the one file |
| T-01-09 | Information Disclosure | Synthetic fixture prose could inadvertently carry a real credential, token, or private path copied from a reference project | medium | mitigate | Fixture prose is authored from `gsd-core` templates and kept to one to three lines per section; no content is copied from `~/studio-portal` (D-01/D-04), and `! grep -rq "studio-portal" test/ src/` is an acceptance criterion |
| T-01-10 | Tampering | The dense `config.json` carries deliberately unknown keys, which is the prototype-pollution reachability surface DATA-03 opens | high | mitigate | The open-map JSON reader strips `__proto__`, `constructor`, and `prototype` at every depth (plan `01-01`, threat `T-01-03`); this plan does not author those key names into the fixture, so the guard is proven separately in plan `01-04` with an in-memory hostile case rather than by committing one |
</threat_model>

<verification>
1. All three fixture trees load to `loadStatus.status: ok` through `npm run snapshot`.
2. `sparse-started` reports zero warnings; every warning the `dense` tree reports names either
   `02-01-PLAN.md` or `HANDOFF.json`, and at least one warning exists at stage `frontmatter` and at
   least one at stage `structured-extraction`. Warning totals are never asserted against the dense
   tree — D-02 makes its warning channel permanently non-empty and plan `01-03` may add stages.
3. Three separate directories in the dense tree carry phase number `01`, spanning the live milestone
   and both archived ones.
4. `SPEC.md`, `AI-SPEC.md`, `01-COST-MODEL.md`, and `v3.0-CAPACITY-PLAN.md` all appear in the dense
   snapshot; the invented type arrives as `kind: 'unknown'`.
5. `fixtures/README.md` cites a `templates/` or `bin/lib/` source for every fixture file across all
   three trees, and registers every deliberate defect.
6. `find fixtures -type l` returns nothing and `grep -rq "studio-portal" test/ src/` finds nothing.
</verification>

<success_criteria>
- Three fixture trees exist and all three load successfully (D-03; DATA-06 extended and flagged).
- The dense tree's shape traces to `gsd-core` templates and sources, never to a real project (D-01).
- Deliberate corruption sits among healthy files and degrades in isolation (D-02).
- Unknown config keys and unknown artifact types survive the round trip (DATA-03).
- Multi-milestone handling is exercised at three milestones with colliding phase numbers.
- No committed assertion depends on `~/studio-portal` (D-04).
</success_criteria>

<output>
Create `.planning/phases/01-read-layer-domain-model/01-02-SUMMARY.md` when done.
</output>
