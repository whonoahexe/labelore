---
phase: quick-260910-0x4
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
gap_closure: false
requirements: [TD-01, TD-02, TD-03, TD-04, TD-05, TD-06, TD-07, TD-08, TD-09, TD-10, TD-11, TD-12, TD-13, TD-14]
files_modified:
  - .planning/WINDOWS.md
  - .planning/STATE.md
  - .planning/v1.0-MILESTONE-AUDIT.md
  - .planning/phases/03-search-browsing-traceability/deferred-items.md
  - .planning/phases/03-search-browsing-traceability/03-01-SUMMARY.md
  - .planning/phases/02-situational-awareness-artifact-reading/02-06-SUMMARY.md
  - .planning/phases/02-situational-awareness-artifact-reading/02-SECURITY.md
  - package.json
  - vite.config.ts
  - index.html
  - src/planning-repo/crossref.ts
  - src/planning-repo/assemble.ts
  - src/planning-repo/snapshot.ts
  - src/planning-repo/handlers/generic.ts
  - src/planning-fs/in-memory-fs.ts
  - src/web/styles/globals.css
  - src/web/pages/artifact-page.tsx
  - test/crossref.test.ts
  - test/fs-equivalence.test.ts
  - test/portability.test.ts
  - test/web/visual-contract.test.ts
  - test/web/refresh-contract.test.ts
  - test/web/mermaid-theme.test.ts

estimate:
  tokens: 190000
  raw_tokens: 95000
  tasks: 7
  confidence: low

must_haves:
  truths:
    - "WINDOWS.md reports open_count 0, and its markdown table and trailing JSON block agree on the status of every id."
    - "A malformed non-array depends_on records a warning in the snapshot warning channel, while a well-formed but unresolvable reference (SLICE-99) still records none, and load() still never throws."
    - "No file under src/ other than src/planning-fs/local-fs.ts imports the Node filesystem module in any spelling, and a planted violation makes the gate fail."
    - "npm run build exits 0 and emits no chunk-size advisory line, without any log-suppression flag or output redirection."
    - "Every uppercase micro-label in the .artifact-metadata summary family computes at or above 10px in both themes, with contrast no worse than 4.74 light / 6.99 dark."
    - "Mermaid diagrams take node, cluster, edge, note and text colours from app CSS custom properties through toMermaidColor(), with securityLevel strict, a dynamic import, and all three fallback rungs intact."
    - "Keyboard reachability and operability of the Refresh control is MEASURED at 320/700/992px in both themes, and the measured result — pass or fail — is what gets recorded."
    - "v1.0-MILESTONE-AUDIT.md's tech_debt record matches the true end state, with every item that did not close still listed with its reason."
  artifacts:
    - ".planning/WINDOWS.md with open_count 0"
    - "package.json with an engines field derived from measured toolchain floors"
    - "vite.config.ts or an equivalent split producing a warning-free build"
    - ".planning/v1.0-MILESTONE-AUDIT.md rewritten tech_debt section"
  key_links:
    - "WarningCollector -> assembleDomainModel -> resolveCrossReferences (the new warning must reach snapshot.warnings)"
    - "toMermaidColor() stays the single oklch->sRGB conversion seam for every mermaid themeVariable"
    - "STATE.md Deferred Items rows are cleared only by the actual landing of Task 3's items 8 and 9"
---

<objective>
Close the fourteen tech-debt items recorded in `.planning/v1.0-MILESTONE-AUDIT.md` so a re-audit
reads zero open debt — or, for any item that does not genuinely close, leave it listed with an
honest reason and do not flip the audit status.

Purpose: the v1.0 milestone is functionally done. What remains is bookkeeping, dead code, three
deferred code-quality items, one unmeasured accessibility claim, one unmeasured keyboard claim, and
one un-reconciled accepted risk. Carrying them into v1.1 costs more than closing them now.

Output: closed ledger entries, four source fixes, one config fix, one measured verification, one
risk reconciliation, and a truthful rewritten audit record.

**No tracer task.** Tracer-first exists to prove an unproven architecture end-to-end on early
context. This plan's architecture is already shipped and audited (v1.0, 45/45 requirements, 555
tests). The fourteen items are independent remediations across disjoint surfaces with no shared
vertical path; a thin slice through them would prove nothing. Tasks are therefore grouped by shared
file surface and shared verification, and ordered so dependencies hold.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/STATE.md
@.planning/WINDOWS.md
@CLAUDE.md
</context>

<measured_baseline>
Every figure below was measured live at planning time, on the working tree at `bcc9ce1` with only
untracked `.gsd/` and `.planning/quick*` present. **These are the authorized starting points.**
Any figure quoted from `v1.0-MILESTONE-AUDIT.md` that is NOT restated here is a prior measurement
and is an investigation lead only — never edit authority. Re-measure before acting; if a number
below has moved, the tree changed and your scope must follow the tree, not this block.

| Fact | Measured value |
|---|---|
| `npm test` | 555 passed, 39 files, 1.82s |
| `npm run typecheck` | exit 0 |
| `npm run lint` | exit 0, silent |
| `npm run build` | exit 0, 4261 modules, one chunk-size advisory |
| Chunks over 500 kB | exactly two: `dist/assets/index-*.js` **571.58 kB** (gzip 180.76) and `dist/assets/chunk-FOHPRMQF-*.js` **662.12 kB** (gzip 143.15) |
| Next largest (all under limit) | `cytoscape.esm` 434.92, `katex` 258.69, `chunk-DU6HZSFF` 235.49, CSS 77.38 |
| Identity of the 662 kB chunk | mermaid's shared internals — imported by `mermaid.core`, `mermaid-parser.core`, and every diagram chunk (`pie`, `radar`, `railroad*`, `treemap`, `wardley`, `architecture`, `cynefin`, `eventmodeling`, `gitGraph`, `info`, `packet`, `treeView`). Already async-only. |
| `index.html` | 297 lines |
| `index.html` dead-by-grep selectors | `.position-card`, `.error-card`, `.card-heading`, `.progress-block`, `.progress-row`, `.progress-track`, `.progress-fill`, `.skeleton` — 0 hits in `src/web` |
| `index.html` live-or-ambiguous selectors | `.screen`, `.eyebrow`, `.lede`, `.status`, `.metadata`, `.muted` — non-zero hits, but the planning-time grep was **substring-fuzzy** (`.status` likely matched `snapshot-status`, `.metadata` likely matched `artifact-metadata`). Re-grep with word boundaries. |
| Root font-size | no `html`/`:root`/`body` `font-size` declaration exists anywhere in `globals.css` — the root is the browser default 16px, so `0.62rem` is 9.92px with **no em compounding** |
| `0.62rem` declarations in `globals.css` | lines 2250 (`.artifact-metadata > summary span`), 2983 (`.warning-fields dt`), 2998 (`.warning-fields-label`) |
| `0.6rem` font-size declarations (9.6px) | lines 528, 1133, 1298 |
| `fromDirectory` callers | exactly one: `test/fs-equivalence.test.ts:30` |
| `node:fs` importers under `src/` | exactly two: `src/planning-fs/local-fs.ts:4` and `src/planning-fs/in-memory-fs.ts:5`. Other hits under `src/` are prose in comments, not imports. |
| `tsconfig.server.json` | `include: ["src/**/*.ts", "test/**/*.ts", "*.config.ts"]`, `exclude: ["src/web/**", ...]` — so `test/helpers/*.ts` IS typechecked |
| `vitest.config.ts` | `include: ['test/**/*.test.ts']` — a non-`.test.ts` helper under `test/` is importable and will not be collected as a suite |
| `resolveCrossReferences` | signature `(project: Project): void` at `crossref.ts:68`; sole caller `assemble.ts:359`; `assembleDomainModel(parsed, _warnings, rootPath)` at `assemble.ts:169` **ignores its warnings param**; `snapshot.ts:76` passes `warnings.all()` and `snapshot.ts:87` re-reads the live collector |
| Fixtures with a non-array `depends_on` | **none** — therefore no golden should change for item 6 |
| `SLICE-99` dangling reference | `fixtures/sparse-started`, and `test/__golden__/sparse-started.json` records **0 warnings** — that golden already pins D-10's silence |
| Golden warning counts | `dense.json` 7, `sparse-empty.json` 0, `sparse-started.json` 0 |
| `test/web/refresh-contract.test.ts` dependency assertion | inspects `packageJson.dependencies` keys only — adding `engines` does NOT trip it |
| Playwright | `@playwright/test@1.62.1` global at `/home/cinedise/.npm-global/lib/node_modules`; browsers at `~/.cache/ms-playwright` (`chromium-1234`, `chromium_headless_shell-1234`) |
| `gsd-tools` | `$HOME/.claude/gsd-core/bin/gsd-tools.cjs`; `windows` subcommands: `status`, `append`, `waive`, `fixed` |
| `02-09-SUMMARY.md` | **does not exist**. `02-09-PLAN.md` carries `status: superseded`, `superseded_by: 02-13`. Its 2026-08-29 run was NOT approved. |
| `02-06-SUMMARY.md` coverage | D1 (automated gate, pass), D2 (host-browser UAT, pass, human_judgment), D3 (light/dark contrast pass, **status: unknown**, human_judgment) at line 86; plan `status: complete` at line 92 |
| `02-SECURITY.md` | `threats_open: 0`, `block_on: high`, `asvs_level: 1`; R-02-01 at line 67; audit-trail row at line 103 |
</measured_baseline>

<global_rules>
These bind every task. Violating any of them fails the plan regardless of item outcomes.

1. **Read-only guarantee is absolute.** Nothing in this plan may give Labelore the ability to write
   to a target `.planning/` directory. `capabilities = { watch: false, write: false }` stays.
2. **No new project dependency.** `package.json` `dependencies` and `devDependencies` gain nothing.
   `git diff -- package.json package-lock.json` must show only Task 2's `engines` addition (and, if
   npm mirrors it, the lock's root `engines` block — name that in the commit if it appears).
   Do not run `npm install`.
3. **Measure before changing anything scoped by an estimate.** The audit's "~150 of 297 lines",
   "9.92px" and ">500 kB" are prior measurements. Re-derive each from the live tree first. If your
   measurement disagrees with the audit, your measurement wins and you say so in the summary.
4. **Every behavioral change gets a regression test** pinning the new behavior.
5. **Commit atomically per item or tight group** so any single item can be reverted alone. A task
   below carries several commits; do not squash them.
6. **Never silently skip, never overclaim.** If an item is already closed, or is not closable as
   described, say so explicitly with evidence. A truthful "left open because X" is the better
   outcome, and Task 7 must then reflect it rather than flipping the audit status.
7. **End state:** `npm test`, `npm run typecheck`, `npm run lint`, `npm run build` all exit 0, with
   the build now also advisory-free. Test count starts at 555 and may only grow.
8. **Context boundary.** Seven tasks over fourteen items is a large single plan (see `estimate`).
   Each task commits independently and reads only its own file surface, so **the boundary between
   Task 3 and Task 4 is a clean resumption point** if context runs short. If you must stop, stop on
   a task boundary with everything committed, record which items closed in the outcome table, and
   say plainly that Tasks N..7 were not reached — do not compress later tasks to fit.
</global_rules>

<tasks>

<task type="auto">
  <name>Task 1: Ledger and planning-doc sync (audit items 1-2)</name>
  <files>.planning/WINDOWS.md, .planning/phases/03-search-browsing-traceability/deferred-items.md, .planning/phases/03-search-browsing-traceability/03-01-SUMMARY.md</files>
  <precondition>`$HOME/.claude/gsd-core/bin/gsd-tools.cjs` exists and `node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs" windows status` runs. If it does not, halt — do not hand-edit WINDOWS.md's table and JSON block, since keeping the two in sync is exactly what the tool is for.</precondition>
  <action>
Verify each open ledger entry against the tree BEFORE closing it. Closing an entry that is still
genuinely open is the failure mode this task exists to avoid.

Entry 3 (`no-regex-spaces` at `test/web/visual-contract.test.ts:438`): read the actual lines and
confirm the regex literals no longer contain consecutive literal spaces, then confirm `npm run lint`
exits 0 over the whole repo. Both were true at planning time; re-confirm on the live tree.

Entry 4 (human must confirm the unreadable disclosure and empty-document notice read as one
coherent explanation): confirm `04-UAT.md` records a `pass` result for test 1, and that the test
described there is actually this question.

Entry 5 (human must decide whether the 04-05 tone prohibition includes disclosure prose): confirm
`04-UAT.md` records a `pass` result for test 6, and that the test described there is actually this
question. Do not accept "there are nine passes in the file" as proof for either entry — match the
entry's question to the specific test's stated subject.

If any entry's evidence does not hold, leave that entry open and record why. Otherwise close each
with `gsd-tools windows fixed <id>`. Run `windows fixed --help` (or read the subcommand's argument
handling) first to learn whether it accepts a reason argument; if it does, pass a reason naming the
concrete evidence (the file and the verdict, not "verified"). If it does not, put that same evidence
in the commit message and check afterwards whether the tool left the reason column blank — a blank
reason column is acceptable only when the tool provides no way to fill it.

After closing, verify the two representations agree: the markdown table and the trailing JSON block
must record the same status and the same `resolved_at` for every id, and the frontmatter counters
(`open_count`, `fixed_count`, `total_count`) must match the rows. If the tool leaves them
inconsistent, that is a finding to report, not something to paper over by hand-editing one side.

Item 2 — the stale lint note. `deferred-items.md` is a live queue of open out-of-scope issues, so
remove the `03-01` lint entry from it outright; if that leaves the file with no entries, say so
explicitly in the file rather than leaving an empty section that reads as truncation.
`03-01-SUMMARY.md` is a historical account of what happened during that plan's execution, not a
queue — deleting the entry there would falsify the record. Annotate item 6 under "Out-of-Scope (Not
Fixed — Logged)" with a dated resolution line naming what closed it (the regexes now use a counted
quantifier; `npm run lint` exits 0; ledger entry 3 closed by this task). Keep the deviation count
line consistent with whatever you do.

Commit these as two commits: one for the ledger closures, one for the doc strike.
  </action>
  <verify>
    <automated>node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs" windows status && test "$(grep -c '| open |' .planning/WINDOWS.md)" = "0" && test "$(grep -c '"status": "open"' .planning/WINDOWS.md)" = "0" && grep -q '^open_count: 0' .planning/WINDOWS.md && ! grep -n 'no-regex-spaces' .planning/phases/03-search-browsing-traceability/deferred-items.md</automated>
  </verify>
  <done>`open_count` reads 0; no row in either the markdown table or the JSON block has status open; the two representations agree on every id's status and resolved_at; the stale lint entry is gone from the live queue and annotated as resolved in the historical summary. Any entry left open is named with its evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Non-web source and config debt (audit items 4, 5, 6, 7)</name>
  <files>package.json, src/planning-repo/handlers/generic.ts, src/planning-repo/crossref.ts, src/planning-repo/assemble.ts, src/planning-repo/snapshot.ts, src/planning-fs/in-memory-fs.ts, test/helpers/from-directory.ts, test/fs-equivalence.test.ts, test/crossref.test.ts, test/portability.test.ts</files>
  <behavior>
    - A plan whose `depends_on` frontmatter is present but not an array degrades to `[]` AND records exactly one warning in the snapshot warning channel naming the plan and the wrong-shaped field.
    - `load()` and `refresh()` still return rather than throw on that same input (D-12).
    - A plan whose `depends_on` is a well-formed array containing an id with no matching sibling records NO warning (D-10). The same silence holds for the `SLICE-99` requirement reference.
    - A `depends_on` key that is absent entirely records no warning. Decide the `depends_on:` -with-empty-value (YAML null) case explicitly and pin whichever way you decide.
    - `GenericMarkdownHandler` produces byte-identical titles to today for: frontmatter title present and non-empty, frontmatter title present but empty string, frontmatter title non-string, no frontmatter title with a `# ` heading, and neither (path fallback).
    - No file under `src/` except `src/planning-fs/local-fs.ts` imports the Node filesystem module under any spelling, and a deliberately planted import makes the gate fail.
  </behavior>
  <action>
Four commits, in this order.

**(a) Title dedupe — 01-REVIEW IN-01.** `src/planning-repo/handlers/generic.ts` declares a local
`deriveTitle(content, path)` whose body duplicates `handlers/title.ts`'s exported
`deriveTitleFromHeading`, and its inline frontmatter-title branch duplicates `title.ts`'s exported
`deriveTitle(frontmatterData, body, path)`. At planning time the two were observed equivalent —
same regex, same trim, same path fallback, same `typeof === 'string' && length > 0` guard. Re-read
both before collapsing. If they have since diverged in any respect, **report the divergence and do
not force them together** — a silent behavior change hiding inside a "dedupe" commit is worse than
the duplication. If equivalent, delete the local copy and call the shared `deriveTitle` directly.
Byte-identical output is the whole point: `test/__golden__` must pass with nothing regenerated.

**(b) `engines` — 01-REVIEW IN-02.** Derive the floor; do not assume it. Read the `engines` field of
each installed toolchain package that actually gates this project (`vite`, `vitest`, `eslint`,
`typescript`, `@vitejs/plugin-react`, `hono`, `@hono/node-server`, `react`, `tailwindcss`) out of
`node_modules/<pkg>/package.json`, and take the highest floor. Then account for a floor the
dependency graph does not declare: `npm run snapshot`, `npm run dev` and `npm run start` invoke
`node src/**/*.ts` directly, relying on Node's native TypeScript type-stripping being on without a
flag — that has its own minimum Node version, and if it is higher than every package floor then it
is the effective floor. The project floor cannot be below the highest of these. State each measured
floor and the resulting maximum in the commit message. Add no npm constraint unless something you
measured actually requires one. Edit `package.json` only; do not run an install.

**(c) `depends_on` warning.** The warning channel is `WarningCollector` (`planning-repo/warnings.ts`),
and today it does not reach the cross-reference pass: `assembleDomainModel` takes a
`_warnings: ParseWarning[]` it ignores, and `resolveCrossReferences(project)` takes no sink at all.
Thread the collector itself (not a flat array snapshot) from `snapshot.ts` through
`assembleDomainModel` into `resolveCrossReferences`, so a warning added during cross-referencing is
visible to `snapshot.ts`'s post-assembly read of the live collector. Decide and record whether the
new warning also belongs on the owning artifact's own `warnings` array or only in the flat snapshot
list — the per-artifact arrays may already have been materialized before assembly, in which case
flat-only is the honest answer and you say so rather than pretending otherwise.

Two invariants are non-negotiable and each needs its own test. **D-12:** `load()` must still not
throw on this input — the original fix existed precisely because this value crashed it, so a
regression here re-opens a closed critical. **D-10:** a well-formed but unresolvable reference must
still produce no warning; `test/__golden__/sparse-started.json` records 0 warnings while its fixture
carries the dangling `SLICE-99`, so that golden is itself a D-10 silence pin. Add an explicit test
pinning that silence anyway, so the invariant is stated and not merely implied by a fixture.

No fixture carries a non-array `depends_on`, so **no golden should change**. Verify that with
`git diff --exit-code -- test/__golden__`. If a golden does legitimately gain this warning, your
premise was wrong — update that one golden and name it explicitly in the commit and the summary,
with the fixture and the reason.

The commit message must state why this reverses the trade-off documented in `01-REVIEW-FIX.md` and
in `crossref.ts`'s own header comment: D-10's rule is about *well-formed references to things that
do not exist*, which are routine in GSD prose; a *wrong-shaped field* is a different category — it
is structurally invalid regardless of what it points at, and is therefore distinguishable. Update
`crossref.ts`'s header comment so the file no longer documents a trade-off it no longer makes; leave
the dangling-reference rationale intact, because that part still stands.

**(d) `in-memory-fs` seam.** This is a seam cleanup restoring plan 01-01's must-have that
`local-fs.ts` is the only file under `src/` importing the Node filesystem module. DATA-01's
substitution guarantee already holds — the interface methods never touch real disk — so nothing
about correctness changes. Enumerate every caller of `static fromDirectory()` first (exactly one at
planning time: `test/fs-equivalence.test.ts`); if there are now more, all of them move together.
Move the loader to a test helper. `test/helpers/from-directory.ts` is covered by
`tsconfig.server.json`'s `test/**/*.ts` include and is not matched by `vitest.config.ts`'s
`test/**/*.test.ts` collection glob, so it typechecks and imports without being collected as an
empty suite — confirm both of those still hold rather than taking it on faith. `test/fs-equivalence.test.ts`
must pass unchanged in what it asserts; only its import line moves.

Then add the enforcement gate. It must catch the Node filesystem module under every spelling that
would actually work — the `node:`-prefixed and bare forms, the `/promises` variant, static `import`,
`require`, and dynamic `import(...)` — scoped to `src/` only, since `test/portability.test.ts`
legitimately imports the promises API. It must not be fooled by prose: `src/cli/target-path.ts`,
`src/planning-repo/discovery.ts` and `src/server/search-index.ts` all mention the module name in
comments, and `local-fs.ts`'s own comment quotes the import string verbatim. Match on import syntax
(the module specifier in quotes as the argument of an import/require form), not on the bare token,
and exclude comment lines.

A gate matching only today's literal line is theatre. Prove it is not: temporarily plant a real
import in some other file under `src/`, confirm the gate fails, remove the plant, confirm it passes.
Record that positive control in the summary. Add the gate as a durable test in the suite — not only
as a shell one-liner — so it stays enforced after this task; `test/portability.test.ts` already reads
source files for structural proofs and is the natural home.
  </action>
  <verify>
    <automated>npm test && npm run typecheck && npm run lint && git diff --exit-code -- test/__golden__ && node -e "const p=require('./package.json'); if(!p.engines||!p.engines.node) {console.error('engines.node missing'); process.exit(1)} console.log('engines:',JSON.stringify(p.engines))" && ! grep -rInE "(^|[^A-Za-z0-9_])(import|require)\s*\(?\s*['\"](node:)?fs(/promises)?['\"]|from\s+['\"](node:)?fs(/promises)?['\"]" src/ --include=*.ts --include=*.tsx | grep -vE "^src/planning-fs/local-fs\.ts:" | grep -vE ":[0-9]+:\s*(//|\*|/\*)"</automated>
  </verify>
  <done>Golden files are byte-unchanged and pass. `engines.node` is present and its value is justified by measured floors named in the commit. A non-array `depends_on` produces exactly one warning and does not throw; a dangling well-formed reference produces none; both are pinned by tests. No file under `src/` outside `local-fs.ts` imports the Node filesystem module in any spelling, the gate is a committed test, and a planted violation was demonstrated to fail it. `test/fs-equivalence.test.ts` passes with only its import path changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Web presentation debt (audit items 3, 8, 9)</name>
  <files>index.html, src/web/styles/globals.css, src/web/pages/artifact-page.tsx, test/web/visual-contract.test.ts, test/web/mermaid-theme.test.ts</files>
  <behavior>
    - `index.html` still primes the theme class before first paint and still declares a ground colour, foreground colour and colour-scheme for both the light and dark roots, so a pre-hydration paint is on the correct ground in both themes.
    - Every micro-label selector in the `.artifact-metadata` summary family computes at or above 10px at every viewport width, in both themes, with contrast no worse than the 4.74 light / 6.99 dark baseline.
    - Mermaid receives node fill, node border, cluster fill, cluster border, secondary and tertiary seeds, edge colour, edge-label background, cluster/node text colour, note colours and a font size, every one of them routed through `toMermaidColor()`.
    - A mermaid theme variable whose source value cannot be converted still leaves diagrams rendered, because both lower fallback rungs remain reachable.
  </behavior>
  <action>
Three commits, in this order.

**(a) Dead critical-CSS scaffold — 02-REVIEW IN-02.** The audit's "roughly 150 of 297 lines" is a
prior estimate, not authority. Derive the deletion set yourself.

Grep every selector in the `<style>` block against `src/web` with **word-boundary accuracy on the
class token**, not substring matching. The planning-time grep was substring-fuzzy and its "live"
column is not trustworthy: `.status` almost certainly matched `snapshot-status`, and `.metadata`
almost certainly matched `artifact-metadata`. Both may in fact be dead. Conversely `.eyebrow` and
`.lede` returned real class-attribute hits and are probably live. Match against actual `className`
token lists — split on whitespace and compare whole tokens.

Then, separately and second, judge each block on **what it does**, because some blocks are
load-bearing by effect rather than by class name and no grep will catch them:

- The inline `<script>` that reads `localStorage` and toggles the `dark` class runs before first
  paint. It has no selector at all. It stays.
- The `:root` and `:root.dark` custom-property blocks paint the pre-hydration ground: they set
  `color`, `background`, `color-scheme` and `font-family` on the root element itself. Deleting them
  gives a white flash before the stylesheet loads in dark theme — a visible regression, and exactly
  the failure no selector grep would catch. They stay. Trim individual tokens inside them only if
  you can show the token is referenced by nothing that runs before hydration.
- `* { box-sizing }`, the `body` rule (which carries `min-width: 320px` and the ground paint), and
  the `button, input, textarea, select { font: inherit }` reset are all pre-hydration effect. They
  stay unless you can show otherwise.

What is left after those two passes is the component scaffold: card, progress, metadata, status,
error, skeleton chrome, its keyframes, and the narrow-width media query that only styles those
selectors. Delete only what is dead by both tests. If a rule is dead by selector but you are unsure
whether it is load-bearing by effect, keep it and say why.

Pin the anti-FOUC contract with a test so a future cleanup cannot repeat this: assert `index.html`
still contains the theme-priming script and still declares a background, a foreground and a
colour-scheme under both the light and dark root selectors. Report the actual before/after line
count against the measured 297, and whether it matched the audit's estimate.

**(b) The sub-10px micro-label.** Diagnose the chain first. At planning time there is no
`font-size` declaration on `html`, `:root` or `body` anywhere in `globals.css`, so the root is the
browser default 16px and `0.62rem` is 9.92px **directly** — there is no em compounding to unwind.
Confirm that on the live tree, because if a root font-size has since appeared the whole diagnosis
changes.

The real root cause is a design convention, not one leaf: an uppercase micro-label size applied as a
literal across several selectors. `0.62rem` appears at three (`.artifact-metadata > summary span`,
`.warning-fields dt`, `.warning-fields-label`) and `0.6rem` — 9.6px, smaller still — at three more.
Enumerate the live set yourself and **say in the summary which siblings share the root cause**, then
fix the family rather than bolting an absolute pixel value onto the one leaf the audit happened to
name. A single named token used by the whole family is the fix that holds; `0.625rem` is exactly
10px with no headroom against rounding, so prefer a value with margin.

Do not regress contrast. The colour is `var(--muted-foreground)` and size does not change a contrast
ratio, so 4.74 light / 6.99 dark should hold by construction — but measure it rather than reasoning
to it, since you may end up changing more than size. Verify at narrow widths too, not just desktop:
grep for these selectors inside media queries, because a narrow-width override could reintroduce a
sub-floor size that a desktop-only check would never see.

Pin it in the visual-contract suite. The pin must cover the whole cascade including media queries,
not just the one base declaration — an assertion that passes while a media query re-shrinks the text
is worse than no assertion.

**(c) Mermaid palette.** Only five variables are set today, so most of mermaid's palette falls back
to its own defaults against the app's oklch ground — that is the root of the "very ugly" verdict.
The 02-13 diagnosis measured the offending diagram background as the dark `--secondary` value, which
carries chroma and sits off the app's zero-chroma neutral palette; `--card` is the neutral surface
the rest of the app paints on. That is a concrete lead for the node-fill seed.

Before mapping anything, work out which variables are **seeds** and which mermaid **derives**. Under
`theme: 'base'` mermaid computes a large part of its palette from a small set of seeds; a mapping
that sets a derived value to something inconsistent with its seed can look worse than one that seeds
well and lets derivation do its job. Establish the seed set first, seed those deliberately, and set
derived values explicitly only where derivation demonstrably produces something wrong against this
palette.

Cover: node fill and border, cluster fill and border, the secondary and tertiary colours, edge
colour and edge-label background, cluster and node text colour, note background/text/border, and
font size. Source every one from an existing CSS custom property read off the document root and
convert it through `toMermaidColor()`, which stays the **single** conversion seam — do not add a
second converter, and do not hand mermaid a raw property value unconverted.

Two traps to check rather than discover at runtime. First, custom properties whose value is a
`color-mix(...)` expression come back from `getPropertyValue` as that expression, not as a resolved
colour; `toMermaidColor()` returns anything it cannot parse unchanged, so such a value reaches
mermaid's colour library verbatim. Prefer properties whose values are plain `oklch(...)`. Second,
some border tokens carry a low alpha in dark theme; converted faithfully that produces a nearly
invisible node border. Judge whether the faithful conversion actually reads well and pick something
that does if it does not — you own the aesthetic call here and there is no screenshot gate.

Keep intact: all three rungs of the fallback ladder (custom variables, then colourless base options,
then per-node readable source), `securityLevel: 'strict'`, and the dynamic `import('mermaid')`.
Pin all three plus the single-seam rule with a source-level contract test: every value in the theme
variables object flows through the converter, the strict security level is present, the import is
dynamic, and both lower fallback rungs are still reachable.
  </action>
  <verify>
    <automated>npm test && npm run typecheck && npm run lint && npm run build</automated>
    <human-check>Open the dashboard against a project containing a mermaid diagram, in both themes, and confirm the diagram reads as part of the app rather than as mermaid's default palette. Aesthetic judgment is the executor's per the task scope; this check is a sanity look, not a gate.</human-check>
  </verify>
  <done>`index.html` retains its theme-priming script and both root ground-paint blocks, has lost only rules dead by both selector-grep and effect-analysis, and the retention is pinned by a test; the real before/after line count is reported against 297. Every micro-label in the identified family computes at or above 10px at every width in both themes with contrast no worse than baseline, the sibling selectors sharing the root cause are named, and the pin covers media queries. Mermaid takes the full named set of colours and a font size through `toMermaidColor()` alone, with strict security level, dynamic import and all three fallback rungs pinned by test.</done>
</task>

<task type="auto">
  <name>Task 4: Warning-free build (audit item 10)</name>
  <files>vite.config.ts</files>
  <precondition>Task 3 is committed, so the measured chunk sizes reflect the final `src/web` tree rather than a tree that is about to change.</precondition>
  <action>
Measure first, decide second.

Re-run the build and record every chunk over 500 kB with its raw and gzip size. At planning time
there were exactly two: the entry chunk at 571.58 kB (gzip 180.76) and a 662.12 kB chunk (gzip
143.15). The larger one is **not** the entry — it is mermaid's shared internals, imported by
`mermaid.core`, `mermaid-parser.core` and every one of mermaid's ~18 per-diagram chunks. Mermaid is
already behind a dynamic import, so it is already async-only; that is why the audit's instinct to
suspect mermaid needs restating precisely rather than dismissing. The advisory fires on chunk size
regardless of whether the chunk is async, which is the actual reason it appears.

Then check the more interesting question the task scope raises: is any of the markdown pipeline
reaching the browser bundle at all? It is meant to be server-side only. Grepping minified output for
package names is weak evidence and returned nothing conclusive at planning time. The decisive check
is at the import graph, not the bundle: if nothing under `src/web/**` imports the unified/remark/
rehype/shiki/minisearch/gray-matter modules, directly or transitively through a module that does,
they cannot reach the client entry. Run that check and report the result either way — a positive
finding here is more consequential than the advisory itself and would change what you do next.

With numbers in hand, prefer real splitting. For the entry chunk the obvious candidate is
route-level lazy loading of the page components. Evaluate it honestly: measure what it actually buys
on a loopback-only, single-user tool served from localhost, where the request cost that code
splitting optimizes is close to zero and the cost is extra suspense boundaries and load states in a
read-only dashboard. For the mermaid chunk, note that rolldown has already split mermaid into many
chunks behind one dynamic boundary; further splitting inside it is not something this project
controls without fighting the bundler.

If, after measuring, splitting buys nothing that matters here, raising the chunk-size warning
threshold is the acceptable resolution — but only then, and only with a comment in `vite.config.ts`
that records the measured numbers, the date, and why the threshold is set where it is. Set it just
above the largest measured chunk, not at some round number far above it, so a real future regression
still trips it.

Three ways of making the advisory disappear are forbidden because they suppress output rather than
resolve it: lowering the build's log verbosity, passing a quiet flag to the build script, and
redirecting or filtering the build's output. Do not use any of them, and do not restate their option
names in a code comment in `vite.config.ts` — the verification greps that file for them, and a
comment naming them would fail the check for no reason.

Report before and after numbers in the summary.
  </action>
  <verify>
    <automated>npm run build > /tmp/lab-build.log 2>&1 && ! grep -qiE "chunks are larger|\(!\)" /tmp/lab-build.log && ! grep -qE "logLevel|--silent|--quiet" vite.config.ts package.json && npm test && npm run typecheck && npm run lint</automated>
  </verify>
  <done>`npm run build` exits 0 and its full unredirected output contains no advisory line. No log-level setting, quiet flag, or output redirection was introduced. Per-chunk before/after numbers are reported, the markdown-pipeline-in-the-client question is answered with evidence either way, and if the threshold was raised the config carries a comment with the measured justification.</done>
</task>

<task type="auto">
  <name>Task 5: Measured keyboard reachability of the Refresh control (audit item 11)</name>
  <files>test/web/refresh-contract.test.ts</files>
  <precondition>The global Playwright install and its bundled chromium are reachable: `@playwright/test` under `/home/cinedise/.npm-global/lib/node_modules` and browsers under `~/.cache/ms-playwright`. If either is absent, do not add Playwright to this project — report item 11 as not closable and say why.</precondition>
  <action>
The CR-02 residual is that keyboard operability was never exercised. The prior pass measured
visibility, hit-box geometry and overflow, none of which establish that a control can be reached or
operated from the keyboard. Reasoning from the JSX that "it is a real `<button>` in normal flow so
Tab order follows from the markup" is exactly the inference this task exists to replace with
measurement. **Record measured results, not derivations.**

Drive the already-installed bundled chromium from a throwaway script kept **outside the repository**
— use the session scratchpad. Resolve Playwright from the global install path; nothing enters
`package.json`, `package-lock.json`, or the working tree.

Measure per width in {320, 700, 992} × per theme in {light, dark} — six cells, none skipped:

- The actual focus order through the header: press Tab repeatedly from document start and record,
  for each stop, which element holds focus, identified by tag, class and accessible name.
- Whether the Refresh button appears in that order at all, and at which stop.
- Whether it gets a **visible** focus indicator, measured in both themes separately. Compare the
  computed outline and box-shadow before and after focus, and measure the indicator's contrast
  against the surface immediately behind it. A ring that passes in light can vanish against the dark
  ground; a single-theme check would miss exactly that.
- Whether **Enter and Space each independently** trigger a refresh. Test them as two separate
  measurements, counting the resulting requests to the refresh endpoint. They are genuinely
  different: a div with a click handler passes Enter and fails Space, so a combined check that fires
  one and assumes the other proves nothing.

The snapshot-age element is not interactive. If it is not Tab-reachable, that is correct behavior —
record it as expected and say so, rather than logging it as a failure.

Report the six cells as a table of measured values. If any width or theme fails, **report item 11 as
still open** with the failing cell named; do not partially close it.

Then add a durable assertion to `test/web/refresh-contract.test.ts` alongside the one-off
measurement, so the property does not silently regress after the throwaway script is gone. That
suite proves properties from source, so pin the things that make keyboard operability true by
construction — the control being a native button element rather than a handler on a non-interactive
element, and a focus-visible treatment declared for it on both theme grounds — and make it clear in
the test's own naming that it is the structural guarantee behind the measured behavior, not a
substitute for having measured it.
  </action>
  <verify>
    <automated>npm test && npm run lint && git diff --exit-code -- package.json package-lock.json</automated>
    <human-check>Review the six-cell measured table. Confirm each cell states an observed value (which element took focus, which stop, measured indicator contrast, request counts for Enter and for Space) rather than an inference from the source. Confirm the working tree gained no Playwright dependency and no stray script.</human-check>
  </verify>
  <done>Six measured cells recorded with observed values. Enter and Space each measured separately. Focus-indicator visibility measured per theme. The non-interactive snapshot-age element's Tab behavior is characterized correctly rather than reported as a defect. A durable structural assertion is committed. If any cell failed, item 11 is reported still open with that cell named.</done>
</task>

<task type="auto">
  <name>Task 6: Phase-02 D3 reconciliation and R-02-01 closure (audit item 12)</name>
  <files>.planning/phases/02-situational-awareness-artifact-reading/02-06-SUMMARY.md, .planning/phases/02-situational-awareness-artifact-reading/02-SECURITY.md</files>
  <action>
The substantive work here is evidentiary, not editorial. R-02-01 is closed by producing a mapping,
not by re-running the 02-06 checklist — that disposition is already decided.

Enumerate 02-06's actual checklist. Its `coverage` block names three deliverables: D1 (the full
automated gate, `status: pass`, no human judgment), D2 (host-browser UAT of the D-01..D-17 flows,
`status: pass`, human judgment, closed on the user's explicit close-out decision), and D3 (the
light/dark contrast pass, **`status: unknown`**). D3 is the one that matters, and it names its own
surfaces individually: prose, muted text, links in every state, badges, table chrome, highlighted
code, mermaid output, and reference previews. Those named surfaces are the checklist items. Map each
one to the specific place downstream where it was **actually exercised**, with a file and a locator.

One correction you must carry rather than repeat: R-02-01 and the milestone audit both describe the
mitigating chain as "02-09 → 02-13 → 02-17". `02-09-SUMMARY.md` **does not exist**. `02-09-PLAN.md`
carries `status: superseded`, `superseded_by: 02-13`; its 2026-08-29 run was not approved, and its
outcome — four defects and five quality objections recorded in `02-UAT.md` — is what produced the
gap-closure plans 02-10 through 02-13. So 02-09 contributed a *failed* gate, not passing evidence.
The evidence that can actually close D3 lives in 02-13 and 02-17. State this rather than citing
02-09 as though it were a pass; that citation is the kind of convenient reading this task
specifically forbids.

Useful anchors: 02-17 carries per-theme, per-viewport verdicts including a nine-selector muted-surface
table with measured contrast ratios, a mermaid verdict in both themes, a code-scrollbar verdict, and
a table-chrome verdict. Check each of D3's named surfaces against that evidence individually.
**Anything unmapped stays open and gets named** — links in every state, badges and reference
previews in particular deserve a specific look rather than absorption into a blanket pass.

Then reconcile D3's status. A SUMMARY is a historical account of one plan's execution, so do not
overwrite the original `unknown` verdict — that would falsify the record of what 02-06 actually
knew. Add a second, dated verification entry to D3 recording what the downstream evidence shows,
with its refs, and adjust D3's overall status only if the mapping is complete. If any named surface
is unmapped, D3 stays partly unresolved and you say which surface and why.

Then record R-02-01 in `02-SECURITY.md`. Add a dated resolution to its accepted-risk row naming the
mapping you produced, and add a **new** dated row to the audit trail rather than editing the
existing one. Keep the frontmatter semantics correct: `threats_open` counts OPEN *threats* at or
above the `high` block severity. R-02-01 is a *risk*, and its underlying threat T-02-20 is medium —
below the threshold either way. `threats_open` is 0 today and must stay 0; changing it would be
wrong regardless of this reconciliation's outcome. Do not touch it unless evidence about a
high-or-above threat actually changed, which nothing in this plan does.
  </action>
  <verify>
    <automated>grep -q '^threats_open: 0' .planning/phases/02-situational-awareness-artifact-reading/02-SECURITY.md && grep -q 'R-02-01' .planning/phases/02-situational-awareness-artifact-reading/02-SECURITY.md && grep -q 'D3' .planning/phases/02-situational-awareness-artifact-reading/02-06-SUMMARY.md</automated>
    <human-check>Read the surface-by-surface mapping. Confirm every one of D3's named surfaces has either a specific downstream locator or an explicit "unmapped, still open" line — no surface absorbed into a blanket pass, and 02-09 characterized as the failed gate it was rather than as evidence.</human-check>
  </verify>
  <done>A per-surface mapping from D3's named checklist to concrete 02-13/02-17 evidence exists, with unmapped surfaces named and left open. D3's original verdict is preserved and a dated reconciliation entry added. R-02-01 carries a dated resolution and a new audit-trail row. `threats_open` is unchanged at 0.</done>
</task>

<task type="auto">
  <name>Task 7: Closing record — STATE.md and the milestone audit (audit items 13, 14)</name>
  <files>.planning/STATE.md, .planning/v1.0-MILESTONE-AUDIT.md</files>
  <precondition>Tasks 1 through 6 are complete and their outcomes — closed, partly closed, or left open — are known. This task records outcomes; it cannot run before there are outcomes to record.</precondition>
  <action>
**STATE.md.** Its Deferred Items table carries exactly two rows: the mermaid styling item and the
9.92px span item. Clear a row **only if** Task 3's corresponding item actually landed and verified.
If either was left open, its row stays, updated with what is now known. Do not clear both reflexively
because the task ran.

**The milestone audit.** Rewrite the `tech_debt:` frontmatter block and the "Tech Debt Summary"
section so they record the true end state: what closed, by which commit, with what evidence; and
what did not, with its reason. This section stops being a backlog and becomes a record.

Before writing, reconcile the audit's own bullet list against this plan's coverage, item by item.
Doing that turns up something you must handle explicitly rather than paper over: **the audit lists
two stale UI reviews that this plan's scope never covered.** `03-UI-REVIEW.md` names its own re-run
trigger (the shared shell, theme tokens, or Phase 3 responsive breakpoints changing) and
`04-UI-REVIEW.md` gathered its evidence at a viewport below the then-current breakpoint. Neither is
a defect; both are re-run conditions. And Task 3 of this very plan modifies `globals.css`,
`artifact-page.tsx` and `index.html` — which fires both re-run conditions **again**. This plan makes
those two items more stale, not less.

So the audit cannot honestly read zero open debt on the strength of items 1-12 alone. Handle it
directly: list both stale reviews as remaining open with that reasoning stated plainly, including
the fact that this plan re-triggered them. Then flip `status:` from `tech_debt` to `passed` **only**
if every item genuinely closed. Given the two stale reviews, the likely honest outcome is that the
status does not flip, and the audit instead reads as a much shorter, precisely-scoped debt list.
Surface that conclusion to the user as a decision rather than deciding unilaterally that a
stale-review condition does not count as open debt — and do not flip the status to make the number
look better. An audit that reads `tech_debt` with two named, understood, non-blocking items is a
truthful artifact; one that reads `passed` while two re-run conditions are live is not.

Apply the same rule to every other item: anything Task 1-6 left open stays listed with its reason.
Update the `scores` and top-of-file verdict prose only where evidence actually changed them.
  </action>
  <verify>
    <automated>npm test && npm run typecheck && npm run lint && npm run build > /tmp/lab-build-final.log 2>&1 && ! grep -qiE "chunks are larger|\(!\)" /tmp/lab-build-final.log && node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs" windows status && grep -qE '^status: (passed|tech_debt)$' .planning/v1.0-MILESTONE-AUDIT.md</automated>
    <human-check>Read the rewritten tech-debt record against the outcomes of Tasks 1-6. Confirm every claimed closure names a commit and its evidence, every remaining item names its reason, the two stale UI reviews are listed with the note that this plan re-triggered them, and the `status:` value matches what actually happened rather than the desired result.</human-check>
  </verify>
  <done>STATE.md's Deferred Items table reflects only what genuinely remains. The audit's tech_debt frontmatter and Tech Debt Summary record actual outcomes with per-item evidence or per-item reasons. The two stale UI reviews are explicitly dispositioned rather than dropped. `status:` is `passed` only if every item closed; otherwise it stays `tech_debt` with the short remaining list, and the decision is surfaced to the user.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Target `.planning/` → Labelore | Read-only. No change in this plan may grant write capability. |
| Artifact content → rendered HTML | Untrusted markdown becomes DOM. Task 3 touches the mermaid path on this boundary. |
| Host shell → repository | Task 5 runs a browser-driving script; it must not deposit anything in the tree. |
| npm registry → runtime | No package is installed by this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-Q4-01 | Tampering | `src/planning-fs/in-memory-fs.ts`, `test/helpers/from-directory.ts` (Task 2d) | high | mitigate | The moved loader stays read-only (directory walk and file read only) and `capabilities = { watch: false, write: false }` is unchanged; `test/fs-equivalence.test.ts` continues to prove byte-identical snapshots across both implementations. |
| T-Q4-02 | Elevation of Privilege | mermaid initialization in `src/web/pages/artifact-page.tsx` (Task 3c) | high | mitigate | `securityLevel: 'strict'` is a named retention requirement and is pinned by a source-level contract test; the markdown sanitize ordering is not touched. |
| T-Q4-03 | Tampering | `index.html` (Task 3a) | medium | mitigate | Deletion set is derived from word-boundary selector grep **plus** effect analysis; the theme-priming script and both root ground-paint blocks are retained and pinned by test, so no pre-hydration behavior is silently removed. |
| T-Q4-04 | Information Disclosure | Task 5 browser script | medium | mitigate | Script lives in the session scratchpad outside the repository; `git diff --exit-code -- package.json package-lock.json` is a verification gate on that task. |
| T-Q4-05 | Repudiation | `.planning/*` record edits (Tasks 1, 6, 7) | medium | mitigate | Historical SUMMARY records are annotated, never overwritten; ledger closures carry evidence; the audit's `status:` may only flip on genuine closure, and unclosed items stay listed with reasons. |
| T-Q4-06 | Denial of Service | `resolveCrossReferences` warning path (Task 2c) | medium | mitigate | D-12's never-throw contract is pinned by a dedicated regression test; the warning is added to the existing collector rather than a new channel, so no unbounded accumulation path is introduced. |
| T-Q4-SC | Tampering | npm/pip/cargo installs | high | mitigate | **No package is installed by this plan.** `test/web/refresh-contract.test.ts`'s dependency-set assertion remains the enforcing gate, and `git diff` on the manifest and lockfile is a verification gate on Tasks 2 and 5. No package-legitimacy checkpoint is required because the install count is zero. |
</threat_model>

<verification>
Run at the end of the plan, on the final tree:

- `npm test` — exits 0, count ≥ 555 (it should be higher; new regression tests were added in Tasks 2, 3 and 5)
- `npm run typecheck` — exits 0
- `npm run lint` — exits 0, silent
- `npm run build` — exits 0 with no advisory line in unredirected output
- `git diff --exit-code -- test/__golden__` — clean, unless a golden change was explicitly named and justified
- `git diff -- package.json package-lock.json` — only the `engines` addition
- `node "$HOME/.claude/gsd-core/bin/gsd-tools.cjs" windows status` — `open_count` 0
- No file under `src/` outside `local-fs.ts` imports the Node filesystem module in any spelling, proven by the committed gate test and by the planted-violation positive control
</verification>

<success_criteria>
Every one of the fourteen items is either closed with named evidence or reported open with a stated
reason. No item is silently skipped and none is claimed closed without evidence. The four gates all
exit 0 with the build now advisory-free. The tree gained no dependency. Each item or tight group is
independently revertible. `.planning/v1.0-MILESTONE-AUDIT.md` records the true end state, and its
`status:` reflects what actually happened.
</success_criteria>

<output>
Create `.planning/quick/260910-0x4-close-every-tech-debt-item-in-planning-v/260910-0x4-SUMMARY.md` when done.

The summary must include, as its central artifact, a **fourteen-row outcome table**: one row per
audit item, each with `closed` / `partly closed` / `left open`, the commit that closed it, and the
evidence or the reason. That table is what Task 7's audit rewrite is derived from, and it is what a
re-audit will read.
</output>
