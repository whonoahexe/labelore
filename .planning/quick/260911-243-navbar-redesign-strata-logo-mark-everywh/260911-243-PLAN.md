---
phase: quick-260911-243
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/presentation/shell-header.ts
  - src/web/components/labelore-mark.tsx
  - public/favicon.svg
  - src/web/components/app-shell.tsx
  - src/web/components/snapshot-status.tsx
  - src/web/components/search-field.tsx
  - src/web/styles/globals.css
  - test/web/navbar-contract.test.ts
  - test/web/refresh-contract.test.ts
  - test/web/visual-contract.test.ts
files_deleted:
  - src/web/components/refresh-control.tsx
autonomous: true
requirements: [NAV-01, NAV-02, NAV-03, NAV-04, NAV-05, NAV-06]

estimate:
  tokens: 160000
  raw_tokens: 160000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "The header brand tile and the browser favicon both show the Strata mark (path M3 3h5v18H3Z M10 10h5v5h-5Z M10 16h11v5H10Z), drawn by one shared LabeloreMark component in the app and on a primary oklch(0.553 0.195 38.402) tile with a primary-foreground mark in public/favicon.svg. No lucide glyph stands in for the brand anywhere (NAV-01, D-01)"
    - "The brand block reads [tile] LABELORE / {project name}, with a mono meta line under the name built from presentation.state as 'milestone · Phase n/total · percent%'. Missing parts are omitted. The name falls back to the rootPath basename, then 'Planning intelligence'. Hovering shows the full rootPath. Against fixtures/dense the meta line reads exactly 'v3.0 · Phase 1/2 · 67%' (NAV-02)"
    - "Exactly three tabs render: Dashboard, Roadmap, Traceability. The active tab has foreground text and a 2px --primary underline, with no filled background. Hover uses --state-hover (NAV-03, D-02)"
    - "Search is an icon button with a ⌘K (macOS) or Ctrl K hint. Clicking it, or pressing ⌘K/Ctrl+K anywhere outside another editable field, opens a large centred modal over a full-screen scrim. Typing gives debounced, abortable, ranked results. Arrow keys move the highlight. Enter opens the highlighted row, or /search?q= when nothing is highlighted. 'See all N results', indexing, error and empty states all still work. Esc, selecting a result, or following a link closes the dialog (NAV-04)"
    - "The snapshot status is one compact pill: a status dot plus 'Read 2m ago', re-rendered every 30s, inside a <time dateTime> with the exact time in the tooltip. Clicking it runs the existing refresh (unfiltered invalidateQueries, scroll to top, fixed-copy error toast). The dot pulses while refreshing and stays static under prefers-reduced-motion. The dot and text use --destructive when metadata fails. data-snapshot-read-at and aria-label='Refresh the project snapshot' are kept (NAV-05)"
    - "All new CSS uses var() tokens, and new named tokens (overlay scrim, dialog geometry) are declared in :root, plus .dark where theme-dependent. test/token-guard.test.ts passes. Both themes render. Corners stay square. At 400px the header is at most two rows with no horizontal page scroll (NAV-06)"
    - "npm test, npm run typecheck, npm run lint and npm run build all pass"
  artifacts:
    - path: "src/web/components/labelore-mark.tsx"
      provides: "Shared Strata brand mark SVG component (single currentColor path, 24x24 grid)"
      contains: "M3 3h5v18H3Z M10 10h5v5h-5Z M10 16h11v5H10Z"
    - path: "public/favicon.svg"
      provides: "Favicon: Strata mark on the primary tile"
      contains: "oklch(0.553 0.195 38.402)"
    - path: "src/presentation/shell-header.ts"
      provides: "Pure header helpers: projectDisplayName, formatProjectMeta, formatReadAt, formatRelativeReadAt, isSearchShortcut, isEditableTarget, shortcutHintFor"
      exports: ["projectDisplayName", "formatProjectMeta", "formatReadAt", "formatRelativeReadAt", "isSearchShortcut", "isEditableTarget", "shortcutHintFor"]
    - path: "src/web/components/snapshot-status.tsx"
      provides: "Status pill that absorbs the former refresh control's mutation, toast and aria contract"
      contains: "data-snapshot-read-at"
    - path: "src/web/components/search-field.tsx"
      provides: "SearchDialog: trigger button, global shortcut, base-ui Dialog over an inline base-ui Combobox"
      contains: "@base-ui/react/dialog"
    - path: "test/web/navbar-contract.test.ts"
      provides: "Unit tests for shell-header helpers plus source-text contracts for mark, favicon, brand, tabs, pill, dialog and tokens"
  key_links:
    - from: "src/web/components/app-shell.tsx"
      to: "src/web/components/labelore-mark.tsx"
      via: "<LabeloreMark /> inside .brand-mark"
      pattern: "<LabeloreMark"
    - from: "src/web/components/app-shell.tsx"
      to: "src/presentation/shell-header.ts"
      via: "projectDisplayName + formatProjectMeta fed from presentation.data"
      pattern: "formatProjectMeta\\("
    - from: "src/web/components/snapshot-status.tsx"
      to: "/api/refresh"
      via: "useMutation POST then queryClient.invalidateQueries()"
      pattern: "invalidateQueries\\(\\)"
    - from: "src/web/components/search-field.tsx"
      to: "/api/search"
      via: "useQuery keyed on the debounced query with the TanStack AbortSignal"
      pattern: "queryFn: \\(\\{ signal \\}\\)"
    - from: "src/web/styles/globals.css"
      to: ".search-dialog-backdrop"
      via: "--overlay-scrim declared in :root and .dark"
      pattern: "--overlay-scrim"
---

# Quick 260911-243: Navbar redesign (Strata mark, brand lockup, segmented tabs, ⌘K search dialog, status pill)

<objective>
Redesign the Labelore header, delivering all six parts of the user's request:

- NAV-01: the new "Strata" Labelore mark replaces the lucide brand glyph everywhere, via one shared `LabeloreMark` component plus `public/favicon.svg`.
- NAV-02: brand lockup of tile, LABELORE wordmark, "/" separator and project name, with a mono meta line from `presentation.state`, graceful fallbacks, and the rootPath as the tooltip.
- NAV-03: the same three tabs (Dashboard, Roadmap, Traceability) restyled as a tighter segmented bar with a --primary underline on the active tab and no filled blocks.
- NAV-04: search becomes an icon button with a ⌘K/Ctrl K hint that opens a large centred @base-ui/react Dialog over a full-screen scrim. It keeps every existing search behaviour.
- NAV-05: the snapshot readout becomes a compact status pill that also is the refresh control (RefreshControl merged in).
- NAV-06: every value goes through tokens, adding named tokens where needed. token-guard stays green. Light and dark themes, square corners, and no overflow down to 400px.

Locked user decisions:
- D-01 (this task): the logo is "B · Strata", three bars stepping into an L, path `M3 3h5v18H3Z M10 10h5v5h-5Z M10 16h11v5H10Z`. Do not revisit.
- D-02 (this task): no new tabs. Tabs remain Dashboard / Roadmap / Traceability. The proposed "Lore" tab is explicitly out of scope.

Naming note: code comments in this area already cite Phase-4 decisions D-01..D-05. Those are the refresh contract: Phase-4 D-02 no layout shift, D-03 scroll to top, D-04 retained snapshot plus fixed toast copy, D-05 unfiltered invalidation. They are preserved, and this plan calls them "Phase-4 D-0N" to keep them distinct from this task's D-01/D-02.

Purpose: open the dashboard and know at a glance which project, which milestone and phase, how fresh the read is, and how to search. The header should do that without the current three-row wrap at narrow widths.

Output:
- a shared mark component and favicon
- a pure, unit-tested header-helper module
- a merged status-pill component
- the search dialog
- restyled header CSS with new named tokens
- updated and new contract tests
- a live screenshot pass at 1440/820/400px in both themes
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.claude/CLAUDE.md

Source files the executor must read before editing. Read each once. For globals.css, read only the named line ranges.
@src/web/components/app-shell.tsx
@src/web/components/search-field.tsx
@src/web/components/refresh-control.tsx
@src/web/components/theme-toggle.tsx
@src/web/components/ui/button.tsx
@test/web/refresh-contract.test.ts
@test/web/shell-contract.test.ts

Interface facts, verified at planning time. Do not re-derive them.
- `ProjectPresentation` (src/server/project-presentation.ts, around line 140) has `readAt: string`, `rootPath: string` (the project root, not `.planning`), `projectName: string | null`, and `state: ProjectStateDto | null`. `ProjectStateDto` (lines 26-40) has `milestone: string | null`, `phaseNumber: string | null` (a raw frontmatter string such as "04" or "1"), `phaseName`, `status`, and `progress: { totalPhases, completedPhases, totalPlans, completedPlans, percent }`, all `number | null`.
- fixtures/dense/.planning/STATE.md has milestone v3.0, current_phase 1, total_phases 2 and percent 67. The expected meta line is exactly "v3.0 · Phase 1/2 · 67%". This repo's own STATE.md has no percent, so its meta line omits that part.
- Tests run under vitest in the node environment (`test/**/*.test.ts` only). There is no DOM library. tsconfig.server.json covers `src/**/*.ts` and `test/**/*.ts` but excludes `src/web/**`, so tests can only real-import `src/presentation/*.ts` (this is why the pure helpers live there). Web components are covered by source-text contracts that use the `source()` readFile helper, as in test/web/refresh-contract.test.ts.
- test/token-guard.test.ts checks every declaration outside `:root`, `.dark`, `@theme inline` and `@font-face` blocks:
  - spacing properties (padding, margin, gap, inset, top, right, bottom, left and their longhands) must be 0, auto, a % or wholly var(). Raw rem/px/em/vw/vh fail, including inside calc() or clamp().
  - font-size, line-height, letter-spacing, font-weight and font-family must be var() tokens.
  - colours must be tokens. A color-mix() recipe that recurs twice, or that duplicates a token's own value, fails.
  - every `--space-N` step must be a plain rem multiple of 0.125rem, which is why dialog geometry tokens must NOT use the `--space-` prefix. Every defined `--space-N`, `--fs-*`, `--lh-*`, `--ls-*` and `--fw-*` token must be consumed somewhere.
  - no two `:root` palette tokens may hold identical values in both themes, and `.dark` must not re-declare a `:root` value identically. The scale block is also `:root`, so any new non-`--space/--fs/--lh/--ls/--fw/--font` token joins this check and must have a unique value.
  - width, height, max-height, min-width, transform, backdrop-filter, box-shadow offsets and z-index are NOT scanned. Still follow the user's rule: tokens for space, type and colour, and add a named token rather than a one-off.
- Existing tokens to reuse: `--primary`, `--primary-foreground`, `--foreground`, `--muted-foreground`, `--border`, `--ring`, `--popover`, `--popover-foreground`, `--destructive`, `--destructive-border`, `--state-hover`, `--state-active`, `--shadow-popover`, and the full `--space-*`, `--fs-1..7`, `--fs-fluid-*`, `--lh-*`, `--ls-*`, `--fw-*` and `--font-heading/--font-mono/--font-sans` scales. The `@keyframes loading-pulse` animation already exists (globals.css around line 1553) and can drive the pill dot.
- globals.css line map:
  - palette `:root` 53-113 and `.dark` 115-133
  - scale `:root` 140-224
  - shell and brand 344-480
  - theme toggle 1486-1507
  - `@media (max-width: 62rem)` 1559-1613
  - `@media (max-width: 42rem)` 1615-1721
  - reduced motion 1723
  - search field and dropdown 2759-2888
  - refresh control and keyframes 3183-3210
- @base-ui/react 1.7.0:
  - `@base-ui/react/dialog` exports Root (`open`, `onOpenChange`, `modal`), Trigger, Portal, Backdrop, Viewport, Popup (`initialFocus` and `finalFocus` accept a ref), Title, Description and Close.
  - Combobox.Root supports `inline` (documented pattern: `<Combobox.Root inline open>`, bind its `open`/`onOpenChange` to the Dialog's so its transient state resets on close), `onItemHighlighted(value | undefined, details)` and `onValueChange`.
  - merged event handlers receive `event.preventBaseUIHandler()`.
- Server: `npm start -- <projectPath> --port <n>` serves the built `dist/` in production mode on 127.0.0.1. Do NOT stop the user's server on port 4210. Use port 4211.
- Browser automation on this machine: the browser MCPs are broken. Write a throwaway Node .mjs script in your own scratch directory (never in the repo). It imports `chromium` from `/home/cinedise/.npm-global/lib/node_modules/@playwright/test/node_modules/playwright-core/index.mjs` and launches with `executablePath: '/home/cinedise/.cache/ms-playwright/chromium-1234/chrome-linux64/chrome'`.
- Existing negative source pins (read them before writing any comments). test/web/visual-contract.test.ts ("header search dropdown" describe, around line 449) forbids certain literals anywhere in search-field.tsx, including comments. test/web/refresh-contract.test.ts forbids certain literals in the refresh component and in app-shell.tsx. That task retargets these pins onto snapshot-status.tsx, and the forbidden words must not appear there either. Before adding comments to those files, read each test's `not.toContain`/`not.toMatch` assertions and keep comment prose clear of those exact strings.
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1 (tracer): Strata mark + brand lockup + segmented tabs, end to end from shared helper to header, favicon and build</name>
  <files>src/presentation/shell-header.ts, src/web/components/labelore-mark.tsx, public/favicon.svg, src/web/components/app-shell.tsx, src/web/styles/globals.css, test/web/navbar-contract.test.ts</files>
  <precondition>node_modules is present in the worktree. If it is absent, run `npm ci`, which installs only lockfile packages and adds none.</precondition>
  <read_first>src/web/components/app-shell.tsx, src/web/styles/globals.css lines 344-480 and 1559-1721, test/web/shell-contract.test.ts, test/web/visual-contract.test.ts lines 1-30 (ruleBlocks helper)</read_first>
  <behavior>
    - projectDisplayName('Labelore', '/a/b') returns 'Labelore'. With a null or whitespace-only name it returns the rootPath basename: '/home/u/acme' and '/home/u/acme/' give 'acme', 'C:\\work\\acme' gives 'acme', and '/home/u/acme/.planning' gives 'acme' (a trailing .planning segment is skipped). With a null name and an empty rootPath it returns 'Planning intelligence'.
    - formatProjectMeta(null) returns null. For {milestone:'v3.0', phaseNumber:'1', progress:{totalPhases:2, percent:67}} it returns 'v3.0 · Phase 1/2 · 67%'.
    - Phase numbers drop leading zeros per integer part: '04' with totalPhases 4 gives 'Phase 4/4', and '02.1' gives 'Phase 2.1'. A non-numeric phaseNumber is shown as-is. A null totalPhases gives 'Phase 3'. A null phaseNumber omits the phase part entirely.
    - A null or non-finite percent is omitted, and 66.6 renders as '67%'. When every part is missing, including a null or empty milestone, it returns null. The separator is ' · ' (space, middle dot, space).
    - labelore-mark.tsx contains the exact path string and fill="currentColor". favicon.svg contains the same path string, both oklch literals and no rx attribute.
    - app-shell.tsx renders <LabeloreMark inside .brand-mark. Its lucide-react import names only the tab icons. It sets title from rootPath on the brand link and has exactly three NavLinks using navigationClass.
    - The CSS rule block `.shell-nav-link.active {` declares no background and references var(--primary).
  </behavior>
  <action>
RED first. Create test/web/navbar-contract.test.ts in the style of refresh-contract.test.ts: a `source()` readFile helper, plus a local copy of visual-contract's `ruleBlocks()` helper for CSS rule extraction. Add two describes:
- "shell-header helpers" real-imports `../../src/presentation/shell-header.ts` and covers every projectDisplayName and formatProjectMeta case in `<behavior>`.
- "Strata mark, brand lockup and tabs (D-01, D-02, NAV-01..03)" holds the source-text assertions from `<behavior>`. The three-tab assertion also checks the labels Dashboard, Roadmap and Traceability, and that no fourth primary tab exists (D-02). The lucide assertion parses the `from 'lucide-react'` import specifier list in app-shell.tsx and expects exactly the tab icon names, so no brand glyph can hide there.

Run it and see it fail. Then GREEN:

1. Create src/presentation/shell-header.ts as a pure module: no DOM, no node:* imports, no React. Export `projectDisplayName(projectName: string | null, rootPath: string): string` and `formatProjectMeta(state): string | null`. Type the state parameter structurally (milestone, phaseNumber and progress.totalPhases/percent, all nullable, plus null) rather than importing from src/server, so presentation never depends on server. Split the basename on both / and \, and drop empty trailing segments and a final `.planning` segment. Join the meta parts with ' · '. Tasks 2 and 3 add more exports to this file.

2. Create src/web/components/labelore-mark.tsx per D-01. Export `LabeloreMark(props: React.ComponentProps<'svg'>)`. It renders an svg with viewBox "0 0 24 24", fill="currentColor", aria-hidden="true", focusable="false" and props spread last. Inside is a single path with d exactly `M3 3h5v18H3Z M10 10h5v5h-5Z M10 16h11v5H10Z`. CSS owns the size, so set no width or height attributes.

3. Rewrite public/favicon.svg per D-01 and NAV-01 as a square tile with no rounded corners:
   - a full-bleed rect filled oklch(0.553 0.195 38.402), the light --primary
   - the same path filled oklch(0.98 0.016 73.684), the light --primary-foreground
   - use viewBox 0 0 24 24; you may wrap the path in a group transform that scales it about the centre if the mark needs more breathing room at 16px
   - leave index.html's existing `<link rel="icon" href="/favicon.svg">` untouched
   The planning-time grep found the brand only in app-shell.tsx and favicon.svg. invalid-project-screen.tsx uses a status icon, not the brand. Re-run a repo-wide grep over src/, index.html and public/ for the lucide brand-glyph import and for "brand-mark". If any other brand occurrence turns up, swap it to LabeloreMark too.

4. app-shell.tsx brand (NAV-02). Keep the brand as the single NavLink to presentationRoutePatterns.dashboard (shell-contract pins that). Set title to presentation.data?.rootPath. Set an aria-label that starts with "Labelore" and contains the display name, so the accessible name survives narrow-width hiding of the wordmark. Its children, in order:
   - `span.brand-mark` wrapping `<LabeloreMark />`
   - `strong.brand-wordmark` "Labelore", uppercased by CSS
   - `span.brand-separator` with aria-hidden and text "/"
   - `span.brand-project` containing `span.brand-project-name` (projectDisplayName(presentation.data?.projectName ?? null, presentation.data?.rootPath ?? '')), plus `small.brand-meta` rendered only when formatProjectMeta(presentation.data?.state ?? null) is non-null

   Remove the lucide brand glyph from the import. Keep the three tab NavLinks, their icons, their labels and navigationClass exactly as they are (D-02). Leave the snapshot/search/theme markup alone; Tasks 2 and 3 replace it.

5. globals.css header skeleton (NAV-03 and NAV-06), all via tokens:
   - `.shell-header` becomes a grid with template areas "brand nav controls". Keep the existing side columns `minmax(12rem, 1fr) auto minmax(12rem, 1fr)`, or loosen them to `minmax(0, 1fr)`, whichever keeps the nav visually centred. Assign grid-area on .brand, .shell-nav and .shell-controls.
   - Below 62rem the areas become "brand controls" over "nav nav" with columns `minmax(0, 1fr) auto`.
   - Below 42rem, keep that same two-row arrangement. Delete the old single-column three-row stack. Keep the existing non-sticky `position: relative` there unless it breaks. The nav runs full width with a top hairline, and tabs flex equally.
   - Brand styles:
     - .brand: flex row with gap var(--space-2-5), existing padding tokens, min-width 0
     - .brand-mark: keep its tile rules
     - .brand-mark svg: sized about 1.25rem
     - .brand-wordmark: font-heading, var(--fs-3), var(--ls-widest), var(--fw-semibold), uppercase
     - .brand-separator: color var(--border), var(--fw-medium)
     - .brand-project: grid, min-width 0
     - .brand-project-name: var(--fs-4), var(--fw-semibold), var(--foreground), single-line ellipsis
     - .brand-meta: var(--font-mono), var(--fs-1), var(--ls-wide), uppercase, var(--muted-foreground), single-line ellipsis
   - Delete the obsolete `.brand > span:last-child`, `.brand strong` and `.brand small` rules. `.brand small` shares a rule with `.snapshot-status small`; keep the snapshot half until Task 2 removes it.
   - Below 42rem, hide .brand-wordmark and .brand-separator with display:none (the aria-label keeps the name).
   - Segmented tabs:
     - .shell-nav: hairline dividers between tabs using 1px var(--border)
     - .shell-nav-link: tighter padding, about var(--space-2-5) var(--space-4); keep font-heading, var(--fs-2), var(--ls-wider), uppercase and muted colour
     - :hover: var(--state-hover) background with foreground text
     - .shell-nav-link.active: color var(--foreground) and `box-shadow: inset 0 -2px var(--primary)`, with no background declaration
   - Keep the `.shell-content` and `.tree-navigator` rules and the ResizeObserver-driven `--shell-header-height` untouched.

6. Run token-guard, shell-contract, visual-contract and the new contract file until all pass.
  </action>
  <verify>
    <automated>npx vitest run test/web/navbar-contract.test.ts test/token-guard.test.ts test/web/shell-contract.test.ts test/web/visual-contract.test.ts test/web/refresh-contract.test.ts && npm run typecheck && npm run lint && npm run build && grep -c 'M3 3h5v18H3Z M10 10h5v5h-5Z M10 16h11v5H10Z' dist/favicon.svg && grep -l 'M3 3h5v18H3Z' dist/assets/*.js</automated>
  </verify>
  <done>Helper unit tests and mark/brand/tab contracts pass. token-guard is green. The build ships the Strata favicon, and the JS bundle carries the mark path. The header renders tile, wordmark, separator, project name and meta line, with three segmented tabs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Snapshot status pill absorbs the refresh control (relative time, dot states, same mutation contract)</name>
  <files>src/presentation/shell-header.ts, src/web/components/snapshot-status.tsx, src/web/components/refresh-control.tsx (delete), src/web/components/app-shell.tsx, src/web/styles/globals.css, test/web/refresh-contract.test.ts, test/web/navbar-contract.test.ts</files>
  <read_first>src/web/components/refresh-control.tsx, test/web/refresh-contract.test.ts (every assertion, including the negative ones), globals.css lines 344-480, 1559-1721 and 3183-3210</read_first>
  <behavior>
    - formatRelativeReadAt(readAt, nowMs) returns null for an unparseable readAt.
    - It returns 'just now' when the gap is under 60s, including a future readAt from clock skew or a just-completed refresh.
    - Otherwise it returns '{m}m ago' under 60 minutes, '{h}h ago' under 24 hours, and '{d}d ago' beyond that, using floored units.
    - formatReadAt is moved unchanged: an invalid date returns the raw string, and a valid one returns toLocaleString().
    - snapshot-status.tsx keeps the former control's contract:
      - a native `<Button>` with onClick calling mutation.mutate()
      - disabled and aria-disabled bound to the pending flag
      - the aria-label literal "Refresh the project snapshot"
      - invalidateQueries() with no arguments, and window.scrollTo({ top: 0 }) on success
      - the fixed toast copy containing "Showing the last successful read from"
      - data-snapshot-read-at, and a `<time dateTime=...>` with no tabIndex
      - the strings 'Refreshing…' and 'Snapshot read'
    - The old refresh-control module no longer exists, and no src file imports it.
    - CSS: the refreshing dot uses the loading-pulse animation, a prefers-reduced-motion rule sets it to none, and the error state uses var(--destructive). No rule anywhere sets display:none on a `.snapshot-` selector.
  </behavior>
  <action>
RED first. Extend test/web/navbar-contract.test.ts with the relative-time, formatReadAt and pill cases from `<behavior>`. The deletion check uses node:fs existsSync on src/web/components/refresh-control.tsx, plus a scan that no .tsx under src/web imports it.

Retarget test/web/refresh-contract.test.ts so every existing guarantee now reads src/web/components/snapshot-status.tsx instead of the deleted file:
- The Phase-4 D-01 test asserts that app-shell.tsx renders `<SnapshotStatus` inside the `.shell-controls` block. It keeps the single-ToastProvider count, and asserts that snapshot-status.tsx contains `className="snapshot-status"` wrapping the Button.
- The Phase-4 D-02 strings test reads snapshot-status.tsx.
- The lowercase-word check runs over BOTH app-shell.tsx and snapshot-status.tsx.
- The keyboard and structure tests and the `<time>` tabIndex test read snapshot-status.tsx.
- Replace the CR-02 test, which was anchored on the first `.snapshot-status {` after the 62rem media query and would now be fragile. The new test extracts every rule whose selector contains `.snapshot-` from the whole stylesheet, media queries included, and asserts none declares display: none. It keeps the CR-02 comment explaining why: the pill must stay in the accessibility tree at every width.
- Leave the toast-viewport test and the dependency-set test exactly as they are.
- Add a one-line header note that quick-260911-243 merged RefreshControl into SnapshotStatus.

Run the suite and see it fail. Then GREEN:

1. Move formatReadAt out of app-shell.tsx into src/presentation/shell-header.ts, unchanged. Add formatRelativeReadAt(readAt: string, nowMs: number): string | null.

2. Create src/web/components/snapshot-status.tsx exporting `SnapshotStatus({ readAt, phase })`, where readAt is string | null and phase is 'pending' | 'error' | 'ready'.
   - Move postRefresh, RefreshResponse and the whole useMutation block from refresh-control.tsx verbatim, keeping their existing rationale comments: Phase-4 D-03/D-04/D-05, same toast title, same fixed description built with formatReadAt(readAt), falling back to 'the previous read'.
   - Derive `isRefreshing = mutation.isPending`.
   - Add a `now` state initialised lazily with Date.now(). A `RELATIVE_TICK_MS = 30_000` setInterval in a useEffect updates it, and the cleanup clears the interval. Only call setState inside the interval callback, never synchronously in the effect body, so react-hooks v7 lint passes.
   - Compute `state`: 'refreshing' when isRefreshing, otherwise the phase prop.
   - Render `<div className="snapshot-status">` containing:
     - a `<Button variant="ghost" size="sm" type="button" className="snapshot-pill">` with:
       - data-state={state}
       - onClick calling mutation.mutate()
       - disabled and aria-disabled from isRefreshing
       - aria-label "Refresh the project snapshot"
       - aria-describedby pointing at the label span's useId id
       - data-snapshot-read-at={readAt ?? undefined}
       - a title tooltip: ready gives `Snapshot read {formatReadAt(readAt)} — click to refresh`, refreshing gives 'Refreshing the project snapshot', error gives 'Snapshot metadata unavailable — click to retry', pending gives 'Reading snapshot…'
       - inside it, `<span className="snapshot-dot" aria-hidden="true" />` then `<span className="snapshot-label" id=...>`. The label shows 'Reading snapshot…', 'Metadata unavailable' or 'Refreshing…', or for ready: "Read " followed by `<time dateTime={readAt}>`. The time holds formatRelativeReadAt(readAt, now), falling back to formatReadAt(readAt) when that is null.
     - a sibling `<span className="sr-only" aria-live="polite">`. It announces only the non-ticking state text: 'Reading snapshot…', 'Snapshot metadata unavailable', 'Refreshing…', or `Snapshot read {formatReadAt(readAt)}`. The 30s relative ticks must never re-announce.
   - Per Phase-4 D-02, the text swaps in place inside one element. No node is added or removed on state change, and the header height never changes.
   - No bespoke keyboard handler and no render-prop polymorphism on this Button. A native button already gives Enter and Space.

3. app-shell.tsx:
   - Delete the lifted isRefreshing state, the old `.snapshot-status` markup, the Radio and RefreshControl imports and the local formatReadAt.
   - Render `<SnapshotStatus readAt={presentation.data?.readAt ?? null} phase={presentation.isPending ? 'pending' : presentation.isError ? 'error' : 'ready'} />` as the FIRST child of `.shell-controls`, followed by the search component and then ThemeToggle.
   - Keep the shell-notice alert and fetchPresentation export exactly as they are.

4. Delete src/web/components/refresh-control.tsx.

5. globals.css:
   - Delete the dead rules: `.snapshot-status > svg`, `.snapshot-status small`, `.snapshot-status > span` and `.snapshot-status time`; the snapshot-specific rules in both media blocks; `.snapshot-error` (keep `.shell-notice`); `.refresh-control`, `.refresh-icon` and `.refresh-icon.spinning`; `@keyframes refresh-spin` and its reduced-motion block.
   - Add:
     - .snapshot-status: flex, align center, min-width 0
     - .snapshot-pill:
       - gap var(--space-2), padding-inline var(--space-2-5), 1px solid var(--border), square corners
       - var(--font-mono), var(--fs-1), var(--ls-normal), text-transform none, color var(--muted-foreground). This overrides the Button variant's uppercase and tracking.
       - on :hover, border-color var(--ring) and color var(--foreground)
     - .snapshot-pill[data-state='refreshing']: opacity 1, so the disabled pill stays legible
     - .snapshot-dot: width and height var(--space-2), flex none, background var(--primary)
     - `[data-state='pending'] .snapshot-dot`: background var(--muted-foreground)
     - `[data-state='refreshing'] .snapshot-dot`: animation loading-pulse, about 0.8s ease-in-out infinite alternate
     - `.snapshot-pill[data-state='error']`: color var(--destructive), border-color var(--destructive-border)
     - `[data-state='error'] .snapshot-dot`: background var(--destructive)
     - .snapshot-label: single-line ellipsis, min-width 0
     - .snapshot-label time: color var(--foreground), font-variant-numeric tabular-nums
     - an explicit `@media (prefers-reduced-motion: reduce)` rule setting the refreshing dot's animation to none
   - Everything must pass token-guard.

6. Run the target tests, typecheck and lint until green.
  </action>
  <verify>
    <automated>npx vitest run test/web/refresh-contract.test.ts test/web/navbar-contract.test.ts test/token-guard.test.ts test/web/shell-contract.test.ts test/web/invalid-project-contract.test.ts test/web/build-splitting.test.ts && npm run typecheck && npm run lint</automated>
  </verify>
  <done>The pill renders the dot plus "Read {relative}" with the exact-time tooltip, and clicking it refreshes with the unchanged mutation, toast and invalidation contract. The dot pulses while refreshing (static under reduced motion) and turns destructive on metadata error. The old refresh-control module is gone. All retargeted refresh-contract guarantees pass against snapshot-status.tsx.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Search becomes a ⌘K dialog over a full-screen scrim; full-suite plus live responsive/dual-theme verification</name>
  <files>src/presentation/shell-header.ts, src/web/components/search-field.tsx, src/web/components/app-shell.tsx, src/web/styles/globals.css, test/web/visual-contract.test.ts, test/web/navbar-contract.test.ts</files>
  <read_first>src/web/components/search-field.tsx, test/web/visual-contract.test.ts lines 449-492 (every search pin, including the negative ones), globals.css lines 53-133 and 2759-2888, node_modules/@base-ui/react/combobox/root/AriaCombobox.d.ts lines 160-180 (inline plus Dialog composition note)</read_first>
  <behavior>
    - isSearchShortcut({key, metaKey, ctrlKey, altKey, shiftKey}) is true for key 'k' or 'K' with metaKey or ctrlKey, and false with altKey or shiftKey, with no modifier, or for any other key.
    - shortcutHintFor('MacIntel'), 'iPhone' and 'iPad' return '⌘K'. 'Win32', 'Linux x86_64' and '' return 'Ctrl K'.
    - isEditableTarget({tagName, isContentEditable}) is true for INPUT, TEXTAREA and SELECT, and for isContentEditable true. It is false for BUTTON and A, and for null.
    - search-field.tsx imports from '@base-ui/react/dialog' and renders Dialog.Backdrop, Dialog.Viewport, Dialog.Popup and a Dialog.Title. Combobox.Root carries `inline`. The trigger carries aria-keyshortcuts. A window keydown listener is added in an effect and removed in its cleanup. The existing query machinery is unchanged: DEBOUNCE_MS, DROPDOWN_LIMIT = 8 via .slice(0, DROPDOWN_LIMIT), the queryFn receiving the TanStack signal, and refetchInterval polling while status is 'building'.
    - CSS: `--overlay-scrim` is declared in both :root and .dark with different values, and `.search-dialog-backdrop {` uses var(--overlay-scrim). `.search-dialog-results {` has max-height: min( and overflow-y: auto. `.search-dialog-item .search-result-path {` keeps direction: rtl and text-overflow: ellipsis. The 42rem media section hides `.search-trigger-kbd`. The old .search-field and .search-dropdown rules are gone.
  </behavior>
  <action>
RED first:
- Add the helper cases and the dialog/token source contracts to test/web/navbar-contract.test.ts.
- In test/web/visual-contract.test.ts, rename the "header search dropdown" describe to "header search dialog". Retarget Test 4's selector to `.search-dialog-item .search-result-path {` and Test 5's to `.search-dialog-results {`, keeping their assertions identical. Leave Tests 1-3 and the raw-HTML test untouched, since they still pin search-field.tsx.
- See it fail. Then GREEN:

1. Add isSearchShortcut, shortcutHintFor and isEditableTarget to src/presentation/shell-header.ts. Give them structural parameter types so they stay DOM-free.

2. Rewrite src/web/components/search-field.tsx, keeping the path so the existing pins still apply. It exports `SearchDialog`; rename every import site.
   - Keep verbatim: DEBOUNCE_MS = 160, DROPDOWN_LIMIT = 8 with the same slice, fetchSearch, the debounce effect, and the useQuery (same key ['search','dropdown',trimmed], AbortSignal-driven queryFn, enabled guard, 500ms refetchInterval while building). Keep their existing Phase-3 comments.
   - Open state: `open` plus a `handleOpenChange(next)` that sets it. On close it also clears query and debounced and resets a `highlightedRef`, all inside the handler and never in an effect.
   - Global shortcut. A useEffect registers a window keydown listener and its cleanup removes it. The listener returns early when `event.repeat`, when `event.defaultPrevented`, or when isSearchShortcut is false.
     The documented rule: a ⌘K/Ctrl+K typed inside some other editable field (isEditableTarget true and not inside the dialog popup, checked via a popupRef contains test) is left alone. Inside the dialog's own input it toggles the dialog closed. Otherwise the listener calls preventDefault and toggles the dialog.
     Write this rule as a short code comment.
   - Trigger: `Dialog.Trigger` styled as a ghost Button via the exported `buttonVariants({ variant: 'ghost', size: 'sm' })` plus class `search-trigger`, so no nested render composition is needed. If that is awkward, `render={<Button …/>}` is acceptable.
     It carries aria-label "Search planning artifacts" and aria-keyshortcuts "Meta+K Control+K". Its contents are a `<Search aria-hidden="true" />` icon and a `<kbd className="search-trigger-kbd" aria-hidden="true">`. The kbd holds shortcutHintFor(navigator.platform).
   - Dialog: `Dialog.Root open onOpenChange={handleOpenChange}` (modal default), then `Dialog.Portal` holding a `Dialog.Backdrop className="search-dialog-backdrop"` and a `Dialog.Viewport className="search-dialog-viewport"`.
     Inside the viewport, `Dialog.Popup ref={popupRef} className="search-dialog" initialFocus={inputRef}` contains a `Dialog.Title className="sr-only"` reading "Search planning artifacts".
     Inside the popup, `Combobox.Root inline open={open} onOpenChange={handleOpenChange}` has items and filteredItems set to hits, filter={null}, and onInputValueChange={setQuery}. onItemHighlighted stores the hit, or null, in highlightedRef.
     - `Combobox.InputGroup className="search-dialog-field"` holds a Search icon, `Combobox.Input ref={inputRef} className="search-dialog-input"` (placeholder "Search plans, phases, decisions…", aria-label "Search planning artifacts") and an Esc hint kbd.
     - The input's onKeyDown handles Enter only. It calls event.preventDefault() and event.preventBaseUIHandler(), so Base UI never also fires the item and navigation happens exactly once. It navigates to highlightedRef.current.url when a row is highlighted. Otherwise it commits to the search page, built from presentationRoutePatterns.search with an encoded query, never a literal path string. Then it closes via handleOpenChange(false).
   - Results region: `div.search-dialog-results` holds the same state ladder as today:
     - error: the destructive notice with role alert, "Search is unavailable."
     - indexing: the aria-live status "Indexing…" with the auto-resolve hint
     - empty: `Combobox.Empty` "No matches"
     - otherwise `Combobox.List`, whose rows are `Combobox.Item` with the value hit, render `<Link to={hit.url} />`, className "search-dialog-item", onClick closing the dialog, and the .search-result-title and .search-result-path spans
     - when the trimmed query is empty: a short hint line plus a key legend (↑↓ move, Enter open, Esc close)
   - Keep the "See all {total} {total === 1 ? 'result' : 'results'}" footer Link and its presentationRoutePatterns.search href expression byte-identical. Add a class "search-dialog-footer" and an onClick that closes the dialog.
   - Dialog.Root handles Esc and closes on it.

3. app-shell.tsx: render `<SearchDialog />` between SnapshotStatus and ThemeToggle in .shell-controls.

4. globals.css (NAV-04 and NAV-06):
   - New tokens:
     - palette `:root`: `--overlay-scrim: color-mix(in oklch, var(--foreground) 38%, transparent);`
     - `.dark`: `--overlay-scrim: color-mix(in oklch, var(--background) 72%, transparent);` so the scrim darkens in both themes
     - scale `:root`: `--dialog-offset-top: 12vh; --dialog-max-width: 40rem; --dialog-results-max-height: 28rem;`. These are deliberately not `--space-` prefixed, since the step test only accepts rem multiples. Each value must be unique against every other `:root` token.
     - Scale tokens are not mirrored into index.html.
   - Delete every `.search-field*` and `.search-dropdown*` rule.
   - Add:
     - .search-trigger: gap var(--space-2), color var(--muted-foreground); hover to var(--foreground)
     - .search-trigger-kbd: 1px solid var(--border), padding var(--space-0-5) var(--space-1), var(--font-mono), var(--fs-1), var(--ls-normal), muted colour
     - .search-dialog-backdrop: position fixed, inset 0, z-index 70, background var(--overlay-scrim)
     - .search-dialog-viewport: position fixed, inset 0, z-index 71, display flex, justify-content center, align-items flex-start, padding var(--dialog-offset-top) var(--space-4) var(--space-4)
     - .search-dialog: width min(var(--dialog-max-width), 100%), display grid, 1px solid var(--border), background var(--popover), color var(--popover-foreground), box-shadow var(--shadow-popover), square corners
     - .search-dialog-field: flex, gap var(--space-3), padding var(--space-3-5) var(--space-4), bottom hairline, muted icon; `:focus-within` gives an inset var(--ring) bottom border and foreground colour, so focus stays visible in both themes
     - .search-dialog-input: flex 1, min-width 0, border 0, transparent background, inherit colour, var(--font-sans), var(--fs-fluid-1); :focus outline none, since the field's focus-within treatment replaces it
     - .search-dialog-results: max-height min(var(--dialog-results-max-height), calc(100vh - var(--dialog-offset-top) - var(--space-28))), overflow-y auto
     - .search-dialog-item: grid, gap var(--space-0-5), top hairline, padding var(--space-2-5) var(--space-4); [data-highlighted] gives var(--state-active) with `inset 3px 0 var(--primary)`
     - `.search-dialog-item .search-result-path {`: copy the rtl head-truncation declarations from the old dropdown rule
     - status, empty, notice, hint, legend and footer rules on --space and --fs tokens
     - inside the 42rem media block, hide .search-trigger-kbd with display none
   - Base UI exposes data-starting-style and data-ending-style for an optional fade. The global reduced-motion block already neutralises it.
   - Everything must pass token-guard.

5. Full gate: npm test, npm run typecheck, npm run lint and npm run build must all pass.

6. Live verification. Start `npm start -- fixtures/dense --port 4211` in the background. Poll http://127.0.0.1:4211/api/presentation until it answers, and never touch port 4210.
   Run a throwaway playwright-core script from your scratch directory, never committed, using the paths in `<context>`. Seed the theme with an addInitScript that sets localStorage 'labelore-theme' to 'light' or 'dark'. For each viewport width 1440, 820 and 400, in each theme:
   (a) screenshot `.shell-header`
   (b) assert document.documentElement.scrollWidth <= window.innerWidth
   (c) at 820 and 400, assert the header has at most two rows: .brand and .shell-controls share a top within 4px, and .shell-nav's top is at or below .brand's bottom
   (d) assert three `.shell-nav-link` elements, `.brand-meta` text exactly "v3.0 · Phase 1/2 · 67%", and a `.brand` title ending in "fixtures/dense"
   (e) assert `.snapshot-pill time` has a dateTime attribute and the pill title contains "Snapshot read"
   Then, at 1440 and 400 in both themes:
   - press Control+K and assert `.search-dialog` is visible and `.search-dialog-backdrop` covers the viewport
   - type "phase" and wait for a `.search-dialog-item`
   - press ArrowDown and assert an item has data-highlighted
   - screenshot the full page with the dialog open
   - press Escape and assert the dialog is gone
   - reopen, type "phase", press Enter with nothing highlighted, and assert the URL path is /search with q=phase and the dialog closed
   Finally, click `.snapshot-pill` and assert the label passes through or settles at "Read just now". Kill only the 4211 server.
   Record every assertion result and screenshot path in the SUMMARY for end-of-phase human review.
  </action>
  <verify>
    <automated>npm test && npm run typecheck && npm run lint && npm run build</automated>
    <human-check>Screenshots from step 6, covering the header at 1440/820/400px in light and dark plus the open search dialog at 1440 and 400px in both themes. The Strata tile, the LABELORE / project lockup and the meta line read cleanly. The active tab shows only an underline. The pill dot sits in primary. The dialog is centred near the top over a dark scrim in both themes. All corners are square. The 400px header is two rows with no overflow.</human-check>
  </verify>
  <done>The ⌘K/Ctrl+K shortcut and the icon button open a centred modal search over a themed scrim. Debounced, abortable ranked results, keyboard highlight, Enter to the row or /search?q=, "See all N results", and the indexing, error and empty states all work, and the dialog closes on Esc, selection or navigation. The full suite, typecheck, lint and build pass. The live script's assertions pass at every width in both themes, with screenshots recorded in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| target .planning corpus → header DOM | projectName, milestone, phaseNumber, rootPath, search hit titles and paths all come from files in an arbitrary target project, so they are untrusted text |
| browser → local API | POST /api/refresh and GET /api/search on the loopback-only Hono server. Neither is changed by this plan; no server file is modified |
| global keydown listener ↔ other page inputs | a window-level ⌘K/Ctrl+K handler could intercept keystrokes meant for other editable fields |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-243-01 | Tampering (markup injection) | app-shell.tsx brand/meta, snapshot-status.tsx, search-field.tsx results | medium | mitigate | Corpus strings render only as React text children or attributes, never as raw HTML. visual-contract's existing raw-HTML pin on search-field.tsx is kept, and navbar-contract adds the same pin for app-shell.tsx and snapshot-status.tsx |
| T-243-02 | Denial of Service | global keydown listener in search-field.tsx | low | mitigate | The listener is registered in a useEffect whose cleanup removes it (asserted in navbar-contract), so there are no duplicate subscriptions across re-renders or StrictMode double-mount |
| T-243-03 | Elevation / UI redress | ⌘K/Ctrl+K interception | low | mitigate | isEditableTarget rule: the shortcut is ignored inside any editable field other than the dialog's own input (unit-tested). Ignored on event.repeat and defaultPrevented |
| T-243-04 | Denial of Service | pill-triggered POST /api/refresh | low | mitigate | The Button stays disabled and aria-disabled while the mutation is pending, preserving the existing Phase-4 guard, so no concurrent refresh storm |
| T-243-05 | Information Disclosure | rootPath shown in the brand title tooltip | low | accept | A loopback-only personal tool. The path is the user's own CLI argument and is already shown on the invalid-project screen |
| T-243-06 | Tampering | read-only invariant | low | mitigate | No server, planning-fs or planning-repo file is in files_modified. The pill reuses the existing refresh endpoint and no new endpoint or write path is added |
| T-243-SC | Tampering | npm installs | low | mitigate | No package is installed. refresh-contract's dependency-set test (PHASE_3_DEPENDENCY_KEYS) stays unchanged and fails on any new dependency. `npm ci` (precondition) installs only lockfile packages |
</threat_model>

<verification>
- `npm test` passes, including test/token-guard.test.ts (zero violations, no stale allowlist entries, every scale token consumed), test/web/navbar-contract.test.ts (new), and the retargeted test/web/refresh-contract.test.ts and test/web/visual-contract.test.ts.
- `npm run typecheck`, `npm run lint` and `npm run build` pass. dist/favicon.svg carries the Strata path.
- The live playwright pass against fixtures/dense on port 4211 records every assertion from Task 3 step 6 as passing, with screenshots listed in the SUMMARY.
- Source audit, with every source item covered:

| Source | Item | Covered by |
|--------|------|------------|
| GOAL/REQ | NAV-01 Strata mark as shared component, header tile, favicon, any other brand spot | Task 1 |
| GOAL/REQ | NAV-02 lockup, meta line with graceful omission, name fallback, rootPath tooltip | Task 1 |
| GOAL/REQ | NAV-03 same three tabs, segmented, --primary underline, no filled block | Task 1 |
| GOAL/REQ | NAV-04 icon button, ⌘K/Ctrl K hint, Dialog with scrim, debounce/abort, indexing/error/empty, ranked results, keyboard nav, Enter → /search?q=, See all N | Task 3 |
| GOAL/REQ | NAV-05 pill with dot, relative time, exact-time tooltip, `<time dateTime>`, click-to-refresh merged, toast/invalidate/aria-label kept, animated dot, destructive error | Task 2 |
| GOAL/REQ | NAV-06 tokens only, named scrim/dialog tokens, token-guard green, light and dark, square corners, 400px | Tasks 1-3 CSS + Task 3 live pass |
| GOAL | update pinned header tests only as needed, add coverage for new pieces | Tasks 1-3 (navbar-contract new; refresh-contract and visual-contract retargeted) |
| CONTEXT | D-01 Strata mark, exact path | Task 1 |
| CONTEXT | D-02 no new tabs | Task 1 (three-tab assertion) |
| CONTEXT | design intent: kbd hidden when narrow, Esc closes, documented shortcut rule, 30s tick, reduced motion, data-snapshot-read-at, Phase-4 D-02 no layout shift | Tasks 2-3 |
| Excluded | Lore tab (declined, explicitly out of scope) | not planned |
</verification>

<success_criteria>
- Every must_haves truth holds, observed live against fixtures/dense in both themes at 1440/820/400px.
- No lucide glyph stands in for the brand. One LabeloreMark component plus the favicon carry the Strata path.
- The refresh contract (unfiltered invalidate, scroll-top, fixed toast copy, disabled while pending, aria-label, data-snapshot-read-at) is unchanged in behaviour and now lives in snapshot-status.tsx.
- The search keeps every Phase-3 behaviour pinned by visual-contract Tests 1-3 and the raw-HTML pin, inside a modal dialog.
- token-guard is green, with the new tokens named and declared in :root and .dark where theme-dependent.
</success_criteria>

<output>
Create `/home/cinedise/gsd-lore/.planning/quick/260911-243-navbar-redesign-strata-logo-mark-everywh/260911-243-SUMMARY.md` when done. It must list the live-verification assertion results and every screenshot path for end-of-phase human review.
</output>
