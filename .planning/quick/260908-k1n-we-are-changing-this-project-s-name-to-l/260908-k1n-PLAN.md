---
quick_id: 260908-k1n
type: quick
description: Rename the project and product to Labelore everywhere
status: planned
tasks: 3
---

# Quick Task: Rename the project to Labelore

Replace the old product identity across runtime code, package metadata, tests, fixtures, active and
historical project documentation. Replace the book logo with a label-shaped mark and add a matching
SVG favicon. The physical checkout directory is outside repository-controlled content and remains
managed by the workspace; the already-renamed `labelore.git` remote stays unchanged.

## Task 1 — Product identity and visual mark

**Files:** `package.json`, `package-lock.json`, `index.html`, `public/favicon.svg`,
`src/web/components/app-shell.tsx`, `src/web/main.tsx`, `src/web/components/theme-toggle.tsx`,
`src/server/index.ts`, and any source/test files containing the old identity.

**Action:** Rename display strings to `Labelore`, machine/package/storage identifiers to `labelore`,
and environment identifiers to `LABELORE`. Replace the shell's book icon with a tag/label mark and
publish the same visual language as the browser favicon. Add favicon metadata and accessible page
branding.

**Verify:** source scan has no old brand variants; package metadata is internally consistent; build,
typecheck, lint, and tests pass.

## Task 2 — Repository documentation and fixture identity

**Files:** `.claude/CLAUDE.md`, `.planning/**/*.md`, fixture planning content, golden snapshots, and
project-local configuration containing the old identity.

**Action:** Rename the project in active and historical documentation so repository search returns one
current identity. Remove the obsolete backlog phase that exists solely to request this rename, while
leaving milestone execution history intact. Update recorded paths, temporary filenames, theme keys,
and test expectations consistently.

**Verify:** a repository-wide text scan excluding dependency/build/git internals finds no `Labelore`,
`labelore`, `LABELORE`, or `labelore` occurrence.

## Task 3 — Browser verification

**Files:** no additional product files expected.

**Action:** Build and open representative light/dark desktop/mobile surfaces. Confirm the favicon link,
page title, shell brand, label logo, startup banner, smoke output, and existing routes remain correct.

**Verify:** visual screenshots and computed checks confirm `Labelore` and the new mark; no overflow,
console errors, or failed requests.
