---
phase: quick-260910-jz8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/web/styles/globals.css
  - index.html
  - test/token-guard.test.ts
  - test/web/visual-contract.test.ts
autonomous: true
requirements: [JZ8-01, JZ8-02, JZ8-03, JZ8-04]

estimate:
  tokens: 170000
  raw_tokens: 170000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "Every padding/margin/gap/inset/top/right/bottom/left value in globals.css outside the token blocks is 0, auto, or a var() of a --space-* / --space-fluid-* token, except named allowlist entries (JZ8-01)"
    - "Every font-size, line-height, letter-spacing, font-weight and font-family value outside the token blocks is a token var(), except named em-relative allowlist entries; no static type step or rendered size is below 10px (JZ8-02)"
    - "No usage-site colour repeats a named token's recipe, no unnamed color-mix recipe recurs, no two palette tokens hold the same value in both themes, .dark repeats nothing :root already says, and the research-confirmed dead tokens are gone from globals.css and index.html (JZ8-03)"
    - "npm test fails when a raw spacing, type or colour literal is reintroduced outside a token block — a planted-fixture positive control proves each family is caught and a clean fixture passes (JZ8-04)"
    - "Tailwind's reserved theme namespaces (--spacing, --text-*, --font-weight-*, --tracking-*, --leading-*) are never declared, so button.tsx renders exactly as before"
    - "Rendered sweep in both themes: distinct sizes, space, lh, weights and fams all fall below the baseline, fams is exactly 2, no new off-palette colour appears, and no rendered font size is below 10px"
  artifacts:
    - path: "test/token-guard.test.ts"
      provides: "Declaration scanner + allowlist + planted-fixture positive control for spacing, type and colour literals"
      contains: "positive control"
    - path: "src/web/styles/globals.css"
      provides: "Theme-independent scale block (--space-*, --fs-*, --lh-*, --ls-*, --fw-*, fluid tokens), named colour recipes, deduped palette"
      contains: "--space-4: 1rem"
    - path: "index.html"
      provides: "Pre-hydration token snapshot mirroring every palette edit byte-for-byte"
      contains: "--font-mono"
  key_links:
    - from: "test/token-guard.test.ts"
      to: "src/web/styles/globals.css"
      via: "readFile + declaration scanner run against the live stylesheet"
      pattern: "styles/globals.css"
    - from: "index.html"
      to: "src/web/styles/globals.css"
      via: "snapshot-parity assertions in test/web/focus-ring-contrast.test.ts (snapshot tokens must equal live :root/.dark tokens)"
      pattern: "keeps every"
    - from: "src/web/pages/artifact-page.tsx"
      to: "src/web/styles/globals.css"
      via: "getPropertyValue of --font-sans, --background, --card, --foreground, --border, --secondary, --muted (Mermaid theming)"
      pattern: "getPropertyValue"
---

# Quick 260910-jz8: Centralize spacing, type and colour tokens with a guard check

<objective>
User request (verbatim): "we need proper spacing scale and type scale. also, if any color has been used without a name and exists in the source, make it use the source's name. also add a check."

The scope has four parts, and all are required:
- JZ8-01: a spacing scale, with every padding/margin/gap/inset value in `src/web/styles/globals.css` migrated onto it.
- JZ8-02: a type scale. Font-size steps with paired line-heights, letter-spacing steps, weight tokens, and `--font-mono`. It keeps the `--font-size-micro-label` semantics and the 10px floor.
- JZ8-03: colour naming. Duplicated recipes use the existing name, recurring recipes get a name, identical tokens are deduped, dead tokens are deleted, and the stale comment is fixed.
- JZ8-04: an automated vitest guard, modelled on `test/portability.test.ts`.

Locked decisions (orchestrator, NON-NEGOTIABLE):
- **D-01 (A1):** both weight-650 declarations (`.requirement-list` link area near line 2161, `.artifact-metadata` summary near line 2248) snap to 600 and become `var(--fw-semibold)`. The research suggested 700; this plan uses 600 per the orchestrator decision.
- **D-02 (A2):** the three hard-coded system-monospace stacks (`.artifact-path`, `.metadata-record dt`, `.plan-section-label`) migrate to `var(--font-mono)`, which puts JetBrains Mono first and then the system mono fallbacks. The visible font change is accepted.
- **D-03:** every value snaps to the nearest step of the defined scale. No redesign. Squared corners stay unchanged. Both themes are covered.

Planner discretion. Record each item under "Decisions for the user" in the SUMMARY:
- **P-01, spacing scale.** The scale is aligned to Tailwind's 0.25rem grid, with 0.125rem half-steps below 1rem. It is named `--space-N` = N × 0.25rem, so `--space-1-5` is Tailwind `gap-1.5` and `--space-6` is `px-6`.
  - This deviates from the research's 0.05rem unit. That unit keeps almost every ad hoc value (0.3/0.35/0.45/0.55/0.65rem) as its own token, so it is a lookup table rather than a scale, and rendered distinct spacing would barely drop.
  - Maximum snap drift is 1.2px below 1rem and 2.4px above.
- **P-02, type scale.** Six static steps on a near-constant ratio of about 1.09 (0.64 / 0.7 / 0.76 / 0.82 / 0.9 / 1rem), plus 1.3rem.
  - The dominant 0.78rem body size moves to 0.76rem (−0.32px).
  - The 13 fluid `clamp()` font sizes consolidate to 8 named tokens. The research's 9-step table kept 0.68/0.7 and 0.78/0.82 as separate steps 0.3–0.6px apart.
- **P-03, scroll-margin out of scope.** `scroll-margin-top` (anchor-scroll offsets tuned to the sticky header) stays outside both the migration and the guard. The user named padding/margin/gap/inset, and `test/web/visual-contract.test.ts` pins `scroll-margin-top: 6rem;`.
- **P-04, hero label floor.** The `.position-copy h1 span` label (0.2em of a clamp whose minimum is 2.8rem) renders 8.96px at narrow widths, which is under the floor. It becomes `max(0.2em, var(--fs-1))`, the same kind of floor fix as 0.58rem.
- **P-05, alias direction.** `--state-active` and `--state-hover` become aliases of `--sidebar-primary` and `--sidebar-accent`, the direction the research recommended. The sidebar names are already in the index.html snapshot, so no snapshot entry is invented.

Purpose: one scale per axis, every usage site on a named step, and a check that keeps it that way.

Output:
- `globals.css` with a scale block, migrated usage sites, and a deduped palette.
- `index.html` snapshot mirrored to match.
- The new `test/token-guard.test.ts`.
- Two intent-preserving assertion updates in `test/web/visual-contract.test.ts`.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.claude/CLAUDE.md
@.planning/quick/260910-jz8-centralize-spacing-type-and-color-tokens/260910-jz8-RESEARCH.md
@test/portability.test.ts
@test/web/focus-ring-contrast.test.ts

Large files. Do NOT read them whole. Grep for a selector, then Read with offset/limit.
- `src/web/styles/globals.css` (3,271 lines). The palette `:root` is at lines 67-121, the palette `.dark` at 123-156, `@theme inline` at 24-65, and the `--table-zebra` `:root`/`.dark` pair at about 2468-2475.
- `test/web/visual-contract.test.ts`. Only the task-list block (about 344-361), the link-weight block (about 166-178) and the micro-label block (about 700-760) matter here.
- `index.html`. The token snapshot is in the `<style>` block at about lines 21-100.

Mutable-scope rule: every line number and count in this plan was observed on master at 1c3e3ec at planning time, and serves as a pointer only. Locate rules by selector. The guard's own violation list, generated live in your worktree, is the authoritative worklist.
</context>

## Token Specification

Everything below is referenced by the task actions. Values are exact.

### Placement
- **New scale block.** Add ONE new top-level `:root {` block directly after the palette `.dark { … }` block and before `@layer base`. Head it with a comment saying it holds theme-independent scale tokens and is intentionally not mirrored into index.html. It must never contain the text `font-size:`, not even in a comment: visual-contract has a test that forbids any `font-size:` inside a `:root`/`html`/`body` block. Do NOT put scale tokens in the palette `:root`, and do NOT put them in the `--table-zebra` `:root` block (visual-contract requires `--table-zebra` to be that block's first declaration).
- **Tailwind namespaces.** Never declare `--spacing`, `--spacing-*`, `--text-*`, `--font-weight-*`, `--tracking-*` or `--leading-*`. These are Tailwind namespaces that `button.tsx` depends on. Scale tokens are plain custom properties and stay out of `@theme`.

### Spacing scale (JZ8-01, P-01)
- **Steps.** `--space-0-5: 0.125rem`, `--space-1: 0.25rem`, `--space-1-5: 0.375rem`, `--space-2: 0.5rem`, `--space-2-5: 0.625rem`, `--space-3: 0.75rem`, `--space-3-5: 0.875rem`, `--space-4: 1rem`, `--space-5: 1.25rem`, `--space-6: 1.5rem`, `--space-7: 1.75rem`, `--space-8: 2rem`, `--space-10: 2.5rem`, `--space-12: 3rem`, `--space-22: 5.5rem` (the sticky `.document-outline` offset), `--space-28: 7rem`.
- **Snap rule.** Convert to px at 16px. Below 1rem, go to the nearest multiple of 2px. From 1rem up, go to the nearest multiple of 4px. 5.5rem and 7rem map exactly. A non-zero value never snaps to 0, so sub-2px values go to `--space-0-5`. A negative value becomes `calc(-1 * var(--space-N))`.
- **Planning-time snap map:**

| Observed rem values | Token |
|---|---|
| 0.05, 0.08, 0.12, 0.15, 0.18 | `--space-0-5` |
| 0.2, 0.24, 0.25, 0.3 | `--space-1` |
| 0.35, 0.38, 0.42 | `--space-1-5` |
| 0.45, 0.5, 0.55 | `--space-2` |
| 0.6, 0.65 | `--space-2-5` |
| 0.7, 0.75, 0.8 | `--space-3` |
| 0.85, 0.9 | `--space-3-5` |
| 1, 1.05, 1.1 | `--space-4` |
| 1.15, 1.2, 1.25, 1.35 | `--space-5` |
| 1.4, 1.5, 1.55 | `--space-6` |
| 1.75, 1.8 | `--space-7` |
| 2 | `--space-8` |
| 2.5 | `--space-10` |
| 3 | `--space-12` |
| 5.5 | `--space-22` |
| 7 | `--space-28` |

  Any value not listed follows the rule. Define only the steps that are actually consumed.
- **Task-list pair.** The task-list padding (`padding-left: 1.55rem`) and its checkbox hang (`margin-left: -1.55rem`) both use `--space-6`, so they move together.
- **Fluid spacing.** Every `clamp()` in a spacing property moves into a `--space-fluid-N` token in the scale block, numbered in ascending order of the max endpoint. The clamp literal lives only in the token definition.
  - Merge clamp shapes greedily, starting from the most-used shape. Two shapes merge when their vw slopes are within 1vw and both endpoints are within 0.25rem.
  - A merged cluster adopts its most-used member. On a tie, take the member with the larger max.
  - Multi-value shorthands (for example `padding: clamp(…) clamp(…) 7rem`) tokenize each component independently.

### Type scale (JZ8-02, P-02, D-01, D-02)
- **Static font-size steps.** `--fs-1: 0.64rem`, `--fs-2: var(--font-size-micro-label)`, `--fs-3: 0.76rem`, `--fs-4: 0.82rem`, `--fs-5: 0.9rem`, `--fs-6: 1rem`, `--fs-7: 1.3rem`.
  - `--font-size-micro-label: 0.7rem;` stays a literal rem declaration where it is today, in the palette `:root`. visual-contract parses it with a `[\d.]+rem` regex.
  - The six `MICRO_LABEL_SELECTORS` keep `var(--font-size-micro-label)`. Every other 0.7rem site uses `var(--fs-2)`.
- **Font-size snap map.** Ties snap up, for legibility.

| Observed rem values | Token |
|---|---|
| 0.58 (floor fix), 0.63, 0.64, 0.65, 0.66 | `--fs-1` |
| 0.67, 0.68, 0.7, 0.72 | `--fs-2` |
| 0.74, 0.75, 0.76, 0.77, 0.78 | `--fs-3` |
| 0.79, 0.8, 0.82, 0.84, 0.85 | `--fs-4` |
| 0.86, 0.9, 0.92 | `--fs-5` |
| 0.95, 1 | `--fs-6` |
| 1.3 | `--fs-7` |

- **Fluid and display font sizes.** 13 shapes become 8 tokens:

| Token | Value | Absorbs |
|---|---|---|
| `--fs-fluid-1` | `clamp(0.95rem, 2vw, 1.15rem)` | the 0.94/1.4vw/1.02 shape |
| `--fs-fluid-2` | `clamp(1.05rem, 2vw, 1.35rem)` | the 1.08/2vw/1.35 shape |
| `--fs-fluid-3` | `clamp(1.15rem, 2.5vw, 1.65rem)` | — |
| `--fs-fluid-4` | `clamp(1.35rem, 3vw, 2rem)` | — |
| `--fs-display-1` | `clamp(1.8rem, 4vw, 2.7rem)` | the 2/4vw/3 shape |
| `--fs-display-2` | `clamp(2rem, 6vw, 4.5rem)` | — |
| `--fs-display-3` | `clamp(2.8rem, 6vw, 5.75rem)` | the 2.6/7vw/5.6 and 3/7vw/5.5 shapes |
| `--fs-display-narrow` | `clamp(2.65rem, 15vw, 4.3rem)` | — |

- **Line-height steps.** `--lh-display: 0.9`, `--lh-none: 1`, `--lh-tight: 1.2`, `--lh-snug: 1.4`, `--lh-normal: 1.55`, `--lh-relaxed: 1.7`. Ties snap up.

| Observed values | Token |
|---|---|
| 0.8, 0.86, 0.9 | `--lh-display` |
| 0.98, 1 | `--lh-none` |
| 1.1, 1.2, 1.25 | `--lh-tight` |
| 1.3, 1.4, 1.45 | `--lh-snug` |
| 1.5, 1.55, 1.6 | `--lh-normal` |
| 1.65, 1.72, 1.75 | `--lh-relaxed` |

- **Paired line-heights.** Each static step gets a companion `--fs-N--lh` that aliases its default line-height: `--fs-1--lh` and `--fs-2--lh` are `var(--lh-tight)`, `--fs-3` through `--fs-6` are `var(--lh-normal)`, and `--fs-7--lh` is `var(--lh-tight)`.
  - A rule that declares `font-size: var(--fs-N)` together with a line-height that snaps to that pair uses `var(--fs-N--lh)`. Otherwise it uses the `--lh-*` step directly.
  - After migration, delete any companion with zero consumers.
- **Fixed-length line-height.** `line-height: 1.15rem` on `.attention-list .item-kind` is a fixed length.
  - Divide it by the element's resolved font-size and snap the unitless ratio onto `--lh-*`.
  - If the sweep screenshot shows the row misaligned against its icon, keep the length as an allowlist entry and record the reason instead.
- **Letter-spacing steps.** `--ls-tighter: -0.07em`, `--ls-tight: -0.035em`, `--ls-normal: 0`, `--ls-wide: 0.05em`, `--ls-wider: 0.08em`, `--ls-widest: 0.12em`.

| Observed em values | Token |
|---|---|
| −0.08, −0.07, −0.065 | `--ls-tighter` |
| −0.04, −0.035, −0.03 | `--ls-tight` |
| 0.02 | `--ls-normal` |
| 0.04, 0.05, 0.06 | `--ls-wide` |
| 0.08, 0.09 | `--ls-wider` |
| 0.12, 0.13 | `--ls-widest` |

- **Weights.** `--fw-medium: 500`, `--fw-semibold: 600`, `--fw-bold: 700`. The two 650s go to `--fw-semibold` (D-01). The `@font-face` weight-range descriptors are untouched.
- **Mono font (D-02).** Put the `--font-mono` value in the palette `:root`, next to `--font-heading`: `'JetBrains Mono', ui-monospace, 'SFMono-Regular', Menlo, Monaco, Consolas, 'Liberation Mono', monospace`.
  - `--font-heading` becomes `var(--font-mono)`.
  - Add `--font-mono: var(--font-mono);` to `@theme inline`, in the same self-mapping pattern as `--font-sans`/`--font-heading`. Do not put the value inside `@theme inline`, because inline theme variables are not emitted at runtime.
  - Mirror both `:root` lines into the index.html snapshot `:root`.
- **em-relative sizes stay em.** `.position-copy h1 span` becomes `max(0.2em, var(--fs-1))` (P-04). Inline code at `0.86em` (around globals.css:2525) stays `0.86em`. Both are allowlisted by exact selector, property and value.

### Colour (JZ8-03, P-05)
- **Delete dead tokens.** These are research-confirmed, and planning-time grep found no consumer in `src/`, `node_modules/shadcn/dist/tailwind.css` or `tw-animate-css`. Delete them from the palette `:root`, the palette `.dark` and the index.html snapshot:
  - `--chart-1` … `--chart-5`
  - `--accent`, `--accent-foreground`
  - `--sidebar-ring`
  - `--radius`

  Also delete their `@theme inline` mappings: `--color-chart-1..5`, `--color-accent`, `--color-accent-foreground`, `--color-sidebar-ring`, and `--radius-sm` … `--radius-4xl`. Then reword the micro-label comment's "same pattern as --radius above" so it no longer names a deleted token.
- **Aliases in both themes.** `--card-foreground`, `--popover-foreground` and `--sidebar-foreground` become `var(--foreground)` in the palette `:root`. Their values are identical to `--foreground` in both themes.
- **`.dark` redundancies to remove.** Each of these is identical to, or now covered by, `:root` (`.dark` is on `<html>`, so `:root` `var()` recipes resolve with dark values):
  - `--primary-foreground`, `--card-foreground`, `--popover-foreground`
  - `--sidebar-foreground`, `--sidebar-primary`, `--sidebar-primary-foreground`, `--sidebar-accent`
- **State aliases.** `--state-active: var(--sidebar-primary)` and `--state-hover: var(--sidebar-accent)`.
- **Keep literal.** Never alias, rename or delete `--font-sans`, `--background`, `--card`, `--foreground`, `--border`, `--secondary`, `--muted` (read by name in `artifact-page.tsx` for Mermaid) or `--ring` (focus-ring contrast test).
- **Named recipes.** Define these in the palette `:root` under a "named recipes" comment, and replace every usage site:
  - `--card-veil: color-mix(in oklch, var(--card) 94%, transparent)` — `.notice`, `.artifact-metadata`, `.search-result-card`, `.invalid-project-panel`
  - `--destructive-tint: color-mix(in oklch, var(--destructive) 8%, transparent)` — `.discrepancy-callout`, both destructive/warning `.status-chip` tones
  - `--destructive-border: color-mix(in oklch, var(--destructive) 55%, var(--border))` — the same three rules
  - `--primary-tint: color-mix(in oklch, var(--primary) 9%, transparent)` — `.status-chip[data-tone='complete']`, `.search-snippet mark`
  - `--shadow-popover: 0 1rem 2.5rem color-mix(in oklab, var(--foreground) 18%, transparent)` — `.reference-preview`, `.search-dropdown` box-shadows

  Single-use recipes stay inline. In particular, visual-contract pins `color-mix(in oklch, var(--primary) 65%, var(--border))`, `var(--secondary) 70%` and `var(--muted) 55%` in place.
- **Why hoisting is safe.** Planning-time grep found no rule re-declaring a palette token locally. The only custom property declared outside the palette blocks is `--table-zebra`, in its own top-level `:root`/`.dark` pair. Re-confirm this before hoisting.
- **Stale comment.** Fix the comment above `.artifact-document pre` (around line 2489). Dark `--border` resolves to `oklch(1 0 0 / 16%)`, not 10%.

### Sweep procedure (reused by Tasks 1 and 3)
Set `S=/tmp/claude-1000/-home-cinedise-gsd-lore/280dde0d-bd34-4a34-9be5-f4d8e530ca5c/scratchpad`. From the worktree root, in ONE shell command:
1. `mkdir -p "$S/shots-TAG"`.
2. `npm run build`.
3. Start the server in the background: `NODE_ENV=production node src/server/index.ts /home/cinedise/studio-portal --port 4211`. Redirect its output to `"$S/server-4211.log"` and write `$!` to `"$S/server-4211.pid"`.
4. Wait with `curl -sf --retry 30 --retry-connrefused --retry-delay 1 -o /dev/null http://127.0.0.1:4211/`. No shell sleep.
5. Run `SWEEP_BASE=http://127.0.0.1:4211 node "$S/sweep.mjs" "$S/shots-TAG" > "$S/sweep-after-TAG.json"`.
6. Always finish with `kill "$(cat "$S/server-4211.pid")"`.

Never touch port 4210. The baseline is `$S/sweep-baseline.json`. Its light and dark distinct counts are: sizes 33, space 43, lh 47, ls 25, weights 5, fams 3.

## Multi-Source Coverage Audit

| Source | Item | Covered by |
|---|---|---|
| GOAL | Proper spacing scale, every padding/margin/gap/inset migrated | Task 1 |
| GOAL | Proper type scale | Task 2 |
| GOAL | Unnamed colours that exist in the source use the source's name | Task 3 |
| GOAL | Add a check | Tasks 1-3 (guard grows per family) |
| REQ | JZ8-01 / JZ8-02 / JZ8-03 / JZ8-04 | Task 1 / Task 2 / Task 3 / Tasks 1-3 |
| CONTEXT | D-01 (650→600) | Task 2 |
| CONTEXT | D-02 (mono stacks→--font-mono) | Task 2 |
| CONTEXT | D-03 (nearest step, no redesign, both themes) | Tasks 1-3 snap maps; Task 3 sweep (both themes) |
| RESEARCH | Non-reserved namespace; `--spacing` untouched | Task 1 (reserved-namespace assertion) |
| RESEARCH | 0.58rem floor fix; `--font-size-micro-label` kept | Task 2 |
| RESEARCH | Clamp consolidation into named fluid tokens | Task 1 (spacing), Task 2 (type) |
| RESEARCH | `-1.55rem` hang verified visually | Task 1 snap + Task 3 human-check |
| RESEARCH | Dedupe sidebar/state pairs; delete dead tokens; fix stale comment; name recurring recipes | Task 3 |
| RESEARCH | Pitfall 1 (Mermaid names) / 2 (shiki vars) / 3 (.sr-only) / 4 (gap:1px) / 5 (em sizes) / 6 (shorthand) | Task 3 keep-literal rule / var() ignored by scanner / Task 1 allowlist / Task 1 allowlist / Task 2 allowlist / Task 1 component splitting |
| RESEARCH | Guard shape = portability.test.ts (comment strip, allowlist, tmpdir positive control) | Task 1 |
| RESEARCH | A3 (0.85/0.9 share a step) | Superseded by P-02 (0.85 goes to --fs-4, 0.9 goes to --fs-5); reported to user |

No item is unplanned. Scroll-margin (P-03) is excluded by user scope, not dropped.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Spacing family end-to-end: scanner and positive control, spacing scale, full padding/margin/gap/inset migration (JZ8-01, JZ8-04)</name>
  <files>test/token-guard.test.ts, src/web/styles/globals.css, test/web/visual-contract.test.ts</files>
  <precondition>node_modules resolves in the worktree (run `npm ci`, or symlink /home/cinedise/gsd-lore/node_modules), and $S/sweep.mjs plus $S/sweep-baseline.json exist at the Token Specification's scratchpad path.</precondition>
  <read_first>test/portability.test.ts lines 283-438 (the fs-gate idiom to mirror); the Token Specification section of this plan; globals.css lines 1-160 only</read_first>
  <behavior>
    - The scanner flags a raw rem/px/em/vw/vh length in any component of a padding, margin, gap, inset, top, right, bottom or left value (all longhands included). It ignores components that are 0 (with or without a unit), auto, keywords, percentages, or wholly inside var() (fallbacks included).
    - It flags raw lengths inside calc(), clamp(), min() and max() at usage sites. It accepts calc(-1 * var(--space-N)).
    - It skips declarations whose enclosing block, at any nesting depth, has the exact prelude `:root`, `.dark`, `@theme inline` or `@font-face`. It still flags compound selectors such as `.dark .x`. It is not fooled by comments, by `@apply` or `@custom-variant` statements, by multi-line comma selectors, or by strings.
    - An allowlist entry exempts only its exact selector + property + value, and every entry must match a live declaration (a stale entry fails).
    - The real globals.css has zero spacing violations. Every static --space-* value is a positive multiple of 0.125rem. Every defined --space-* token is consumed at least once, matched with a boundary so --space-1 never counts --space-1-5. No Tailwind-reserved namespace is declared.
    - Positive control: fixtures planted in an mkdtemp dir are each caught. They cover a raw padding shorthand component among var() components, raw margin-top, raw gap, raw top, a raw negative margin, a raw length inside calc, and a usage-site clamp. The same declarations inside `:root`, `.dark`, `@theme inline` and `@media { :root { } }` are NOT caught. A clean fixture yields zero violations.
  </behavior>
  <action>
**Guard file.** Create test/token-guard.test.ts, shaped exactly like the fs-gate section of test/portability.test.ts, for JZ8-04:
- A header comment names the gate and its rationale.
- `REPO_ROOT` and `CSS_PATH` come from `import.meta.url`.
- `stripCssComments(css)` replaces each block comment with the same number of newlines, so reported line numbers stay true.
- `parseDeclarations(css)` is a single-pass character walker. It tracks quote state and paren depth, and keeps a stack of whitespace-collapsed block preludes. It emits `{ selector, property, value, line, inTokenBlock }` for each declaration. It ends a declaration at a depth-0 semicolon or at the closing brace, strips `!important`, and skips at-rule statements.
- `splitTopLevel(value)` splits on whitespace at paren depth 0.
- `withoutVarCalls(text)` removes balanced var(...) segments.
- A `FAMILIES` table holds the spacing property matcher. Scroll-margin and scroll-padding are deliberately excluded; per P-03, say so in a comment.
- An `ALLOWLIST` array of `{ selector, property, value, reason }`.
- `scanTokenViolations(css, families)` returns readable strings of the form "line selector { property: value } [family]".

Use only vitest and node built-ins. No postcss or lightningcss (research: undeclared transitive deps).

**Tests.** Write these four, following the behavior block:
1. The live gate.
2. An allowlist-by-name test. It asserts every entry both exists live and is exempted, the way portability's prose-only-files test works.
3. A scale-sanity and reserved-namespace test.
4. The positive control. It writes fixtures under `mkdtemp(join(tmpdir(), 'labelore-token-guard-'))`, reads them back through the same scanner, and removes them in try/finally. It never writes under src/.

**Scale and migration.**
- Run the live gate once before migrating. Its violation list is your worklist; note its length in the SUMMARY.
- Add the new theme-independent `:root` scale block, following the Token Specification placement rules, with the spacing steps and the `--space-fluid-*` tokens.
- Migrate every spacing declaration outside the token blocks using the snap map and snap rule.
- Do the bulk rewrite with a throwaway script in your own session scratchpad (never committed) that reuses the same declaration-walking approach. Have it print every value it cannot map, and resolve those by hand. Then review `git diff` rule by rule rather than reading the whole file.

**Allowlist entries.** Each carries its exact live selector and a reason:
- `.sr-only` `margin: -1px` (visually-hidden idiom).
- The two `gap: 1px` grid-divider rules, around lines 1193 and 2264. Resolve their selectors live.
- The two em-relative heading-margin rules, around line 1772 (`margin: 1.8em 0 0.65em`) and lines 2371-2372 (`margin-top: 2.3em`, `margin-bottom: 0.75em`).

**Existing test update.** The migrated checkbox hang reads `calc(-1 * var(--space-6))`, which no longer starts with a minus sign. Update the visual-contract assertion "hangs the task checkbox into the indent" so it accepts either a leading minus or `calc(-1 * var(--space-`. Keep its intent (a negative hang) and do not loosen it further. Change no other assertion in that file.

**Commit** only when the whole suite is green.
  </action>
  <verify>
    <automated>npm test -- test/token-guard.test.ts test/web/visual-contract.test.ts test/web/focus-ring-contrast.test.ts && npm test && npm run typecheck && npm run lint</automated>
    <automated>S=/tmp/claude-1000/-home-cinedise-gsd-lore/280dde0d-bd34-4a34-9be5-f4d8e530ca5c/scratchpad; mkdir -p "$S/shots-t1"; npm run build && (NODE_ENV=production node src/server/index.ts /home/cinedise/studio-portal --port 4211 > "$S/server-4211.log" 2>&1 & echo $! > "$S/server-4211.pid") && curl -sf --retry 30 --retry-connrefused --retry-delay 1 -o /dev/null http://127.0.0.1:4211/ && SWEEP_BASE=http://127.0.0.1:4211 node "$S/sweep.mjs" "$S/shots-t1" > "$S/sweep-after-t1.json"; kill "$(cat "$S/server-4211.pid")"; node -e "const fs=require('fs');const [a,b]=process.argv.slice(1).map(p=>JSON.parse(fs.readFileSync(p,'utf8')));const k=s=>s.split(' ×')[0];const bad=[];for(const t of ['light','dark']){const A=a.themes[t],B=b.themes[t];const base=new Set(A.offPalette.map(k));for(const e of B.offPalette)if(!base.has(k(e)))bad.push(t+' new off-palette: '+e);if(!(A.distinct.space > B.distinct.space))bad.push(t+' space not below baseline: '+B.distinct.space);console.log(t,JSON.stringify(B.distinct))}if(bad.length){console.error(bad.join('\n'));process.exit(1)}" "$S/sweep-baseline.json" "$S/sweep-after-t1.json"</automated>
  </verify>
  <done>
- test/token-guard.test.ts exists with the four spacing-family tests, and each passes.
- Its positive control catches every planted spelling and passes the clean fixture.
- globals.css has a theme-independent scale block, and zero spacing violations outside token blocks apart from the named allowlist.
- The rendered sweep shows distinct space strictly below 43 in both themes, with no new off-palette colour.
- The full suite, typecheck and lint pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Type family: font-size steps with paired line-heights, letter-spacing, weights, --font-mono, fluid type; guard extended (JZ8-02, JZ8-04, D-01, D-02)</name>
  <files>src/web/styles/globals.css, index.html, test/token-guard.test.ts, test/web/visual-contract.test.ts</files>
  <read_first>the Token Specification "Type scale" section; test/web/visual-contract.test.ts micro-label block (about lines 700-760) and link-weight block (about 166-178); index.html lines 21-65</read_first>
  <behavior>
    - The scanner's type family flags these values outside token blocks:
      - a font-size carrying any raw length (rem/px/vw, or em not allowlisted) or a usage-site clamp()
      - a line-height that is a bare number or a raw length
      - a letter-spacing that is a non-zero raw length
      - a numeric font-weight
      - a font-family that is anything other than var(...) or inherit

      Keywords (inherit, normal) and var() pass.
    - Every static --fs-* literal is at least 0.625rem (10px floor). Every defined --fs-*, --fs-N--lh, --lh-*, --ls-* and --fw-* token is consumed at least once.
    - The real globals.css has zero type violations. The positive control plants one violation per type property (including a `.dark .x` compound selector), and each is caught. Their token-block twins and the clean fixture pass.
  </behavior>
  <action>
**Guard first.** Extend test/token-guard.test.ts with a type family covering font-size, line-height, letter-spacing, font-weight, font-family and the font shorthand (only inherit is allowed). Extend the live gate, the scale-sanity test (floor, plus consumption for every type token family) and the positive control. Run the live gate to get the type worklist.

**Scale and migration.** Add the Token Specification type tokens to the scale block:
- `--fs-1` … `--fs-7`
- the `--fs-N--lh` companions
- `--lh-*`, `--ls-*`, `--fw-*`
- `--fs-fluid-*` and `--fs-display-*`

Then migrate every font-size, line-height, letter-spacing and font-weight declaration outside the token blocks, using the snap maps. Ties snap up. Keep `--font-size-micro-label: 0.7rem;` literal where it is, and keep the six micro-label selectors on it.

**Specific fixes:**
- The 0.58rem `.history-milestone > summary small` becomes `var(--fs-1)`: the 10px floor fix.
- The `.position-copy h1 span` label becomes `max(0.2em, var(--fs-1))` (P-04).
- Both weight-650 declarations become `var(--fw-semibold)`, per D-01.
- The three hard-coded system-monospace stacks at `.artifact-path`, `.metadata-record dt` and `.plan-section-label` become `var(--font-mono)`, per D-02.

**Mono token wiring.**
- Put the `--font-mono` value in the palette `:root` next to `--font-heading`, and set `--font-heading` to `var(--font-mono)`.
- Add the `--font-mono` self-mapping to `@theme inline`.
- Mirror the `--font-heading` and new `--font-mono` lines byte-for-byte into the index.html snapshot `:root`. The snapshot-parity tests in focus-ring-contrast enforce this.

**Allowlist additions.** The hero label's `max(0.2em, var(--fs-1))`, and inline code's `0.86em` (resolve its selector live). If `.attention-list .item-kind`'s fixed line-height cannot convert without misalignment, keep it as an allowlist entry with its reason; otherwise convert it as the spec says.

**Existing test update.** The visual-contract link block asserting `font-weight: 600;` must now assert `font-weight: var(--fw-semibold);`: same weight, token spelling. Change nothing else there. The micro-label tests must pass untouched.

**Commit** when green.
  </action>
  <verify>
    <automated>npm test -- test/token-guard.test.ts test/web/visual-contract.test.ts test/web/focus-ring-contrast.test.ts test/web/shell-contract.test.ts && npm test && npm run typecheck && npm run lint && npm run build</automated>
  </verify>
  <done>
- Zero type violations outside token blocks apart from the named allowlist.
- Every static type step is at or above 0.625rem.
- The 650 weights are gone (D-01), and the three mono sites read `var(--font-mono)` (D-02).
- The micro-label and snapshot-parity tests pass unchanged.
- The full suite, typecheck, lint and build pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Colour family: named recipes, palette dedupe, dead-token deletion mirrored to index.html, stale comment; guard complete; final two-theme sweep (JZ8-03, JZ8-04, D-03)</name>
  <files>src/web/styles/globals.css, index.html, test/token-guard.test.ts</files>
  <read_first>the Token Specification "Colour" section; globals.css lines 24-160 and the `--table-zebra` pair (about 2468-2475); index.html lines 21-100; test/web/focus-ring-contrast.test.ts (tokenBlock/tokens helpers and the snapshot-parity tests)</read_first>
  <behavior>
    - The colour family flags these values outside token blocks:
      - any raw colour function (rgb, rgba, hsl, hsla, hwb, lab, lch, oklab, oklch, color) or hex literal, in any value; an `in oklab` interpolation keyword has no parenthesis and is not flagged
      - a basic named colour keyword in a colour-bearing property (transparent and currentColor pass)
      - any usage-site color-mix() whose whitespace-normalised recipe equals a token definition's value
      - any usage-site color-mix() recipe that appears in two or more declarations
    - Palette checks run over the merged top-level `:root` and `.dark` blocks:
      - no two tokens hold identical non-alias values in both themes, where the dark value is the `.dark` override or else the `:root` value, and pure single var() aliases are ignored
      - `.dark` declares no token whose value string equals its `:root` value
      - none of the deleted dead tokens is declared in globals.css or index.html, using a boundary-safe match so `--accent` never matches `--sidebar-accent`
    - The real globals.css has zero colour violations. The positive control plants a hex, an oklch(), a named keyword, a recurring recipe, a recipe duplicating a token, and a cross-theme duplicate pair, and each is caught. The clean fixture passes.
  </behavior>
  <action>
**Guard first.** Extend test/token-guard.test.ts with the colour family and the three palette checks from the behavior block. Extend the positive control, and add a clean-fixture case. Run it to get the colour worklist.

**Palette edits.** Apply the Token Specification colour edits to globals.css:
- delete the dead tokens and their `@theme inline` mappings
- reword the micro-label comment's reference to the deleted radius token
- alias `--card-foreground`, `--popover-foreground` and `--sidebar-foreground` to `var(--foreground)`
- remove the listed `.dark` redundancies
- point `--state-active` and `--state-hover` at the sidebar tokens (P-05)
- define `--card-veil`, `--destructive-tint`, `--destructive-border`, `--primary-tint` and `--shadow-popover` in the palette `:root`, and replace their usage sites
- fix the stale comment above `.artifact-document pre` to say 16%

**Mirror to index.html.** Mirror every palette `:root`/`.dark` deletion and alias byte-for-byte into the index.html snapshot. The focus-ring-contrast parity tests compare each snapshot token against the live block. Do not add the new named recipes or scale tokens to the snapshot.

**Hard constraints.**
- The seven Mermaid-read names and `--ring` keep their literal oklch definitions.
- Single-use recipes pinned by visual-contract stay inline and unchanged.
- Before hoisting, re-confirm that no rule re-declares a palette token locally.

**Final sweep.** Run the sweep procedure with the tag `final` and apply the final gate in verify. Then view the four screenshots in `$S/shots-final`: light and dark dashboard, light and dark plan page.

**SUMMARY.** Record:
- the baseline versus final distinct counts per theme
- the complete snap and fluid-cluster mapping tables
- the allowlist with reasons
- the pre-migration worklist sizes
- P-01 to P-05 and D-01/D-02 under "Decisions for the user"

**Commit** when green.
  </action>
  <verify>
    <automated>npm test && npm run typecheck && npm run lint && git diff --quiet "$(git merge-base HEAD master)" -- src/web/components/ui/button.tsx src/web/pages/artifact-page.tsx src/web/pages/mermaid-theme.ts</automated>
    <automated>S=/tmp/claude-1000/-home-cinedise-gsd-lore/280dde0d-bd34-4a34-9be5-f4d8e530ca5c/scratchpad; mkdir -p "$S/shots-final"; npm run build && (NODE_ENV=production node src/server/index.ts /home/cinedise/studio-portal --port 4211 > "$S/server-4211.log" 2>&1 & echo $! > "$S/server-4211.pid") && curl -sf --retry 30 --retry-connrefused --retry-delay 1 -o /dev/null http://127.0.0.1:4211/ && SWEEP_BASE=http://127.0.0.1:4211 node "$S/sweep.mjs" "$S/shots-final" > "$S/sweep-after-final.json"; kill "$(cat "$S/server-4211.pid")"; node -e "const fs=require('fs');const [a,b]=process.argv.slice(1).map(p=>JSON.parse(fs.readFileSync(p,'utf8')));const k=s=>s.split(' ×')[0];const bad=[];for(const t of ['light','dark']){const A=a.themes[t],B=b.themes[t];const base=new Set(A.offPalette.map(k));for(const e of B.offPalette)if(!base.has(k(e)))bad.push(t+' new off-palette: '+e);for(const f of ['sizes','space','lh','weights','fams'])if(!(A.distinct[f] > B.distinct[f]))bad.push(t+' '+f+' not below baseline: '+B.distinct[f]+' vs '+A.distinct[f]);if(B.distinct.fams!==2)bad.push(t+' fams='+B.distinct.fams);if(B.distinct.weights > 4)bad.push(t+' weights='+B.distinct.weights);for(const s of B.sizes)if(10 > parseFloat(s))bad.push(t+' size below 10px: '+s);console.log(t,JSON.stringify(B.distinct))}if(bad.length){console.error(bad.join('\n'));process.exit(1)}" "$S/sweep-baseline.json" "$S/sweep-after-final.json"</automated>
    <human-check>
In the four `$S/shots-final` screenshots, in both themes, confirm:
- the task-list checkboxes still hang cleanly in their indent (the `--space-6` pair)
- the hero and section headings using the merged display clamps keep their hierarchy
- `.artifact-path`, `.metadata-record dt` and `.plan-section-label` now render in JetBrains Mono (D-02)
- corners are still square

Record findings in the SUMMARY.
    </human-check>
  </verify>
  <done>
Colour:
- Zero colour violations outside token blocks.
- No cross-theme duplicate or redundant `.dark` token.
- The dead tokens are gone from globals.css and index.html, and the snapshot-parity tests pass.
- The stale comment says 16%.

Guard: enforces the spacing, type and colour families, with a passing positive control for each.

Final sweep, both themes:
- sizes, space, lh, weights and fams are all below baseline
- fams is 2 and weights is at most 4
- no new off-palette colour
- no size below 10px

Integrity: button.tsx, artifact-page.tsx and mermaid-theme.ts are byte-identical to the fork point.
  </done>
</task>

</tasks>

<threat_model>
Security enforcement is on at ASVS level 1, and the plan blocks on high. This task only touches CSS and tests. It adds no endpoint and no input path, and it does not touch the server, planning-fs or repository layers.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner → OS temp dir | The guard's positive control writes synthetic CSS fixtures, which must never reach the source tree |
| stylesheet → runtime token readers | artifact-page.tsx (Mermaid) and index.html's pre-hydration snapshot read custom properties by exact name |
| worktree sweep server → local ports | A temporary production server is started for measurement next to the user's own server on 4210 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-jz8-01 | Tampering | test/token-guard.test.ts positive control | low | mitigate | Fixtures are written only under `mkdtemp(join(tmpdir(), 'labelore-token-guard-'))` and removed in `try/finally`. globals.css is read with readFile only. There is no write path under src/, mirroring portability.test.ts's T-04-04-01 idiom. |
| T-jz8-02 | Denial of Service | guard scanner | low | mitigate | A single-pass character walker plus regexes applied per declaration, with no nested quantifiers. There is no whole-file backtracking-prone pattern, so the 3k-line scan stays linear. |
| T-jz8-03 | Tampering | palette tokens read by name (artifact-page.tsx, index.html snapshot, focus-ring test) | medium | mitigate | The seven Mermaid-read names and `--ring` keep literal definitions. Every palette deletion or alias is mirrored into index.html, which the snapshot-parity tests enforce. Task 3 verifies button.tsx, artifact-page.tsx and mermaid-theme.ts are byte-identical to the fork point. |
| T-jz8-04 | Denial of Service | sweep server | low | mitigate | It runs from the worktree on port 4211 only. Its PID is captured and killed in the same command, and port 4210 is never touched. |
| T-jz8-05 | Information Disclosure | read-only target access | low | accept | The server and filesystem layers are unmodified, so the read-only guarantee over the target `.planning/` is unchanged. |
| T-jz8-SC | Tampering | package installs | low | accept | No package is added. `npm ci` only restores the existing lockfile, and the research's Package Legitimacy Audit is not applicable. |
</threat_model>

<verification>
- `npm test` is green, including the new test/token-guard.test.ts and the untouched snapshot-parity, micro-label, shell and degradation contract tests.
- `npm run typecheck`, `npm run lint` and `npm run build` pass.
- The final sweep gate (Task 3 verify) passes in both themes. Distinct counts drop, fams is 2, there is no new off-palette colour, and nothing renders below 10px.
- The guard's positive control demonstrably fails on planted spacing, type and colour violations and passes a clean fixture.
</verification>

<success_criteria>
- One spacing scale (`--space-*`, plus named fluid tokens) carries every padding/margin/gap/inset value outside the token blocks.
- One type scale covers the rest: `--fs-*` with paired `--fs-N--lh`, `--lh-*`, `--ls-*`, `--fw-*`, `--font-mono`, and fluid/display tokens. The 10px floor holds everywhere.
- Colours use their source names. Recurring recipes are named, identical tokens are deduped, dead tokens are deleted in both globals.css and index.html, and the stale comment is fixed.
- A vitest guard fails on any reintroduced raw spacing, type or colour literal outside the token blocks. It has an explicit, stale-checked allowlist and a planted-fixture positive control.
- The SUMMARY reports before/after sweep numbers and every discretion call (P-01 to P-05) plus D-01/D-02, so the user can review them.
</success_criteria>

<output>
Create `.planning/quick/260910-jz8-centralize-spacing-type-and-color-tokens/260910-jz8-SUMMARY.md` when done
</output>
