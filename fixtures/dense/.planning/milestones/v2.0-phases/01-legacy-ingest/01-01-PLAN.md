---
phase: "01"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/legacy/ingest.ts
autonomous: true
requirements: [LEGACY-01]
must_haves:
  truths:
    - "Legacy records are readable in the new store"
  artifacts:
    - path: "src/legacy/ingest.ts"
      provides: "The legacy ingest pipeline"
  key_links:
    - from: "src/legacy/ingest.ts"
      to: "LEGACY-01"
      via: "implements the legacy ingest requirement directly"
---

<objective>
Pull legacy records into the new store.

Output: `src/legacy/ingest.ts` reads a legacy export file and writes it into the new store's format.
</objective>

<context>
@.planning/PROJECT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the ingest pipeline</name>
  <files>src/legacy/ingest.ts</files>
  <action>
Write a minimal function that reads a legacy export and writes it into the new store's shape.
  </action>
  <verify>node --experimental-strip-types src/legacy/ingest.ts ingests a sample record</verify>
  <done>Legacy records are readable in the new store.</done>
</task>

</tasks>

<verification>
1. `src/legacy/ingest.ts` exists and ingests a sample record.
</verification>

<success_criteria>
- Legacy records are readable in the new store.
</success_criteria>

<output>
Create `.planning/phases/01-legacy-ingest/01-01-SUMMARY.md` when done.
</output>
