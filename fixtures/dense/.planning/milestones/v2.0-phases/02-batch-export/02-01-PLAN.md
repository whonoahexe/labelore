---
phase: "02"
plan: "01"
type: execute
wave: 1
depends_on: ["01-01"]
files_modified:
  - src/legacy/export.ts
autonomous: true
requirements: [XPORT-LEGACY-01]
must_haves:
  truths:
    - "A batch export job runs against ingested records"
  artifacts:
    - path: "src/legacy/export.ts"
      provides: "The batch export job"
  key_links:
    - from: "src/legacy/export.ts"
      to: "XPORT-LEGACY-01"
      via: "implements the batch export requirement directly"
---

<objective>
Export ingested batches to the downstream consumer.

Output: `src/legacy/export.ts` reads ingested records and writes a batch export file.
</objective>

<context>
@.planning/milestones/v2.0-phases/01-legacy-ingest/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the export job</name>
  <files>src/legacy/export.ts</files>
  <action>
Write a minimal function that reads ingested records and writes a batch export file.
  </action>
  <verify>node --experimental-strip-types src/legacy/export.ts exports a sample batch</verify>
  <done>A batch export job runs against ingested records.</done>
</task>

</tasks>

<verification>
1. `src/legacy/export.ts` exists and exports a sample batch.
</verification>

<success_criteria>
- A batch export job runs against ingested records.
</success_criteria>

<output>
Create `.planning/phases/02-batch-export/02-01-SUMMARY.md` when done.
</output>
