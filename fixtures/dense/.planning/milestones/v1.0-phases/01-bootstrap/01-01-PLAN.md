---
phase: "01"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/bootstrap/init.ts
autonomous: true
requirements: [BOOT-01]
must_haves:
  truths:
    - "The scaffolding runs end to end"
  artifacts:
    - path: "src/bootstrap/init.ts"
      provides: "The bootstrap entry point"
  key_links:
    - from: "src/bootstrap/init.ts"
      to: "BOOT-01"
      via: "implements the bootstrap requirement directly"
---

<objective>
Stand up the earliest scaffolding for the fixture project.

Output: `src/bootstrap/init.ts` runs without error.
</objective>

<context>
@.planning/PROJECT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the bootstrap entry point</name>
  <files>src/bootstrap/init.ts</files>
  <action>
Write a minimal entry point that prints a single startup line and exits 0.
  </action>
  <verify>node --experimental-strip-types src/bootstrap/init.ts prints a startup line</verify>
  <done>The bootstrap entry point runs and exits 0.</done>
</task>

</tasks>

<verification>
1. `src/bootstrap/init.ts` exists and runs without error.
</verification>

<success_criteria>
- The scaffolding runs end to end.
</success_criteria>

<output>
Create `.planning/phases/01-bootstrap/01-01-SUMMARY.md` when done.
</output>
