---
phase: "01"
plan: "02"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/identity/slice.ts
  - test/identity/slice.test.ts
autonomous: true
requirements: [IDENT-02, IDENT-03]
must_haves:
  truths:
    - "Every artifact type studio-portal has never produced is represented and renders"
  artifacts:
    - path: "test/identity/slice.test.ts"
      provides: "Regression coverage for the identity resolver"
  key_links:
    - from: "test/identity/slice.test.ts"
      to: "IDENT-02"
      via: "proves the resolver survives unfamiliar input shapes"
---

<objective>
Harden the identity slice tracer and finish authoring the phase's unfamiliar/invented artifact types.

Output: a tested `src/identity/slice.ts`, plus `01-SPEC.md`, `01-AI-SPEC.md`, `01-COST-MODEL.md`.
</objective>

<context>
@.planning/phases/01-identity-slice/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add regression tests for the resolver</name>
  <files>test/identity/slice.test.ts</files>
  <action>
Add tests covering the resolver's documented behavior:

- Known id resolves
- Unknown id returns null
- Never throws for malformed input (empty string, non-string coerced to string)
  </action>
  <verify>npm test -- identity/slice</verify>
  <done>All resolver tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Author the unfamiliar artifact types</name>
  <files>.planning/phases/01-identity-slice/01-SPEC.md, .planning/phases/01-identity-slice/01-AI-SPEC.md, .planning/phases/01-identity-slice/01-COST-MODEL.md</files>
  <action>
Author `01-SPEC.md` from `gsd-core/templates/spec.md`, `01-AI-SPEC.md` from
`gsd-core/templates/AI-SPEC.md` (no frontmatter), and `01-COST-MODEL.md` as a genuinely invented type
with no template at all.
  </action>
  <verify>All three files exist and 01-AI-SPEC.md has no frontmatter</verify>
  <done>All three unfamiliar/invented artifact types exist under phases/01-identity-slice/.</done>
</task>

</tasks>

<verification>
1. `npm test -- identity/slice` passes.
2. `01-SPEC.md`, `01-AI-SPEC.md`, `01-COST-MODEL.md` all exist.
</verification>

<success_criteria>
- Every artifact type studio-portal has never produced is represented and renders.
</success_criteria>

<output>
Create `.planning/phases/01-identity-slice/01-02-SUMMARY.md` when done.
</output>
