---
phase: "02"
plan: "01"
type: execute
wave: 1
depends_on: ["01-02"]
files_modified:
  - src/transport/client.ts
autonomous: true
requirements: [XPORT-01, XPORT-02]
must_haves:
	truths:
    - "Transport requests round-trip through the identity slice"
  artifacts:
    - path: "src/transport/client.ts"
      provides: "The transport layer tracer implementation"
  key_links:
    - from: "src/transport/client.ts"
      to: "XPORT-01"
      via: "implements the transport round-trip requirement directly"
---

<objective>
Deliver a minimal end-to-end tracer for the transport layer, calling into Phase 1's identity resolver.

Output: `src/transport/client.ts` sends a request and receives a resolved identity back.
</objective>

<context>
@.planning/phases/01-identity-slice/01-02-SUMMARY.md
@.planning/phases/02-transport-layer/02-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the transport tracer</name>
  <files>src/transport/client.ts</files>
  <action>
Write a minimal client that calls `resolveIdentity` from Phase 1's slice and returns its result over
an in-process "transport" (a plain function call standing in for a real network boundary). Keep it
small:

- A single exported `fetchIdentity(id: string): Identity | null` function
- Properties it must satisfy:
  - Delegates to `resolveIdentity` without transformation
  - Never throws

```ts
import { fetchIdentity } from "./client";
const identity = fetchIdentity("user-1");
```
  </action>
  <verify>node --experimental-strip-types src/transport/client.ts round-trips a known id</verify>
  <done>The transport tracer round-trips a known identity through Phase 1's resolver.</done>
</task>

</tasks>

<verification>
1. `src/transport/client.ts` exists and exports `fetchIdentity`.
</verification>

<success_criteria>
- Transport requests round-trip through the identity slice.
</success_criteria>

<output>
Create `.planning/phases/02-transport-layer/02-01-SUMMARY.md` when done — not yet created, this phase
is deliberately mid-flight in this fixture.
</output>
