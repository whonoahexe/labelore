# v3.0 Capacity Plan

**Milestone:** v3.0 Live Read Surface
**Prepared:** 2026-08-22

This is a version-stamped document matching `CANONICAL_PATTERNS`' `^v\d+\.\d+(?:\.\d+)?-.*\.md$` but
matching no exact-name entry in `CANONICAL_EXACT` — pattern-matched canonical detection is exercised
here, separately from exact-name matching.

## Capacity by Phase

| Phase | Estimated tokens | Actual tokens (if known) |
|-------|-------------------|---------------------------|
| 1. Identity Slice | 60,000 | 71,830 |
| 2. Transport Layer | 40,000 | — |

## Notes

This document is not defined by any `gsd-core` template — it is a project-specific note that happens
to satisfy the version-stamped filename pattern, which is exactly the case a dashboard's "is this
canonical" check must handle via pattern matching rather than an exact-name lookup table alone.
