---
phase: 01-first-slice
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/slice.ts
autonomous: true
requirements: [SLICE-01, SLICE-02]
must_haves:
  truths:
    - "The first slice runs end to end without a real backend"
    - "A human can observe the slice working before further phases begin"
  artifacts:
    - path: "src/slice.ts"
      provides: "The tracer implementation for the fixture's first slice"
  key_links:
    - from: "src/slice.ts"
      to: "SLICE-01"
      via: "implements the first slice requirement directly"
---

<objective>
Deliver a minimal end-to-end tracer for the fixture project's first slice.

Output: `src/slice.ts` runs and produces observable output.
</objective>

<context>
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the tracer</name>
  <files>src/slice.ts</files>
  <action>
Write a minimal function that demonstrates the slice mechanism end to end. Keep it small:

- A single exported function
- A short list of the properties it must satisfy:
  - Runs without a network dependency
  - Prints a single observable line of output
- An example invocation in a fenced code block:

```ts
import { runSlice } from "./slice";
runSlice();
```
  </action>
  <verify>node --experimental-strip-types src/slice.ts prints one line</verify>
  <done>The tracer runs and prints its output.</done>
</task>

</tasks>

<verification>
1. `src/slice.ts` exists and exports a runnable function.
</verification>

<success_criteria>
- The first slice runs end to end without a real backend.
- A human can observe the slice working before further phases begin.
</success_criteria>

<output>
This is a fixture PLAN.md — it is never executed. Its `01-01-SUMMARY.md` intentionally does not exist,
proving the parser tolerates a planned-but-unexecuted phase.
</output>
